# KICKOFF — S36 continuation (written 2026-06-22) — net-of-cost the flow divergence/exhaustion reversal edge

Branch `claude/crypto-trading-platform-plan-MpqwG` (PUSHED). Read order: `CLAUDE.md` (S36 delta top) ->
`SESSION_HANDOFF_2026-06-22_S36.md` -> this file. All S35 memories apply. Crypto trading platform only;
zero synthetic; Result Discipline; commit + push regularly (git is source of truth); never pool cells.

## STATE COMING IN (S36 result)
A real, robust, per-cell **flow-based trend-continuation-vs-FLIP edge** is built + committed:
- `odcore/info_dipole.divergence(buy_vol, sell_vol, price_drift)` -> 2-factor flow-state read.
  DIVERGENCE (flow opposing price) + EXHAUSTION (dipole collapsing toward 0.5 = leader weakening) stack:
  oppose+exhaust = **64% reversal** (n=317) ... with-trend+strengthen = 49% (healthiest trend). Strong
  divergence alone ~65% reversal, temporally stable, consistent across 6/7 cells.
- The DIRECTIONAL probe (cell_signal/DEPLOY) is a TREND ARTIFACT -> `DEPLOY_VALIDATED=False`, do NOT trade
  off it. The FLIP/reversal read is the edge.
- Validation is on thin 1-min/2-day `test_bars` (1,560 onsets); needs the local 1-sec history to confirm.

## TODO (priority order)
1. **NET-OF-COST BACKTEST of the reversal gate, PER CELL (decisive).** Build it: at each onset where
   `divergence().expect == "reversal"` (or graded by `reversal_conviction`), take the FADE (against the
   prior price drift), exit at a fixed horizon / the trade's horizon, apply real fees+slippage (5bps/side
   per the audit), walk-forward, per cell. A 64% reversal HIT-RATE is only an EDGE if the reversal MOVES
   beat cost. Report per-cell net bps + which cells clear. This is the platform bar (net-of-cost).
   Data: `fingerprint_dataset/test_bars/` (1-min, in git) for a first cut; flag the thin-data caveat.
2. **Confirm on the local 1-sec multi-regime history** (E:\ / refrag-side, not in git) -- more N, finer
   entropy/MI, multiple trend regimes so the base rate isn't hostage to one trend. The S36 caveat.
3. **Wire `divergence()` per cell** into (a) the regime classifier (flow-opposed/collapsing -> reversal/
   transition; confirm/strengthen -> CHANNELED/CASCADE), (b) the fingerprint as a flip feature, (c) sizing
   via `reversal_conviction`. Gate live use on net-of-cost passing + per-cell sign consistency.
4. **The 2 inverting cells** (btc_bybit_buy, eth_kraken_sell break the divergence->reversal rule) -- why?
   Per-bucket physics; could be a venue/microstructure tell.
5. **Sharpen with the info-dipole C ratio** (H_self/H_cross; the paper's autonomy/coupling -> collapse vs
   decouple modes) as a 3rd flow-state factor.

## TOOLS (run any bar-free, off committed data)
`python _info_dipole_trend_flip.py` (the edge), `_info_dipole_flow_detrend.py` (artifact vs real),
`_info_dipole_flow_robustness.py`, `_info_dipole_flow_probe.py`. Operator: `odcore/info_dipole.py`.

## DO NOT
- Do not deploy/trade off the directional `cell_signal`/`DEPLOY` map (trend artifact, DEPLOY_VALIDATED=False).
- Do not pool cells to deploy. Do not average/smooth/flatten -- distinct per-bucket signals are the point.
- Do not wire any live signal before net-of-cost passes (and, for the fingerprint encoder, the onset canary).
