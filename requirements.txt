# OD / Operator-Discovery toolchain — pinned for reproducibility (BUILD_PLAN Phase 0).
#
# The Julia backend (1.11.9) is pulled by PySR via juliacall and precompiles on the first
# fit; it cannot be pinned through pip (it is provisioned by scripts/session_start.sh and
# the local toolchain — see the pysr-julia-local-toolchain note). PySR is pinned to 1.5.10
# per the research log; the numeric stack is floored to the versions the engine was built on.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pysr==1.5.10
pytest>=7.4
