# S52 KICKOFF — JOB 1: the SIZING-ON-WINNERS accounting (Greg), then the Bybit venue-cell measurement

Read first: `SESSION_HANDOFF_2026-07-02_S51.md` → `S51_REBATE_FINDINGS.md` → the S51 delta atop CLAUDE.md.
Branch = designated `claude/crypto-liquidity-signals-*` (reconcile to canonical `5c5vg9` at start — the paper
cron moves it; `git fetch origin && git merge --ff-only origin/claude/crypto-liquidity-signals-5c5vg9`, or
reset --hard if the harness cut stale; keep 5c5vg9 synced on every push). Default branch `claude/new-session-
o3vnm` carries the crons (Coinbase books + paper + NEW Bybit books) — LEAVE IT unless Greg approves a change.
Coinbase books: materialize `/tmp/<coin>_coinbase_book.jsonl.gz` from `data/<coin>-book`. Bybit venue books
(NEW, accruing 0 */6 since 07-02): `data/{sol,eth}-bybit-book` → `<coin>_bybit_book.jsonl.gz` — same schema,
the whole pipeline reads them unchanged.

## ⚠ THE STANDING CORRECTION (Greg, S51 close)
**Never cite a single "$X/hr ceiling."** Every $/hr is one cell of the matrix {venue, maker fee, FLAT|SIZED,
v1|v2 fill, window}. S51's measured spread on the SAME SOL legs: mk0-flat-v1 +$18/hr … −1bp +$71 … −2bp+wide
+$142 (v1); v2 mk0 −$7 → −1bp +$20; and ALL pre-Bybit (10x tape). Cite the cell, or cite the matrix.

## JOB 1 (Greg, do FIRST): make the SIZING-ON-WINNERS accounting airtight
Greg's concern: the $/hr projections under-credit the size-up on winners — "you aren't factoring in the size
up on the winners." What we have so far (verify, reconcile, extend — don't assume it's complete):
  - Forward ledger (25,845 trades, multi-window): sized_net beats flat on ALL 5 cells (sol +9,225→+10,705,
    +16%; doge +47%; xrp +15%; eth +27%; btc +34%). This is the OOS fact the sizing earns its keep on.
  - Capacity model @ deploy sizes: sized > flat (+8–21% SOL depending on fee scenario).
  - At the FLOW-CAPPED ceiling the sized/flat gap →0 — because `_dollars` caps size×$S by the SAME leg flow:
    sizing multiplies DESIRED notional, and at the ceiling everyone is flow-bound. **Job 1a: is that model
    right, or does it under-credit sizing?** (e.g. conviction legs may coincide with FATTER-flow turns —
    then size×cap correlation is real $ the current min() model does capture… measure corr(size, leg_cap)
    and the sized-$ at each S with honest accounting, per cell, per scenario.)
  - Known limits of ENTRY-conviction sizing (S47/S50, verified): corr(size,net)=+0.03 — it loads BIG MOVES
    (half wrong-tail), winners not separable AT ENTRY. But Greg's ask is broader than entry conviction:
    **Job 1b: winner-side sizing MECHANISMS beyond entry** — (i) post-entry ADD on confirmation (leg moving
    in favor + flow still opposing-at-our-price = the market delivering size); (ii) asymmetric size ramp
    (start small, add as the leg confirms; the S45/S51 adverse-flow result says the market force-feeds size
    to LOSERS — can we invert that by only accepting adds when the leg is green?); (iii) rebate interaction.
    EVERY variant: `assert_no_leakage` on any new feature + random/shuffle control + evaluate on the forward
    ledger (multi-window), never one-window. The scale-in lesson (S51): mark exits honestly (netting/queue),
    or the numbers are fiction.
  - Deliverable: a per-cell table — flat vs entry-sized vs winner-side-sized $/hr at each fee scenario and
    fill model (v1/v2), with the sizing contribution EXPLICIT at every capacity level. This becomes the
    canonical "$/hr matrix with sizing factored" that all future citations use.

## JOB 2: the Bybit venue-cell measurement (the decisive test, data should be ready)
`data/{sol,eth}-bybit-book` accrue 4 runs/day (~5h50m each). Once ≥1 full window is on the branches:
materialize, then run the FULL stack per venue-cell: median spread, turn structure (turns/hr, flip lean),
v1/v2 capacity + rebate scenarios at Bybit's reachable MM tiers (−0.1 bp MM1 … −1.25 bp MM3, verified S51),
and the netting sim WITH the queue-honest fill. Per-cell rule: sol_bybit and eth_bybit are NEW cells — the
S51 projections (10x tape → ~10x wall) are hypotheses these books confirm or kill. Compare against the
Coinbase cells at the same timestamps where windows overlap.

## JOB 3 (Greg action): send the Bybit MM application
institutional_services@bybit.com (subject "Market Maker Application"; maker-share bar 0.03%, 1-month trial)
+ optionally vip@backpack.exchange (confirm their image-locked rebate bps). Nothing technical gates this.

## DEAD — do not re-chase (unchanged + S51 additions)
Entry-timing retiming; wrong-tail ENTRY gates; spread/dive as timing; stacking net-screened levers; sizing
as a MULTIPLIER (entry-conviction sizing is +8–25% depending on regime/fee — real, kept, compounds — but not
an order of magnitude); **size-cap tightening below hi_clip=4.0 (S51: falsified on the forward ledger)**;
**per-leg scale-in with a single mark-to-close flatten (S51: inventory 4–20x exit flow = fiction)**;
**front-of-queue inventory sims as strategy validators (S51: reversed control outfills conviction — the
class rewards volume, not signal)**. Wrong-tail is IRREDUCIBLE at entry — size around it. OD-BOOK
`.ttest_committed.json` sentinel FROZEN.

## FILE MAP — change these IN PLACE, do not reinvent (verified on disk at S51 close)
**Deploy pipeline / Job 0+1:**
- `scripts/paper_trade.py` — forward trader: `cell_trades()` (per-cell legs + sizing features), per-cell
  `GRACE` map, deduped `paper_ledger.jsonl` append. RUN FIRST each session.
- `paper_ledger.jsonl` — the 25,845-trade multi-window forward OOS record = Job 1's evaluation set.
- `scripts/_s49_window_confirm.py` — segments the ledger into windows (the multi-window eval harness — Job 1
  extends THIS for per-window sizing lift, don't write a new segmenter).
- `scripts/_s49_conviction_leakage.py` — the leakage-gate runner for the sizing axes (extend for any NEW
  Job-1b feature; `odcore/leakage.assert_no_leakage` is the mandatory gate).
- `odcore/swing_maker.py` — `simulate_swing_maker(cover_grace=…)` (the validated one-legged executor — DO NOT
  fork it) + `size_legs(legs, quality, size_axis, alpha, roll, lo_clip, hi_clip=4.0)` (entry-conviction
  sizing, leakage-clean, hi_clip VALIDATED — don't tighten) + `SwingLeg.size`.
- `odcore/flip_detector.py` — `lean_series`, `detect_flips` (`retime_flips` exists but DEAD/OFF).
- `odcore/quiet_registry.py` — the per-cell params/deploy-flag registry pattern (S49 named it the template
  for wiring per-cell sizing params into the emit path — reuse the pattern, not a new registry).
- `odcore/sizing.py` — the S20 OD-native sizing module (CHECK it before writing any new sizing helper).
**Capacity / fill models (Job 1a + 2):**
- `scripts/_capacity_model.py` — `cell_scenarios()` (SCEN sweep maker×spread), `_leg_caps()` → (v1 front-of-
  queue, v2 queue-honest), `_leg_features()` (the paper_trade sizing features, verbatim), `_dollars()`
  (min(size×S, flow) — Job 1a audits THIS), `FILL_W=10`, `REP_S`. Wire venue cells HERE.
- `odcore/maker_book.py` — `simulate_arm()`, `_first_fill_index()` (the honest queue discipline).
- `scripts/_scale_in_probe.py` (per-leg scale-in, Q1 flow asymmetry) · `scripts/_inventory_sim.py` (netting
  sim, `--control` reversed-side falsifier). Results: `_capacity_model_results.json`,
  `_scale_in_probe_results.json`, `_inventory_sim_results{,_control}.json`.
**Data layer:**
- `_birth_probe.py` — `load_book` (single-parse, now carries `spread`; `_depthK` prefix sums — bit-identical,
  4x verified), `to_grid`, `lean`.
- `_liquidity_dive.py` — `build_channels(path, K, W, raw=)`, `median_spread_bps(path, raw=)` — pass `raw=`
  (one parse per book) in ANY new script.
- `bybit_book_collector.py` / `coinbase_book_collector.py` — symbol-parameterized collectors, SAME row schema.
- Workflows (DEFAULT branch — don't touch without Greg): `bybit_book_collectors_durable.yml`,
  `book_collectors_durable.yml`, `paper_trade.yml`. Data branches: `data/<coin>-book` (Coinbase 5 coins),
  `data/{sol,eth}-bybit-book` (NEW, accruing).

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy (partial coverage ≠ failure); never pool sign-blind; NEVER tune
off one window; git = source of truth (commit+push often; rebase onto 5c5vg9 first — the cron moves it);
falsification-first (`assert_no_leakage` + random/shuffle control on every new signal); VERIFY venue fees
from primary sources.
