# S58 KICKOFF — THE MID-BAND MACHINE, ONE PIECE AT A TIME (fine precision in, mid-band
# conviction on, fine collapse out — at REAL fees on US-lawful ground)

## ⛔ STANDING BANS: BYBIT — never for anything (US-ineligible; struck S57). MEXC — dead.
No bybit data/tape/cells/citations/proposals, ever. Record: `S57_VENUE_FINDINGS.md`. If any
residue surfaces, strike it. VERIFY VENUE ELIGIBILITY (jurisdiction + fee reality, primary
sources) BEFORE any venue work.

Read first: `SESSION_HANDOFF_2026-07-03_S57.md` -> `S57_VENUE_FINDINGS.md` -> CLAUDE.md S57
delta. Branch: designated claude/*; reconcile to canonical 5c5vg9 (fetch + ff-only, else
reset --hard); sync 5c5vg9 every push. Default branch = crons (Coinbase book collectors +
paper_trade only; bybit workflow DELETED). Run `python scripts/paper_trade.py` FIRST
(baseline +N>=0, ledger 26,784+ intact; SANDBOX registry EMPTY by design).

## THE FRAME (S57 verdicts, carried)
- FEES ARE THE REGIME: no lawful rebate exists. STANDING FEE MODELS on every test:
  cb_entry 40/60 | cb_early 10/20 | cb_real 8/18 | cb_scale 3/10 | cb_top 0/6 (ceiling only).
- FINE BAND DEAD at every reachable tier (toll:edge 8:1). MID BAND (60-100bp) = the only band
  whose swings pay real fees (Greg: "there's no other way").
- THE TWO BLEEDS: fine = fee toll x cadence; coarse-as-built = theta-giveback at entry
  (SOL -31bp/leg gross with REVERSED positive; ETH theta80 +21bp/leg gross with REVERSED
  negative, n=22 — the shape to chase, not yet a result).
- THE MEASURED CUTS (S55 R5/R8, Greg's dipole-dive recall): entry 151bp->26bp via fine 25bp
  reversal confirm + dipole-dive read (P(real) 0.36->0.48; R4 `continue`/rc~0 veto for false
  fires); exit 151bp->~28bp via lean-collapse (R8; `lean_exit` wired, never aimed).
  ~250bp/leg recoverable; S55 prize table: theta100 +106..122bp/leg at these costs.
  S56's ARM kill was rebate-regime-graded — re-testing at mid-band ARMs under real fees with
  the R4 veto is legitimate (the question changed).
- STAGED-COMMIT SPEC (frozen S57): starter every leg; ALL-IN = $5k (no $10k); adds MAKER-
  POSTED; confirm threshold SCALES WITH THE BAND (+2bp confirmed everything at coarse = no
  filter; sweep c as a fraction of theta: 12.5%/25%/50%); across-trades probe = NO signal
  (P(w|prev w)==P(w|prev L)); the probe lives INSIDE the trade.

## JOBS — ONE PIECE AT A TIME (Greg; one round = one defined test, renders read together)
1. **PIECE 1 — THE ENTRY.** Armed at mid-band extension (theta 60/80/100 class), confirmed by
   dipole dive + 25bp fine reversal, R4 veto on `continue`-class dips. Entry anchored at the
   fine flip nearest the PIVOT (not after the coarse theta-confirm — that was S57's -31bp/leg
   mistake, measured). Baseline = theta-confirm entry. Score at ALL fee models, NO-GATES
   (net $/hr AND legs/hr), controls mandatory (shuffle + reversed + leakage gate + per-week).
   MULTI-WINDOW: 30d x 5-coin Binance-spot bins (`backfill_binance_spot.py`, canary PASS S54,
   never swept — research tape only; trade venue remains Coinbase) + the Coinbase books.
   Sweep the knobs (ARM level, confirm fraction) — never promote a grid point off its curve.
2. **PIECE 2 — THE EXIT.** Aim the wired `lean_exit` (R8 collapse) on the piece-1 legs at
   coarse scale vs the fine-flip exit and theta-exit. Same scoring/controls.
3. **PIECE 3 — THE STAGED ALL-IN.** Greg: all-in on confirmed winners from the start when
   confidence is ~100% (measured: 91-95% once a band-scaled confirm prints). Starter -> all-in
   $5k at c=fraction-of-theta on the piece-1+2 machine; L$:W$ asymmetry + controls.
4. Verification queue (background): Kraken Pro US spot maker tiers (mk0 ~$10M/30d? + tier-
   climb bleed math); Coinbase CFM perp fee filing (maker rebate UNCLEAR); fold any late
   S57 agent syntheses into S57_VENUE_FINDINGS.
5. Greg (external): UI-delete data/{sol,eth}-bybit-book; the Coinbase fee-upgrade 60-day
   window is a card to play only once volume exists somewhere lawful — hold it.

## DATA (/tmp dies with the container)
- Coinbase books x5: for c in sol doge xrp eth btc; do git fetch origin data/$c-book;
  git show FETCH_HEAD:${c}_coinbase_book.jsonl.gz > /tmp/${c}_coinbase_book.jsonl.gz; done
- Binance spot 30d bins (piece-1 multi-window): python backfill_binance_spot.py per coin
  (~40min x5 parallel; see S54 kickoff commands).

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy; never pool sign-blind; NEVER tune off one
window; no-gates law (size, don't skip); confirm threshold scales with the band; adds maker-
posted; sandbox-first; one round = one defined test with worst-10/smallest-10 renders read
with Greg; git = source of truth; falsification-first; eligibility before venue work;
pip install matplotlib, MPLBACKEND=Agg; token 403 on GHA dispatch (Greg clicks Run workflow).
