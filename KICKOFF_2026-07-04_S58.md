# S58 KICKOFF — THE MID BAND ON US-LAWFUL GROUND (fine entries, staged commits, real fees)

## ⛔ STANDING BAN (Greg, S57): BYBIT — NEVER USE FOR ANYTHING. MEXC = DEAD too.
No bybit data, tape, cells, citations, or proposals — ever. Record: `S57_VENUE_FINDINGS.md`.
If any bybit residue surfaces (branches, scripts, docs referencing it as active), strike it.

Read first: `SESSION_HANDOFF_2026-07-03_S57.md` → `S57_VENUE_FINDINGS.md` → CLAUDE.md S57 ban.
Branch: designated claude/*; reconcile to canonical 5c5vg9 (fetch + ff-only, else reset);
sync 5c5vg9 every push. Default branch = crons (bybit collectors DELETED; Coinbase book
collectors + paper_trade cron remain). Run `python scripts/paper_trade.py` FIRST (baseline
+N>=0, ledger 26,784+ intact; SANDBOX registry is EMPTY by design — no bybit cells).

## THE FRAME (Greg S57): fees are a fact now — no rebate venue is lawfully reachable.
The fine 2-4bp band is unharvestable at US fees. THE MID BAND (60-100bp swings) pays fees at
3-10% of swing. The S57-validated STAGED-COMMIT trigger (+2bp fine confirm; adds beat random
10-14 sigma on two venues; adds must be MAKER) is the lag-cheap confirm S55's mid-band
pricing was missing.

## JOBS
1. **THE MID-BAND MACHINE (Greg's pivot — "there's no other way"):** fine-flip starter at
   the turn -> staged adds (s0 small, +2bp maker adds, $10k cap) escalate into the ride ->
   fine-flip exit at the far turn. Score on Coinbase books (5 cells) vs the mid-band oracle,
   NO-GATES (net $/hr AND legs/hr), shuffle + reversed controls, fee COLUMNS = the real US
   menu: retail Coinbase spot tiers / mk0 (upgrade-window) / flat $0.15/contract perp model.
   Use `odcore/swing_accum.py` mechanics (Greg: "zig zag already has this designed — don't
   reinvent") through `platform.run_stream`.
2. **PROBE-THEN-COMMIT (Greg S57 close):** does trade i predict trade i+1? Measure per cell
   on the forward ledger + books legs: P(win_{i+1}|win_i), corr(net_i, net_{i+1}), and the
   probe->bail/all-in policy P&L at real fees vs flat + controls. (twxv4t's anti-martingale
   kill attaches to the OLD class — re-measure honestly; size-don't-skip.)
3. **Venue verification queue:** Kraken Pro US spot maker tiers (mk0 at ~$10M/30d organic?);
   fold any late eligibility-agent syntheses into S57_VENUE_FINDINGS; Coinbase CFM perp
   fee schedule from the CFTC filing PDF (maker rebate question UNCLEAR).
4. Greg (external): UI-delete data/{sol,eth}-bybit-book; decide whether to pursue the
   Coinbase fee-upgrade 60-day window when we have provable volume somewhere lawful.

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy; never pool sign-blind; NEVER tune off one
window; no-gates law (judge on net $/hr AND legs/hr — size, don't skip); sandbox-first;
one round = one defined test, renders read with Greg every round; git = source of truth;
falsification-first; VERIFY VENUE ELIGIBILITY BEFORE ANY VENUE WORK (the S57 lesson —
weeks of bybit work died on an unchecked jurisdiction gate).
