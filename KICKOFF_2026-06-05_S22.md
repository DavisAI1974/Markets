# KICKOFF — Session 22 (Markets) — 2026-06-05

Start here for the next Markets session. Branch: `claude/crypto-trading-platform-plan-MpqwG`
(DavisAI1974/Markets). Read `SESSION_HANDOFF_2026-06-05_S21.md` first, then this. The
SessionStart hook installs the OD toolchain (numpy/scipy/sklearn + PySR + Julia) and
materializes the real bins into gitignored `realbins/`.

## 0. Honest status to carry forward (do NOT relitigate without new data)
- The OD coupling layer is now wired through the whole platform (backend endpoints, frontend
  Coupling tab, executor sizing/gates) — but it is DIAGNOSTIC/shadow, not the live signal source.
- Re-verified S21: every unblocked OD signal LOSES net-of-cost (dipole_direction WF -11.8%,
  tautology z 1.2; ofi/momentum far worse). Don't promote a losing signal. The bar to clear is
  coded in `odcore/validation.py`.

## 1. FIRST PRIORITY — unblock the chem dipole (still the #1 lever)
Add `DavisAI1974/Basic_equations` to scope (also consider `od-engine`, `evolution`). Then port
`_markets_algebraic_dipole.py`, `s13_chemistry_residual.py`, `s12_coupling_decomposition.py`
VERBATIM into `odcore/` and DIFF against the reconstructions. CRITICAL: capture the EXACT channel
construction (what series H_a/H_b are entropy OF, window, normalization) — S20/S21 found real
buy/sell entropies near-symmetric => trivial identity, c~=0, so the original recipe almost
certainly uses different channels/preprocessing. Re-fit the QUADRATIC dipole + the H_a>H_b
predictor on real bins; validate with walk-forward + the tautology null (`scripts/od_backtest.py`,
`odcore/validation.py`).

## 2. "What to make OD the source" — the concrete sequence (Greg's S21 question)
EARN it first, then WIRE it. Order:
- (a) EARN: get at least one OD signal to clear the gate on real bins — net>0 after fees+slip,
  beats baseline + buy-hold under walk-forward (embargoed), tautology z >> 2-3, small
  random-vs-WF gap. Prime candidates: the real chem dipole (#1) and/or cheaper execution
  (maker-rebate venue or tick data — cross-venue reversion is real, tautology z 3.0-3.6, but
  cost-bound; Greg's open question #2).
- (b) WIRE (plumbing, all small, do only after (a)):
  1. Flow OD fields through `/api/signals` + `/api/stream`: attach
     `od_residual_fraction / od_dipole_r2 / od_leadlag_stability / od_walkforward_edge` to each
     SignalEvent from the CouplingStore snapshot. This also feeds the OD-native sizing already
     wired in `executor/executor.py::_size_notional` (currently receives nothing -> falls back to
     fixed size). Safe to do in SHADOW now even before (a) — it just annotates signals.
  2. Switch the emit decision in `api_server._poll_one` from "regime transition" to the OD
     generator's signal (or gate the regime signal by OD agreement). The OD generators already
     exist (`odcore.generators.make_od_generators`) and are in the `adaptive_backtester` pool;
     promote the rolling-Sharpe winner.
  3. THEN unwire PELT: flip the live backend chunker `mode="hybrid"` -> `"fixed"` (keep the PELT
     code for offline/research). The OD windowed operators define their own segmentation
     (window/stride), so PELT leaves the signal path. This is why the kickoff pairs them — do it
     only once OD is the source.
- DEFERRED DECISION: the PELT unwire (step b3) is on hold until (a) is met (asked Greg S21; his
  reply was the question itself). Don't flip it before OD earns the source role.

## 3. Continue the build (lower priority than 1-2)
- Backend: flow OD fields through signals/stream (step 2.b.1 above).
- Frontend: the Coupling tab is in; consider wiring OD context into SignalCard once fields flow.
- Executor: env-gated ccxt adapter (PaperExchange stays default); the OD sizing + circuit breaker
  are in but OPT-IN (set `od_sizing`, `max_consecutive_losses` per-source in executor config).

## 4. Greg's open questions (BUILD_QUESTIONS.md)
- Maker-fee/rebate venue or tick data? (would change the cost math — the path to flip the
  cross-venue reversion positive).
- One labeled known-law fixture to prove PySR rediscovery? (only synthetic; currently omitted).

## 5. Loose end
Delete the stale `claude/session-upload-handoff-cy7wQ` remote branch (a redundant master-context
fold on a base missing odcore/; remote delete failed with a git-proxy network error in S21).
