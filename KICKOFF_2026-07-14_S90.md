# KICKOFF S90 — Kalshi. Finish the RAW year to S3, then rework scoring to read the raw tape.

Run the **`kalshi-session-start` skill FIRST** (branch check, read order). You'll likely land on the
stale S70 tip `3c70ff5` -- `git log --oneline -1`, then rebase onto the trunk
`claude/kalshi-s79-kickoff-ij8t9o` (= repo default; pull/rebase before push).

READ (in order): `SESSION_HANDOFF_2026-07-13_S89.md` -> `research/kalshi/AWS_INGEST_SETUP_S89.md` ->
`CLAUDE.md` -> `KALSHI_TRADING.md`.

## Where S89 left it
The durable RAW-ingestion is BUILT and the full-raw MBP-10 year (CL+NG, 2025-07..2026-07) is being
pulled to an AWS S3 bucket, month-at-a-time, zero-filter. Bucket
`bento-568968024170-us-east-2-an` (us-east-2), prefix `nymex/` -> `nymex/nymex_cont/{CL,NG}_YYYYMMDD.jsonl.gz`.
Split: the S89 container ran Jan-Jun 2026, Greg's machine runs Jul-Dec 2025 (disjoint). Resumable.

## SECRETS to re-paste (not in git)
- `DATABENTO_API_KEY` (db-...)
- `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` (IAM user `Claude`), `AWS_DEFAULT_REGION=us-east-2`
Greg pastes these. Without them you cannot monitor/resume the pull.

## JOB 1 — finish + verify the year
1. `pip install databento boto3 pandas`.
2. List the bucket, see which months landed:
   `python3 -c "import boto3;print(sorted({o['Key'].split('/')[-1][:9] for o in boto3.client('s3').list_objects_v2(Bucket='bento-568968024170-us-east-2-an',Prefix='nymex/nymex_cont/').get('Contents',[])}))"`
   (with AWS_* exported).
3. Resume any missing months: `python research/kalshi/pull_year_mbp10.py --start <YYYY-MM> --end <YYYY-MM>
   --dest s3://bento-568968024170-us-east-2-an/nymex --scratch /tmp/nymex_scratch` (skips months already there).
4. VERIFY a sample day end-to-end: download one gz from S3, confirm 76-field raw rows (all 10 bid+ask
   levels + action/side/depth/flags/sequence/ts_event/ts_recv). See AWS_INGEST_SETUP_S89.md "Verify".

## JOB 2 — rework the scoring to read the RAW S3 tape (Greg S88 principle)
`month_characterize.py`, `bucket_continuation.py`, `forecaster_month_pass.workflow.js` are SCAFFOLDING
that pre-processes on the ingest side. Move gating/derivation to the TRADE-SIGNAL side; the scoring reads
the FULL raw tape from the bucket. Add an S3 reader (stream a day's gz from
`s3://bento-568968024170-us-east-2-an/nymex/nymex_cont/`), keep leakage gates, per-cell distributions,
$/c never bps. The agent sifts the raw tape for driver->price correlations.

## Rules
git is source of truth (code/docs; NOT the tick corpus -- that's on S3 now). No emojis. Branch
discipline. DATABENTO_API_KEY + AWS keys are SECRETS. Weather forecaster = Greg's spec, HANDS OFF.
Historical data RAW / keep-all-info / gates only on the trade side.
