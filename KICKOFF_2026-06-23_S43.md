# KICKOFF — S43 (next session)

Branch `claude/crypto-liquidity-signals-5c5vg9` (S42 pushed; dev here). Read `SESSION_HANDOFF_2026-06-23_S42.md`
then the S42 delta atop `CLAUDE.md`. Rules: crypto only; zero synthetic; per-cell deploy; never pool sign-blind;
git is source of truth; commit+push regularly; never tune off one window; falsification-first. Method note (Greg):
run the agnostic 5-step coupler and read what it couples — do NOT impose a direction (`coupling_scanner.score_pair`
/ `dipole_trade.trade_coupling_vector`, all 5 steps in order).

## State inherited (committed, S42)
- **Liquidity is the layer with structure.** `depth_imb` (book-depth imbalance, top-K) predicts the SIGNED next
  move: OOS_r +0.164 next-cell, null_z +60, 63% hit; LEADS price +0.1s; survives the tautology null in 7/10
  time-slices (NOT a fluke). Symmetric withdrawal is NULL for direction; content is in the imbalance. Moves are
  sub-bp -> a MAKER/quoting signal, below the active fee floor.
- **QUIET FLOOR gate (`odcore/quiet_floor.py`).** Chat's quiet AR(1) relaxation (φ_quiet=0.944) used as a gate:
  fire only when `|innov|>k·σ` (imbalance breaks the floor). Silent between trades (opens 2.3× more on shock
  cells), keeps the level's 62% directional hit. Stops the dipole churning through trends. Leakage-safe, fit per cell.
- Single cell/window (btc_coinbase, 11.67h) = characterization, NOT deploy-grade.

## Open tasks (priority)
1. **More book data — THE bottleneck.** `book_collector_btc.yml` GHA accrues `data/btc-book` but needs Greg's
   manual "Run workflow" click (my token 403s on triggers). Add other venues/coins (the collector exists only for
   btc_coinbase). Multi-day/multi-venue book is required before ANY book finding is deploy-grade.
2. **Re-run per cell once multi-cell book exists:** `_liquidity_dive.py` (confirm depth_imb edge + sign/lead are
   stable across venues) and fit a `QuietFloor` per cell. Cross-segment consistency here is the stand-in until then.
3. **Maker-fill / queue-position model** — turn the 63% next-cell signal into a net-of-rebate quoting edge (the
   realistic path to monetizing a sub-bp predictor: which side to post, when to pull, adverse-selection).
4. **Wire the QuietFloor gate into the live dipole** (gate decides WHEN to fire; the imbalance level decides
   DIRECTION) so the production dipole stops firing through trends.
5. OD-BOOK committed T_test (`research/od_book/`) once book is multi-day. Cross-domain falsification still open.

## Cautions
- realbins + `/tmp/book.jsonl.gz` + PNGs gitignored; re-extract the book from `git show origin/data/btc-book:btc_coinbase_book.jsonl.gz | gunzip > /tmp/book.jsonl.gz`.
- GHA runs need Greg's manual trigger. Avoid backticks in `git commit -m`.
- dipole_r2~1 on signed channels is the H_a=−H_b tautology; use structure_z (tautology-controlled), not dipole_r2.
