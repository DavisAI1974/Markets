# S56 KICKOFF — GATE THE ARMING RULE (S55's armed fine-confirm zigzag printed positive on all
# 20 coin×ARM cells with reversed negative — now it faces the gate that kills things)

Read first: `SESSION_HANDOFF_2026-07-03_S55.md` -> `S55_WALKTHROUGH_NOTES.md` (all 15 rounds,
numbers) -> CLAUDE.md S55 delta. Branch: designated `claude/*`; reconcile to canonical `5c5vg9`
at start (`git fetch origin claude/crypto-liquidity-signals-5c5vg9 && git merge --ff-only` —
harness cut stale? `git reset --hard` to it); keep 5c5vg9 synced on EVERY push. Default branch
`claude/new-session-o3vnm` carries crons — LEAVE IT.
Run `python scripts/paper_trade.py` FIRST (standing; it now runs THE ONE VERSION —
odcore/platform.py — and must print +N>=0 with the 25,845-trade ledger intact).

## DATA (/tmp dies with the container)
- Bybit perp 30d bins (~40min, parallel):
  `for s in SOLUSDT ETHUSDT BTCUSDT DOGEUSDT XRPUSDT; do python backfill_bybit.py --symbol $s
   --days 30 --bins-path /tmp/backfill/${s}_30d_bins.json & done`
- Coinbase books (~2min): `for c in sol doge xrp eth btc; do git fetch origin data/$c-book;
  git show origin/data/$c-book:${c}_coinbase_book.jsonl.gz > /tmp/${c}_coinbase_book.jsonl.gz; done`
  (+ sol/eth `data/<c>-bybit-book` -> /tmp/<c>_bybit_book.jsonl.gz)
- Binance SPOT: `backfill_binance_spot.py` (canary PASS S54; never yet swept) — for JOB 2.
- Loader: `scripts/_s54_backfill_sweep.py::load_bins`. ⚠ BTC Coinbase book = 155h gap of 196h.

## THE PLATFORM (S55 — everything runs through it now)
`odcore/platform.py` = the ONE decision layer (paper=live=research): `run_cell` (deployed cells,
sizing ALWAYS on), `run_stream` (ANY flip stream -> the same executor/sizing/descriptors),
`DEPLOYED` registry. `paper_trade.py` = thin harness. Executor has: cover-grace (S48),
lean-collapse exit (S55R8, opt-in), `exit_gate` = structure-says-out/FLAT (S55R13),
`fill_mode="taker"` for bins (S55R12, equivalence-proven), dipole entry gate socket fed
(`--dipole-entry`). Ledger rows carry the dipole descriptors (R1). ⚠ entry_gate=False = HOLD
not flatten (S46 doc/code mismatch, corrected S55) — use exit_gate to go flat.
NEVER fork mechanics in a probe again — feed flips + gates to the platform.

## JOB 1 — THE FULL GATE on the arming rule (the whole job; nothing adopts without it)
`scripts/_s55_armed_zigzag_probe.py::armed_fine_zigzag(mid, ARM, 25)` — two-stage confirm
zigzag (extension >= ARM arms during the ride; first 25bp fine reversal confirms at the turn).
S55 first pass: positive 20/20 cells, reversed negative, net/leg = the R14 lag projection.
Gate it exactly like S54 killed the big line:
  a. shuffle (joint price+flow) + reversed + per-week splits x 5 coins, z-stats per cell —
     n is THIN outside SOL (eth had 2 legs at ARM>=75): widen the leg count first (ARM sweep
     finer {40..150}, fine sweep {15,25,35}, and/or pool weeks properly). NO tuning off one
     window; report the grid, not the best cell.
  b. DIPOLE-GATED variant: R4 reversal-class / rev_conv veto on the post-arm dips (the known
     flaw: entries fire on the FIRST post-arm dip, often mid-leg). Judge NO-GATES style:
     net $/hr AND legs/hr, gated vs ungated.
  c. maker tiers (mk0 / MM3 −1.25bp) as columns; sized column via platform size_legs (axes
     still unearned at coarse — flat is the base claim).
  d. leakage gate (`odcore/leakage.py assert_no_leakage`) on any new feature before wiring.
  PASS -> promote armed_fine_zigzag into odcore (platform component) + add a sandbox paper cell.
  FAIL -> the middle band stays priced-but-locked; document which prong killed it.

## JOB 2 — R4 descriptor second venue (Binance spot 30d x 5) — the queued round-4 completion.
## JOB 3 — infra: BTC book collector 155h-gap cause + Bybit book cron (same 5.83h window since
S52) — both on the default branch; both block deploy-cell truth (and Greg's BTC-wins guess).

## Greg standing
Bybit MM application (institutional_services@bybit.com) — the S55 zz150 leg table flips positive
AS-IS at MM3 −1.25bp (+13bp/leg shift); the armed zigzag's taker numbers roughly double.

## THE MONEY MAP (S55, for orientation — full numbers in the handoff)
Fine zigzag: +$132/hr front-of-queue optimistic / ~$19/hr honest capacity (S50), fill-capped.
Coarse: capture parity (36%) but 0.4 legs/hr — its lever is SIZE. The MIDDLE BAND (Greg's pencil,
theta 60-100): theta-confirm loses −25..−40bp/leg = unharvested; at measured fine-confirm costs
+$2,900-3,700/day 5-coin @$5k flat (PROJECTION, no fakeout cost — the arming rule's false-arm
rate is the whole question = JOB 1).

## DEAD — S55 adds (full lists in S54/S53)
Two-scale v0 (free-running fine exits, −80/hr) + v1 trailing-X (churn); entry_gate-as-flatten
(never existed — HOLD semantics); coarse dive_depth->|move| (sign-inconsistent 3/5); coarse
sizing axes as validated (unearned, shuffle-muddied); lean-exit at FINE scale (inert — the flip
detector already IS the lean-collapse exit there). All S54 DEAD stands.

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy; never pool sign-blind; NEVER tune off one window;
one round = one defined test (Greg S55 pace rule); git = source of truth; falsification-first;
sandbox-first (variants -> paper_ledger_sandbox.jsonl, gitignored); fees from primary sources;
Bybit REST geo-blocked (CDN dumps fine); pip install matplotlib, MPLBACKEND=Agg; token 403 on
GHA dispatch.
