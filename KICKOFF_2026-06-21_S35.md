# KICKOFF — finish the build: the live per-cell FINGERPRINT winner-predictor (S35 reframe)

Open in worktree `E:\Markets\.claude\worktrees\xenodochial-montalcini-f21fb6` (branch
`claude/crypto-trading-platform-plan-MpqwG`). `CLAUDE.md` auto-loads (S35 delta top). Read order:
`MISSION.md` → `CLAUDE.md` (S35 top) → `SESSION_HANDOFF_2026-06-21_S35.md` → this file → `BUILD_PLAN.md`.
Memories auto-load: `bucket-distinctiveness-is-the-goal`, `markets-deploy-feature-parity-gap`,
`markets-box-workflow-constraint`, `tools-are-complementary-not-competing`,
`deploy-signal-per-cell-not-universal`, `markets-dipole-deconfound-verdict-s34`.

> NOTE: this kickoff was REWRITTEN mid-S35. The original plan (Direction-2 balanced re-run → net-of-cost
> on a win/lose STACK separator) is SUPERSEDED by Greg's reframe below. Discovery is DONE; the balanced
> re-run ran (separation view null — the wrong lens). Do not re-run the separator/net-cost line.

## THE FRAME (Greg, S35 — this is the whole point; see `MISSION.md`)
PREDICT WINNING TRADES by their DISTINCTIVE FINGERPRINT, as EARLY and as ACCURATE as possible. The
fingerprint is built by STACKING the whole complementary toolkit — the dipole (128-dim OD coeffs), the
**6 micros** (mean_dipole, dipole_acl1, volume_zscore, trade_present_score, trade_recent_2chunk_bps,
trade_from_onset_bps), refrag, and any other BUILD_PLAN tool. Buckets = **asset x venue x side** (coins
differ across venues, coins differ, buy != sell → never pool). Within a cell, WINNERS vs LOSERS differ in
HEAD vs TAIL pressure (onset/head vs recent/tail) — that is what makes winners fingerprintable.
Do NOT balance classes, average into win/lose centroids, or grade by separation AUC — the dipole's
separation strength is irrelevant. The stack of tools STAYS (it is the fingerprint); only separation-grading
is dropped. Report + deploy PER CELL; partial coverage is success on the cells it works.

## STATE
- Discovery DONE: ~1,919 distinctive cand_sp coeff signatures across all 12 `markets_<pair>_win_cand_sp\`
  domains (every cell meated). Scheduled task `markets_cand_disc` is `Ready`; `schtasks /delete /tn
  markets_cand_disc /f` to clean up.
- The build = RECOVER + PORT the live fingerprint encoder. **Port spec already RECOVERED verbatim from git**
  (see `SESSION_HANDOFF_2026-06-21_S35.md` "STEP 1 RECOVERED"): heavy tier (128-dim coeff + centroid margin)
  is live+validated in `_markets_gate_v2.py`; the cheap feature math (`_signed_bps`, head/tail bps,
  `_present_trade_score`, `dipole_acl1`) is at `backend/api_server.py:932-994` and
  `mock_trade_replay.py:873-890,973-975` @ commit c486d3b, using `MarketChunker`/`MarketChunkEncoder` that
  already exist in `markets_adapter.py`.

## BUILD STEPS (pick up here)
0. **DIAGNOSE the reproduction gap (IMMEDIATE next):** the encoder + canary are BUILT and `signed_bps` is
   verified exact, but the canary's recomputed micros do NOT match stored values (5/48; large + systematic —
   bps off 10-100x, wrong sign, chunk_id mismatch; see handoff "STEP 2 RESULT"). Find the missed-winner
   audit-gen code (how it built `visible_bars` + picked the chunk per trade; likely stripped to git like
   `mock_trade_replay`) and settle: (1) window/bar-source mismatch (benign — normalized realbins? different
   window? chunk by id not "last"?) vs (2) LOOK-AHEAD in stored micros (serious — can't fingerprint
   pre-entry). `chunk_id` (embedded in each `source_id` as `ASSET|venue|<chunk_id>|side`) confirms the exact
   window. Do NOT wire the encoder until the canary passes.
1. **DONE — Live fingerprint encoder** (`odcore/fingerprint.py`): verbatim ports (`signed_bps` /
   `trade_score_band` / `present_trade_score`) + the MarketChunker(30/15/10,hybrid)+MarketChunkEncoder recipe;
   the 3 head/tail bps + mean_dipole/volume_zscore/dipole_acl1 + the `present_trade_score` composite. Combine
   with `GateV2` for the live 128-dim coeff + centroid margin once the canary passes.
2. **DONE — Canary** (`_canary_fingerprint.py`): recompute micros from real pre-entry bars vs stored.
   Currently FAILS reproduction → step 0. (`signed_bps` math itself is unit-test verified.)
3. **Per-cell fingerprint predictor:** score a live trade's fingerprint by DISTINCTIVENESS (match the cell's
   distinctive winning-trait profile), not a balanced-class separator. Wire as a `SignalGenerator`
   (adaptive_backtester.py:52) via `make_od_generators` (odcore/generators.py:68) → `AdaptiveSelector` →
   OD-native sizing `od_size_fraction` (odcore/sizing.py:29). Deploy PER CELL.
4. **(enhancement) head/tail PRESSURE:** the recovered head/tail features are PRICE bps; add order-flow
   imbalance (pressure) at head vs tail to the fingerprint.

## BACKLOG (after the build plan — Greg parked it)
- SENTINEL-style fingerprinting SWARM for trading (`sentinel-swarm-for-trading-backlog`): same paradigm as
  the 10k-agent pathogen-attribution swarm. Roles: ensemble matching, trait-discovery swarm, adversarial
  pre-trade verify, per-cell specialists; offline 10k-scale discovery vs lean fast runtime ensemble; markets
  noisier than pathogens so keep walk-forward/tautology discipline. Review `E:\sentinel\` when revisited.

## SCOPE / RULES (hard)
- Crypto trading platform per `BUILD_PLAN.md` only. Quote service OUT. Zero-synthetic (no synthetic trading
  data). Fragile 4-CPU box — direct work + canary over big workflows. Do NOT branch-switch shared `E:\Markets`.
  **Commit + push working files (code/docs) REGULARLY — git is the source of truth** (`git-is-source-of-truth`);
  the old "no commit/push unless asked" rule is REMOVED (Greg S35). Data/bins stay LOCAL (too big for git;
  `markets-data-lives-local-not-git`). Every new build gets a `MISSION.md` (`mission-statement-per-build`).
