# KICKOFF S80 — Kalshi, build the release-triggered book signal

**Read in order:** `SESSION_HANDOFF_2026-07-12_S79.md` → CLAUDE.md S79 delta (top) → this file.
(Background for the pivot: `SESSION_HANDOFF_2026-07-12_S78.md` + `KALSHI_BUILD_SCOPE.md`.)

**Branch:** `claude/kalshi-s79-kickoff-ij8t9o` (all S71–S79 code present; PUSHED).
Continue on this branch (or the S80 branch if the harness assigns one — ff from this tip, don't cut from stale).

## Where we are (end S79)
- Full Kalshi pipeline validated TURNKEY on live data (collector, news→tag, coupling `--source kalshi`, score
  harness). Consensus accrual kicked off + wired into the durable workflow. Event-weight study committed.
- The intellectual result that reorganized the plan (S79, load-bearing — the MERGED signal architecture):
  **news release = CATALYST + coarse size; book imbalance + dipole exhaustion = the DIRECTION + magnitude read.**
  The surprise number cannot call direction (proven null, hit 0.52); the book does. Build the release-triggered
  book signal, NOT a surprise→move regression.

## FIRST: check data + freshen
- `git ls-remote --heads origin data/kalshi-bins` — if present, `git fetch` it and gunzip `<SERIES>_bins.jsonl.gz`
  + `consensus.jsonl.gz` into `data/kalshi/`. (Durable run #4 was pushed at end of S79; the recurring 6h cron needs
  the DEFAULT-branch click — see below.)
- Poll consensus: `python research/kalshi/consensus_poll.py` (upserts `data/kalshi/consensus.jsonl`; run it near
  releases — before for forecast, after to join actuals from EIA/BLS).

## THE BUILD (S80 main) — release-triggered book signal (merged architecture)
Build the direction+magnitude reader that fires on a release, on the accruing Kalshi order-book probability series:
1. **Direction = book imbalance SIGN** via `research/shape_s71/early_signal.book_imbalance()` on the unified YES book
   (the collector already folds NO→YES so early_signal consumes it unchanged).
2. **Magnitude-class + fade = book imbalance MAGNITUDE + dipole EXHAUSTION** via `odcore/info_dipole.divergence()`
   (exhaustion = dipole collapsing toward 0.5). Lopsided un-exhausted book → big move w/ room; exhausting book →
   small move / fade (this is the sell-the-news catcher).
3. **Catalyst + gate = the release calendar** (consensus_poll dates): arm the signal into the release; coarse size
   from |surprise| (a lot/a little — a RANGE at most, no precise regression); `PAUSE_NEW_ENTRIES` through the
   first-seconds spike (gap risk), then read the book on the decay.
4. **Test (needs accrued order-book bins around a real release):** does book imbalance at the release call the
   direction, and does exhaustion call the fade? Per-contract, placebo-baselined, provisional-until-live.
- Mandatory before wiring: `odcore/leakage.assert_no_leakage` (strictly pre-decision features).

## GREG'S CLICKS
1. **Make `claude/kalshi-s79-kickoff-ij8t9o` the repo DEFAULT branch** → the 6h cron fires → continuous accrual of
   bins + consensus (the data the book signal + coupling/score tests need). This is THE unblock. (Default is
   currently `claude/new-session-o3vnm`.)
2. RSA demo creds for `demo-api.elections.kalshi.com` (paper loop, later).

## ALSO QUEUED
- Coupling test with REAL street-consensus residual surprise (vs the seasonal proxy) once consensus + bins span a
  release; `kalshi_score.py` with `--lead-minutes` baselines.
- OD-weather forecaster → `kalshi_score.py` (it IS a storage-print forecaster, R²=0.85 → candidate direct edge on
  any storage-NUMBER-settled contract; honest scope = beat climatology/persistence/MOS).
- AGA pre-2002 natgas storage history extension (bonus N; AGA=Wed vs EIA=Thu timing).

## STANDING RULES (S79, carry forward)
- Score events PER BUCKET (surprise×reaction), never pooled |move|.
- Merged architecture: surprise = catalyst + coarse size; book imbalance + exhaustion = direction + magnitude.
- Release-day quiet→spike→decay: measure INTRADAY not daily; pre-quiet = PAUSE trigger; stand aside through spike.
- Two-phase: underlying = event impact (prior); Kalshi bins = transfer-function calibration.
- Weather = ROOT signal for weather+electricity+natgas (double-humped demand); honest scope = local
  operator/bias-correction, beat climatology+persistence+MOS, not out-forecast ECMWF.
- Per-contract deploy; provisional-until-live; annualized-return not $/hr; never size off one window; zero synthetic.
- `news_coupling_research --source kalshi`: `--events` is joined onto `--data-dir` → pass the BASENAME.
