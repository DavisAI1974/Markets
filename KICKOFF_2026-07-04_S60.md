# S60 KICKOFF — THE KRAKEN TAPE VERDICT + PIECE 2 (EXIT) OPENS

⛔ STANDING BANS unchanged: BYBIT never; MEXC dead. ELIGIBILITY BEFORE ANY VENUE WORK
(`S57_VENUE_FINDINGS.md`).

Read first: `S59_NOTES.md` + `SESSION_HANDOFF_2026-07-03_S59.md` (entry DoD promotion,
canary record, kr_mk0 re-price, Kraken data machine) -> `S58_ENTRY_NOTES.md` for the
entry-piece record (mistakes ledger, five-verdict board, PARKED exit/hold items).
Branch: designated claude/*; reconcile to canonical `5c5vg9` tip FIRST (S58 AND S59 were
both cut from the wrong default/crons parent — fetch + ff-only else reset --hard);
sync 5c5vg9 every push.

## STATE (S59 close)
- **ENTRY = DONE.** `odcore/entry_coinbase.py` + `COINBASE_MIDBAND` registry promoted,
  canary 3/3, sandbox forward record accruing via the paper cron (baseline ledger pure).
- **kr_mk0 is real and every surviving config is paper-positive at it** (~+$6/hr
  best-per-coin) — but that's Binance-instrument gross. Two unknowns remain: Kraken TAPE
  (historical, pulled) and Kraken FILLS (live books, accruing).
- Fingerprint micros-tier dual-print = NULL (recorded); 5th member narrows to the S35
  encoder tier (onset canary + Greg's E: drive archives are the gates).
- FINE-GRAIN untouched (standing: it returns later).

## JOBS (one round = one defined test; update S59_NOTES or successor EVERY round)
1. **KRAKEN TAPE MACHINE RUN (first)**: 30d Kraken bins in /tmp/kraken_backfill
   (re-pull if /tmp died: `python backfill_kraken_trades.py --pair {SOLUSD,XBTUSD,XDGUSD,
   XRPUSD,ETHUSD} --days 30 --bins-path /tmp/kraken_backfill/<PAIR>_30d_bins.json` —
   sequential, ~1 req/s, checkpointed, a few hours). Run k0 machines at registry thetas
   + kr_mk0 fees (0/10) per cell. Flow maps RE-DERIVED on the Kraken tape if at all
   (venue law) — never ported. Verdict per cell: does the mid-band gross exist on
   Kraken's own prices?
2. **PIECE 2 — THE EXIT (open):** R8 lean-collapse exit (~123bp/side prize at coarse
   theta) through `platform.run_stream` (`dipole_exit` socket already exists). Baseline =
   the promoted mid-band machine's flip-at-next-confirm exit. Same discipline as entry:
   one round one test, all controls, worst-10 renders with Greg, fee columns incl. kr_mk0.
   **RUN IT WITH THE AGENT FLEET — read `AGENT_PLAYBOOK_PIECES.md` FIRST** (the S58 entry
   roles + rules + the exit-piece translation: exit leg dump -> scrap-heap miner ->
   per-coin exit agents w/ falsification duty -> re-earn as machines -> books check).
3. **Kraken books accrual check** (data/<coin>-kraken-book x5; first segments from the
   6h cron or Greg's click). When windows exist: taker-share + fill-depth reality at
   kr_mk0 cadence (the fine-band conditions 1-3 start here too).
4. Coinbase accrual: BTC mid-band activation check (btc_mb80 flips active when books
   deep enough); DOGE clmxexh per-venue validation as windows accrue.
5. Background: Jul-9 Kraken schedule post-confirm; Greg's CFM tier tab read.

## DATA (/tmp dies): Coinbase books x5 from data/<coin>-book branches; Binance 30d bins
via backfill_binance_spot.py x5 parallel (~40min); Kraken 30d bins via
backfill_kraken_trades.py (sequential, hours — start it FIRST).

## Rules (standing): crypto only; zero synthetic; per-cell deploy; never tune off one
window; graded beats gated; scale-locality; venue law (no flow read ports without
per-venue pass); sandbox-first + baseline canary bit-identical for any platform change;
git = source of truth; falsification-first; MPLBACKEND=Agg; token 403 on GHA dispatch
(Greg clicks Run workflow).
