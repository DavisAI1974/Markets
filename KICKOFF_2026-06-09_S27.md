# KICKOFF — Session 27 (next chat) — written 2026-06-08 (end of S26)

Branch: `claude/beautiful-shaw-040328` (OD/coins line; pushed to remote). Plan: `BUILD_PLAN.md`. Master: `CLAUDE (5).md` (S26 sections). Live state: `STATUS_S26_async.md`.
Memory auto-loads: `dipole-real-on-128dim-per-pair`, `s26-async-state`, `pysr-julia-local-toolchain`, `markets-canonical-plan`, `follow-canonical-plan-rule`.
KEEP BUCKETS SEPARATE (per-pair, never pool); zero synthetic data; Result Discipline.

## STATE COMING IN (S26 close)
- **(A) DONE — dipole validated at scale.** 128-dim per-pair, honest tier cs100: mean acc 0.947, perm-null **z=+9.6**, 12/12 z>3; deterministic grouped split also holds (~0.945) = not fold-optimism. Harness + results committed (`od_larger_set_val.py`, `od_larger_set_results.json`).
- **(B) STOPPED + ROOT-CAUSED — the 16k coeff-gen was too slow.** cs1075 run hit ~9 s/trade (vs ~2.3 at small scale) → ~30-40h. Cause: refrag `OperatorOrchestrator` accumulates a per-domain evidence graph rewritten+grown every trade (+ global index rewrite + closed-loop refinement + 4.8GB artifacts). KEPT on disk: `markets_btc_bybit_buy_win_preentry_cs1075` 482 + `_lose_` 726. Full analysis: `E:\refrag\docs\_PERF_orchestrator_slowdown_S26.json` (+ Factory KB copy).
- **Quote service** (separate workstream; Greg leads): plan + kickoff written and pushed to the phase-2 branch (`QUOTE_SERVICE_PLAN.md`, `KICKOFF_QUOTE_SERVICE.md` on `claude/continue-phase-2-pipeline-UFiGY`).

## TODO — EVERYTHING THAT NEEDS TO GET DONE

### 1. OD coin coeff-gen — BUILD THE CHUNKED DRIVER (the blocker)
**Set this up as a `Workflow`** (the multi-agent orchestration tool — Greg's call): drive the loop deterministically — build lists → run each interleaved 100-trade batch → clear evidence graphs → resume — so it's resumable, parallel where safe, and observable. Implement the **chunked clear-every-100-TRADES, interleaved-across-ALL-buckets** generation (this is the slowdown fix; design in the analysis JSON + STATUS):
- Build trade lists for ALL 24 btc/eth buckets at the target size; coefficients write to per-bucket dirs (keep separate).
- Generate in **100-trade batches**, each batch holding ~equal share of EVERY bucket → (a) balanced partial progress if it stops, (b) tiny between-clear accumulation.
- After each 100 trades: **DELETE `E:\refrag\discoveries\evidence_graphs\*_evidence.json`** (safe — orchestrator starts fresh on a missing graph) to reset per-trade cost to ~2.3 s. Optionally compact `discoveries/index.json`.
- **Resume-safe**: skip already-done trades (the kept 1208 + any new) by UUID/trade key.
- **Re-validate** the result with `od_larger_set_val.py` permutation null (expect z ~ +9.6). Caveat: clearing drops cross-trade learning, so coeffs may differ slightly — re-validate, do NOT assume identical (they should be closer to the validated cs100 regime).
- (optional pre-check) confirm whether the closed-loop `max_iterations` (default 100000) is also a material per-trade cost.
- THEN: **doge/link/xrp** — needs the UPSTREAM trade-gen first (raw bins in `live_data_history`; NO eligible pools yet; chain is refrag-bound via `markets_bar_loader`/orchestrator → runs on this machine, not cloud). THEN: **44h full-set** (all 57,017) in chunks.
- **Net-of-cost PnL still owed** (per-trade JSONs have no net_bps/timestamp; join `summary.json` by run_id, or use the full pipeline).

### 2. KNOWLEDGE-BASE POLICY (Greg) + sync
- **POLICY: every knowledge json gets 2 copies — Factory (`F:\Factory\knowledge`) AND OD's KB — plus refrag if it gets one.** OD's KB is SEPARATE from Factory and will have a LOT more (it's the primary). That's expected/fine.
- **OD's KB = `E:\refrag\discoveries`** (Greg confirmed). So the 2 copies = Factory (`F:\Factory\knowledge`) + OD (`E:\refrag\discoveries`), plus refrag (`E:\refrag\docs`) if it gets one.
- **SYNC TASK:** once OD KB location is set, copy the OD-relevant Factory jsons into OD too (Factory has ~1477 jsons incl. od-domain dirs: bioage_od, crypto_od, grid_od, uews_od, refrag, Markets). Add the OD copy of `_PERF_orchestrator_slowdown_S26.json` (currently in `E:\refrag\docs` + Factory).

### 3. QUOTE SERVICE (separate; Greg leads, after the coins)
- Open on `claude/continue-phase-2-pipeline-UFiGY` → read `KICKOFF_QUOTE_SERVICE.md` → follow `QUOTE_SERVICE_PLAN.md`. Answer its 6 open questions (maker-rebate access, paper fee assumption, Bybit-perp, decoupling threshold, live-exec location, dipole bar).

## ENVIRONMENT
- PySR/Julia: fixed + hooked on THIS (OD) branch (`scripts/session_start.sh`). The quote/phase-2 branch needs NEITHER (odcore import is PySR-free).
- `E:\Markets\.claude` exFAT-locked; work from refs/worktrees. `E:\refrag` + `F:\Factory` are freely writable.
