# KICKOFF - S99 (paid-data discussion FIRST, finish the gate, then G12)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-20_S98.md` -> `research/kalshi/DATA_GATE_S98.md`
(THE build list, feeds A-T with inline STATUS, gate-closure checklist at the bottom) -> the
DISCUSSION PACKET: `research/kalshi/EIA_BALANCE_OPTIONS_S98.md` + `LNG_FEEDGAS_SIZING_S98.md` +
`KALSHI_NG_COVERAGE_S98.md` -> `research/kalshi/knowledge/ng_brain.json` (**s100.3, 23 plays** -
the C2 rescope is the newest merge; its forward test rides G12).

Branch: `claude/ng-coach-agent-loop-5ha5bf`. Confirm the tip message begins "S98 close-out"
(`git log --oneline -1`); if the harness cut a stale tip:
`git fetch origin claude/ng-coach-agent-loop-5ha5bf && git reset --hard origin/claude/ng-coach-agent-loop-5ha5bf`.

---

## ITEM ZERO - THE PAID-DATA DISCUSSION (Greg, load-bearing: BEFORE any build)

Inputs all delivered (see the packet): the STEO-vintage find (free monthly as-of balance, 1-34d
stale), J's costed vendor survey (only PipeRiv publishes prices), N's dead-free-weekly finding,
the S as-quoted candidate, the Databento scope note (IFUS/NDEX/IFEU available, no fundamentals).
THE PROCUREMENT POLICY (standing): free-first -> build the free composite -> MEASURE its coverage
per-date, gaps named -> pay ONLY for measured gaps. Decide: subscribe / request quotes / decline /
defer, and which builds the decision gates.

## Env on-ramp

- `scratchpad/aws.env` carries ALL THREE keys, rotated + verified S98 (AWS / DATABENTO / EIA).
  Verify with Greg the OLD AWS key was deactivated in the console. Rotations: Greg edits the file.
- `pip install boto3 databento matplotlib pandas`
- `python research/kalshi/forecast_harness.py --selftest` must PASS (17-block decision_state);
  `python research/kalshi/forecast_harness.py audit-joins` must print 0 violations / 101 days.
- DATA: `python research/kalshi/platform_sync.py list` = the S3 inventory; `pull --prefix X`
  restores any store. DO NOT RE-PULL what S3 holds. git = CODE, S3 = DATA, local = cache.

## JOBS AFTER ITEM ZERO (order matters)

1. **Feed T - STEO vintage balance** (store + `steo_vintage_asof` + selftest; JOIN ON RELEASE
   DATES - the workbook Last-Modified trap is named in the gate).
2. **Feed A phase 1 - cycle-level MOS as-of** (hour resolution; the raw archive likely already
   holds the cycles; the weekend 00Z/12Z gap is the walk's most reproduced residual). Note the
   two documented builder traps (index overwrite; normals cache ignores the window).
3. **Feed I - NG options on GLBX** (Databento authorized): phase i = OI-by-strike/pin map + opex
   dates. **REQUIRED FOR G13.**
4. **Feed M - lag/fill/fee on the KXNATGASD life** (store on S3 `kalshi/`): execution economics
   ONLY - the lag is ESTABLISHED, never retest. Fill-tradeoff map per strike; the S81/S82
   size-vs-fee wall is the binding constraint on this leg. Book depth: live-collector books only
   (no historical book endpoint exists).
5. **Feeds Q + R** - EIA-930 grid stack (solar displacement measured; T+2, revision mechanics
   measured first) + outages (EIA `nuclear-outages` DAILY API route 2007->present; ISO aggregate
   outage reports second; NRC scraping NOT needed).
6. **Tier 3 doctrine proposals** (ALL printed to Greg before merge): usage guidance per source
   family, flip driver checklist, evidence-day registry, two-books scoring split (day-book
   primary), squeeze-regime doctrine, TWO_COACH_SPEC off feed M's measured numbers.
7. **THEN G12** (Sun Feb 1 - Fri Feb 13 2026), one-shot blind per the gate-closure checklist.
   Roll check via a SUBAGENT returning ONLY roll date + spread. Anchor off the last session before
   Feb 1. The brain's C2-rescoped flip confirm gets its forward test here.
8. **G13** (Sun Feb 15 - Fri Feb 27) = THE SQUEEZE TEST - opex Feb 24, expiry Feb 25, GSCI roll
   Feb 6-12, bidweek Feb 23-27 all inside; requires feed I wired. The agent's only view of a
   squeeze is the calendar-front spread + the expiry clock + (new) the options pin map - that is
   the designed test.

Cheap checks worth folding in when adjacent: ICE gas contract codes in the CFTC disaggregated
files (positioning half of the ICE-HH blind spot); Databento IFUS pricing (market half).

## OPS NOTES (from S98)

Subagent completion monitors FIZZLE - when an agent goes quiet with work-products on disk, verify
directly and supersede. Serial wiring = ONE HAND, always. Missing-is-explicit caught real bugs all
session - never soften it. Every feed's `*_NOTES_S98.md` carries its traps; read before touching.

## GUARDS (unchanged, non-negotiable)

PER-EVENT, never pool/average as a conclusion; drift is a DESCRIPTOR; general rules only, no
point-fitting; blind wall decision-time only (storage strictly-prior, MOS as-of, COT
publication-keyed, cash knowable_from, STEO release-date); one-shot canonical; refine per group,
renders PRINTED first, lessons merged BEFORE the next group; Sunday-start / Friday-end blocks;
refined NEVER overrides blind, promotion gated on forward evidence; rolls marked never traded;
flips: confirm bar per s100.3 (C1+C3+C4 on the modern class, C2 scored everywhere, applied in
scope), NEVER front-run a flip; thin AMPLIFIES delivery, DAMPS holds; net-of-fee maker AND taker;
git = CODE, S3 = DATA; NG != WTI; weather forecaster HANDS OFF; provisional-until-live; keys are
SECRETS; no emojis.
