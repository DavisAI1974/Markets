# KICKOFF — S41 (next session)

Branch `claude/crypto-backfill-validation-31tubb` (all S40 work pushed; dev here, never another branch
without explicit permission). **Read first:** `SESSION_HANDOFF_2026-06-22_S40.md` then the S40 delta atop
`CLAUDE.md`. Memories: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git is source of
truth; commit+push regularly; never tune off one window; falsification-first.

## State you inherit (committed)
- **14-cell 1-sec deterministic coeff basis** in `_alt_labels/coeffs/alt_coeff_index.json.gz` (bybit x5 coins
  + kraken x{btc,eth}; coinbase was a REST dud). AWS package finished (Bedrock IDs, FNO prefill_embeds,
  venue-agnostic discovery, Lightsail runbook).
- **Per-cell distinctive fingerprint predictor** (`odcore/fingerprint_predictor.py`): coeff "merge" solved
  (deterministic decoder = 91% shared shape; edge is the centered ~9% residual; buy/sell mirror -1.0).
- **Full turn anatomy** mapped + the **causal flip detector** (`odcore/flip_detector.py`, leak-free) +
  harness. Verdict: detector TIMING works (1-4 bps to turn) but net-of-cost LOSES (over-fires the breakeven
  middle); the **dive gate is exhausted** (survivorship; ~0 on full pop and even at the tails).

## THE THREE LESSONS THAT GATE EVERYTHING (do not re-learn them)
1. Never pool sign-blind (buy/sell are mirrors -> directional signals cancel to ~0). Always sign-align.
2. **Cut by BIG vs SMALL price CHANGE (swing magnitude), NOT win/lose** (Greg's S40 clarification): the pop is
   ~breakeven, so any feature averaged over it -> ~0; predict |move|. This cut is what surfaced lean-depth.
3. Winner-only data is survivorship -> you need the NEGATIVES (dud/small-move turns) to learn "which turns are big".

## TASK 1 (the unblock) — predict |MOVE| (big vs small swing) per cell, via the dipole STATE
**Already have the lead:** lean DEPTH at the pivot weakly-but-consistently predicts big moves (corr ~0.05-0.07,
4/4 cells, full population) — the first real swing-size gate input. dive STEEPNESS is noise; DEPTH is the signal.
Build per-cell big-|move| vs small-|move| labels, then test the STRONGER features through that cut: the dipole
STATE = **lean depth + divergence/exhaustion** (the 64% S36 read, NOT dive-steepness), the coeff fingerprint,
the volume climax. Deploy per cell where it separates big from small moves. (Win/lose is the SEPARATE second
question — Greg's "buy/sell AND win/lose" bar — addressed by the loser coeffs in TASK 2, not conflated with move-size.)

## TASK 2 — loser coeffs (greenlit S40)
Discover loser-onset coeffs (mirror of `_build_alt_winner_labels` + `_run_alt_coeffs`) so win-vs-lose
fingerprint distinctiveness is testable. Feeds TASK 1.

## TASK 3 — the swing-size GATE + FNO
Off whatever separates the tails, build the gate (timing is solved; filter is missing) and re-run net-of-cost.
Train the FNO decoder on AWS GPU (prefill_embeds ready; the merge finding says the deterministic coeff is
shape-collapsed -> the FNO is the real distinctiveness upgrade).

## Side threads
- OD-BOOK committed T_test once `data/btc-book` is multi-day (exploratory panel = KILL-lean; lstsq fix in).
- Cross-domain falsification (Greg, serious): run the SAME turn operator on a non-market contested flow
  (World Cup momentum around the cooling break). 3-4 domains with even-mirror+odd-edge = a real law. Frame,
  test it, don't declare it.

## Cautions
- realbins are local/gitignored (re-pull via backfill_*.py or the Lightsail/S3 path). GHA runs need Greg's
  click. Avoid backticks in `git commit -m` (shell substitution blanked words in an S40 message).
