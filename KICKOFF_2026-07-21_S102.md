# KICKOFF - S102 (G14 one-shot blind FIRST - the first SHOULDER-SEASON block; then G15; smalls)

**FIRST ACTION, BEFORE ANYTHING ELSE: SWITCH TO THE BRANCH IN THIS BOX.** The harness assigns a
different auto-named branch - it is NEVER the work. Run:
`git fetch origin claude/ng-coach-agent-loop-5ha5bf && git checkout -B claude/ng-coach-agent-loop-5ha5bf origin/claude/ng-coach-agent-loop-5ha5bf`
The tip message must begin "S101".

**THE AWS KEY: USE THE PAIR WHOSE SECRET BEGINS `tx`** (ID `AKIAYI6JDCBVLKYQGLMH`, account
...4170). GOOD - never re-diagnose. InvalidClientTokenId = the container's placeholder env vars;
run AWS via `bash -lc` or explicit creds (CLAUDE.md "AWS KEY"). Greg pastes the pair; write
`scratchpad/aws.env` (KEY=VALUE, no `export` - platform_sync parses it), `~/.aws/credentials`,
profile exports; STS verify (pass/fail + account tail only). Databento key `db-3ba8...`: only
needed if a NEW pull is required - the rest-of-year raw is already landing on S3 from the box.

Read FIRST, in order: `SESSION_HANDOFF_2026-07-21_S101.md` -> this kickoff ->
`research/kalshi/knowledge/ng_brain.json` (**s101.6, 27 plays** - day-class OVERARCHING,
within-class scoring, gap ownership + dead-sponsor sizing, squeeze fingerprints F1/F2, pricing-time
rule, chain-state-is-tape-state, holiday registry, seasonal salience slider). The S100.1/S100.2
docs are the parallel session's SEPARATE pieces - do not fold.

## WHY THIS SESSION IS FRESH (doctrine_tier3.blind_run_hygiene)

S101 built G12/G13 rules, read both tapes, and extended March feeds - contaminated as a G14
referee. Carry ONLY this box + brain s101.6 + the masked decision_state into the blind.

## ENV ON-RAMP

`pip install boto3 databento matplotlib pandas openpyxl requests` -> creds ->
`platform_sync.py pull` the store prefixes (list first; pull all EXCEPT nymex/nymex_cont/ [28GB
raw], kalshi_echo/, deploy/; pull nymex/nymex_cont_n0/ + _n1/ + nymex/contract_structure/ +
nymex/nymex_curve/ to data/<store> per S101's dest mapping; eia/eia_surprise.json copies to
data/eia_surprise.json) -> `forecast_harness.py --selftest` PASS ->
`audit-joins` (default span) 0 violations.

## STATUS CHECKS BEFORE JOB 1 (from S101's in-flight work)

1. BOX JOBS: read s3 logs/pull_rest_2026.log + logs/cl_redecode.log + DONE markers. If both DONE
   and the box still runs, STOP the box (S101's trigger may have already). Report totals to Greg.
2. MOS FAMILY: check whether S101's extension landed (mos_asof index end date; mos_cycle/
   mos_freeze/model_disagreement spans; the handoff's IN FLIGHT section). If NOT through
   2026-03-13 minimum: run the extension FIRST (traps: build_mos_asof OVERWRITES the index -
   full-range rebuild 2025-11-01..target only; load_normals returns the cache wholesale - verify
   normals cover the span; verify pre-existing days bit-identical). G14 needs through Mar 13;
   through Jul 20 preferred. If an uncommitted mos_asof_index.json sits on the branch, verify
   bit-identity before committing anything.
3. CL JULY TOP-UP: verify nymex/nymex_cont/ has CL July files; if not, re-run
   research/kalshi/pull_july_2026_cl.py (resumable, $0).

## JOB 1 - G14 (Sun Mar 1 - Fri Mar 13 2026), the one-shot blind - FIRST SHOULDER BLOCK

Sessions (12): 20260301 Sun, 0302-0306, 20260308 Sun (DST BEGINS Mar 8 - clocks shift), 0309-0313.
Anchor = Fri 2026-02-27 hr24 from data/nymex_cont_n0/NG_20260227 (compute close + last-hour dir +
net; G13 scored the day at close 2.857 area on the roll-adjusted basis - verify from tape).

1. **THE BASIS DECISION (do FIRST, via the roll-check SUBAGENT - S101 handoff item 10):** the
   walk store's March days are the verified instrument-1008 (calendar-front April) series; the
   box also landed Databento's LITERAL NG.n.0 for the same days, which maps to the LESS-ACTIVE
   contract (the OI-rank quirk, 3rd appearance). The subagent reads BOTH series' instrument maps
   + the definitions raw (nymex/contract_structure/raw_2026ext/ ngfut_definition_*.dbn.zst -
   authoritative symbol<->instrument_id), returns ONLY: per-basis instrument/roll table + its
   recommendation. ORCHESTRATOR + GREG pick the walk basis (the S101 lean: the 1008/calendar-front
   continuation, matching how G12/G13 effectively walked; the PASS2 define-rolls-ourselves fix is
   the eventual answer). Record the decision in grp14.json price_basis verbatim.
2. decision-state for the 12 days `--mask-after 20260227` (the masking fix, proven G13) ->
   renders/ng_refine_s95/grp14_state.json. Structure store ending Feb 27 is FINE for the blind
   (the mask freezes at the 0227 anchor-close vintage); the store EXTENSION from raw_2026ext is
   for the refine/audit, not the blind.
3. Spawn the blind agent: brain s101.6 + masked state + anchor + roll metadata ONLY. It must:
   classify day-class FIRST every day; declare gap_owner at both closures (0227->0301, 0306->0308)
   per gap_ownership_scoring; read the SEASONAL SALIENCE SLIDER at the block open (first shoulder
   block - the injection-season turn approaches, maintenance season, weather salience fading);
   apply the pricing-time rule; flip bar C1+C3+C4 (C2 scoped); squeeze fingerprints are
   needs_intraday_reveal (not open-time); MULT=10000; write forecasts/grp14.json (grp13 schema +
   day_class + the basis record + holdout_caveat if applicable).
4. Score per-event (build g14_rt via the run_g13_rt_s101.py pattern on the DECIDED basis; roll
   offsets from the subagent), render, **PRINT to Greg. HARD PAUSE.** Refine only after his
   review - REFINE UNTIL IT SCORES (the S101 standard: every day inside its band, drift = named
   irreducibles only; sizes from measurable anchor/state quantities, never fitted). Weekend gaps
   first, always. Lessons merge (proposal -> Greg -> merge) BEFORE G15.

## JOB 2 - G15 (Sun Mar 15 - Fri Mar 27) after G14's merge

Carries the NGJ26/April expiry Mar 26 + opex Mar 25 inside the window (flow_calendar has the
gauntlet); the dead-sponsor weekend watch then lands Mar 28-29 (G16's open - the sizing rule's
first true forward test if a premium strands). 20260315 is a Databento DEGRADED-quality day
(named) - the roll-check subagent should report its condition.

## JOB 3 - smalls (when adjacent)

- **pyth_collector SUNSET - Jul 31 DEADLINE, ESCALATE AT OPEN** (trunk branch
  `claude/kalshi-s79-kickoff-ij8t9o`; Greg's explicit go required for the trunk).
- Structure/options STORE extensions from the raw already on S3 (raw_2026ext, ext_2026) - needed
  before the G14 REFINE wants true in-block state; no Databento key needed.
- audit-joins span extension past Feb 27 (with the March joins).
- CME NG event contracts scoping (the new third-lane candidate - on Globex, our data venue;
  Polymarket STRUCK: intl geoblocked + US version banned in Ohio). Hub-mapping doc (55 MMBtu
  products measured; map the ~25-30 non-HH codes to hub names + OI).
- Two-coach spec nod (pending); repo-private TODO; Jul 3 tape verify; ISO clocks.

## PENDING GREG

Two-coach spec approval; pyth sunset go (DEADLINE); the G14 basis pick (Job 1.1 presents it);
repo-private.

## OPS NOTES

Greg runs AUTO-APPROVE: hard pause after questions; short turn-breaks between groups. Background
tasks ride through interrupts. Subagent fetch-waypoint stalls: nudge continue-to-completion
(recurred 3x in S101). Box work via SSM bash -lc + boto3 (container cannot reach raw TCP; awscli
absent on box). Docs sites: crawler-UA fetch. Selftest pins = measured-then-pinned. Keys are
SECRETS. The stop-hook will nag about live-agent WIP files - leave them uncommitted until the
agent's verification passes.

## GUARDS (unchanged, non-negotiable)

PER-EVENT never pool/average as conclusion; drift = descriptor; general rules only; blind wall
decision-time only (+ the ONE-SHOT MASK on price-derived blocks, --mask-after, proven G13);
one-shot canonical FROM A FRESH SESSION; renders PRINTED before refine merges; REFINE UNTIL IT
SCORES (weekend gaps first; magnitudes derived from measurable state, never fitted);
Sunday-start/Friday-end; refined never overrides blind until forward evidence (supersession =
single ownership); DAY-CLASS FIRST - classify then apply only that class's toolbag, no
cross-class scoring ("no Mondays scoring Fridays"), holidays same-holiday-only (Sunday-like
default prior), extended weekends own sub-class; seasonal salience read at every block open;
rolls marked never traded; flips C1+C3+C4 (C2 scoped) never front-run; squeeze doctrine inside
delivery windows; thin amplifies delivery damps holds; net-of-fee maker AND taker; git = CODE,
S3 = DATA; NG != WTI; weather forecaster HANDS OFF; provisional-until-live; no emojis.
