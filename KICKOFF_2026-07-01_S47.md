# KICKOFF — S47: confirm maker-at-the-turn on a 2nd window, then refine (scale-in, wrong-tail, fees)

Branch: dev/push on `claude/crypto-liquidity-signals-s46-gwvm31` (S46 tip) AND keep canonical
`claude/crypto-liquidity-signals-5c5vg9` synced. If the harness lands on stale S37 (`7256abd`),
`git merge --ff-only origin/claude/crypto-liquidity-signals-5c5vg9` first (it carries the S45/S46 line).

READ FIRST: `SESSION_HANDOFF_2026-06-30_S46.md` → `STRATEGY_maker_at_the_turn_S45.md` → S46 delta atop `CLAUDE.md`.

RULES: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git=source of truth (commit+push
often); **never tune off one window** (all S46 numbers are ONE 11.7h window); falsification-first. OD-BOOK
`.ttest_committed.json` sentinel FROZEN.

## Where S46 landed (one paragraph)
The maker-at-the-turn executor (`odcore/swing_maker.py`) is wired to Greg's full spec: post only the conviction
side (best BID valley/up, best OFFER peak/down), HAVE the best price (front of queue), NO time windows, taker
last-option. Leakage PASS. SOL walk-through inverts every old BID-into-peak loser (loser #1 −9.98 → SHORT +12.66).
All 5 cells net+ gross/leg (sol +2.19 … btc +0.60). The dipole exhaustion wrong-tail gate is built (`entry_gate`)
but only MARGINAL on this one window (kept off by default). EVERYTHING IS PROVISIONAL on one window.

## ⚠ DO NOT REINVENT — use the EXISTING files; only change them if needed (Greg, hard rule)
The corrected logic from S45+S46 lives in these files. Do NOT rebuild from scratch, do NOT write new parallel
tools. Extend in place only where a job needs it. CRITICAL FILES (read/use these, don't recreate):
- `odcore/swing_maker.py` — THE executor (front-of-queue, no window, taker last-option, `confirm`/`entry_gate`).
  Everything maker-at-the-turn goes through `simulate_swing_maker`. Edit here for scale-in / timing, don't fork.
- `_render_trades.py` — `swing` (per-cell verdict + wrong-tail A/B + render), `swingwalk` (the 10-trade OLD-vs-NEW
  walk + `edge_gap`), `exhaustion_gate()`. The driver for everything; add modes here, don't make a new driver.
- `odcore/flip_detector.py` — the CAUSAL turn detector (`lean_series`, `detect_flips`, `make_signal_at`). The
  TIMING fix (below) lives around here, not a new detector.
- `odcore/info_dipole.py` — `divergence()` (the exhaustion/reversal FILTER). `odcore/quiet_floor.py` — the shock
  gate. `odcore/leakage.py` — `assert_no_leakage` (run before trusting any new signal).
- `_liquidity_dive.py` (`build_channels`, `median_spread_bps`) + `_birth_probe.py` (`load_book`, `to_grid`, `lean`)
  — the book→grid data pipeline. `STRATEGY_maker_at_the_turn_S45.md` — the spec.

## THE S47 JOB
1. **2nd-window confirmation (do FIRST).** Re-fetch the books (cron has accrued more):
   `for c in sol doge xrp eth btc; do git fetch origin data/$c-book; git show origin/data/$c-book:${c}_coinbase_book.jsonl.gz | gunzip > /tmp/${c}_coinbase_book.jsonl.gz; done`
   Re-run `python _render_trades.py <coin> <K> 1.5 swing` (btc K=10, alts K=1) and the `swingwalk`. Do the
   per-cell net/win + the loser-#1 inversion REPRODUCE on the newer/longer window? This is the gate before any sizing.
2. **★ ENTRY TIMING — we're leaving money on the table (Greg, S46).** The walk-through's `[X bps off the top]`
   column shows shorts entering 6–10 bps BELOW the peak (longs above the valley) because the flip CONFIRMS late
   (W=600 lean + REV=0.10 retrace). The S36b architecture is the fix: **dipole/flip = the FILTER (which turns are
   real), 1-sec PRICE REVERSAL = the TIMING (enters ~5–6 bps off the true turn).** Right now the flow lean does
   BOTH detection and timing, which lags. Add a price-reversal entry trigger (around `flip_detector`) so we post
   the best offer/bid CLOSER to the extreme — keep the flip as the filter. Re-run `swingwalk`, watch the
   `bps off the top/bottom` shrink. This is the biggest remaining edge.
3. **Scale-in refinement.** "Re-quote the ask at each new lower price, EXTENDING the short all the way down" is
   currently a SINGLE entry per leg in `simulate_swing_maker`. Model the scale-in (multiple favorable fills) for
   a better average entry — extend the executor in place.
4. **Wrong-tail, take 2.** The `entry_gate`/`exhaustion_gate` (`info_dipole.divergence`) was marginal. Try
   CLIMAX-VOLUME confirmation (S40 ~2x volume AT the turn) as the gate; and/or per-cell (NOT tuned on one window).
5. **True net-of-cost.** S46 net is gross-of-fee. Add the taker fee on the last-option exits + any maker rebate.
6. Once validated on >1 window: lock per-cell operating points into `odcore/quiet_registry.py`, wire the emit path.

## THE 10 EXAMPLE TRADES (SOL, K=1) — KEEP WALKING THESE (Greg: still leaving money on the table)
From `python _render_trades.py sol 1 1.5 swingwalk` (PROVISIONAL, one 11.7h window). `[off]` = bps the entry is
from the extreme it should hit (the money left on the table — job #2). NEW sum +68.82 vs OLD +2.58.
```
 # cell    OLD          NEW maker-at-the-turn                                          net    [off top/bot]
 0 397372  BID -9.98 -> SHORT entry 75.090 -> 74.995 (maker)                          +12.66  [6.0 off top]  loser#1 inverts
 1 277789  BID -3.29 -> SHORT entry 75.850 -> 75.760 (maker)                          +11.89  [7.2 off top]
 2 310761  BID -1.98 -> SHORT entry 75.485 -> 75.400 (maker)                          +11.28  [10.6 off top]  <- worst entry lag
 3 417576  BID -0.66 -> SHORT entry 75.080 -> 75.110 (maker)                           -3.99  [2.0 off top]  wrong-tail (price rose)
 4 308089  BID -0.65 -> SHORT entry 75.510 -> 75.550 (maker)                           -5.28  [2.0 off top]  wrong-tail (price rose)
 5 361388  ASK +0.01 -> SHORT entry 75.520 -> 75.500 (maker)                           +2.66  [0.7 off top]
 6 294106  ASK +0.67 -> SHORT entry 75.880 -> 75.870 (maker)                           +1.34  [5.9 off top]
 7 293939  BID +0.67 -> SHORT entry 75.880 -> 75.870 (maker)                           +1.34  [5.9 off top]
 8 288295  ASK +2.65 -> SHORT entry 75.930 -> 75.790 (maker)                          +18.46  [5.9 off top]
 9 288289  ASK+15.16 -> SHORT entry 75.930 -> 75.790 (maker)                          +18.46  [5.9 off top]  the big peak
```
Read: the inversions are right (every old BID-into-peak loser -> short), but entries sit 6–10 bps off the top
(job #2) and trades 3,4 are the wrong-tail rip (job #4). Re-render `_render_trades_sol_swingwalk.png` to walk visually.

## TOOLS (S46)
`odcore/swing_maker.py` (executor: front-of-queue, no window, taker last-option, `entry_gate`/`exhaustion_gate`),
`_render_trades.py` modes `swing` (per-cell verdict + A/B + render) / `swingwalk` (OLD-vs-NEW 10-trade walk).
Leakage: `odcore/leakage.assert_no_leakage` on `flip_detector.make_signal_at(600,0.10)` = PASS.

## Notes
- Front-of-queue assumes best-price priority; mean swing ~2–4 bps (most below the 20bps fee floor) on this window
  — the edge is real but small & PROVISIONAL. btc (spread-starved) is net+ on pure swing capture, not spread.
- Book accrual: 6h cron self-sustains; alts near the 100 MiB push cap in a few days → off-git storage (blocked on
  cloud auth) is the durable fix. The integration token can't dispatch GHA (403) — runs are the button/cron.
