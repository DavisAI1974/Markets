# KICKOFF S83 — Kalshi: sub-second lag on live Pyth ticks + the futures-move join + Thursday's natgas

**Read in order:** `SESSION_HANDOFF_2026-07-13_S82.md` → CLAUDE.md S82 delta (top) → `KALSHI_TRADING.md`
(file index) → this file.

**Branch:** `claude/kalshi-s79-kickoff-ij8t9o` — repo DEFAULT; push there. If the harness assigns a fresh
branch, check `git log --oneline -1` FIRST — a **stale S70 tip `3c70ff5`** is the recurring trap. Work on s79.

## Where we are (end S82)
- **The per-trade LEVEL-HIT dataset is BUILT** (`level_hit_dataset.py`, 200,421 events, leakage-clean). It's
  a Result-Discipline negative that sharpens the program: level-hits **mean-revert at 1¢** (cont 0.383), **no
  cell pays even at maker fees** (confirms S81 size-vs-fee), and the **internal order-flow context is a weak
  big-run predictor** (lift +0.00…+0.03). **Meta-conclusion: the edge is EXTERNAL — the futures→Kalshi lag,
  not the book's own flow.** The level-hit that runs is most likely the one trailing a fresh NYMEX move.
- **Pyth feed UNSTUCK.** Deleted 11 legacy crypto workflows that were hogging the runners; kept the 2 Kalshi
  ones. The Pyth durable collector is running; `data/pyth-ticks` accrues from the Sun-eve reopen onward.
- **Thu 7/16 EIA natgas STAGED** — selftest leakage PASS 30/0; `--test` turnkey on accrued bins.

## S83 priorities
1. **SUB-SECOND LAG on accrued `data/pyth-ticks` (priority-1, feed now unblocked; the decisive test).**
   Fetch+gunzip the `data/pyth-ticks` branch. Per Kalshi contract (never pooled): align its ms trade tape
   vs the Pyth tick tape at 1–5s — WHICH strikes lag, by how many SECONDS, and does the whole reprice
   (captured before decay) clear the toll? Use `score_hold` (fire-quick-then-hold) at real resolution.
   Distributions + winner fingerprints, no means. First check `data/pyth-ticks` actually has ticks (the
   feed was only unstuck end-S82; if the reopen hadn't happened yet, ticks may be thin — verify before running).
2. **JOIN the Pyth futures move onto the level-hit dataset (the external-driver hypothesis).** Add a
   `futures_move_bps` / `lag_seconds` context column to each level-hit event in `level_hit_dataset.py`:
   do big-run continuations concentrate on level-hits that TRAIL a fresh futures move? This is the test the
   S82 dataset was built to set up — the internal context is weak precisely because the driver is external.
3. **Thursday 7/16 EIA natgas (14:30 UTC) LIVE.** Re-run `release_book_signal.py --test --series KXNATGASD`
   on the release-spanning bins once they accrue; busy-day natgas lag on live NGDQ6 Pyth ticks; run
   `consensus_poll.py` before (forecast) + after (actual via EIA).
4. **WEATHER scoreboard (scoreboard-side; forecaster is Greg's spec, HANDS OFF).** The baseline bar is on
   record in `WEATHER_BASELINE_S82.md` (per-regime, not pooled: persistence transition-cell Brier NY 1.45 /
   CHI 1.58 / DEN 1.38; the edge is the transition/frontal cell). When Greg's OD operator emits `(value,
   sigma)`, score it through `kalshi_score.py` **PER REGIME** (Denver/NY/Chicago transition cell first) vs
   those baselines. PER-BUCKET-NEVER-POOLED — 2 regimes is the minimum, refine toward synoptic cells. The
   real market baseline (vs post-hoc placeholder) activates once `KXHIGH*` bins accrue over a settled event.
5. Standing: **front-month roll re-point** (WTIQ6 exp 7/21, NGDQ6 7/29, BRENTU6 7/31 — update FEEDS in
   `pyth_collector.py`); paper-loop RSA creds.

## Watch-outs
- If the Pyth run goes `queued` again, it's account-level Actions (billing/minutes or runner cap) — check the
  repo Actions settings, not the workflow. The runner-hog cause was fixed end-S82 (crypto workflows deleted).
- `data/pyth-ticks` and `data/kalshi_hist_trades/` are LOCAL/gitignored — re-fetch/re-pull each session.

## RULES (load-bearing)
EACH TRADE INDIVIDUALLY — never average; distributions + per-trade fingerprints not means. Per-cell always
(moneyness × side × velocity/lag-class × release). Exclude the settle window. Catalyst = trigger + coarse
size; book/flow imbalance + exhaustion = direction + magnitude; herd breadth = continuation, whale =
scalp-only. Leakage gate before any backtest. Zero synthetic. Provisional-until-live. Weather = Greg's spec,
HANDS OFF. `--events` on news_coupling_research = BASENAME. Keep `KALSHI_TRADING.md` current.
