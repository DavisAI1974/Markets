# KICKOFF - S100 (key rotation + live smoke FIRST, the paid determination, then A/E/M + doctrine, then G12)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-20_S99.md` (SECURITY block first) ->
`research/kalshi/DATA_GATE_S98.md` (STATUS lines current incl. S99 updates) ->
`research/kalshi/knowledge/ng_brain.json` (s100.3, UNCHANGED in S99 - no group ran).

Branch: `claude/ng-coach-agent-loop-5ha5bf`. Confirm the tip message begins "S99 close-out"
(`git log --oneline -1`); if the harness cut a stale tip:
`git fetch origin claude/ng-coach-agent-loop-5ha5bf && git reset --hard origin/claude/ng-coach-agent-loop-5ha5bf`.

---

**S100 UPDATE — THE AWS KEY (read before item 1): USE THE PAIR WHOSE SECRET BEGINS `txRGHd`**
(ID `AKIAYI6JDCBVLKYQGLMH`, account ...4170) — verified live 2026-07-20. Rotation DEFERRED to
go-live per Greg. If STS says InvalidClientTokenId on this key, it is the CLOUD CONTAINER's
placeholder AWS env vars overriding the real pair — run AWS commands via `bash -lc` or pass
creds explicitly. Do not diagnose the key; it is good. See CLAUDE.md "AWS KEY" section.

## ITEM ZERO - TWO OPENERS BEFORE ANY BUILD

1. **KEY ROTATION (Greg's console; the S99 photo exposure).** New AWS key -> typed DIRECTLY into
   `scratchpad/aws.env` -> deactivate the photographed key -> verify the old S97-era key is
   deactivated too. Then STS-verify from the file (print nothing).
2. **THE LIVE SMOKE TEST (the saved canary).** Bento Standard is SUBSCRIBED (S99 close).
   `python research/kalshi/databento_live_smoke.py` during RTH: expect trade prints with
   sub-second latency on GLBX NG. This is the live-loop's first breath; log the observed latency
   as the first telemetry datum.

Then env on-ramp: `pip install boto3 databento matplotlib pandas openpyxl requests`;
`python research/kalshi/forecast_harness.py --selftest` must PASS (21-block decision_state);
`audit-joins` must print 0 violations / 101 days / 12 classes.
`platform_sync.py list` = the S3 inventory; DO NOT RE-PULL what S3 holds.

## THE PENDING DETERMINATION (Greg, rested)

The S99 paid-data recommendation awaits his verdict: defer-and-measure + optional quotes
(Criterion / East Daley / PipeRiv demo) + decline flagships. **Feed S (as-quoted L48) is gated on
it.** Also pending his call: **CL Mondays** (51 stubs, whole year) - paid re-pull ~$130-165 vs
FREE redecode from the still-alive year jobs (**window closes ~Aug 12-14**); and the
**pyth_collector sunset** (free Hermes dies 2026-07-31 - close the WTI/XAU/XAG corpus cleanly or
repoint; Bento live covers the canary need now).

## JOBS AFTER THE OPENERS (order matters)

1. **July 1-18 NG tape pull** (never pulled - year-pull boundary; small paid batch) - feed M's
   life coverage + the missing settle-delta days. FIRST verify the Monday repair that S99 left
   running DETACHED at close: tail `E:\Markets\scratchpad\monday_repull.log` (expect
   "DONE: 22/22"); if any days remain, re-run `scratchpad\monday_repull_driver.py`
   (self-discovering, idempotent); load-test one repaired day, e.g.
   `event_move_baseline.load_cont_day("NG", "20260202", source="s3")`.
2. **Feed A phase 1 - cycle-level MOS as-of** (hour resolution; check the raw S3 archive FIRST -
   it likely already holds the 00z/06z/12z/18z cycles; the weekend 00Z/12Z gap is the walk's most
   reproduced residual). The two documented builder traps: index overwrite; normals cache
   ignoring the window.
3. **Feed E - freeze-off basin temps** (with/after A; same cycle discipline; producing-basin MOS
   stations; thresholds exposed as data, never tuned).
4. **Feed M - lag/fill/fee on the KXNATGASD life** (execution economics ONLY - the lag is
   ESTABLISHED). Now grounded by S99: settlement mechanics verified (Pyth per-contract NGD 17:00
   EDT close; 5bd-forward underlying roll - respect it), `expiration_value` = the settle print,
   settle-delta baseline measured (median 0.1c matched; roll-window days need the per-NAMED-
   contract pass), Mondays repaired, July tape from job 1. Fill model: candle bid/ask OHLC is the
   book ceiling historically; live-collector books (accruing since 2026-07-12) are the depth
   source; 60s cadence undersamples the lag - carry the seconds-scale collector requirement into
   the two-coach spec.
5. **Tier 3 doctrine proposals** (ALL printed to Greg before merge): usage guidance, flip driver
   checklist, evidence-day registry, two-books scoring split, squeeze-regime doctrine,
   TWO_COACH_SPEC off M's measured numbers.
6. **Queued smalls when adjacent:** cot_feed ICE extension (codes measured: 023391/023392/
   0233AG/0233AH); ISO day-ahead clock into information_clock (verify each ISO's published
   deadline first - never assume); the non-Thursday 10:00-10:30 ET tape characterization
   (live-coach); apr26+ STEO vintages (measure release dates first).
7. **THEN G12** (Sun Feb 1 - Fri Feb 13 2026) one-shot blind per the gate checklist - roll check
   via a SUBAGENT returning ONLY roll date + spread. **G13** (Feb 15-27, the SQUEEZE TEST)
   follows; feed I is DONE+WIRED, feed M is its remaining gate.

## OPS NOTES (from S99)

Greg runs AUTO-APPROVE: hard pause after any question to him; short turn-breaks between task
groups so his feedback can drop in. Background tasks ride through interrupts - stop only via
TaskStop; explain "canary" in plain words before running one. The browser pane wedges on JS SPAs
(twice now) - prefer APIs, or ask Greg to read his own screen. Selftest pins against
independently-recorded values catch real bugs (a 1000x unit slip died same-day) - keep pinning.

## GUARDS (unchanged, non-negotiable)

PER-EVENT, never pool/average as a conclusion; drift is a DESCRIPTOR; general rules only, no
point-fitting; blind wall decision-time only (storage strictly-prior, MOS as-of, COT
publication-keyed, cash knowable_from, STEO release-date, 930 period+2, nuclear period+1,
options session-prior); one-shot canonical; refine per group, renders PRINTED first, lessons
merged BEFORE the next group; Sunday-start / Friday-end blocks; refined NEVER overrides blind,
promotion gated on forward evidence; rolls marked never traded; flips: confirm bar per s100.3
(C1+C3+C4 modern class, C2 scored everywhere applied in scope), NEVER front-run a flip; thin
AMPLIFIES delivery, DAMPS holds; net-of-fee maker AND taker; git = CODE, S3 = DATA; NG != WTI;
weather forecaster HANDS OFF; provisional-until-live; keys are SECRETS; no emojis.
