# KICKOFF — Session 23 (Markets) — 2026-06-07

WORKING BRANCH: `claude/crypto-trading-platform-plan-MpqwG` (DavisAI1974/Markets).
Read `SESSION_HANDOFF_2026-06-07_S22.md` first, then this. Master context: `CLAUDE (5).md`
(canonical through S22 once the S22 note is folded + header bumped). The SessionStart hook
installs the OD toolchain (numpy/scipy/sklearn + PySR + Julia) and materializes the real bins
into gitignored `realbins/`.

## 0. Status carried forward (do NOT relitigate without new data)
- The chem-dipole CONSTRUCTION is resolved and the core is ported + pushed (commit `d129c19`):
  `odcore/dipole_predictor.py` (centroid-projection, verbatim from `E:\Markets\_markets_algebraic_dipole.py`)
  + `odcore/dipole_trade.py` (per-trade 5-step coupling vector -> centroid projection).
- NOT yet validated. Reproducing the R^2 is STRUCTURAL, not an edge. Every prior unblocked OD signal
  LOSES net-of-cost (dipole_direction WF -11.8%, tautology z 1.2). The bar is `odcore/validation.py`.

## 1. FIRST PRIORITY -- finish the plumbing + validate (EARN it)
Write the runner (e.g. `scripts/od_trade_dipole_run.py`) that:
  (a) pulls labeled win/lose trades from the executor / `adaptive_backtester` trade store
      (MAP THE DATA SHAPES -- this is the unmapped part);
  (b) for each trade, materializes the pair's two pre-entry channel arrays via `odcore.channels`
      over `[entry_ts - pre_entry, entry_ts]`;
  (c) builds c_i via `dipole_trade.trade_coupling_vector`, fits with WALK-FORWARD centroids
      (train fold only; project the test fold) -- NOT the in-sample `fit_trade_dipole` (that is the
      structural-reproduction positive control only);
  (d) validates via `odcore/validation.py`: net>0 after fees+slip, beat baseline + buy-hold under
      embargoed walk-forward, tautology z>>2-3, small random-vs-WF gap.
Sequence = reproduce-then-adapt: first confirm in-sample R^2 ~ original (~0.94) as a positive
control that the port is faithful, THEN the honest walk-forward predictor.

## 2. IF it clears the gate -- WIRE it (else STOP; do not ship a losing signal)
- Flow od_* fields (`od_residual_fraction / od_dipole_r2 / od_leadlag_stability / od_walkforward_edge`)
  through `/api/signals` + `/api/stream` (shadow first; feeds `executor/_size_notional`).
- Switch the emit decision to the OD generator; THEN unwire PELT (hybrid -> fixed). DEFERRED until earned.

## 3. Open questions for Greg
- Dimensionality: c_i is 15-dim (the 5-step model output) vs the original's 128-dim refrag
  `operator_coefficients`. If the signal is thin, enrich the operator basis or revisit the c_i choice.
- Label source: which trade store is canonical for win/lose outcomes (paper exchange vs
  `adaptive_backtester`)?

## 4. Housekeeping for the next session
- Commit the three docs the user is uploading manually: `SESSION_HANDOFF_2026-06-07_S22.md`,
  `KICKOFF_2026-06-07_S23.md`, `CLAUDE_session_note_2026-06-07_S22.md` -- and FOLD the session note
  into `CLAUDE (5).md`: bump header line to "Updated 2026-06-07 Session 22 -- body canonical through
  S22" and prepend the S22 note above the existing S21 note (per the master's START-HERE workflow).
- Delete the stale `claude/session-upload-handoff-cy7wQ` remote branch (pending since S21).

## 5. Environment / loose ends
- `E:\Markets\.claude` is HARD-LOCKED on the local Windows box (exFAT). Work from git refs + plumbing,
  or use the container. Avoid `Remove-Item` while cwd is `E:\Markets`.

## Branch
WORKING BRANCH: `claude/crypto-trading-platform-plan-MpqwG` (DavisAI1974/Markets)
