# Direct runtime roots. The launch workflow resolves these exact versions before
# credentials exist, records every transitive wheel/version/SHA-256 in
# requirements-frankie-fullstack-20260824.lock, and installs offline with
# --require-hashes. Never install this file directly on the remote host.
boto3==1.40.0
openai==1.99.9
databento==0.81.0
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
matplotlib==3.9.2
