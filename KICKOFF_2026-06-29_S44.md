# KICKOFF — S44 (next session)

Branch `claude/crypto-liquidity-signals-5c5vg9` (S43 pushed; `crypto-backfill-validation-31tubb`
synced to the same tip `46f09ea`; dev/push on `5c5vg9`). **The harness keeps landing on the stale
S37 branch `claude/crypto-liquidity-signals-2txotq` — switch to `5c5vg9` FIRST.**

Read `SESSION_HANDOFF_2026-06-29_S43_ODBOOK.md` then the S43 delta atop `CLAUDE.md`.

Rules: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git is source of truth
(commit+push often); never tune off one window; falsification-first; agnostic 5-step coupler reads
what couples (don't impose a direction). dipole_r2~1 on signed channels is the H_a=−H_b tautology —
judge by structure_z, not dipole_r2. One-shot gated tests: touch T_test exactly once (sentinel).

## State inherited (committed, S43)
- **OD-BOOK gated T_test = KILL** (sentinel frozen). Legs 1+3 PASS (OD DMD out-forecasts VAR/ridge
  OOS; operator stable), leg 2 FAIL (turn-net-of-22bps both −39.6). KILL-mode #2 (no edge after
  fees). Threads 2/3 NOT built. DMD operator salvaged as a stable feature/gate. `research/od_book/`.
- **Leg-2 failure is the TAKER FEE FLOOR, not the signal** (`_leg2_diagnosis.py`): predicted moves
  ~0.11–0.16 bps p99 vs 22 bps fee (~140×); gross PnL is positive; fee=0 → +~4600 bps (optimistic
  upper bound). Fixed `metrics.label_turns` (was always 0 turns). The maker thread is NOT falsified.
- **Maker-fill/queue model BUILT** (`odcore/maker_book.py`, `_maker_fill_model.py`): honest queue +
  adverse selection, QuietFloor gate, held-out arms. On btc_coinbase: making bleeds ~0.5–0.8 bps/fill
  to adverse selection; signal is a real-but-weak side filter (`fav−anti +0.068`); **QuietFloor gate
  cuts ~30% of the per-fill loss** (best lever); **no net edge before rebate (breakeven ≈ −0.32
  bps/leg)** because the book is 1-tick (half-spread ~0.0008 bps). **Binding constraint = absent
  spread, not signal.**
- Book on `data/btc-book` is now ~1.68M cells but STILL btc_coinbase only → single-cell
  characterization, not deploy-grade.

## Open tasks (priority)
1. **Multi-venue/multi-coin book — THE bottleneck.** Greg's manual GitHub → Actions →
   `book_collector_btc.yml` → "Run workflow" (token 403s on triggers). Clone the collector to other
   venues/coins (it exists only for btc_coinbase). Everything below is gated on this.
2. **Re-run `_maker_fill_model.py` PER CELL once a wider-spread cell exists — the decisive deploy
   test.** On a normal-spread book half-spread (2–5 bps) ≫ adverse selection (~0.5 bps), so the gated
   signal-as-filter + spread + rebate should clear net. This is what the model was built to show.
3. Re-run `_liquidity_dive.py` + fit `QuietFloor` per cell across venues; confirm the depth_imb
   edge + sign/lead are stable (cross-segment consistency is the stand-in until then).
4. Wire the QuietFloor gate into the live dipole (gate = WHEN to fire, imbalance level = DIRECTION)
   so the production dipole stops churning through trends.
5. OD-BOOK threads 2/3 stay NOT built (gate held). Cross-domain falsification still open.

## Cautions
- realbins + `/tmp/od_book.jsonl.gz` + PNGs gitignored. Re-extract book:
  `git show <ref>:btc_coinbase_book.jsonl.gz | gunzip > /tmp/od_book.jsonl.gz` (fetch `data/btc-book` first).
- Pure-Python per-tick loops are slow on ~250k+ rows — run in background, wait with an until-loop.
- Avoid backticks in `git commit -m`. GHA runs need Greg's manual trigger.
- OD-BOOK sentinel `.ttest_committed.json` refuses a second T_test pass — do NOT delete it (that
  invalidates the pre-registration). The KILL is final for the taker thread.
