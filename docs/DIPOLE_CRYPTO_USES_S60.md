# DIPOLE USES IN CRYPTO — working notes (S60; Greg's directive)

Status: v1 from the dive specialist's chapter (docs/DIPOLE_DIVE_CHAPTER_S60.md §3) + the
program's standing record. TO BE EXTENDED when the main workflow's crypto researcher lands
(the general-dipoles paper covers all constructions, not just the dive). House rules apply:
every entry is a CANDIDATE with a first falsifiable test and a deflationary read; nothing
here is a deploy claim.

## WHAT IS ALREADY EARNED (the record, not candidates)

1. **Flip detection / entry confirm (the lean dipole)** — the deployed fine-scale flip
   detector (S36/S40: lean zigzag, REV=0.1) and the mid-band armed machine's confirm
   grading (S58 member maps — venue-gated, research-only on Coinbase until books pass).
2. **Wrong-side corrector (the dive, office ii)** — S60: the one exit office that earned a
   machine. DOGE cascade-join flip +1.10 $/hr structure premium (instrument frame); SOL
   armed-before information 8/8 cells. Per-cell conditioning strict (sol armed-stop / doge
   BUY+depth flip / xrp dv40+uw10 hypothesis / btc plain price stop) — the structure is
   universal, the config is per-cell.
3. **Dipole descriptors on every ledger row** (dive_depth, lean_flip, lean_close) — free
   correlates accruing in the forward record.

## RANKED CANDIDATES (dive chapter §3, verbatim ranking)

1. **Wrong-side position corrector** — VALIDATED, deploy-gated (see above). Value frame is
   kr_mk0/low-fee; at cb_real it shrinks bleed, does not turn cells.
2. **Liquidation-cascade CONFIRMATION** (not early warning) — deep opposing dive at >=X bp
   extension -> continuation magnitude, per side, vs shuffle floor. The DOGE cascade law in
   miniature. DEFLATIONARY: literature says flow imbalance is a CONSEQUENCE of stress;
   depth metrics warn earlier — the early-warning version needs book depth, not flow.
3. **Maker-quote placement timing (fill office)** — UNMEASURED, highest unknown upside,
   and the S60 dive experiments RAISED the burden of proof: dive states are THIN-tape
   (volume x0.70-0.92 baseline, z -13) — the fill moment is the WITH-RIDE CLIMAX side,
   not the dive side. The queue-level test (one-sided against-price fill rate) is the
   real experiment; tape volume is the wrong proxy.
4. **Cross-venue cascade propagation** — does a Binance dive lead the Coinbase/Kraken dive
   by a measurable lag? Tautology-nulled lagged cross-correlation. DEFLATIONARY: S20 found
   venues synchronous at 1s (cc 0.656, z 580); the edge may live entirely below 1s (tick
   data needed).
5. **Spoof/absorption detection** — deep lean with NO price response = absorption or spoof;
   the thin-dive finding helps separate "real thin dive" from "absorbed thick dive."
   DEFLATIONARY: trades alone cannot distinguish spoof from absorption (needs cancels).
6. **Funding/squeeze detection (perps)** — dive against extreme funding = crowded side
   capitulating -> funding mean-reversion test. DEFLATIONARY: untested; venue ban list
   constrains usable perp tapes (BYBIT banned, MEXC dead).

## STANDING CONSTRAINTS (apply to every entry)

- Per-cell law: nothing pools across coins/venues; member sign flips are the norm.
- Venue law: flow reads never port without a per-venue books pass; price mechanics port.
- Scale-locality: every (band, read) pair revalidates (S40 depth->|move| = fine-only,
  re-confirmed twice at mid-band).
- Office discipline: the dipole's value = (operator x what it is conditioned against);
  grade each office separately per (cell, band, venue, role).
- Fee frame: at cb_real the exit-side dipole reads are riders; cell economics live in the
  fee column (S60 R4 decomposition).

## PENDING EXTENSIONS
- Main workflow crypto researcher (all dipole constructions: algebraic, flow dMI/dt, C
  ratio, raw-covariance, centroid dual-print) — fold its ranked list here when it lands.
- KRAKEN EXITS analysis — PARKED per Greg (S60); the corrector's Kraken pass waits.
- The S35 fingerprint tier (centroid dual-print) remains the named path to the winner-side
  prize (+18-25bp/leg hindsight ceiling) — encoder-tier gates unchanged.
