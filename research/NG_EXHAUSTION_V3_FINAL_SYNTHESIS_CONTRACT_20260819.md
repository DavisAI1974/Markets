# NG Exhaustion V3 Final Synthesis Contract — 2026-08-19

Status: **PREDECLARED SYNTHESIS CONTRACT. PROPOSAL ONLY. NO PERMANENT FRANKIE MUTATION.**

## Required authoritative inputs

The final synthesis may run only when all three durable V3 reconciliations exist on the agents branch:

1. `research/generated/ng_exhaustion_d0_d5_full_causal_v3_20260819/NG_EXHAUSTION_D0_D5_FULL_CAUSAL_V3_ALL_RESULTS_20260819.json`
2. `research/generated/ng_exhaustion_v3_foundation_trade_20260819/NG_EXHAUSTION_V3_FOUNDATION_TRADE_ALL_RESULTS_20260819.json`
3. `research/generated/ng_exhaustion_d0_d3_model_specific_trade_v3_20260819/NG_EXHAUSTION_D0_D3_MODEL_SPECIFIC_TRADE_ALL_RESULTS_20260819.json`

The second input contributes only the D0/D1 complement calibration and D2/D3 root-retained-versus-ablated evidence. Its older D0 next-event-capped trade prototype is **not authoritative trade evidence** and must be ignored by final synthesis.

The third input is the authoritative trade layer because it uses fixed causal horizons and never uses a future canonical event as an exit.

## Historical evidence that must travel forward

`research/NG_EXHAUSTION_CHAIN_PHASE2_ALL_AGENT_FINDINGS_20260818.md` remains frozen historical truth. The synthesis must preserve, not re-litigate:

- recurring pair/triplet modular grammar;
- stable extension cases and below-baseline/regime-changing cases;
- D2/D3 timing/lifespan families;
- true/false context decomposition and deeper-ancestry splits;
- independent-module novelty candidates;
- settled P-O-X reset/re-entry management;
- `FLAG_AND_DECOMPOSE_NOT_AUTO_KILL`;
- no additional Phase-2 play promotion.

V3 prediction does not convert Phase-2 recurrence into a trade rule by itself.

## Authoritative V3 interpretation

The V3 pass answers three separate questions:

1. chain existence/continuation;
2. eventual exact depth;
3. P/O/S/X structural state/family.

Target polarity is not a primary prediction question. SAME/FLIP is retained as secondary context only.

At every checkpoint the primary `FULL_CAUSAL` surface contains the continuously observable market movie: dense causal price path, dense roll-20/dipole path, signed flow, book path, clock/session state, and all predecessor/root facts already causally known.

Unknown future target polarity may never block PRIOR or suppress raw market direction.

## Clock doctrine

- D0 uses `ROOT_AGE`, not H.
- D1-D5 PRIOR is strictly before frozen target `t0` and reports actual lead-to-birth.
- H is reserved for seconds after frozen target `t0` only when that exact model/target failed PRIOR.
- A post-birth H result is historical timing/economics until the upstream event-mark/discovery timestamp is proven. Frozen `t0` alone is not assumed to be a live detector notification timestamp.

## Model doctrine

Logistic, ExtraTrees and KNN are independent evidence lanes.

- no 2-of-3 gate;
- no median probability;
- no ensemble validity vote;
- model disagreement is preserved evidence;
- a valid independent OOT finding remains valid even if another predeclared model fails.

## Ablation doctrine

The synthesis must preserve at least:

- FULL_CAUSAL vs NO_PRICE_CAUSAL, for incremental price value;
- FULL_CAUSAL vs PRICE_POLARITY_ONLY, for incremental structural/microstructure value;
- root-retained vs root-ablated D2/D3, with identical rows and live market state;
- D0 vs matched `1 - D1` as implementation/calibration evidence only, not duplicated independent evidence.

## Trade doctrine

Only the corrected V3 fixed-horizon trade layer is authoritative.

- each model reproduces its own validated signal;
- direction may use confirmed predecessor polarity or contemporaneous live price/flow;
- realized target polarity is forbidden as a direction source;
- exit is a fixed discovery-selected horizon;
- future target birth, next exhaustion, endpoint or later chain event may not choose/cap the exit;
- report 0.5/1/2 tick costs, MFE, MAE, path efficiency and week stability;
- PRIOR strategies and post-birth H strategies remain separately labeled;
- historical validation is not live promotion.

## Sparse boundary

D4/D5 remain case studies. The synthesis may describe individual timing/state evidence but may not infer a universal D4/D5 law from current support.

## Required synthesis outputs

The synthesis writes:

1. one machine-readable all-findings JSON;
2. one human final findings Markdown record;
3. one proposal-only Frankie brain JSON;
4. one proposal-only trade-strategy JSON;
5. one unresolved-model/target queue for later causal checkpoints.

Every output must state `promotion_performed=false` and all protected mutations false.

## Old-proposal correction

The historical chain-birth V2 brain proposal is not deleted. Its old pre-birth `H`, characteristics wall and 2-of-3 doctrine remain provenance only and are superseded for this recovery line by:

- separate PRIOR and H clocks;
- FULL_CAUSAL information when genuinely known;
- continuous live direction/flow/book;
- independent model validation;
- P/O/S/X as the primary structural-family target;
- fixed-horizon causal trade execution.

## Promotion boundary

The synthesis may propose lessons and strategies. It may not mutate permanent Frankie, Frankie 1, the frozen detector, canonical rows, Phase 1, Phase 2, runway clock, `spawn.py`, or SSOS.

Any permanent brain merge or live play promotion requires a later deliberate adjudication.
