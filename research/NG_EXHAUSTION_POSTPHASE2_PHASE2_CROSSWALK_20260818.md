# NG Exhaustion Post-Phase-2 — Phase-2 Reconciliation Crosswalk — 2026-08-18

Status: **CROSSWALK DURABLY BACKFILLED; PHASE 2 NOT REOPENED; NO PLAY PROMOTED; PERMANENT FRANKIE UNCHANGED.**

The original crosswalk workflow did not commit its output. This durable backfill applies the already-committed deterministic crosswalk rules to the first post-Phase-2 eight-agent outputs. It does **not** recompute, retune, or reselect Phase-2 findings.

## Classification result

The 25 seeded structures reconcile as:

- **13** `NEW_EXECUTION_HYPOTHESIS_ON_PHASE2_STRUCTURAL_MODULE_PROSPECTIVE_REQUIRED`
- **6** `METHODOLOGY_OR_SIGN_CONVENTION_MISMATCH_BLOCKED_NOT_PHASE2_CONTRADICTION`
- **3** `CONSISTENT_EXTENSION_OF_PHASE2_CANDIDATE_PROSPECTIVE_REQUIRED`
- **2** `CONFIRMS_PHASE2_SIGN_CHANGE_DEEPER_DECOMPOSITION_REQUIRED`
- **1** `PHASE2_INVESTIGATOR_DELAYED_REEXPRESSION_THREAD_DIRECT_TRADE_BLOCKED`

No seeded causal-contract failure was found in the first-group causality output.

## Pair/triplet structural modules → new execution hypotheses

Phase 2 established these as recurring structural modules, not frozen trades. The first post-Phase-2 group may therefore attach a historical execution hypothesis, but every one still requires a fresh prospective/OOT promotion contract:

`PP|S`, `PO|S`, `OO|F`, `OP|F`, `XP|F`, `SS|S`, `PP|F`, `XP|S`, `PPP|SS`, `PPX|SS`, `OOO|FF`, `PPS|SS`, `POP|SF`.

## Six methodology/sign-convention mismatches

The first pass independently re-selected direction from its train block for several contexts whose Phase-2 structural orientation was already frozen. These are **not contradictions** and do not rewrite Phase 2:

| Pattern | Phase-2 orientation | First-pass reselected orientation |
|---|---|---|
| `OOSS->FLIP` | AGAINST_CURRENT | WITH_CURRENT |
| `SOOS->SAME` | WITH_CURRENT | AGAINST_CURRENT |
| `OOO->SAME` | AGAINST_CURRENT | WITH_CURRENT |
| `XSX->FLIP` | AGAINST_CURRENT | WITH_CURRENT |
| `OSP->FLIP` | WITH_CURRENT | AGAINST_CURRENT |
| `P->FLIP` | WITH_CURRENT | AGAINST_CURRENT |

The corrected post-Phase-2 runner retains the frozen Phase-2 orientation for these named contexts.

## Clean Phase-2 extensions

Three named context candidates kept the frozen Phase-2 orientation and remained historically interesting in the first-group red-team pass:

- `OSP->SAME` — WITH_CURRENT; minimum OOT block mean about **+0.44 ticks**, held about **+0.50**, leave-one-week-out minimum about **+0.81**.
- `PSOS->FLIP` — WITH_CURRENT; minimum OOT block mean about **+0.36 ticks**, held about **+2.00**, leave-one-week-out minimum about **+0.75**.
- `SXOO->FLIP` — WITH_CURRENT; minimum OOT block mean about **+0.23 ticks**, held about **+1.00**, leave-one-week-out minimum about **+0.18**.

These remain historical candidate-queue items, not frozen plays.

## Phase-2 investigator failures confirmed

Two sign-changing cases again show why failures must be decomposed rather than deleted:

- `O->FLIP`: minimum pre-held OOT block mean about **+0.03 ticks**, then held about **-0.48**.
- `OOO->FLIP`: minimum pre-held OOT block mean about **+0.065 ticks**, then held about **-0.77**.

Crosswalk classification: **confirms Phase-2 sign change; deeper ancestry/timing/regime decomposition required.**

`P->FLIP` is not counted here because the first pass changed its frozen orientation, so the crosswalk blocks that result at the methodology layer before treating it as a substantive Phase-2 investigator result.

## P-O-X same-current remains a delayed thread

`POX->SAME` remains a Phase-2 investigator / delayed-reexpression thread. It does **not** become an automatic direct trade.

## Known overlap sanity checks

- `POX->FLIP` is the already-known P-O-X-opposite mechanism. Phase 2 showed zero unique instances after known-setup removal. **Not new.**
- `SOS->FLIP` lost stable confirmation after SSOS overlap removal. It cannot graduate from a broad recurrence atlas without that overlap adjudication. **Not independently promotable.**

## Systematic atlas

The first broad systematic lane contained **1,057 historical candidate motifs**, with **287** meeting its stable-preheld screen. The crosswalk does not treat those as 287 trades. They must be compressed/de-duplicated against Phase-2 mechanisms, overlap rules, causal availability, execution costs, and future prospective evidence.

## Brain implications

1. Phase-2 named-context orientation/status is authoritative when revisited.
2. Train-fixed direction disagreement with a frozen Phase-2 context is a methodology/sign-selection diagnostic, not evidence to rewrite Phase 2.
3. Pair/triplet recurrence is structural memory; trade direction is a separate hypothesis.
4. Failed folds remain `FLAG_AND_DECOMPOSE`, never delete-to-improve.
5. `POX->FLIP` is known P-O-X-opposite, not a new recurrence motif.
6. `SOS->FLIP` remains overlap-blocked by SSOS adjudication.
7. `POX->SAME` remains delayed/reexpression watch/investigator logic, not direct auto-entry.

## Promotion boundary

No play is frozen by this crosswalk. Surviving hypotheses require fresh prospective/OOT validation. Permanent Frankie, Frankie 1, the detector, canonical rows, runway clock, `spawn.py`, and the frozen SSOS play remain unchanged.
