# NG Exhaustion V4 current-state decision — 2026-08-22

Status: **AUTHORITATIVE OPERATIONAL DECISION ADDENDUM. STEP-1 FIVE-YEAR STRUCTURAL CENSUS BUILD/LAUNCH AUTHORIZED BY USER; LATER PREDICTIVE/TRADING/PROMOTION WORK IS NOT.**

## 2026-08-21 current-state continuation addendum

This document makes the current branch state explicit without rewriting frozen historical science.

1. Exact V4 engineering candidate `e1f42d71629836e0c262c3d409c31795c60062c2` is `PROVISIONAL_READY_FOR_EXACT_CANDIDATE` after the registry hash-only repair. Engineering readiness is not empirical authority.
2. The five-year native MBO acquisition/consolidation is source preparation, not a completed five-year exhaustion census. No new D0-D5 counts may be inferred until an authoritative census receipt freezes them.
3. Prefix-wide DBN enumeration is forbidden because duplicate and overlapping archive objects exist. Scientific input must be an explicit exact object list derived from exact segment manifests/job provenance and validated object-by-object.
4. Step-1 remains a dual structural census: `LEGACY_CONTROL` for continuity with frozen historical semantics and `V4_NATIVE_FULL` for richer causal MBO structure. The latter may introduce a separately versioned taxonomy; it may not silently overwrite or force novel structure into D0-D5.
5. `LEGACY_CONTROL` must pass the predeclared overlap/equivalence gate on already-revealed data before its five-year result is trusted. Any mismatch is retained and decomposed rather than deleted.
6. Every root/event/chain/censored/unresolved/short/long/sparse/contradictory case remains in the population with provenance and integrity reasons. `FLAG_AND_DECOMPOSE_NOT_AUTO_KILL` is binding.
7. Durable year/segment execution partitions may restart/resume independently but must reconcile exactly into one population. Heartbeat/progress writes are observability only and may not mutate scientific outcomes.
8. User authorization on 2026-08-22 to “finish those pieces” authorizes the missing Step-1 census plumbing and launch after its fail-closed gates pass. It does not authorize the later per-D prediction experiment, trade edge claims, six P0 real empirical receipts, release holdout use, permanent Frankie/Frankie 1 mutation, detector/canonical/Phase-1/Phase-2/runway/`spawn.py` mutation, or play promotion.

## Required completion condition

The build phase is complete only when exact-source selection, overlap/equivalence verification, structural census/crosswalk code, restart/resume/heartbeat contracts, and a launch workflow have deterministic tests/receipts. A five-year run is “launched” only when an actual durable execution identifier/receipt exists; a source inventory or workflow definition alone is not launch evidence.
