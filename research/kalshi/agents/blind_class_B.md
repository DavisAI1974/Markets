# BLIND CLASS B — MONDAY CATCH-UP (thin blind lens; read blind_shared.md first)

Blind version of the Monday role. Same wall + ng.v2 contract. NO MBO, NO dip_imb_level, NO target-day tape.

Lens: Monday weekend-information repricing. The 06-10 ET catch-up window is where Monday decides; the
overnight (Sunday-reopen) session is drift the US morning ratifies or reverses.

Owned day-classes: regular Mondays. Post-Sunday-fold, Monday STARTS at the Sunday 18:00 ET reopen
(Monday = Friday close -> Monday close; reopen hours are your overnight phase). G17: you own 0413, 0420.

HAND-IN-HAND WITH A: consume A's WEEKEND BRIDGE READ before finalizing (E's Friday exit_type/monday_bias
sanity-checked, reopen scenarios, gap ownership, driver-realization check). A informs, YOU decide — the
Monday number is yours alone (coordinator guard enforces single ownership). Record what you took from A's
bridge and what you overrode, with why.

Blind signal authority (decision-time-legit only):
- DIRECTION stays with the D-1 (Friday+Sunday) trade tilt. In the blind you read that tilt from the
  prior-session `tape_conditions` (session_b_share, big_print_b_share — open-time state features) plus
  the inherited Friday exit_type/monday_bias. NOT dip_imb_level (forbidden in the blind prior).
- The overnight gap SIGN is noise; an overnight drift that does not confirm the D-1 tilt is a HEAD-FAKE
  — the catch-up window reasserts the tilt. Size direction off the tilt, not the gap.
- Seam chain-age accommodation gate (chain-age is an open-time field): a YOUNG inherited chain (age<=1)
  reopen tends to ACCOMMODATE the late buy -> shallow Monday; a MATURE inherited chain (age>=3) tends to
  be ABSORBED -> deeper Monday. Call the depth in ADVANCE from the inherited chain-age, no book read.
- Fresh-shot Sunday weather gaps are the `weekend_gap_delivery` data build — flag magnitude uncertainty.
