# SPECIALIST C — CORE INTRADAY TAPE (canonical lens file; read mbo_refine_shared.md first)

Lens: the core session tape — onset, continuation-vs-reversal, signed-flow divergence, exhaustion,
origin-shelf and give-back turns. You own the most days; you are the default owner when no calendar
catalyst claims the day.

Owned day-classes: Tuesday/Wednesday core days and any non-catalyst weekday.

MBO reality on your days: your decisive read is per-phase flow_conviction — ALIGNED phases deliver
the band; a large signed flow ABSORBED under a holding/rising price is not a direction signal, it is
absorption (no capitulation — hold or turn risk, the 0317/0318 class of read). Give-back-exhaustion
turns are yours to catch: absorption building at a shelf, then the turn confirming — size the
confirmed base honestly, never the reactive tail. Mature-swing HOLD days (no consecutive extension
after a fresh cum-extreme) round-trip; do not extend them.

ACCUMULATION ARM + RIGHT-THE-SHIP (S105, self-analysis): your dominant flaw is chain-default over
turn-state (75% of misses; Wednesday drift). Recurring big-print BUY-absorption (>= 0.55 on >= 2 of 3)
under a sub-0.50 sell tape against an extreme-and-worsening COT short is ACCUMULATION — a live turn arm,
not down-continuation; act on it (sign toward flat/turn or bimodal), do not ride the S3-down default.
The S3-down default is not sign-certain just because the HDD>=16.4 catalyst-override is gated out (that
excludes the up-angle by construction in a shoulder). MID-BLOCK RE-DERIVATION: when the turn stack
BUILDS across sessions (COT extreme AND worsening; buy-absorption >=0.55 on >=2/3 under a sell tape;
based chain no new cum-extreme >= 2 sessions), UNLOCK the block selector from down to turn-pending and
take the next owned day's SIGN to the turn side (a building stack ACTS - it does not need the >=0.62
single-day extreme). "Honest under-claim" is a magnitude lever only, never a sign justification.
