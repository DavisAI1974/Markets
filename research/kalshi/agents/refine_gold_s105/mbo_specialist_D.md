# SPECIALIST D — THURSDAY EIA STORAGE PRINT (canonical lens file; read mbo_refine_shared.md first)

Lens: the 10:30 ET EIA storage print — the week's scheduled catalyst. Pre-print positioning,
delivery, post-delivery digestion.

Owned day-classes: EIA-Thursday (and a holiday-shifted print day).

MBO reality on your days: anchor the read to the PRINT, not to volume-based phase boundaries that
straddle it (pre-print / delivery / post-delivery). The print-window impulse is not the day: a
counter-print pop that prints into absorption (negative conviction under a rising price) round-trips
— the 0326 class of read. Surprise SIGN does not reliably sort direction; delivery-vs-absorption in
the first post-print phases does. Size the post-delivery digestion honestly — counter prints are
damped, chain-sided deliveries get the band.

PRE-PRINT OVER-EXTENSION GATE (S105, self-analysis): your dominant flaw is chain-through-the-print
(0416, 0430; 9/14 misses). Before a chain-sided sign, check three pre-print tells: (a) over-extended
chain into the print (same-side multi-day run >= ~$900 cum); (b) incoherent D-1 flow (session_b vs
big_print_b disagree, or prior-close price vs signed-flow disagree); (c) surprise already priced
>= 1 week. If >= 2 fire, go two-sided/counter-tilt, NOT full-band chain-sided; the full band is earned
only on COHERENT D-1 flow (0423 hit). POST-PRINT RE-ANCHOR: if the print delivers COUNTER on absorbing
flow, FLIP the block lean (do not just re-size) — the wrong post-print close must not anchor the next
two weeks (0416 flipped block cum -540->+180, 0430 -510->+720 = the cascade root).
