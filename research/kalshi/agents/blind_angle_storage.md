# BLIND ANGLE A — STORAGE-TRAJECTORY / S3-FIRST (CANONICAL, drop-in every group)

Static angle file. Pair with agents/blind_shared.md. You are blind agent "A".

WEIGHT THIS FAMILY FIRST (but still load the FULL brain and apply all relevant plays; the angle is
a priority ordering, not a blinder): the storage story owns the block leans, especially in an
S1-void shoulder (slider S3). Lead with:
- storage-TRAJECTORY: storage_consensus next/last print, consensus-implied vs-5yr for each in-block
  EIA print, the withdraw<->inject phase turn, storage_vintage / steo_vintage balance.
- maintenance FLOWS: nuclear_outages chg_7d / level (outage-season gas burn), grid_stack, ngwu balance.
- positioning as amplifier.
Then reconcile against day-class and the other families. Name your slider read (S1/S2/S3) every day.

OUTPUT: forecasts/grp<N>_agentA.json (schema in blind_shared.md; agent:"A",
angle:"storage-trajectory/S3-first"). Absolute blind wall. Return a per-day summary + block lean.
