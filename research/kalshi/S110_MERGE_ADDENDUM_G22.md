# S110 MERGE ADDENDUM — from the G22 refine (rides S109_MERGE_PROPOSAL_G22.md; adjudicate together)

**PROPOSAL ONLY. No brain edit has been made.** Incumbents byte-identical until Greg's go (D8).
Sources: the ten r1 posteriors in `forecasts/g22_refine_perday/` (each carries its own
proposal_contribution text); scoreboard r1 10/10, sum|err| 500, drift −80 vs blind 4/10 / 5,965 /
−1,815. Attribution rule stands: the CDD-ladder/seam-warning DATA repair (A11) changes inputs —
no G23 improvement gets banked as evidence for any play below.

## A1 — THE BURN-CONVERSION GATE (completes P0.7; the block's central mechanism)
`grid_stack est_gas_burn_bcfd` (+ wind/solar components) gates the weather slope's TRANSMISSION
to gas call. Measured at BOTH polarities inside G22: wind-on suppressed the hill (0629: CDD +4.8
arrived, burn −4.2 Bcf/d, −1,160) vs burn-up delivered it (0624 +800 on burn 33→39.3; 0630 +780
into 40.2; 0703 +410 at 44.8 block-max; 0625/0702 confirming mirrors). Scope caveats MEASURED, not
guessed: wind is a DAMPER never a SORTER (0630 printed up under a rising-wind slice); WEEKDAY
NORMALIZE (Sat/Sun burn rows collapse mechanically); staleness D+2 (a Monday serves Saturday —
the Sat-over-Sat form survives, B-0629). Sources: E's corrected two-limb flip test (executed both
ways: 0629 HOLD DOWN, 0703 PASS UP), A, B-0629, C-0623/0624/0630, D-0625/0702.
`requires`: grid_stack burn+wind decision-legit. `scope`: CDD-regime slope reads; both-ways check
(wind must not contradict an up-lean, D's P3). `forward_evidence`: 6 in-block instances, 2
polarities. **Falsifier**: a burn-confirmed CDD add that fails to deliver, or a wind-on day that
delivers gross-CDD anyway, in G23+.

## A2 — SEAM LEVELS RULE, GENERALIZED (P2 grown)
Across ANY multi-cycle seam, delta-typed channels are structurally ~zero; difference the LEVELS.
n=2 channels x 2 instances (run_delta + sunday_reopen on 0629; E's 0626 grammar; B-0629 executed
the level rule by hand and got the sign pre-tape). **Falsifier**: an intra-week day where run
delta and level difference disagree.

## A3 — SEAM SIGN FROM THE STABILITY LADDER + WIDE-BAND GAP EMISSION (NEW; resolves P3 mechanically)
The weekend gap is NOT ownable as a NUMBER (confirmed negative, B-0622) but IS ownable in SIGN:
`model_disagreement.stability` multi-horizon CDD revision deltas called all three measured seams
(0614 −750, 0621 +1,210, 0628 +50 — 3/3; positioning 1/3). GAIN from positioning extremeness
(|gap|/sigma 1.2–2.0 at a ≤5-pctile worsening book vs 0.08 mid-book); add-size→gap-size INVERTED
across instances; reopen participation refuted both directions. EMISSION: sign only on a coherent
multi-horizon add; width 0.5–1.0x sigma mid-book, 1.5–2.5x at extremes; score gap and session
SEPARATELY (P3). Bonus n=2: high-gain seams round-trip intraday (0615 gave 121% back, 0622 46%).
+ LABEL FIX (data): stability's "uncertainty conditioner" label steered readers away —
served-but-MISLABELED, the newest face of the enemy. **Falsifier**: a coherent multi-horizon add
followed by a wrong-signed gap.

## A4 — EXPIRY-DAY PROGRAM EXTENSION (E; n=2 spanning G21/G22)
`flow.resting_program_inverts_aggressor_tilt` gains `is_expiry_day` on the roll-destination leg
(G21 0608 resting/inverted form; G22 0626 aggressor/saturated form — the largest scheduled program
in the NG calendar was its missing trigger). CONSEQUENCE GUARD: big-print instruments (0.547 bar,
absorption proxy, handoff confirm limbs) lose authority on program days — read the RESIDUAL.
Control: 0625 (opex+EIA, also scheduled-flow) shows none of the fingerprints.

## A5 — EIA WINDOW DOCTRINE (D; n=4 spanning G17/G22)
Pre-print window = reopen→10:30 (not 08:00–10:30). Gate-fired days put the chain's residual
OVERNIGHT and give the post-print window to the counter side. Catalyst up-size applies to RANGE,
not NET, on exhaustion-class prints (0702: 1,380 travel, 0 net). D's own blind falsifier window
was drawn too narrow — the corrected window is the rule.

## A6 — SHOT vs SLOPE SCOPE SPLIT (A; n=2 vs n=2)
`carry_realization_flip`'s REALIZING arm is winter shot-class; a slope-class crest (priced ramp)
is the wrong scope — split the play's scope fields rather than repointing bars (the P0 KIND
lesson applied to a flow play).

## A7 — THIN-SESSION PARTICIPANT TAXONOMY (A; second consecutive forward pass)
sqrt(participation) survives pass 2 on the no-deadline-participant branch (net 410 vs 453
predicted). NEW: participant TAXONOMY — volume-bound programs preserve RANGE; risk-bound
participants (holiday-eve weekend squaring) tilt SIGN without preserving range.

## A8 — RALLY-QUALITY MAGNITUDE RUNG + COVERING-FINANCED UP-CAP (C-0701, D-0702; g19 class + g22)
D-1 rally quality sorts next-day magnitude: ALIGNED rally → hold class; CONVICTION-NEGATIVE pop →
50–70% give-back (0624→0625 vs 0630→0701). Covering-financed up-legs: full credit to the
reversal-reclaim, DENY post-reclaim extension absent an aligned phase — resolves the
absorption_is_reversal "never a cap" vs round-trip tension mechanically.

## A9 — RESIDUAL-TILT HANDOFF FIELD (B-0629; n=2 G21 0616 + G22 0629)
The Monday gate consumes the PROGRAM-DECONTAMINATED D-1 tilt, carried as an explicit handoff
field — never the roll-contaminated aggregate (the +871 that hid a net-selling residual).

## A10 — "READ-AT-THE-WRONG-LEVEL" AS AN AUDIT CLASS (E, co-signed B + A; role-file edit, SOP change control)
One failure class, three instances in one block: GROSS read where the tradeable object is the
RESIDUAL (aggressor flow gross vs ex-program residual; CDD gross vs burn residual; tilt gross vs
decontaminated tilt). Proposed as the sibling of served-but-unread in `agents/state_auditor.md`'s
known-kinds list. Requires Greg's go (canonical role file).

## A11 — DATA REPAIRS (session authority on go; attribution-fenced)
1. **The CDD-ladder ARTIFACT hole (nonconformance, D11 class, 3rd instance this session):** S109
   P4 landed the harness code; the committed g22/g23 states were never rebuilt — fwd7 span,
   sunday_reopen CDD, seam_delta_warning, ladder_basis_note all ABSENT in both. Repair: restage
   the weather-family blocks in cold from the committed weather/MOS stores if they suffice, else
   a declared derivation-note repair pointing specialists at the in-state ladder that DOES exist
   (model_disagreement per-model horizon levels + stability deltas — four specialists found it
   unprompted). G23's blind should not run without one of the two.
2. **Label fix** for model_disagreement.stability (A3).
3. Session close time on shortened sessions (S109 open item, re-bitten by A only via
   convention-luck): serve session_open_et/close_et.

## A12 — BOOKKEEPING (rides the same adjudication)
- Brain STATUS TAXONOMY normalization: {HYPOTHESIS, PROPOSED, PROVISIONAL, STABLE, RETIRED,
  WIRED_UNPROVEN} + status_note migration (68 plays, ten free-text variants today).
- promotion_review first pass: 9 promotion candidates (verify each against posteriors before
  promoting), 1 retirement flag (structure.chain_polarity_flip — refuted-language x2; HUMAN
  VERIFY: the strings may refer to the C2 arm, not the play).
- S109 P1 play (`weather.absolute_hdd_bar_unevaluable_in_cdd_regime`) stands as proposed; A1–A3
  are its constructive complement (what to read instead of the void bars).
