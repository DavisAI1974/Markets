# C15R3 six-column raw teacher increment

Authority: `parallel_r3_contracts/CLAUDE_C15_TARGET_SEMANTICS_PROPOSAL_R2_20260904.md`
sections 3.2, 3.4, 3.5, 4.2 and 5.5, accepted with rulings in
`CODEX_REPLY_TO_CLAUDE_C15_R2_REVIEW_20260907.md`; implementation authorized by
`tasks/boss-production-plan.md` item 4. Existing C15R2 source bytes stay unchanged.

The six slots retain their original order under a new C15R3 candidate identity.
At horizons 64 and 1024 completed groups, B2 is matched fill removal divided by
all top-three far-side economic removals. F is deferred until same-group C/M
reconciliation; T is flow only. Zero removals is MISSING. Ambiguity is INVALID.
C1 uses the boundary snapshot at i-k with the cutoff's 64-group flow anchor:
size-weighted identity survival, and capped original-size retention. Any C,
full removal or reset terminates the cohort lifecycle; reprice preserves it.
ID reuse never revives it. Snapshot origin does not invalidate these sizes.
Member/session crossings invalidate C1; missing cohort references and reset do
too. Empty cohorts and insufficient windows are MISSING. Nonterminal rows are
MISSING. Every non-PRESENT value is zero with mask zero.
Because public book snapshots are instrument-owned and omit publisher ownership,
mixed publishers for one instrument are explicitly rejected as unresolvable scope.

This increment emits raw share values, masks, reasons, candidate/code identity,
per-cutoff source prefix and content binding. The caller must supply an already
verified full journal prefix and trusted manifest/terminal-prefix identities;
hash binding does not authorize a source or independently verify its replay.
Tests use the unchanged public adapter with synthetic records only.

Acceptance: known answers at both horizons, fill reconciliation/no-removal masks,
cohort reprice/cancel/reuse/scope behavior, full-prefix ordering and time rejection,
repeat reconstruction identity, and unchanged R2 control attachment.
The governed JournalTeacherR3 attachment combines unchanged R2 raw definitions
for the other thirteen columns with these six shares. It emits all nineteen
columns through separate R3 identity or updating/frozen normalization, one
DipoleTarget per cutoff with exact context matching and receipts. Normalization
uses the unchanged exclusive-prefix R2 median/MAD math and scale floors; every
attachment restores its initial checkpoint, so retries cannot double update.
R3 exports and receipts carry new identities and reject R2 checkpoints. The
same attach/binding interface permits explicit runner selection; the default
runner remains unchanged. No training, calibration or empirical acceptance is
claimed. Reconstruction presently scans the complete prefix for both raw
builders; production throughput remains unmeasured.
