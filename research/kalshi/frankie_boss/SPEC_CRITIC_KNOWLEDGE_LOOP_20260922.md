# Frankie–Granite knowledge loop — 2026-09-22

Authorized by Greg: "Let's close that loop then", following the proposal to attach causally available Frankie knowledge to the dedicated critic and retain the reply for later cycles.

## Objective and contract
The next dedicated Granite critic request must carry all eligible accumulated Frankie lessons, including preserved prior critique exchanges. Knowledge is a separately typed, hashed attachment; it is not raw market evidence and cannot acquire market evidence authority. Only verified lesson records with available_ns <= the request receive cutoff may enter. The attachment binds request ID, cutoff, ordered full records and per-record hashes. Unknown availability refuses; future records are excluded. Existing history is never rewritten.

The stacked market codec and exact native inverse remain unchanged. An optional knowledge member extends the request envelope. A knowledge-bearing response must echo its knowledge hash and assess every attached lesson by hash. This proves transport and explicit response acknowledgment; it does not prove internal cognition or predictive quality. Market references still resolve only against native market fields. No arbitrary lesson, output, text or hypothesis caps are added. Physical context admission continues to refuse an oversized complete request rather than truncate knowledge.

A frozen cycle knowledge stage is created before preflight admission. Preflight, controller intent, actual provider request, durable replay and controller readback must bind that same attachment. An existing request cannot acquire changed knowledge on retry. The verified critic exchange is saved with subsequent principal feedback availability, then reaches the next eligible request. Rejected/empty critique remains labeled rejected and is preserved; it is never converted to evidence of absence.

## Integration
- critic_knowledge.py: canonical JSON attachment, validation, response acknowledgment and compact exchange preservation.
- granite_context_stacked_route.py / granite_context_route.py: separate envelope member, prompt contract, independent scoring and parser code pin.
- frankie_controller.py / controller_journal.py: exact knowledge in durable request and reconstructed critic context.
- feedback_cycle.py: frozen knowledge stage, verified availability/index checks, preserved reply in new lessons.
- sunday_native_runtime.py / operations/run_actual_sunday.py / sunday_execution.py: real preflight/host same-attachment path.
- Focused tests in research/kalshi/frankie_boss/tests; GitHub-hosted Linux CI only.

## Ordered tasks
1. Commit specification and failing regressions; record the intended RED result.
2. Implement the attachment and stacked request/response contract; verify exact native inverse.
3. Connect coordinator, controller, preflight and host; test two cycles, causal exclusion, retries and tampering.
4. Run focused/preservation tests and independent code, security and test reviews; resolve blocking findings.
5. Record exact commits/CI. Re-pin through original rerun step 1b before first authorized production cycle. Preserve real request/response/reading receipts. Do not claim runtime closure before those exist.

## Boundaries
This is one capability within the existing Monday continuation. The incomplete historical root tasks/plan.md and tasks/todo.md are preserved; this focused plan does not supersede them. Monday recovery continues independently, with no source replay or ingest restart. No canary, provider test, pinned Pod bootstrap edit, local filesystem access, runner stop or resource termination. Current launch blanks remain refusing gates. Existing physical context limit and native architecture remain.

## Validation
Remote CI runs pytest against new attachment, route, controller, coordinator, durable service and host-preparation tests. Acceptance includes full prior lesson text in the exact request; future knowledge absent; wrong hashes, omitted acknowledgment, changed cutoff/request, same-attempt mutation and dropped preflight knowledge refusing; prior rejected critique retained; complete retry causing no second provider call; all native inverse/market reference invariants preserved.

## Status
Specification accepted by the user's explicit instruction above. Implementation and production evidence pending.

## User refinement — positive, time-neutral historical priming
Greg subsequently directed priming from the prior six-hour run using only positive/helpful supported knowledge, excluding failed approaches and missing-data notes. He explicitly raised that these lessons may be future knowledge for earlier cycle cutoffs and that later cycles can carry such knowledge.

The chosen mode is knowledge_primed_learning_replay. The full audit retains truthful source availability, source request and receipt hashes. The model-visible projection contains only selected supported statements and opaque lesson/evidence hashes; it omits source dates, future prices/outcomes, failed approaches, missing-data commentary and critique diagnostics. Results under this mode are marked as a learning replay and cannot be reported as blind forecast results. Ordinary causal lessons still obey availability <= as_of.

Historical priming is a separate explicit capsule, independently bound to the retained response, host attestation/record and derivation receipt. It does not fabricate a completed training receipt. Successful verified calculation methods may enter; unstructured positive-sounding prose alone is insufficient. This selection preserves the full original history and must be carried through preflight and exact retry.
