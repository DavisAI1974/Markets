# Remaining-context output and explicit incomplete-response alert

The actual stacked route uses output_budget=remaining_context. LocalTokenizerAdmission.with_remaining_output(body) measures the unchanged complete chat prompt once and returns a canonical body plus admission with max_tokens=context-input_tokens. The selected runtime remains physically limited to 131,072 tokens; EOS can end generation earlier. No application-specific 1,200 or 8,192 cap applies to this candidate. Legacy defaults remain unchanged.

The proxy reads the pinned GRANITE_MAX_MODEL_LEN into its service context. Proxy, tokenizer, service identity and stacked request-builder gates accept output up to the selected physical context; exact local admission requires positive output and input plus output no greater than context. Only max_tokens changes in the request.

finish_reason=length raises IncompleteModelOutput with the visible message `INCOMPLETE RESPONSE — context capacity exhausted`. The durable client first preserves raw outcome bytes, then writes output-incomplete.json carrying request binding, requested output, measured input/context, result hash, and only usage counts actually present as valid integers in the backend response. Missing counts remain absent. The exception carries details and artifact_path. _serve_request propagates it, so no normal critic verdict or feedback can be accepted. Re-observation raises the same alert from retained raw outcome without any HTTP call or repeated inference.

Verification: a new remaining-output seam passed (3.68s), covering one tokenizer invocation, exact 38,645 remaining tokens for 92,427 input, 8,193 accepted where space remains, and over-total/zero-remaining refusal. A new typed-alert seam passed (3.48s), proving persisted actual usage, scorer bypass, and replay without redispatch. Earlier passing seams were not repeated. Changed actual request SHA07cc305a5bf7aff15517122624f8337b5f2fc483ff74f74a4c50e3a7365ca770 is151132bytes: 92,427input+38,645output=131,072. No prompt/native preparation/source database/model inference was changed or run.

Claude findings 6/7 follow-up: all output gates now use explicit service_context (4096 or131072), independently of compact/stacked encoding. The request builder takes service_context=4096 by default; host supplies its pinned context. Two new compact/stacked cross-gate cases passed, then one new finite-worker case passed proving IncompleteModelOutput reaches the caller with its typed error event instead of being wrapped as generic RuntimeError. Earlier tests were not repeated.

Local parity audit: for the exact remaining-output request, the current local HF invocation and the HF invocation derived from official vLLM0.20.2 ChatCompletionRequest/ChatParams/HfRenderer source produced identical92,427 token IDs (SHA6c62456f4b5b2578895920a5048a5c274a6bd21276779eef31b26b32717eab87). The same pinned tokenizer files/versions were used. This verifies local source-derived template parity, not execution inside the retained vLLM process. No model call, cloud operation, or source database read occurred. Live tokenizer count remains an explicitly unverified runtime assumption until the exact retained service provides evidence.
