# NG Exhaustion V3 PRIOR-Hierarchy Synthesis Addendum — 2026-08-19

Status: **SUPERSEDES THE EARLY-BAND TIMING INTERPRETATION INSIDE FINAL V3 SYNTHESIS; DOES NOT DELETE EARLY RESULTS.**

The first V3 core pass intentionally tests a focused early band: PRIOR ages 0..5 and H+1..H+5. That pass is evidence, but a post-birth H result cannot be called the final earliest causal answer until later pre-birth PRIOR has been searched.

Final hierarchy is:

1. preserve any independently validated early PRIOR result immediately;
2. for any model/target whose early result is H or unresolved, run later PRIOR refinement first;
3. if later PRIOR validates, it **supersedes the H timestamp as the earlier causal answer** while the H result remains preserved historical evidence;
4. only after later PRIOR search fails/exhausts its predeclared support/search boundary may an H result remain the fallback answer;
5. unresolved after PRIOR hierarchy may continue to later H checkpoints.

The final synthesis therefore additionally requires:

`research/generated/ng_exhaustion_v3_later_prior_refinement_20260819/NG_EXHAUSTION_V3_PRIOR_HIERARCHY_REFINED_ALL_RESULTS_20260819.json`

Trade alignment rule:

- a trade candidate must be attached to the final hierarchy signal for that model/stage;
- if a trade was researched from H and later PRIOR supersedes that H, the trade evidence is preserved but is **not authoritative for final strategy synthesis**;
- that model/stage trade lane must be rerun from the earlier PRIOR signal before it can enter the final trade proposal;
- no later signal may replace an earlier validated PRIOR merely because its economics are cleaner.

This addendum preserves all early-band outputs and model disagreements. It changes only which timestamp is authoritative for final earliest-causal and trade-handoff interpretation.
