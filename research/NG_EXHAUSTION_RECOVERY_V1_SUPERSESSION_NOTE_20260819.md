# NG Exhaustion Recovery V1 Supersession Note — 2026-08-19

Status: **PRESERVE V1 OUTPUTS; DO NOT USE V1 AS AUTHORITATIVE FOR PRIMARY CHAIN-TYPE OR PRECONFIRM POST-BIRTH RESULTS.**

The first full-causal recovery workflows were launched before two causal/target clarifications were completed.

## What remains usable for comparison

V1 PRIOR continuation/existence and eventual-depth calculations did not require future target polarity as a PRIOR feature. Their target vector was absent in PRIOR, so they remain useful audit/comparison evidence subject to ordinary validation review.

The D0/D1 complement audit is likewise a matched binary calibration check and is not invalidated by the polarity clarification.

## What is superseded

1. V1 primary `CHAIN_TYPE_FAMILY` used a combined `P/O/S/X|SAME/FLIP` label. The active recovery is not a target-polarity prediction experiment. Authoritative chain-type recovery uses P/O/S/X structural state/family only; SAME/FLIP is secondary annotation.
2. V1 post-birth H could expose newborn polarity/family/birth-static classifications before the target's own causal confirmation. V2 keeps causal raw newborn price available but gates target-specific static classifications until their own confirmation/availability time.

Therefore V1 post-birth and transition-inclusive type results must be preserved but not promoted as authoritative recovery findings.

## V2 authority

The V2 recovery implementation and workflows must be used for final D0-D5 synthesis. Unknown target polarity never makes PRIOR ineligible or failed.

No frozen detector, canonical artifact, Phase-1/Phase-2 result, runway clock, permanent Frankie, Frankie 1, `spawn.py`, or SSOS play is modified by this note.

Policy: `FLAG_AND_DECOMPOSE_NOT_AUTO_KILL`.
