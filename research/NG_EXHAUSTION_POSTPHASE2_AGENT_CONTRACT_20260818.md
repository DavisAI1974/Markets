# NG Exhaustion Post-Phase-2 Parallel Agent Contract — 2026-08-18

Status: **AUTHORIZED CONTINUATION RESEARCH; PHASE-2 FREEZE REMAINS IMMUTABLE; NO PERMANENT BRAIN MERGE.**

## Purpose

Continue the exhaustion pairing/grouping line after the finalized frozen 55-week Phase-2 characterization. Investigate reusable pair, triplet, longer grouping, independent-context, true/false, timing/lifespan, continuation, and execution behavior to the end of what the current frozen evidence can causally support.

This is new post-Phase-2 research. It does not reopen Phase 1 or Phase 2 and does not modify any finalized finding.

## Immutable inputs and boundaries

Use the same frozen evidence and causal protocol as the finalized Phase-2 parallel lanes:

- base54 artifact `9281733364`, ZIP SHA256 `f50eaf74a57654334691cbf5cce3b038443f6944a9c00eb5da6ca35b557802b1`;
- held artifact `9281272840`, ZIP SHA256 `21577d01d45241264df714ab6ee5b95f6a774e1475e0d74a9454221fdfdde12e`;
- Phase-1 lineage artifact `9289929292`, ZIP SHA256 `a67caab9de6b183e8c102ebd73a7e542aa909e23f66575290563e40b056efd95`;
- 55-week reconciliation artifact `9306082330`, ZIP SHA256 `f17c130df029429bfbc35067d1cc9d16128ca4fb227dd37f3fb4fbb8bbaf8875`.

Protected and immutable:

- frozen exhaustion detector;
- frozen 54-week canonical base;
- held canonical rows;
- frozen runway clock;
- Phase-1/Phase-2 frozen scores and findings;
- permanent Frankie;
- Frankie 1;
- `research/kalshi/spawn.py`;
- frozen SSOS paper play;
- existing finalized Phase-2 proposal files.

No agent may delete valid losing rows, retune the detector, use future identity before it is causally available, or turn characterization timing centers into live cutoffs.

## Parallel lanes

1. **Continuation-depth agent** — follow known pair/triplet/grouping instances forward and measure strict termination, D1/D2/D3+ continuation, successor state/polarity, and future lifespan by chronological block.
2. **Chain-map agent** — build the reusable local grammar from each surfaced pairing/grouping into its strict successor states and terminal/re-origin outcomes.
3. **Timing/lifespan agent** — test whether triplet/quad grouping behavior differs across the already-frozen D2/D3 timing families. Frozen timing centers are characterization labels only, never live thresholds.
4. **True/false decomposition agent** — preserve winning and losing instances, restore older causal ancestry when available, stratify by timing family and chronological regime, and follow `FLAG_AND_DECOMPOSE_NOT_AUTO_KILL`.
5. **Systematic-atlas agent** — examine the full causally eligible module/context population, not only the named Phase-2 winners. Fix support and orientation from the first 18 train weeks, validate chronologically later, and apply a multiple-pattern BH correction to the week-sign test.
6. **Execution agent** — for historically promising structures, evaluate causal endpoint+5 entry to endpoint+60 reference behavior, +10/+20/+30 path shape, and 0.5/1/2-tick cost stress. No stop or management optimization is allowed in this first execution pass.
7. **Red-team agent** — test week concentration, leave-one-week-out stability, block sign consistency, held behavior, and failure concentration without deleting adverse cases.
8. **Causal-knowability agent** — verify every proposed live trigger has all predecessor h=60 information available and current frozen-detector identity established no later than the endpoint+5 entry wall.

## Seeded structures

The agents must carry forward the known recurrence examples, including `PP|S`, `PO|S`, `OO|F`, `OP|F`, `XP|F`, `SS|S`, `PP|F`, `PPP|SS`, `PPX|SS`, `OOO|FF`, `PPS|SS`, and `POP|SF`.

They must also carry the independent/context queue and investigator cases, including `OOSS->FLIP`, `SOOS->SAME`, `OOO->SAME`, `XSX->FLIP`, `OSP->SAME`, `OSP->FLIP`, `PSOS->FLIP`, `SXOO->FLIP`, `O->FLIP`, `P->FLIP`, `OOO->FLIP`, and `POX->SAME`.

The systematic lane is explicitly required so the study is not anchored only on those named structures.

## Outcome discipline

For strategy research, direction is fixed from the first 18 train weeks for the new pass. Later historical blocks are validation only inside this post-Phase-2 study.

However, because many named patterns were already surfaced during Phase 2 using the 55-week history, **no named candidate may be called freshly untouched or prospectively promoted from these 55 weeks.** Fresh future evidence remains necessary before any new play freeze.

Week-relative controls and losing cases travel with every candidate. Multiple-testing-adjusted results from the systematic lane are research evidence, not automatic promotion.

## Required end products

The work is not complete at a candidate ranking. The reconciled study must produce:

- durable machine outputs for all eight independent lanes;
- a human-readable all-agent findings record, including true and false cases;
- a curated post-Phase-2 brain proposal that extends, and does not replace, the runway and Phase-2 proposals;
- separate trade-strategy proposals with exact causal trigger, direction, reference entry/exit, path behavior, cost stress, invalidators, failure modes, evidence blocks, and promotion status;
- a red-team/falsifier conclusion for each strategy proposal;
- explicit items that must **not** be learned or promoted;
- a final freeze/handoff for this study, while leaving permanent Frankie unchanged.

## Promotion boundary

This study may develop and rank historical strategy candidates. It may not merge anything into permanent Frankie and may not freeze a new play solely from historically surfaced 55-week patterns.

Any candidate that survives must be labeled for a fresh prospective/OOT promotion contract unless genuinely new untouched evidence becomes available after its contract is frozen.
