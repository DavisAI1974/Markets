# NG Exhaustion V4 Build Plan — Step-1 Structural Census Addendum — 2026-08-20

Status: **CURRENT ADDITIVE BUILD-ORDER CLARIFICATION. BUILD/PREPARATION AUTHORIZED. NO LARGE CENSUS OR V4 PREDICTIVE EMPIRICAL RUN AUTHORIZED.**

This addendum corrects one ambiguity in `research/NG_EXHAUSTION_V4_BUILD_PLAN_CURRENT_20260820.md`: the per-D pilot/prediction machinery comes **after** the expanded structural population has been constructed and frozen.

## Revised build/order dependency

### Build 0A — MBO-native causal state adapter

Build and adversarial-test the adapter that teaches Frankie the meaning of true CME/Databento MBO:

- A/C/M/R/T/F/N semantics;
- order identity;
- reconstructed FIFO priority/volume-ahead;
- full resting-order book;
- top-N and full-depth aggregates;
- queue ages/survival;
- add/cancel/modify/trade/fill flow;
- top-level depletion/add activity;
- direct trade aggressor side;
- event/receive/in-delta clocks;
- sequence/channel/publisher/instrument provenance;
- raw contract/continuous mapping provenance;
- snapshot/bootstrap and `F_LAST` validity semantics.

Native DBN remains canonical and immutable.

### Build 0B — dual Step-1 census surfaces

From the same MBO source produce:

- `LEGACY_CONTROL`: compatibility projection for frozen Step-1 equivalence/control;
- `V4_NATIVE_FULL`: full lawful causal V4 state for new structural discovery.

Do not force the V4-native structure to equal the legacy structure. Preserve a frozen crosswalk and every disagreement.

### Build 0C — generalized five-year census manifest/reconciler

Generalize the prior 54/55-week wrapper architecture to arbitrary predeclared weeks/date spans without editing the frozen pilot engines in place.

Required:

- exact source DBN hashes;
- contract/instrument/roll mapping;
- chronological week/year shards;
- boundary/censoring rules;
- no output-dependent shard changes;
- exact union/reconciliation;
- frozen control/native view identities;
- adapter/engine/ruleset hashes;
- preservation of every case.

### Build 0D — tiny overlap/equivalence preflight

Before a large structural census, verify a tiny overlap sample:

- native MBO parses and reconstructs cleanly;
- snapshots initialize state but do not create fake activity;
- state is consumed only at valid `F_LAST` event boundaries;
- `LEGACY_CONTROL` reproduces old detector/event-table behavior closely enough under a predeclared equivalence rule;
- mismatches are enumerated rather than patched using future truth.

This is an engineering/source-equivalence check, not a population finding.

### Build 0E — structural census, later and separately authorized

Only after archive/coverage and adapter/equivalence gates are green:

1. run/freeze the expanded `LEGACY_CONTROL` structural population;
2. run/freeze the expanded `V4_NATIVE_FULL` structural population;
3. build/freeze the crosswalk;
4. characterize support/families/depths/novel structures.

The full five-year run stays last among Step-1 engineering tasks and may be sharded by year/week for runtime/restartability.

## Then resume existing V4 builds

After Step-1 produces the frozen population registry, the existing build plan continues:

- pilot-manifest / D-year-chunk guardrail;
- source/contract/roll coverage;
- actual adapter -> state/lifecycle/ledger/handoff integration;
- detector-intensity semantics;
- nine-gate red-team;
- exact candidate regression/freeze;
- tiny blind V4 predictive pilots;
- D/year predictive runs;
- full multi-year D predictive runs last;
- six real P0 receipts when external dependencies are ready.

## Important semantic wall

`D0`, `D1`, `D2`, `D3`, `D4`, `D5` are **outputs/labels of a frozen structural population and later prediction targets**. The initial structural census is not required to start with a known D target.

In shorthand:

**Discover/freeze the chains first. Predict their continuation/depth second.**
