# Frankie NG Dipole Brain Crosswalk — 2026-08-16

Status: FROZEN BEFORE BLIND. No held-out price outcomes inspected.

## Contract

This is a read-only crosswalk between Frankie's already-frozen NG exhaustion reveal findings and the unchanged current Frankie brain. It does not modify the brain, schema, roles, plays, datapoints, or permanent workflow. It does not revise the reveal memo or the pre-blind dipole reflection.

Frozen source brain SHA256: `bf473faef4d5a1b8fc68a214616e6c6163f0db1794ac98818a5401225563da57`.

Frozen reveal sources:
- `research/FRANKIE_NG_EXHAUSTION_REVEAL_MEMO_20260816.md`
- `research/FRANKIE_NG_DIPOLE_PREBLIND_REFLECTION_20260816.md`

Blind rule: concept-level historical brain knowledge may be used, but any evidence attributable to a held-out event's price/outcome remains structurally sealed. A brain concept that requires unavailable price is explanatory only and may not drive a blind prediction.

## What the brain already knew that maps directly to the dipole families

### 1. `direction.flow_nowcast` -> CURRENT AUTHORITY, not universal horizon

The existing play treats signed dipole imbalance as a held-leg side nowcast when sufficiently strong, explicitly describing it as a nascent-leg nowcast rather than a from-flat forecast. That maps naturally to the reveal result that dipole level identifies who currently owns the local flow state.

Important limitation from the brain itself: the old threshold remains state-changing but its payoff later collapsed toward 50/50 and the quantity is not served in the modern day-state path. Therefore this play is not promoted into a new family-duration rule. It is retained only as evidence that dipole was already an authority/side object in Frankie's prior model.

### 2. `timing.subsecond_reversal_exhaustion` -> EXHAUSTION PATH as turn timing

The brain already hypothesized that dipole EXHAUSTION/collapse, not merely a crossing, marks the prior leader weakening near a turn. It was explicitly under-evaluated and never established as blind doctrine.

The reveal materially sharpens this concept: exhaustion path is not just turn timing. Inside A, persistence versus collapse is associated with a large and replicated runway difference. Thus the old timing hypothesis becomes a conceptual ancestor of the new state-machine view, but is not rewritten or promoted in the brain.

### 3. `fingerprints.measures.turn_exhaustion` + `exit.recruitment_reversal` -> CONTINUATION-COLLAPSE

The fingerprint brain calls turn exhaustion a confirmed continuation-collapse marker: old-direction legs stop continuing at the swing extreme. The associated play says a mature turn is read from old-swing continuation-collapse + turn exhaustion + fade, not from far-side thinning.

This is the closest prior-brain analogue to A-fast-collapse. The reveal says the same object has a price-runway interpretation: once authority collapses, the attached leg is much shorter than when authority persists.

Blind use: this is an interpretive prior. If held-out A shows collapse plus the permitted non-price flow evidence also loses same-side authority, Frankie may increase confidence in the short-runway state. Do not use any price-dependent continuation measurement that is unavailable in the blind packet.

## Blind-usable context Frankie wants available

### 4. `flow.price_free_absorption_proxy` -> EARLY ABSORBER / ACCUMULATION CONTEXT

This is a genuinely price-free brain mechanism under a positive aggregate flow precondition. It uses guard-clean big-print behavior to ask whether the absorber is still present or has disappeared.

Possible family attachment to test blind:
- absorber still present -> reduce confidence that same-side dipole persistence will propagate;
- absorber gone / decisive same-side big-print behavior -> increase confidence that a persistent dipole state can carry wider runway.

This is a modifier only. It may not overwrite the frozen family/post-state prediction.

### 5. `flow.big_print_bshare_thin_tape_guard` -> REQUIRED GUARD

The brain says big-print share thresholds over-fire when the sample is thin, with `big_prints_n <= ~20` selecting a real quarter of the historical distribution. This guard must accompany any blind use of big-print context.

Rule: no big-print-based family modifier without applying the existing sample-size guard first. Sparse prints are not evidence merely because a ratio looks extreme.

### 6. `flow.resting_program_inverts_aggressor_tilt` -> AGGRESSOR-SIGN FALSE FRIEND

The brain already warns that on scheduled-program days, same-signed aggressor tilt can be suspect because the resting program is filled by the crowd and can print the opposite aggressor sign to its own direction.

Possible family attachment:
- B's local/impulsive authority and C's local-vs-broader scale transition are both candidates for distortion by a scheduled resting program.
- if a deterministic roll/program calendar flag is active, do not let aggressor sign alone decide whether B/C authority should propagate.

This is pre-registered as a context/falsifier, not as proof that B or C is caused by roll programs.

## Concepts that are useful explanations but are NOT blind decision inputs unless causal pre-t0 price is explicitly served

### 7. `direction.flow_conviction_sign_gate`

This play requires signed flow AND price change per phase. It says aligned flow/price delivers in the flow direction while negative flow conviction is absorption/reversal. Because the held-out price answer is sealed, this concept may explain a reveal pattern but cannot be computed from withheld future price.

### 8. `direction.absorption_is_reversal`

This play also requires price-vs-flow behavior. It is explanatory support for why loss of same-side authority could accompany A-fast-collapse, but it is not a price-free blind trigger.

### 9. `flow.flowless_reprice_is_not_absorption`

The brain correctly distinguishes withdrawn-book repricing from true absorption and explicitly requires two axes: flow intensity and price efficiency. With future price hidden, Frankie must not infer this label from low flow alone. Low MBO volume is not automatically a withdrawn book.

## Open-time contextual concepts: allowed but deliberately down-weighted

### 10. `daytype.friday_turn_exhaustion_gate`

This play is conceptually compatible with exhaustion: mature chain, extreme cum/positioning, D-1 give-back or opposing flow, stretched chain, crest-realization window. However the brain health audit says the composite barely changes its output in modern forward blocks.

Blind rule: use as weak context only; do not let Friday identity manufacture a family/post-state prediction.

### 11. `daytype.eia_preprint_overextension_gate`

This pre-print play uses over-extension + D-1 flow coherence + surprise-pricing context, but its own health audit records defective/degenerate limbs and stale/masked inputs in modern history.

Blind rule: EIA/event context may qualify uncertainty, but this play cannot override observed dipole family/post-state evidence.

### 12. `monday.seam_chainage_accommodation_gate`

The mechanism is interesting because chain age is proposed as a leading indicator of whether late flow is accommodated or absorbed. But the brain audit says chain age is not a reliably served measurement and has often been a specialist-constructed label.

Blind rule: do not use unless an actual measured chain-age field is legally present. Never construct the label to fit the dipole read.

## Existing brain negative that Frankie must carry

### 13. `boundary.prior_close_flow_direction_disagreement` -> DO NOT RESURRECT

This play looks superficially relevant to a persistence/collapse problem, but the brain's own audit is negative: the trigger splits states cleanly and then predicts the next session incorrectly (recorded ceiling 5/13 on G20-G23).

Blind rule: prior-close flow/price disagreement may be reported as context if served, but it may not be used as positive evidence for a dipole-family prediction. This negative is carried specifically to prevent post-hoc revival.

## New early precursor candidates found in the revealed packet BEFORE blind

These are experiment findings, not changes to the brain.

### A: pre-flip authority already tilts toward the later persistent state

Within revealed family A, post-state was defined from the post-t0 dipole path only. Looking backward strictly to t<0 after that state assignment shows a modest but repeatable precursor:

- last-20s oriented roll-20 dipole mean median: persistent ~0.544 vs collapse ~0.255;
- last-10s oriented roll-20 dipole mean median: persistent ~0.800 vs collapse ~0.463;
- pooled discrimination is modest (AUC ~0.590 for both measures), but the direction is the same on all four independent days; day-level AUCs are roughly 0.57-0.62.

Interpretation: A-persistent tends to arrive at t0 with authority already more continuously established, while A-fast-collapse more often builds into t0 from a weaker pre-state. This is an EARLY CONFIDENCE MODIFIER, not a replacement for observing the post-state.

### A: lower pre-t0 raw MBO trade volume is another precursor candidate, but mechanism is unresolved

On the three days with exact matching MBO, median trade volume in the 20s before t0 is lower for A-persistent than A-collapse (roughly 1 vs 3 in the packet's per-second aggregate measure). The lower-volume side predicts persistence with pooled AUC ~0.629, with all three MBO days around 0.62-0.64.

This must NOT be labelled withdrawn-book continuation automatically. The brain's own `flow.flowless_reprice_is_not_absorption` play requires price efficiency as a second axis. Freeze only the empirical observation: stronger normalized dipole authority under lower raw trade volume may be a persistence precursor worth blind testing.

### B: weak pre-authority precursor, low confidence

B-persistent has a higher last-20s pre-dipole authority mean on the pooled reveal sample (AUC ~0.60), but B has only 37 revealed events and the day-level result is variable. Carry as low-confidence only.

### C: same-side pre-t0 aggressor flow may foreshadow broader persistence, low confidence

C-persistent has stronger oriented aggressor net flow in the 20s before t0 on the pooled reveal sample (AUC ~0.67), but C has only 62 revealed events and the day-level result is uneven, including a flat July result. Carry as a low-confidence precursor hypothesis, not doctrine.

## Frozen blind hierarchy

Frankie will enter blind with this ordering, fixed before any held-out outcome is seen:

1. Frozen A/B/C family identity from pre-only dipole geometry.
2. Frozen reveal post-state rules and horizon relationships from the reveal memo.
3. Pre-t0 precursor confidence modifiers above, with A strongest and B/C explicitly low confidence.
4. Existing price-free brain context when legally served: price-free absorption proxy, big-print thin-tape guard, deterministic resting-program calendar effects, regime/session context.
5. Price-dependent brain mechanisms are explanatory only and cannot drive blind calls unless the needed causal pre-t0 price field is deliberately and legally included in the blind packet.
6. Existing negative findings remain negative; no revival because they sound convenient.
7. No family boundary, post-state centroid, rule, threshold, or brain crosswalk may be retuned after held-out price is revealed.

## Frankie's pre-blind objective

The first blind objective is not merely to match a family to a leg length. It is to test whether the frozen state-machine picture generalizes:

`pre-family -> early authority context -> post-exhaustion state -> horizon-specific price response`

The more valuable secondary test is whether an existing price-free brain concept or the pre-t0 authority precursors identify the eventual post-state EARLIER than waiting for the full exhaustion curve.

This crosswalk is immutable experiment context for the blind phase.