# NG Exhaustion Duration Target Correction — 2026-08-18

Status: **AUTHORITATIVE TARGET CORRECTION FOR THE ACTIVE ENTRY-TIMING REVIVAL.**

User correction: **Exhaustion is being studied as a duration / remaining-runway signal, not as a direction predictor.**

## Correct target

The exhaustion program should answer:

- how long the current exhaustion-backed structure/run is likely to remain alive;
- how much structural runway remains at detector confirmation;
- how that remaining-lifetime estimate updates causally as the structure survives;
- when the structure is approaching collapse/end-of-run.

Direction belongs to the already-established leg/setup/parent context. Exhaustion does not need to infer the sign of the trade.

Known leg polarity may be used only as a conditioning/context feature for duration if causally available. It is never the prediction target and is never promoted as an exhaustion-derived directional call.

## Status of the just-completed signed-return diagnostics

The following durable artifacts are preserved for provenance but are **NON-AUTHORITATIVE WRONG-TARGET DIAGNOSTICS** for the exhaustion research objective:

- `research/generated/ng_exhaustion_entry_timing_revival_20260818/lane3_plus0_profitability/`
- `research/generated/ng_exhaustion_entry_timing_revival_20260818/lane4_fallback_first_crossing/`

Their failure to validate signed endpoint direction does **not** count as evidence against exhaustion duration skill.

Do not delete or rewrite them; simply do not use them to adjudicate the duration hypothesis.

## Corrected next lanes

### Duration Lane 3D — detector-confirmation forecast

At detector confirmation = +0, predict **remaining structural lifetime in seconds** to the frozen descendant boundary using only causally available information. Preserve all 18,837 exact D1 rows. Rows whose detector confirmation is not before the descendant remain explicit non-positive-runway cases rather than being dropped.

Use chronological discovery-fit / discovery-tune / validation / confirmation / held separation. Compare the model against a naive fit-period duration baseline. Report error in seconds and log-time, factor error, interval coverage, week stability, and survival-horizon classification implied by the duration forecast.

### Duration Lane 4D — causal duration updates

For structures still alive after +0, update the remaining-lifetime forecast at the dense checkpoint grid:

`+1,+2,+3,+4,+5,+10,+15,+20`, then every 5 seconds through +3600 while structurally alive.

At each checkpoint use only information causally available by that checkpoint plus the fact of survival. Compare the contextual duration model against an **age/survival-only baseline** at the same checkpoint so that mechanical survivorship is not mistaken for predictive skill.

The key question is: **how early does causal state improve the remaining-lifetime estimate beyond simply knowing that the structure survived to this age?**

## Trade-strategy interpretation

If duration forecasting validates, subsequent strategy research should combine:

**independent leg/setup direction + exhaustion-predicted remaining runway**

for hold/exit/re-entry sizing and timing. It must not infer trade direction from exhaustion.

Price-path directionality/chop may remain descriptive outcome context, but no directional or chop classifier should be promoted as the exhaustion target under this correction.

## Frozen boundaries

This correction changes only the research target. It does not modify the frozen detector, canonical evidence, Phase-1 lineage, finalized Phase-2 findings, frozen runway clock, permanent Frankie, Frankie 1, `research/kalshi/spawn.py`, or the frozen SSOS play. No permanent brain merge or play freeze is authorized.
