# S62 KICKOFF — FLIP-THEN-MANAGE the big-loser prize + get the coefficient tier

⛔ STANDING BANS unchanged: BYBIT never; MEXC dead. ELIGIBILITY BEFORE ANY VENUE WORK.
KRAKEN PARKED until the Coinbase exit deploys (Greg). Fee frame = COINBASE (Greg handles the
fee tier; never label analysis "kr_mk0" — it confused things in S61).

Read first: `S61_BUILD_NOTES.md` (R1–R13f round-by-round) + `SESSION_HANDOFF_2026-07-05_S61.md`.
Branch: designated claude/*; reconcile to canonical `5c5vg9` FIRST (reset --hard if the harness
cut a stale parent — happened 4 sessions running); sync canonical every push; a parallel session
may push — rebase, don't clobber.

## STATE (S61 close)
- **COINBASE EXIT BUILD SHIPPED:** honest fill model wired (`fill_model="queue"`, binding
  constraint = price eligibility not queue depth); 2 exit variants coded + accruing sandbox
  (doge cascflip, btc plainstop X=40); canary bit-identical mod the `stop_exit` key. SOL =
  PROTECT (power bar guards it); BTC rider ops-bar-BLOCKED (needs exchange-side stop).
- **THE BIG-LOSER FLIP ARC (Greg-driven, the session's meat):** the prize is real (flip big
  losers = +6.79/hr; 3-part oracle +26/hr, 508/508 winners kept). No mid-leg CAUSAL signal
  reaches it (twins at the decision moment; winners-invisible). Greg's FLIP thesis = the best
  single-feature lever: flip the deep-fade zone (pre600<-80) = +2.38/hr (+0.61 over baseline),
  pre number only — but WEEK-FRAGILE (wk4 -1.66).

## ⭐ FIRST THING (Greg, explicit at S61 close): FLIP-THEN-MANAGE
Build + grade the R13f refinement — the concrete wk4 mitigation:
- RULE: flip pre600<-80 (reverse the deep-fade entry). Then MANAGE the reversed leg — if it
  moves AWAY (flip was wrong, we flipped a deep-fade winner) FLATTEN small; if it moves our way
  RIDE. A self-contained price-managed stop on the flip; caps the mis-flip downside (the +291bp
  deep-fade winners get cut small instead of riding to a loss).
- GRADE: per-week (does it rescue wk4? the -1.66 week is where the mis-flipped winners live),
  shuffle floor, $/hr, FEE-ACCOUNTED (the flip + the manage-stop are TAKER crosses). SOL bins
  powered first, then the other coins, then Coinbase books shape-check.
- WIN CONDITION: the flip-then-manage clears its shuffle floor AND wk4 is no longer negative =
  the session's fragile +0.61 becomes a candidate robust edge. Wire as a SANDBOX variant only
  (sandbox-first, baseline canary bit-identical) if it clears.

## JOBS (one round = one defined test; update S61_BUILD_NOTES successor EVERY round)
1. **FLIP-THEN-MANAGE** (above) — the live lead.
2. **REGIME GATE** alternative: gate the flip to stand aside in the wk4-type regime, via the
   S61 dipole regime conditioner (rolling cross-coin signed CORRELATION vs BTC, ~400s — the
   1/1000-compute descriptor). Does regime-gating the flip beat flip-then-manage?
3. **⭐⭐ GET THE E: COEFFICIENTS** (Greg brings them): the 128-dim OD coeff signatures
   (`markets_<cell>_win_cand_sp`) + centroid dual-print + onset re-anchor (`_win_onset/`) from
   Greg's LOCAL E: DRIVE (NOT in-container). Then build the HEAVY ENTRY FINGERPRINT on the
   mid-band big-loser-vs-winner population (hist AUC 0.72-0.84 vs the 0.606 cheap floor) —
   the real path to the +6.79/+26 prize. PRECONDITIONS: onset canary MUST pass (encoder
   reproduces onset micros from strictly pre-entry bars) + per-(cell, MID-BAND) revalidation
   (scale-locality) + assert_no_leakage.
4. **COINBASE EXIT finish:** the 2 fee-tier clicks (biggest lever, no tape); BTC ops-bar
   (exchange-side stop + staleness kill-switch) before that rider touches capital.
5. **UN-PARK KRAKEN** exits + books once the Coinbase exit deploys.

## Rules (standing): crypto only; zero synthetic; per-cell deploy; never tune off one window;
graded beats gated; scale-locality; venue law; PROFIT-PER-HOUR is the scoreboard (per-leg =
diagnostics); sandbox-first + baseline canary bit-identical; git = source of truth;
falsification-first; MPLBACKEND=Agg; token 403 on GHA dispatch (Greg clicks Run workflow).
