# KICKOFF — S45 (next session)

Branch: dev/push on `claude/crypto-liquidity-signals-5c5vg9-kb2i5c` (harness-designated) AND
`claude/crypto-liquidity-signals-5c5vg9` (canonical) — keep both synced (they were equal at the S44 tip
`5d51c51`). **The harness keeps landing on the stale S37 branch — if HEAD is at `7256abd`/S37, fast-forward
to the canonical tip FIRST** (`git merge --ff-only origin/claude/crypto-liquidity-signals-5c5vg9`).
Default branch is `claude/new-session-o3vnm` (carries the cron'd collector workflows).

Read `SESSION_HANDOFF_2026-06-29_S44.md` then the S44 delta atop `CLAUDE.md`.

Rules: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git is source of truth
(commit+push often); never tune off one window; falsification-first; agnostic 5-step coupler reads what
couples (don't impose a direction). dipole_r2~1 on signed channels is the H_a=−H_b tautology — judge by
structure_z. OD-BOOK `.ttest_committed.json` sentinel is FROZEN (taker KILL) — do NOT delete it.

## State inherited (committed, S44)
- **Multi-coin book collectors LIVE.** `coinbase_book_collector.py` (symbol-parameterized) + matrix
  workflow `book_collectors_durable.yml` = **eth/sol/doge/xrp** on Coinbase (btc dropped — spread-starved
  1-tick control, frozen at ~46.7h on `data/btc-book`). Workflow is on the DEFAULT branch, so the 6h cron
  self-sustains + the Run-workflow button works. No `push:` trigger (was causing duplicate runs). Each job
  checks out `5c5vg9` for code. Live smoke-test: SOL half-spread ~0.69 bps (~860× BTC) = the wider-spread
  cells the maker test needs.
- **QuietFloor gate WIRED into the live dipole.** `IncrementalQuietGate` (causal O(1)/tick) +
  `RollingFlow.gated_signal()` (`odcore/incremental.py`); `dipole_gated` generator; per-cell driver
  `_wire_quiet_gate_book.py`; canary `_canary_quiet_gate.py`. The gate works on the **book DEPTH imbalance**
  (phi_q≈0.95), NOT trade-flow ofi (quiet cells zero → degenerate). On btc_coinbase book: raw fires 100%
  → gated 6.9% (stands aside 93%), 2.54× shock-vs-quiet selectivity, OOS hit 69.8%→70.9% retained,
  incremental == batch on all 1.68M cells. Deploy form: gate = WHEN, level sign = DIRECTION.

## Open tasks (priority)
1. **Check the alt book accrued** (`data/eth-book` / `data/sol-book` / `data/doge-book` / `data/xrp-book`).
   Extract a cell: `git fetch origin data/sol-book && git show origin/data/sol-book:sol_coinbase_book.jsonl.gz | gunzip > /tmp/sol_book.jsonl.gz`.
   Need a few hours per cell before the tests below are meaningful. If a run failed, it's the button/cron's
   job (token can't dispatch). Note: each alt also nears the 100 MiB push cap after ~4–5 days → off-git/
   sharded storage is the durable fix (blocked on cloud auth).
2. **THE decisive test — re-run `_maker_fill_model.py` PER CELL on a wide-spread alt (SOL first).** This is
   what the maker model was built for: half-spread (~0.7 bps SOL, 2–5 bps thinner alts) should now dominate
   the ~0.5 bps adverse selection, so the gated signal-as-filter + spread + rebate should clear NET. btc was
   the negative control (1-tick, no spread → no edge); SOL/DOGE/XRP are the real test.
3. **Re-run `_wire_quiet_gate_book.py` + fit `QuietFloor` PER CELL** across eth/sol/doge/xrp; confirm the
   churn-cut + retained-direction (and the ~2.5× selectivity) hold beyond btc_coinbase.
4. **Re-run `_liquidity_dive.py` per cell** — confirm the depth_imb → next-move edge + sign/lead are stable
   across venues/coins (cross-segment consistency is the stand-in until multi-cell).
5. Wire `RollingFlow.gated_signal()` / `dipole_gated` into the production emit path once a per-cell deploy
   map exists (the gate is built + canaried; it just needs per-cell QuietFloor coefficients).
6. OD-BOOK threads 2/3 stay NOT built (gate held). Cross-domain falsification still open.

## Cautions
- realbins + `/tmp/*book*.gz` + PNGs gitignored; re-extract from the data branches as above.
- Pure-Python per-tick loops over ~1M+ rows take minutes — run patiently / in background.
- Avoid backticks in `git commit -m`. The integration token can't dispatch/cancel GHA runs (403
  actions:write) — collection runs are Greg's button / the cron.
