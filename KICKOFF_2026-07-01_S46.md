# KICKOFF — S46 (next session): WIRE the maker-at-the-turn strategy + re-evaluate the SOL losers

Branch: dev/push on `claude/crypto-liquidity-signals-s45-y2ni2m` (harness-assigned) AND
`claude/crypto-liquidity-signals-5c5vg9` + `-kb2i5c` (canonical) — keep all three synced (equal at the S45
tip `6050ceb`). Default branch = `claude/new-session-o3vnm` (carries the cron'd book collectors). **If the
harness lands on stale S37 (`7256abd`), FIRST** `git merge --ff-only origin/claude/crypto-liquidity-signals-5c5vg9`
(or `git merge` if it has diverged with collector commits, as S45 did).

READ FIRST: `STRATEGY_maker_at_the_turn_S45.md` (the build spec) → `SESSION_HANDOFF_2026-06-30_S45.md`
(esp. the "S45 FINAL" section) → the S45 delta atop `CLAUDE.md`. Renders are in `docs/renders/`.

RULES: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git=source of truth (commit+push
often); never tune off one window (all S45 maker numbers are PROVISIONAL on one ~11.7h window, tiny fill
counts); falsification-first. OD-BOOK `.ttest_committed.json` sentinel is FROZEN (taker KILL) — do NOT delete it.

## Where S45 landed (the one-paragraph version)
The decisive maker test ran on the accrued multi-coin book. The btc-locked operator (K=10) does NOT transfer
— alts want K=1 (top-of-book), which flipped DOGE NET+. The fill autopsy pinned the bleed on adverse selection
in the queue wait (a passive quote fills only when flow is against it). The flip filter we tried was wired
BACKWARDS (opposing/reversal); CONFIRMING/continuation is the right polarity for a hold=1 maker. But walking the
losers with Greg surfaced the REAL strategy, which supersedes all of that: **maker-at-the-turn**.

## THE S46 JOB — wire maker-at-the-turn and re-evaluate the trades
1. **Set up data** (gitignored, re-fetch ~4 min):
   `for c in sol doge xrp eth btc; do git fetch origin data/$c-book; git show origin/data/$c-book:${c}_coinbase_book.jsonl.gz | gunzip > /tmp/${c}_coinbase_book.jsonl.gz; done`
   Per-cell depth-K: btc=10, alts=1.
2. **Wire the executor** (`odcore/swing_maker.py` new, or extend `odcore/maker_book.py`):
   - position state machine {flat, short, long};
   - ONE-SIDED quoting that swaps the shown side at each flip (PEAK→show offer/pull bid; VALLEY→show bid/pull
     offer; chase the shown side as price moves; the other side stays PULLED);
   - direction = fade the extreme from the flip detector (`odcore/info_dipole.py` divergence / `flip_detector.py`)
     at a CAUSAL price extreme; the QuietFloor confirms the shock;
   - fills = aggressor volume clearing the queue on the SHOWN side (maker_book already models this);
   - hold to the NEXT turn; maker on BOTH legs; taker only as fallback to flatten.
   - leakage-safe (causal turn detection; fit on train; metrics on test).
3. **Re-evaluate the SAME SOL losers** we walked through (`_render_one.py` / `_render_trades.py`). Expectation:
   loser #1 (BID −9.98 at a peak) inverts to a SHORT lifted ~75.09, covered at the ~74.96 valley ≈ +16 bps.
   Re-render to confirm entries now sit short-at-peaks / long-at-valleys.
4. **Per-cell verdict** (sol/doge/xrp/eth, btc control), net of two maker legs. PROVISIONAL one window.

## Validation gates (honest; in the spec)
- Does the flip mark the extreme CAUSALLY and in time (S36b budget: ~5-6 bps off the turn at 1-sec)?
- Enough climax/aggressor volume AT the turn to fill the passive quote (S40 ~2x vol — verify per cell; no
  climax → skip the turn or cross as taker)?
- Round-trip net (peak short + valley cover, both maker) vs the flip's ~36% wrong-tail (one-sided shorting into
  a continued rip — gate on flip+exhaustion/climax, cap inventory); trade only swings ≳ 20 bps (S36b fee floor).

## Notes
- Chat may run the per-coin OD operator (turn/toxicity) in parallel — NOT required to start the wiring (Greg:
  "i don't know if we need the full run yet"). The default `research/od_book/run_experiment.py` is VAL-only/SAFE.
- Book accrual: 6h cron self-sustains; each alt nears the 100 MiB push cap in ~4-5 days → off-git/sharded storage
  (blocked on cloud auth) is the durable fix. The integration token can't dispatch GHA (403) — runs are the
  button/cron.
- Pure-Python per-tick loops over ~1M rows take minutes — run patiently / in background; use a 5-min tool timeout.
