# KICKOFF S82 — Kalshi: sub-second lag on live Pyth ticks + Thursday's natgas release

**Read in order:** `SESSION_HANDOFF_2026-07-12_S81.md` → CLAUDE.md S81 delta (top) → `KALSHI_TRADING.md`
(the file index) → this file.

**Branch:** `claude/kalshi-s79-kickoff-ij8t9o` — repo DEFAULT; push there. If the harness assigns a fresh
branch, check `git log --oneline -1` FIRST — a stale S70 tip is the recurring trap. Work on s79.

## Where we are (end S81)
- **The lag thread is understood.** Futures lead Kalshi; direction is predictable and sharpens with move
  size (0.77 on ≥$0.40 moves); the edge is **size-vs-fee, not direction**. At 1-min it's real but rare
  (+91¢/26 trades). The "full-minute lag" was largely **coarse-bar aliasing** — a direct event-study showed
  the liquid strike repricing within ~7–20 SECONDS. The exploitable lag lives in the less-liquid strikes and
  needs sub-second data to measure cleanly.
- **The sub-second feed is live.** `pyth_collector.py` + `pyth_collector_durable.yml` stream WTIQ6/NGDQ6/
  BRENTU6 (Kalshi's settlement source) → `data/pyth-ticks` every 6h. Ticks accrue from Sun-eve reopen.

## S82 priorities
1. **SUB-SECOND LAG on the accrued live ticks (the decisive tradeability test).** Fetch+gunzip
   `data/pyth-ticks`; align each Kalshi contract's ms trade tape against the Pyth tick tape at 1–5s. Per
   contract (never pooled): which strikes lag, by how many SECONDS, and does the whole reprice — captured
   before it decays — clear the toll? Use `score_hold` (fire-quick-then-hold trailing exit) at real
   resolution. Distributions + winner fingerprints, no means. This answers "is fire-quick-hold real money."
2. **Thursday 7/16 EIA natgas (14:30 UTC).** (a) first LIVE release book test:
   `release_book_signal.py --test --series KXNATGASD` on the accrued `data/kalshi-bins`; (b) busy-day natgas
   lag on live Pyth NGDQ6 ticks. Run `consensus_poll.py` before (forecast) + after (actual via EIA).
3. **THE PER-TRADE LEVEL-HIT DATASET (priority-1, STILL not started).** The each-trade-individually rebuild
   on the 496k WTI tape (re-pull: `kalshi_history.py --series KXWTI --all --top 12 --skip-candles`, ~7 min):
   one row per level-hit event — context {moneyness, side, time-of-day, release flag, velocity, herd/whale
   fingerprint, exhaustion} + outcome {next level hit before reversal? run length, net of that strike's real
   spread+fee}. Distributions + winner fingerprints per cell. Leakage-gate first. The continuation predictor.
4. Standing: **front-month roll re-point** (WTIQ6 exp 7/21, NGDQ6 7/29, BRENTU6 7/31 — update FEEDS in
   `pyth_collector.py` when they expire); paper-loop RSA creds; OD-weather → `kalshi_score.py` (Greg's spec).

## RULES (load-bearing)
EACH TRADE INDIVIDUALLY — never average; distributions + per-trade fingerprints, not means. Per-cell always
(moneyness × side × lag-class × release). Exclude the settle window. Catalyst = trigger + coarse size; book/
flow imbalance + exhaustion = direction + magnitude; herd breadth = continuation, whale = scalp-only. Leakage
gate before any backtest. Zero synthetic. Provisional-until-live. Weather = Greg's spec, HANDS OFF.
`--events` on news_coupling_research = BASENAME. Keep `KALSHI_TRADING.md` current as files are added/retired.
