# KICKOFF S94 — Kalshi. Get the AWS coach agent's LLM invoking, then RUN THE LOOP on the box.

Run the **`kalshi-session-start` ritual** (you'll land on the stale S70 tip -> reset onto trunk
`claude/kalshi-s79-kickoff-ij8t9o`; push there). READ (in order): `SESSION_HANDOFF_2026-07-14_S93.md` ->
`deploy/aws/COACH_AGENT_SETUP_S93.md` -> `research/kalshi/FORECASTER_RUNBOOK_S93.md` -> `CLAUDE.md` ->
`KALSHI_TRADING.md` -> `knowledge/ng_brain.json`.

## SECRETS to re-paste (never git; ROTATE early — also live in `/etc/markets/coach.env` on the box):
`AWS_ACCESS_KEY_ID=AKIAYI6JDCBVLKYQGLMH`, `AWS_SECRET_ACCESS_KEY` (txRGHd...), `DATABENTO_API_KEY` (db-3ba8...).
Bedrock region = **us-east-1**; S3 region = **us-east-2**. boto3 needs both set right (see handoff).

## STATE (S93 built the AWS agent home; the brain did NOT advance — still s92.1)
- AWS access fully resolved; box `i-08cee7171c0a76a04` **Online in SSM** (drive via `scratchpad/ssm_run.py`).
- Bedrock LIVE in **us-east-1** (boto3 converse OK: opus-4-1/4-5/4-6, haiku-4-5). Claude Code 2.1.197 installed
  on the box. Node 20. `/opt/markets` checkout. Data pull (PID 2541) still running (~halfway; Jul-Dec 2025 done).

## JOB 1 (do FIRST) — make the agent's LLM invoke on the box
`claude -p` currently fails on a Claude-Code model-preflight mismatch (NOT access — boto3 works). Pick ONE:
- **Fix Claude Code + Bedrock**: enable opus-4-6 model access in us-east-1; OR set BOTH region vars to us-east-1
  for the `claude` process + pin S3 clients to us-east-2 in code; OR `claude --model <arn>`; OR change CC build.
- **OR switch backend**: Anthropic API direct (`ANTHROPIC_API_KEY`, drop Bedrock) OR **OpenAI** via a small
  `coach_backend.py` (Greg's option — `openai` SDK, same decision-state-in/forecast-JSON-out contract).
GATE: a headless one-shot must return (`Reply with: COACH-ONLINE`) before touching the loop.

## JOB 2 — RUN THE LOOP on the box (the actual goal — build the brain off the data)
Group-2 (12 fresh warm-season NG days, staged): **20250702,20250707,20250710,20250715,20250725,20250728,
20250730,20250818,20250820,20250826,20250828,20250829**. `data/eia_surprise.json` rebuilt (curve = unknown, ok).
Recipe: `forecast_harness.py decision-state --days <grp2>` -> agent BLIND-forecasts from `knowledge/ng_brain.json`
(NO tape peeking) -> `forecast_harness.py overlay` (score vs S3 tape) -> merge into brain (version bump) -> commit.
Then next group -> walk the year -> loop back + refine. Keep the memorization guard (true holdout + forward-live).

## JOB 3 — deploy durable + hygiene
Once a one-shot works: `markets-coach.{service,timer}` (canary one group first, then enable). Standing: box year
finish -> `--reconcile-names` -> final Monday sweep (NG 13 / CL 24 stubs); consolidate the box onto ONE role
(SSM+Bedrock+S3 instead of the separate `Ssm`/`Bedrock`/`S3` roles); ROTATE keys.

## Rules: each event individually / never pool-as-final; per-cell; distributions not means; leakage + blind wall;
net-of-fee maker AND taker; exclude settle window; zero synthetic; provisional-until-live; git = CODE, S3 = ALL
DATA; NG != WTI; weather HANDS OFF; keys are SECRETS.
