# KICKOFF — Session 31 — written 2026-06-13 (end of S30)

Read order: `SESSION_HANDOFF_2026-06-13_S30.md` → this file → `CLEANUP_RUNBOOK.md`. Memories auto-load.
Branch: continue on the S30 working branch. KEEP PAIRS SEPARATE; each bucket separate; zero synthetic; Result Discipline.
**Machine is fragile (4 CPUs) — if any discovery rerun is needed, cap at 3 workers (`--workers 3`), batch-100, resume.**

## STATE COMING IN (S30 result)
- **Win-oracle collapse: root-caused + labels fixed.** Audit read the 6h `live_data` snapshot → every oracle exit clamped to
  the 05-28 audit-run window. Fixed by relabeling each trade with best-exit within its OWN true horizon from the history
  archive (`_relabel_true_horizon.py`; 17.3% of labels flip). [[markets-oracle-audit-snapshot-clamp]]
- **Coefficients are SOUND** (pre-entry window features, label-independent) — re-partitioned by corrected label, not recomputed.
  Index: `_cs2000_coeff_index.json(.gz)` (also the dropped TODO#1 compact coeff copy).
- **Two confounds controlled.** (1) corrupt labels (fixed). (2) win/lose pools disjoint in time+pipeline (win 05-23/24 chunkhash,
  lose 05-04/11 slice). [[markets-win-lose-pool-provenance-confound]]
- **Clean verdict: dipole = real but WEAK** (AUC ~0.6; pooled z=+5.0 chunkhash, z=+13.6 slice, 9/12 pairs z>3). NOT the +9.6
  artifact, NOT dead. [[dipole-real-on-128dim-per-pair]]
- **Net-of-cost standalone test: NO standalone edge** (realistic fixed-horizon exit loses gross; dipole gating edge −0.74 bps).
  This is ONE STORY, NOT THE WHOLE STORY (Greg): the dipole is a GATE / spread-adjuster in the architecture, not a standalone
  entry signal. A negative standalone result does NOT close anything down.

## TODO — back to the plan
1. **(optional, quick) Finish the net-cost combos** deferred from S30 — `_netcost_backtest.py {chunkhash bestexit | slice fixedh
   | chunkhash fixedh}` (matched-objective `fixedh` gives the dipole its best shot; chunkhash confirms the gating edge on the
   other universe). Expect ~0 standalone edge; it rounds out the "one story," nothing more.
2. **BACK TO THE CANONICAL PLAN.** The dipole's role is settled (weak gate piece). Resume the architecture per
   `QUOTE_SERVICE_PLAN.md` (T3.1 `mm_passive` maker-rebate spread capture gated by the regime classifier + OI/CB-premium/basis/liq
   monitors, with OD coupling/lead-lag/decoupling/**dipole as GATES + spread adjusters**) and `BUILD_PLAN.md` (the OD/PySR-Julia
   5-step rebuild). Identify the plan by Greg's PySR/Julia/5-step test, NOT by filename. [[markets-canonical-plan]] [[follow-canonical-plan-rule]]
3. **(upstream, if win labels ever needed verbatim)** Regenerate the hindsight audit using `markets_bar_loader.load_closes`
   (history archive) instead of the 6h `live_data` snapshot, so each trade gets a real per-trade oracle entry/exit. Generator is
   not in the repo (refrag/local). Not on the critical path for the gate-architecture work.

## ENVIRONMENT
- `E:\refrag` mounted (code + data); discovery pipeline PySR/Julia-free. Bars: `markets_bar_loader.load_closes(asset, venue, t_min, t_max)`.
- Discovery driver (if needed): `_run_clean_rerun.py --cross-section --target-size 2000 --pre-entry --batch-size 100 --workers 3` (resume-safe).
