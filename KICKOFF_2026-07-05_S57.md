# S57 KICKOFF — GROW THE VALIDATED CELL (S56's model passed the full gate; now it accrues,
# scales, and waits on exactly one external gate: the Bybit MM application)

Read first: `SESSION_HANDOFF_2026-07-03_S56.md` → CLAUDE.md S56 delta. Branch: designated
`claude/*`; reconcile to canonical `5c5vg9` at start (fetch + `--ff-only`, else reset --hard);
keep 5c5vg9 synced on EVERY push. Default branch `claude/new-session-o3vnm` = crons, LEAVE IT.
Run `python scripts/paper_trade.py` FIRST — it now runs DEPLOYED (Coinbase baseline, must be
+N>=0 with ledger 25,845+ intact) AND the S56 SANDBOX cells (sol/eth bybit @ true MM3 →
paper_ledger_sandbox.jsonl, seeded 3,735 @ 0% taker).

## THE MODEL (S56 verdict — one sentence)
The DEPLOYED fine zigzag (lean_series W=600/REV=0.1 enter+exit) at NATURAL cadence on the
Bybit venue at MM3 fees: full S54 gate PASS all 5 coins on 30d bins (z 6.8–14.4, 20/20
coin-weeks, reversed below the shuffle floor), direction premium +$7–17/hr/coin above the
~$90–106/hr rebate floor; queue-honest capacity today +$3–37 (sol) / +$12–47 (eth) per hr
@$5k. EVERY armed/coarse-confirm variant died under its own gate (handoff §1). Scoring
standard: 10%-taker blend on bins (taker11/std+4.70/mm3−1.15); TRUE fees on books.

## DATA (/tmp dies with the container)
- Bybit books (the model's cells): `for c in sol eth; do git fetch origin data/$c-bybit-book;
  git show origin/data/$c-bybit-book:${c}_bybit_book.jsonl.gz > /tmp/${c}_bybit_book.jsonl.gz; done`
- Coinbase books (baseline cells): same pattern, data/<c>-book, x5 coins.
- Bybit 30d bins (only for gate re-runs): backfill_bybit.py x5, ~40min (S56 kickoff commands).

## JOBS
1. GREG (THE gate): Bybit MM application — institutional_services@bybit.com. The whole cell
   class exists only at MM3 (std +2bp measured NEGATIVE on real books, both fill models).
2. Accrue + re-measure: run paper_trade (sandbox accrues per cron); re-run
   `scripts/_s56_bybit_capacity.py` on the grown books — the v1/v2 bracket is the deploy
   number; report it per cell every session. Extend bybit book collectors to doge/xrp/btc
   (matrix workflow on the default branch — Greg's manual trigger; token 403s on dispatch).
3. Freight-train veto as SANDBOX VARIANT (worst-10 = 10/10 `continue` rc=0.00 counter-entries
   into violent moves): veto fine flips where pivot dipole = continue/rc~0 AND velocity
   extreme. NO-GATES scoring (net $/hr AND legs/hr) vs the un-vetoed sandbox cell + shuffle +
   reversed. Adopt per cell only on a full pass. (S53 law won every test in S56 — expect it
   to cost more than it saves; measure anyway.)
4. S56 JOB 2 (still queued): Binance spot 30d x 5 — the R4 descriptor second-venue check
   (`backfill_binance_spot.py`, canary PASS S54, never swept).
5. S56 JOB 3 (still broken): BTC Coinbase book collector 155h gap — blocks the BTC deploy
   cell + Greg's BTC guess.

## THE LOOP (Greg standing rule)
Every round: print the 10 worst losers + 10 smallest winners
(`scripts/_s56_render_worst.py --v3 --arm 0`, adapt per machine) and read them with Greg
before the next move. One round = one defined test.

## Rules (standing)
Crypto only; zero synthetic (toy sign-checks for tool validation only); per-cell deploy;
never pool sign-blind; NEVER tune off one window (S56: the ARM40 knife-edge was caught by the
response-curve check — sweep the knob before promoting a grid point); shuffle floor = the
fair zero at rebate fees (direction premium above floor is the edge; floor $/hr is a volume
paycheck, never cite it as edge); sandbox-first; git = source of truth; falsification-first;
pip install matplotlib, MPLBACKEND=Agg; token 403 on GHA dispatch; Bybit REST geo-blocked
(CDN dumps + WS fine).
