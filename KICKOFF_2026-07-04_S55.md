# S55 KICKOFF — TWO-SCALE ZIGZAG (Greg: "we have a good enter and exit strategy with zig zag.
# we just have to scale it up and eliminate the chop in between")

Read first: `SESSION_HANDOFF_2026-07-03_S54.md` (the big-line arc + why every alternative died) →
`S52_SIZING_MATRIX.md` (fee/venue matrix). Branch: designated `claude/crypto-liquidity-signals-*`;
reconcile to canonical `5c5vg9` at start (reset --hard if the harness cut stale); keep 5c5vg9
synced on every push. Default branch `claude/new-session-o3vnm` carries crons — LEAVE IT.
Run `python scripts/paper_trade.py` FIRST (standing).

## DATA (the S54 unlock — re-pull takes ~40min, /tmp does not survive containers)
- Bybit perp dumps (WORKS in-container, CDN not geo-blocked):
  `python backfill_bybit.py --symbol SOLUSDT --days 30 --bins-path /tmp/backfill/SOLUSDT_30d_bins.json`
  (x5: SOL/ETH/BTC/DOGE/XRP; parallel; ~40min total).
- Binance SPOT dumps: `backfill_binance_spot.py` (NEW, canary PASS, us/ms ts handled) — same form.
  S54 pulled 30d x 5 but the spot sweep never ran (superseded by Greg's directive) — OPTIONAL now.
- Coinbase books: `git show origin/data/<coin>-book:...` (29-196h; ⚠ BTC book = 155h GAP of
  196h; ETH 23h gaps). Coinbase has NO bulk dumps (REST 1k trades/call — cron-grind only).
- Loader: `scripts/_s54_backfill_sweep.py::load_bins` (bins json -> 1-sec mid/buy/sell arrays).

## THE S54 VERDICTS THAT SHAPE JOB 1 (all on 30d x 5 coins, n in the thousands)
- Coarse dipole bell: DEAD as built — retrace-confirm on a smoothed lean fires with ~72-89% of
  the leg already gone (measured lag 2h+). Not blind, LATE. Fine lean (60s) times turns to
  seconds but rings at every ripple (S40 anatomy).
- Adaptive big line: negative all 5 coins, all 20 coin-weeks on deep tape (~= shuffle). The
  Coinbase +$1-4/hr was one-window; treat as window personality until Coinbase history says else.
- Scaled-up zigzag flip baseline (taker rt11, 30d bins) vs oracle — THE CHOP/GIVEBACK LEDGER:
  theta150: sol gross/leg +10.7 (oracle +$45/hr, engine -0.05) — the engine picks real legs but
  pays ~2xtheta giveback/leg (300bp at theta150 vs ~310bp mean swing). theta60: gross +0.6..+3.1,
  legs/day 18-42, all bleed at taker.

## JOB 1 — the TWO-SCALE machine (Greg's directive, the whole job)
Fine zigzag/flip machinery = ENTER and EXIT (validated: 0.69bp-from-turn, 25,845-trade forward
ledger, OOS-reproduced). Coarse structure = WHICH legs to be in. Chop elimination = rejecting
fine turns that fake being coarse-leg boundaries. Giveback math: coarse execution pays 2xtheta
(~300bp at theta150); fine execution pays ~2x(fine confirm) (~40-60bp) — closing that gap on
theta100-150-class legs is the entire prize (net/leg -0.3 -> +200bp neighborhood if selection
holds).
  a. Build the two-scale simulator on the 30d bins: fine flips (deployed detector params scaled
     to 1s) execute; a coarse leg-state (causal zigzag at theta in {100,150} or HH/HL structure)
     decides WHICH fine flips open/close/flip a position. Taker fills first (no queue fiction),
     mk tiers as columns.
  b. Chop elimination candidates, judged ONLY on net $/hr AND legs/hr (NO-GATES scoring): (i)
     leg-scale veto (trailing zigzag swing scale < k x fees -> stand aside); (ii) coarse-side
     agreement (only fine flips agreeing with the coarse leg direction); (iii) S40 dive depth at
     the fine pivot (validated size predictor) as the leg-start selector. Ablate each.
  c. Controls mandatory: shuffle (joint price+flow), reversed, leakage gate, per-week splits x
     5 coins (the S54 gate that killed big line — nothing adopts without passing it).
  d. Report capture fraction vs the theta100/150 oracle per cell (Greg's algebra: opportunity x
     capture; coin ranking by opportunity terms — BTC guess on record, needs the fixed collector).
- Renders every round — Greg's markups are the spec pipeline (5 corrections came from them).

## JOB 2 — infrastructure (blocks deploy-cell truth)
- BTC Coinbase book collector: find/fix the 155h-gap cause on the default-branch cron.
- Bybit book cron: STILL the same single 5.83h window since S52 — diagnose from default branch.
- Consider gz-pushing the 30d bins to data branches (gz ~15-30MB each) so history persists.

## JOB 3 (optional) — Binance spot sweep for the record
The spot bins may still be in /tmp (or re-pull); running `_s54_backfill_sweep.py --bins-dir`
against spot files needs only the COINS list paths — answers spot-vs-perp for the parked
Coinbase big-line result. Low priority now; the two-scale machine supersedes.

## Greg standing actions
Bybit MM application (institutional_services@bybit.com; matrix: standard SOL -$157/hr vs MM3
+$115/hr @$5k). Optional vip@backpack.exchange.

## DEAD — S54 additions (full lists in S53/S52 kickoffs)
Coarse dipole side-picker (any lean window 10min-2h, retrace-confirm — LATE, not wrong);
pure dive+bounce flip engine; x_PL bounce as a rescue at any coarse lean scale; fixed-theta big
line; adaptive big line ON BYBIT PERP as parameterized (Coinbase variant PARKED not dead —
"nothing is dead, it's up to us to figure out how to draw straight lines between 2 points");
B1-style exit rungs (S53) still dead; NO GATES scoring still law.

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy; never pool sign-blind; NEVER tune off one window
(S54 is the cautionary tale — the 4/5-coin big line died OOS same-day); git = source of truth;
falsification-first; verify fees from primary sources; Bybit REST geo-blocked (dumps CDN is NOT);
`pip install matplotlib`, MPLBACKEND=Agg; token 403 on GHA dispatch.
