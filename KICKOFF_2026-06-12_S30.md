# KICKOFF — Session 30 — written 2026-06-11 (end of S29)

Read order: `SESSION_HANDOFF_2026-06-11_S29.md` → this file → `CLEANUP_RUNBOOK.md`. Memories auto-load.
Branch: continue on the S29 working branch. KEEP PAIRS SEPARATE; each bucket evaluated separately; zero synthetic;
Result Discipline. **Machine is fragile (4 CPUs) — no heavy discovery reruns without batch-100 + clear + parallel,
and expect to resume across crashes.**

## STATE COMING IN
- Trade-pool bugs FIXED (Bug 1 builder regression; Bug 2 strategy-led canonical key). Clean deduped pools in
  `E:\Markets\research\strategy_evolution\per_bucket_clean\` (win 1568 / lose 14181 distinct).
- Clean discovery rerun DONE for 23/24 buckets → `operator_discoveries\markets_*_preentry_cs2000_clean\`. Win coeffs
  now DISTINCT (the +9.6 collapse is fixed).
- Honest dipole = WEAK/MIXED per bucket (acc 0.62–0.76, 6/12 z>3, c_quad≈0). The z=+9.6 was a DEGENERATE ARTIFACT
  — do not cite it.

## TODO — priority order
1. **(DEFERRED FROM S29) Copy the clean coeffs into the Markets repo.** They live only in
   `E:\refrag\discoveries\operator_discoveries\markets_*_{win,lose}_preentry_cs2000_clean\` (refrag/OD store).
   Extract a COMPACT copy (just `result.operator_coefficients` [128-dim] + filename stem + win/lose, NOT the
   multi-MB evidence-graph JSONs) into the repo, gzipped (repo pattern). The S29 extractor was ready (fast
   header-regex over ~15k files; reads ~2GB — light CPU but do it once, off-peak). Suggested out:
   `od_coeffs_cs2000_clean.json.gz`. Then it's the repo's portable copy of the validated coeff set.
2. **Win horizon + oracle-window collapse (UPSTREAM refrag fix).** The audit CSV collapsed BOTH oracle
   entry/exit TIMES and PRICES to one snapshot window, disconnected from the real entry (`ts_utc`). So the win
   `horizon_minutes` is not recoverable from the CSV, and the win `net_bps`/label semantics may reflect the
   snapshot trade, not the real-entry trade. Fix in the refrag trade-gen/audit so each win trade carries its real
   entry/exit (times + prices) → then attach the correct per-trade `horizon_minutes`. (Bar-reconstruction gives a
   varying 3–21 min candidate but doesn't match the recorded oracle window — confirm the definition with Greg
   before attaching.) This also re-opens "are the win LABELS valid?" — verify before leaning on win-side results.
3. **Finish or drop `cb eth sell lose`** (1138/1404 in cs2000_clean). One bucket; low priority.
4. **The two real owed validations:** a true TEMPORAL split (needs mapping discovery JSON → entry_ts) and
   net-of-cost PnL — the things that turn "real separation in-sample" into "tradeable edge." Still not shown.
5. **KB hygiene:** the old tainted buckets (`cs100`, `cs1075`, `preentry`) are still on disk and feed the 3 KBs.
   Mark them superseded by `cs2000_clean` per `CLEANUP_RUNBOOK.md` (don't let a reader pick the collapsed-win set).
6. **Then resume "make it functional"** — wire the clean validated coeffs into whatever the next functional step is
   (per Greg). The dipole is weak, so set expectations accordingly.

## ENVIRONMENT
- `E:\refrag` mounted (code + data); discovery pipeline is PySR/Julia-free. Bars via
  `E:\refrag\adapters\markets_bar_loader.load_closes(asset, venue, t_min, t_max)` from `live_data_history`.
- Clean-rerun driver: `E:\Markets\_run_clean_rerun.py` (resume-safe; `--cross-section --target-size 2000
  --pre-entry --batch-size 100`).
