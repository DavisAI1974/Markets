# KICKOFF — Session 26 (Markets) — written 2026-06-08 (end of S25)

WORKING BRANCH: `claude/crypto-trading-platform-plan-MpqwG` (DavisAI1974/Markets).
**WRITTEN-IN-STONE GUIDE: `BUILD_PLAN.md`** (commit `da9dc63`) — read it FIRST, follow to a T; any deviation
cleared with Greg before acting ([[follow-canonical-plan-rule]]). Master: `CLAUDE (5).md`. Zero synthetic data;
Result Discipline. Background: `SESSION_HANDOFF_2026-06-08_S25.md`.

## State coming in (S25 result — the good news)
The dipole is REAL on the correct basis. On the original 128-dim per-pair `operator_coefficients` the win/lose
classifier validates at **z = +9.6 vs a label-permutation null on PRE-ENTRY data (all 12/12 pairs)**, and PySR
reproducibly discovers a convex/cubic surface per pair (5/12 identical across 3 seeds). S24's collapse was the
off-plan 15-dim pooled basis — that is diagnosed and behind us. Toolchain (PySR/Julia) is FIXED and durable
([[pysr-julia-local-toolchain]] — env `PYTHON_JULIAPKG_EXE` is set; `import pysr` ~12s).

## 0. FIRST THING S26 DOES — run the LARGER trade set on the 128-dim per-pair basis (Greg's directive)
S25 validated on `_preentry_cs100` (100 win / 100 lose per pair). The "larger set we did earlier" / the big data
run goes next. Do, in order:
1. **The larger set (Greg, S25):** "we did a 15-source run with 16-day up to 30-day runs … i had to attach a
   folder from E drive but the trade data is there." So the big run = that **15-source, 16–30 day (~16k-trade)
   set** from the attached E-drive folder (this is the S24 run's trade population). **CRITICAL CHECK FIRST —
   do NOT repeat S24's mistake:** the validated dipole needs the **full 128-dim `operator_coefficients` PER PAIR**.
   So before running, confirm what that folder actually contains: (a) if its trades already carry 128-dim
   `operator_coefficients` (like `E:\refrag\discoveries\operator_discoveries\`), run the validated construction on
   them PER PAIR; (b) if it only has raw trade data or the 15-dim coupling vectors, the 128-dim coefficients must
   be PRODUCED by the refrag/DeepNova discovery pipeline first (out of the markets repo's scope — flag to Greg).
   Locate the folder with Greg at the start of S26. NEVER pool across pairs (that pooling is exactly why S24
   collapsed — Greg).
2. Run the **validated construction** (NOT the 15-dim `dipole_trade` path): per pair, raw 128-dim coefficients,
   centroid projection, nearest-centroid `Ha−Hb>0` classifier + the algebraic surface. Reuse `od_honest_val.py`
   (local temp) as the reference; it is the verified harness. Report per-pair acc / AUC / **permutation-null z**
   and the algebraic R². KEEP PAIRS SEPARATE; do NOT pool; do NOT standardize.

## 1. Make it HONEST at scale (the two tests still owed)
- **Walk-forward / non-random split.** Random k-fold overstates (BUILD_PLAN signal I). The JSONs carry NO trade
  timestamp — find an ordering/grouping key (filename batch, `canonical_trade_key`/`trade_source_id` in
  `summary.json`, or the discovery sweep index) and do a grouped/forward split. If no time key exists, say so and
  rely on the permutation null + a grouped split as the honest floor.
- **Net-of-cost.** Separability ≠ PnL. Use `summary.json`'s `net_bps` per trade: does taking the dipole-predicted
  winners beat buy-hold / the existing generators NET of fees+slippage (BUILD_PLAN acceptance-b, `odcore/validation.py`)?

## 2. Fix the PySR promotion gate + lock per-pair forms
The S25 discovery gate read the x0² coefficient, so it mislabeled the CUBIC pairs "fail." Re-gate on the
DISCOVERED dominant term (or prediction-level cross-seed agreement). Then lock the per-pair promoted forms
(≥3 seeds, <5% coeff variation) — currently 5/12 clean. Harness: `od_pysr_discover.py` (local temp).

## 3. Wire the corrected construction into odcore (then validate before any live wiring)
Replace the S24 15-dim path: `odcore/dipole_predictor.py` already has the verbatim 128-dim primitives
(`build_centroids`/`project`/`algebraic_dipole_over_trades`); feed it `result.operator_coefficients` per pair via a
clean loader (NOT `dipole_trade.py`'s 15-dim `c_i`). Nothing wires live until a promoted form beats baselines net
of cost under walk-forward + tautology null.

## Housekeeping / environment
- Toolchain is fixed locally; `import pysr` works (~12s). If on a fresh container, expect the S3 block — pre-place
  Julia + set `PYTHON_JULIAPKG_EXE` (see [[pysr-julia-local-toolchain]]).
- Data: `E:\refrag\discoveries\operator_discoveries\<pair>_<win|lose>[_suffix]\*.json`. `_preentry_cs100` = balanced
  honest set; `_preentry_cs100_v2` = WIN-ONLY. `E:\Markets\_full_pipeline_winners_preentry_cs100_v2\summary.json` =
  1054 trades with `net_bps` + truncated head.
- `E:\Markets\.claude` exFAT-hard-locked; work from refs + the canonical worktree ([[markets-env-hard-lock]],
  [[git-is-source-of-truth]]).
- Do NOT confuse `MARKETS_OFFENSIVE_LAYER_PLAN.md` (separate older defensive plan) with `BUILD_PLAN.md`.

## Branch
WORKING BRANCH: `claude/crypto-trading-platform-plan-MpqwG` (DavisAI1974/Markets)
