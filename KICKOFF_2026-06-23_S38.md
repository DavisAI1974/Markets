# KICKOFF — next session (S38), 2026-06-23+

Branch: **`claude/divergence-exhaustion-backtest-wj65sm`** (now == repo default, so you'll land here).
Read in order: `CLAUDE.md` (S37 delta at top) -> `SESSION_HANDOFF_2026-06-22_S37.md` -> `S36_NETCOST_BACKTEST_FINDINGS.md` -> `BUILD_PLAN.md` PART 4-5.
Rules: crypto platform only; zero synthetic; per-cell deploy; `tools-are-complementary-not-competing`; git is source of truth; commit+push regularly; **never tune off a single window**.

## State coming in
- **Data pipeline UNBLOCKED + expanded.** The S36b "validate on ONE window" bottleneck was the durable
  collectors silently failing (bins > GitHub's 100 MB push cap since ~06-14). Fixed (gzip on the data branch)
  AND expanded to **5 coins x 3 venues = 15 cells** (added SOL/DOGE/XRP). All accruing in git now via
  `data/{btc,eth,sol,doge,xrp}-bins` (gzipped) on the 6h cron.
- **NEXT #2 built:** `_info_dipole_gated_swing.py` — the unified per-cell STACK (dipole=filter, 1-sec
  price-reversal=timing, regime gate=stand-aside, maker floor), leakage-gated. PROVISIONAL per-cell deploy
  map on one window (4/6 clear). Architecture locked S36b: DIPOLE=filter, 1-sec PRICE=timing, MAKER=lever.
- **OD-BOOK** thread (Chat's order-book dynamics-recoverer) built + parked, collecting -> `data/btc-book`.

## NEXT (priority)
1. **Re-run `_info_dipole_gated_swing.py` on the now-accruing multi-window / multi-coin / multi-regime data**
   (materialize `realbins/` from the `data/*-bins` branches first; they're gzipped now -> `git show
   origin/data/<coin>-bins:<f>.json.gz | gunzip > realbins/<f>.json`). Produce the real, non-overfit per-cell
   deploy map across all 15 cells. THIS is the S36b NEXT #1 finally unblocked. Then wire `divergence()` per
   cell into the regime classifier + sizing.
2. **NEXT #3:** confirm exchange hosting regions (Coinbase=us-east-1; Kraken/Bybit?) + model maker fills for
   the live sub-second maker path (research; unblocked).
3. **Durable off-git storage** (S3 / Render `virginia` = us-east-1) once Greg connects the Render workspace or
   provides AWS creds/IaC — gzip is only a ~6-week band-aid.
4. **OD-BOOK thread-1 T_test** (`research/od_book/run_experiment.py --commit-ttest`) once `data/btc-book` is
   deep enough; advance threads 2/3 only if it clears the KILL gate.

## Housekeeping for Greg (optional, his clicks)
- Delete the stray branch `claude/divergence-exhaustion-backtest-wj65sm-65uts0` from the GitHub Branches UI
  (the env git proxy refuses ref deletions). Harmless if left.
- To resume BTC/ETH collection immediately instead of waiting for the next 6h cron, click "Run workflow" on
  "BTC Collectors (durable)" / "ETH Collectors (durable)" in the Actions tab (my token gets 403 on triggers).
- If you want cloud collection/storage: connect the Render workspace (then I can deploy to `virginia`).
