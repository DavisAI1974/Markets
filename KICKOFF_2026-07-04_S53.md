# S53 KICKOFF — Bybit MM application (Greg) + multi-window venue confirm + accum refinement

Read first: `SESSION_HANDOFF_2026-07-03_S52.md` → `S52_SIZING_MATRIX.md` (the canonical corrected
matrix — ALL pre-S52 matrices are superseded; the old model's mk0 positives were artifacts) →
`STRATEGY_accumulate_S52.md` (Greg's design spec). Branch: designated `claude/crypto-liquidity-
signals-*`; reconcile to canonical `5c5vg9` at start (`git fetch origin && git merge --ff-only
origin/claude/crypto-liquidity-signals-5c5vg9`, reset --hard if the harness cut stale); keep 5c5vg9
synced on every push. Default branch `claude/new-session-o3vnm` carries the crons — LEAVE IT.
Books: `git show origin/data/<coin>-book:<coin>_coinbase_book.jsonl.gz > /tmp/…` (5 coins) and
`git show origin/data/<coin>-bybit-book:<coin>_bybit_book.jsonl.gz > /tmp/…` (sol, eth — accruing 0 */6).

## ⚠ STANDING CORRECTIONS (carry forward)
- Never cite a single "$X/hr" — cite the matrix cell {venue, fee, fill model, size, window}.
- The corrected fill model (price-eligibility, S52) is CANONICAL. mk0 is measured-DEAD at size on every
  cell. The fill asymmetry is structural: fixed limits fill fully when wrong, get rationed when right
  (SOL med $483 W vs $6,161 L) — the rebate is the survivability condition at size.
- Bybit numbers = ONE 5.83h window. PROVISIONAL until multi-window.

## JOB 1 (Greg action, unblocked): send the Bybit MM application
institutional_services@bybit.com — subject "Market Maker Application"; maker-share bar 0.03% weighted,
1-month trial (S51 primary-source verified). THE MEASURED CASE: standard fees SOL −$157/hr / ETH
−$273/hr at $5k/leg vs MM3 −1.25bp +$115 / +$113/hr — the program is the existence condition, and the
tiers are ~10x mk0. Optional: vip@backpack.exchange (confirm their image-locked rebate bps).

## JOB 2: multi-window Bybit confirm (the standing gate on ALL venue numbers)
The 0 */6 cron pushes ~4 windows/day per coin. Each session: materialize the accrued books, re-run
`scripts/_s52_accum_vs_oneshot.py` (or the one-shot rows of it) per NEW window; the S52 venue verdicts
(MM tiers, SOL-first, ETH-second) must reproduce across windows before any sizing. Also gives the accum
zigzag arm usable samples on Bybit (S52 had 1–2 legs/window — meaningless).

## JOB 3: accum refinement (R&D thread — the S52 validated-in-miniature arm)
On the ZIGZAG stream only (micro scales measured-dead for this design). All changes leakage-gated
(`odcore/leakage.assert_no_leakage`) + shuffle & reversed controls mandatory (the S52 harness has them):
  a. Asymmetric confirm/dump bands (S52 used symmetric = 44–62% dump rates; the dump band trips on
     noise mid-swing — try dump floored at fee-floor but confirm at k×med_swing with k fixed).
  b. Confirm modes: 'lean' and 'price+lean' (wired in `swing_accum`, untested) + dipole-resolution.
  c. Starter/complete window sensitivity (fixed 10s/5s in S52 — report, don't tune per window).
  d. Q1 (honest queue) bracket on every headline row.
Deliverable: does the SOL zigzag+dipole edge (+$1.3–1.6/hr vs controls −$3) grow, and does it
reproduce on the accruing Bybit windows? If controls ever win → report and stop (the S51 lesson).

## JOB 4: wire venue cells into `scripts/_capacity_model.py`
`cell_scenarios(coin, K, grace)` still hardcodes the Coinbase path + SCEN. Add (path, scen, taker)
params + a venue-cell list (Bybit tiers mk+2/mk0/−0.1/−0.5/−1.25, tk5.5) so the canonical
`_capacity_model_results.json` carries the venue cells. The head-to-head JSON already has them; this
is consolidation, not new measurement.

## DEAD — do not re-chase (S52 additions at top)
mk0 deployment anywhere (measured-dead 3 ways); the OLD no-eligibility fill model (artifact); accum on
micro-scale turn streams (nothing to confirm at 1–4bps swings — measured, 7 cells); sequence
anti-martingale sizing (twxv4t kill stands); TIF windows as a rescue at mk0 (best ≈ $0). Everything in
the S51 DEAD list stands EXCEPT: "per-leg scale-in falsified" and "front-of-queue sims as validators"
now attach to the MODEL CLASS — Greg's accumulate design is re-opened and lives in `swing_accum.py`
under the corrected model. Wrong-tail at entry remains irreducible (the accum design routes around it
via confirmation, it does not predict).

## FILE MAP additions (S52 — change IN PLACE, don't reinvent)
- `odcore/swing_accum.py` — Greg's executor: `simulate_swing_accum(..., queue_frac, entry_ok, confirm_mode)`.
- `scripts/_s52_accum_vs_oneshot.py` — the head-to-head harness; `_price_zigzag(mid, theta)` +
  `_dipole_gate(flips, mid, buy, sell)` live HERE (import them).
- `scripts/_s52_fill_window_audit.py` / `_s52_tif_sweep.py` — the fill-model correction evidence.
- `scripts/_s52_render_walkthrough.py` → `docs/renders/s52/` (60 trades, INDEX.txt).
- `STRATEGY_accumulate_S52.md` / `S52_SIZING_MATRIX.md` — spec + canonical matrix.
- `scripts/_capacity_model.py` — `_leg_caps(..., window=FILL_W, price_eligible=True)` CORRECTED (legacy
  behavior only via price_eligible=False — comparison, never headline).
All S51-and-earlier file-map entries stand (`paper_trade.py` run FIRST each session; `swing_maker.py`
+ `size_legs` untouched and deployed; leakage/quiet_registry/maker_book patterns).

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy; never pool sign-blind; NEVER tune off one window; git =
source of truth (rebase onto 5c5vg9 first — the cron moves it); falsification-first (leakage gate +
shuffle/reversed controls on every new signal); verify venue fees from primary sources; Bybit REST
geo-blocked from some DCs (WS-only collectors); `pip install matplotlib`, MPLBACKEND=Agg; token = 403
on GHA dispatch (crons fire from default branch).
