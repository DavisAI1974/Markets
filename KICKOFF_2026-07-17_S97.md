# KICKOFF — S97 (next session priorities for the NG forecaster loop)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-17_S96.md` (the session-total block at the top) ->
`research/kalshi/AGENT_RUNBOOK_S95.md` -> `research/kalshi/knowledge/ng_brain.json` (**s99.2, 21 plays** —
the four-condition flip confirm + post_parabolic_bleed are the newest, hardest-won rules).
Branch: S96 pushed to `claude/ng-coach-continuous-curve-7pk2gf`; reset the new session branch onto its tip.
PROTOCOL (settled S96): one-shot block-blind = the canonical skill test; refine after EVERY group
(iterate-to-tracking bar, GENERAL rules only, n>=2 spanning groups, uniform application, irreducibles
declared); renders PRINTED to Greg before each refine merge; refine lessons merged BEFORE the next group.
NEW (Greg): groups START ON SUNDAYS (the Sun-eve reopen) and END at Friday's close.

## Env on-ramp
- Re-paste (ROTATE — again exposed in-chat S96) the AWS pair into gitignored `scratchpad/aws.env` (S3 read
  on `bento-568968024170-us-east-2-an`, us-east-2). DATABENTO key needed this session (JOB 2).
- `pip install boto3 databento matplotlib pandas`; `EIA_API_KEY=DEMO_KEY python research/kalshi/eia_surprise.py
  --out data/eia_surprise.json`; `python research/kalshi/forecast_harness.py --selftest` must PASS.
- `python research/kalshi/precache_window.py 20260116 20260130` (background).

## JOB 1 — G11 blind (SUNDAY Jan 18 reopen -> Friday Jan 30) on s99.2
Anchor = 20260116 hr24 (closed 3.109; last-hour dir via `fast_tape`). Trading days: 0120 Tue (MLK Mon 0119 =
thin flag, the Sunday/Monday files carry the reopen), 0121-0123, 0126-0130 — build the exact day list off the
S3 inventory; the Sunday session folds into Monday's gap per convention. **Run `roll_adjust.roll_offsets`
FIRST: the Feb->Mar roll likely lands ~Jan 26-27 INSIDE the block** (backwardation regime -> expect another
NEGATIVE roll; tell the blind agent the date + spread, scoring roll-adjusts). Regime in: the post-parabolic
BLEED (down-chain from the 5.337 Dec 5 crest, now at 3.11, bounces 1-2 days max) — G11 is the first blind
test of the HARDENED flip confirm (does the bleed finally END, and does the rule catch it without a false
fire?) and of post_parabolic_bleed as a forecastable class. Track per-play hit/miss per event. Then: score,
print renders, BLINDED merge -> s100.1.

## JOB 2 — the two DATA builds (Greg, before or alongside G11)
1. **Forward curve back**: regenerate `data/nymex_curve/` via `forward_curve.py` (Databento, ~$0.07/yr) so
   `curve_regime` stops reading 'unknown' in decision-state — the backwardation/contango axis is live regime
   info the blind agent has been missing (S96 passed it via roll spreads only).
2. **HISTORICAL FORECAST TEMPS (the delta is the driver — Greg S96)**: IEM archives NWS MOS point forecasts
   back to ~2000 (https://mesonet.agron.iastate.edu/mos/, download interface /mos/fe.phtml, API /api/).
   Extend `nws_temp_feed.py` with a `--mos-asof` mode: pull GFS-MOS (fallback NAM-MOS) for the 16 demand
   metros, reconstruct the gas-weighted HDD/CDD FORECAST as-of D-1 evening for D..D+7, and put THREE numbers
   in decision_state per day: forecast_gw_hdd (replaces the realized-as-proxy), forecast_vs_normal, and the
   RUN-TO-RUN DELTA (today's model runs vs yesterday's for the same target days — the repricing driver).
   Blind-safe by construction (as-of joins, strictly prior). Back-fill the walked winter (Nov->Jan) so the
   NEXT refine can re-test every weather-conditioned rule against what the market actually KNEW.
- Also record: LIVE-coach cadence (Greg S96) = rolling week-ahead arc + daily re-anchor pass that may
  OVERRIDE the arc (sequential mode; every strong rule consumes day-N-1 tape). Park until Greg calls it.

## JOB 3 — the per-group refine on G11 -> s100.2 (before any G12)
Standing questions: (a) how does a BLEED end (G10 never showed it — the flip's C1-C4 on the way out);
(b) the bounce-carrier-day residual (one-shot cannot pick 0106-vs-0107 without day-N-1 tape — declared);
(c) crash-tail conditioning; (d) with MOS forecast temps in state (JOB 2), re-test the weather-conditioned
rules on forecast-vs-realized DELTAS instead of realized proxies.

## JOB 4 — the net-of-fee coach replay on the walked winter (G7-G10, the money question)
`coach_replay.py` scaffold: apply the s99.2 playbook per event net-of-fee (maker AND taker), per-event
tables, no pooling as the final word. The walked winter now spans trend-chains, a crash, and a bleed —
enough regime variety for an honest first pass.

## GUARDS (unchanged, non-negotiable)
PER-EVENT, never pool/average · drift is a DESCRIPTOR · general rules only, no point-fitting · blind wall
(decision-time only; storage strictly-prior; MOS joins as-of) · one-shot canonical · refine per group,
renders printed first, lessons merged BEFORE the next group · Sunday-start/Friday-end blocks · true holdout
+ provisional-until-live · net-of-fee maker AND taker · git=CODE, S3=DATA · flag weekday holidays (thin
amplifies delivery, damps holds) · rolls: mark, never trade; expect NEGATIVE rolls in backwardation · flips:
four-condition confirm, never front-run a flip · NG != WTI · weather forecaster HANDS OFF · keys are SECRETS.
