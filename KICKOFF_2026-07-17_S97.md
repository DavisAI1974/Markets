# KICKOFF — S97 (next session priorities for the NG forecaster loop)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-17_S96.md` (the session-total block at the top) ->
`research/kalshi/AGENT_RUNBOOK_S95.md` -> `research/kalshi/knowledge/ng_brain.json` (**s98.2, 20 plays**).
Branch: S96 pushed to `claude/ng-coach-continuous-curve-7pk2gf`; reset the new session branch onto its tip.
PROTOCOL (settled S96): one-shot block-blind = the canonical skill test; refine after EVERY group
(iterate-to-tracking bar, GENERAL rules only, n>=2 spanning groups, irreducibles declared); renders PRINTED
to Greg before each refine merge; refine lessons merged into the brain BEFORE the next group starts.

## Env on-ramp
- Re-paste (ROTATE) the AWS pair into gitignored `scratchpad/aws.env` (S3 read on
  `bento-568968024170-us-east-2-an`, us-east-2). Databento key only if a day needs re-pulling.
- `pip install boto3 databento matplotlib pandas`; `EIA_API_KEY=DEMO_KEY python research/kalshi/eia_surprise.py
  --out data/eia_surprise.json`; `python research/kalshi/forecast_harness.py --selftest` must PASS.
- `python research/kalshi/precache_window.py 20251231 20260117` (background) before scoring.

## JOB 1 — Group-10 blind (Jan 2 -> ~Jan 16 2026) on s98.2
Anchor = 20251231 hr24 (closed 3.711 real; get last-hour dir via `fast_tape`). Trading days: Jan 2 Fri, then
Jan 5-9, Jan 12-16 (Jan 1 New Year closed; check the harness holiday map + MLK Jan 19 lands in G11). Check
`roll_adjust` on the window first (Feb->Mar roll lands ~late Jan, likely AFTER this block — verify). The
regime carried in: post-termination two-sided BASING after the December crash (the declared new regime
class), surplus nearly gone (+15), deep winter. G10 is the first blind test of chain_polarity_flip,
failed_rally_tell, the crash bands, and the polarity-sided print rule on unseen days — track their per-event
hit/miss explicitly in the scorecard. Then: score, print renders, BLINDED merge -> s99.1.

## JOB 2 — the per-group refine on G10 -> s99.2 (before any G11)
Characterize the G10 days into `fingerprints.json`, spawn the refine agent (runbook Step B + the S96 bar),
render + PRINT the refined overlay, review, merge. The standing refine questions: (a) does the January tape
resolve the post-termination BASING class (how does a new chain get born out of basing?); (b) the third-day
pause ambiguity (1210 paused where 1009 continued — n=1 each way, needs a discriminator); (c) two-sided
extreme tails beyond band-high (1223 +5010) — is the tail size conditioned on anything decision-time?

## JOB 3 — standing items
1. The LIVE-COACH design (park until Greg calls it): every strong rule now consumes day-N-1 actual tape —
   the sequential/rolling-anchor mode (S96 experiment + all three refines point there). When Greg wants the
   live coach, it runs day-sequential; the one-shot stays the skill test.
2. Net-of-fee coach replay on the walked winter (G7-G9) with the s98.2 playbook — the money question
   (`coach_replay.py` is the scaffold; per-event, maker AND taker).
3. Keys: rotate the AWS pair (exposed in-chat again this session, image paste).

## GUARDS (unchanged, non-negotiable)
PER-EVENT, never pool/average · drift is a DESCRIPTOR · general rules only, no point-fitting · blind wall
(decision-time only; storage joins STRICTLY prior prints) · one-shot canonical · refine per group, renders
printed first, lessons merged BEFORE the next group · true holdout + provisional-until-live · net-of-fee
maker AND taker · git=CODE, S3=DATA · flag weekday holidays (thin flag alone is not a size-down; thin
amplifies delivery days, both ways in a crash) · rolls: mark, never trade (backwardation rolls are NEGATIVE)
· NG != WTI · weather forecaster HANDS OFF · keys are SECRETS (rotate).
