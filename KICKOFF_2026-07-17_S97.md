# KICKOFF — S97 (next session priorities for the NG forecaster loop)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-17_S96.md` -> `research/kalshi/AGENT_RUNBOOK_S95.md` ->
`research/kalshi/knowledge/ng_brain.json` (s96.2, 16 plays). Branch: continue the dev lineage (S96 pushed to
`claude/ng-coach-continuous-curve-7pk2gf`; reset the new session branch onto its tip). The loop runs IN the
Claude env. PROTOCOL (settled S96): one-shot block-blind = the canonical skill test; refine after EVERY
group (iterate-to-tracking bar, general rules only); renders PRINTED to Greg before each refine.

## Env on-ramp
- Re-paste (ROTATE) the AWS pair into gitignored `scratchpad/aws.env` (S3 read on
  `bento-568968024170-us-east-2-an`, us-east-2). Databento key only if a day needs re-pulling.
- `pip install boto3 databento matplotlib pandas`; regenerate `data/eia_surprise.json`
  (`EIA_API_KEY=DEMO_KEY python research/kalshi/eia_surprise.py --out data/eia_surprise.json`);
  `python research/kalshi/forecast_harness.py --selftest` must PASS.
- `python research/kalshi/precache_window.py 20251118 20251202` (background) before scoring.

## JOB 1 — Group-8 blind (Nov 19 -> Dec 2) on s96.2
Next consecutive block: 1119 Wed, 1120 Thu, 1121 Fri, 1124 Mon, 1125 Tue, 1126 Wed, 1128 Fri(early close),
1201 Mon, 1202 Tue — NOTE 11/27 Thanksgiving CLOSED (harness holiday map has both). Anchor = 20251118 hr24
(closed 4.366, get last-hour dir via `fast_tape`). TWO traps: (1) the **Dec->Jan contract roll lands inside
this block (~Nov 24)** — run `roll_adjust.roll_offsets` on the window FIRST, tell the blind agent the roll
date so it forecasts the real overnight ~0 there, and score roll-adjusted; (2) Thanksgiving week thinness vs
the S96 lesson that a thin flag alone is NOT a size-down when a weather catalyst is live (1111 +1580). Then:
score, print renders to Greg, BLINDED merge -> s97.1.

## JOB 2 — the per-group refine on G8 (the new cadence)
Immediately after the blind merge: characterize the G8 days into `fingerprints.json`, spawn the refine agent
(runbook Step B + the S96 bar: refined curves as close as GENERAL rules carry, n>=2 spanning groups, uniform
application, irreducibles declared), render + PRINT the refined overlay, review the proposal, merge -> s97.2.
G8 is the first test of the three NEW S96 plays (giveback_exhaustion_boundary, mature_swing_alternation,
giveback_origin_shelf) on unseen days — track their per-event hit/miss explicitly in the scorecard.

## JOB 3 — standing frontier (attack inside the refine, not as separate builds)
1. Day-level sequencing: R1 answered it for G7 — does it hold on G8's boundaries?
2. Extreme-day FULL magnitude (the dominant residual): direction + size-up callable, full size not.
3. R5's soft >=75% retrace threshold (n=4) — harden or kill on G8/G9 events.
4. The LIVE-COACH design question (park until Greg calls it): R1/R4 consume day-N-1 actual tape = the
   sequential input; the live coach will run day-sequential (the S96 experiment + refine both point there).

## GUARDS (unchanged, non-negotiable)
PER-EVENT, never pool/average · drift is a DESCRIPTOR · general rules only, no point-fitting · blind wall
(decision-time only; storage joins STRICTLY prior prints) · one-shot canonical · refine per group, renders
printed first · true holdout + provisional-until-live · net-of-fee maker AND taker · git=CODE, S3=DATA · flag
weekday holidays (but a thin flag alone is not a size-down) · NG != WTI · weather forecaster HANDS OFF · keys
are SECRETS (rotate).
