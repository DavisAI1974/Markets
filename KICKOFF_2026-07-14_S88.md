# KICKOFF S88 — Kalshi, continue. S87 built P3 (lag_join) + specced the path FORECASTER; now fill the library.

Run the **`kalshi-session-start` skill FIRST** (branch check, data restore, read order). You'll likely land
on a fresh harness branch cut from the STALE S70 tip `3c70ff5` -- `git log --oneline -1` to confirm, then:
  git fetch origin claude/kalshi-s79-kickoff-ij8t9o
  git checkout -B <your-branch> origin/claude/kalshi-s79-kickoff-ij8t9o
Push all work to the trunk `claude/kalshi-s79-kickoff-ij8t9o` (= repo default; pull/rebase before push).

FIRST: re-export **DATABENTO_API_KEY** (SECRET, `db-` prefix, not persisted -- I'll paste it). EIA = DEMO_KEY.

READ (in order): `SESSION_HANDOFF_2026-07-13_S87.md` -> `research/kalshi/FORECAST_AGENT_DESIGN_S87.md`
(Greg's forecaster spec) -> `research/kalshi/PATH_FORECAST_RESEARCH_S87.md` (cited methods) -> `CLAUDE.md`
(lean) -> `KALSHI_TRADING.md` (note the FILE DISCIPLINE + forecast-workflow TODO at top). Run the
`kalshi-backtest` skill before building/judging any result.

WHERE WE ARE (end S87): P3 is BUILT and pays (provisionally) -- `research/kalshi/lag_join.py`, the lag-join
engine (RELEASE + `--intraday` modes): NYMEX-driven entry (sustained $-move), HOLD via a NYMEX dollar
trailing stop (Greg's trend-hold), exit maker-best-number vs taker-floor, net-of-fee, per-cell, $/c never
bps, tuned per contract. Intraday 06-17 the trend-hold flipped -115c->+202c maker. LEANS on the optimistic
maker fill; tiny-n; Apr-Jul. Then designed the intraday NYMEX PATH FORECASTER as a stacked HOLD-LENGTH
signal (forecast continuation/shape not price; the edge is around releases+high-vol = our setup).

S88 PRIORITIES:
1. **LAUNCH THE YEAR PULL FIRST** (it's the forecaster's analog library; multi-hour, needs a live session,
   resumable). Verify the May driver canary landed: `git ls-tree -r --name-only origin/data/nymex-ticks |
   grep nymex_cont/`. Then:
     git worktree add --force /tmp/nymexdata data/nymex-ticks
     DATABENTO_API_KEY=db-... python research/kalshi/pull_year_mbp10.py --start 2025-07 --end 2026-07
   ~$130 (~$5 over credit), pushes per-month to `data/nymex-ticks:nymex_cont/` (gzipped, pay-once, ~400MB).
2. **Build the forward-curve reader** (Databento deferreds -> backwardation/contango + prompt-vs-term
   horizon map) -- the tightness conditioning axis. Independent of the year pull.
3. **Forecaster v1** on the year library, per FORECAST_AGENT_DESIGN_S87: the BUCKET CONTINUATION TABLE
   baseline first (per cell: release x surprise x coiled x imbalance -> historical forward path +
   continuation rate), then the event-time anchor + residual-z/shape-corr TRACKING overlay wired into
   `lag_join.py` as the hold-length modulator. Method = blind-forecast -> compare -> rescore -> sharpen.
4. Standing: intraday-framework robustness across calm+trending days (needs the year); the forecast
   WORKFLOW (5PM load / morning recalc / mid-session re-match); NGDQ6 fix; weather scoring (HANDS OFF).

RULES (run `kalshi-backtest`): each trade individually / per-cell / distributions not means; all movement
$/c NEVER bps; tune PER CONTRACT but never to grab one trade and lose five; forecast = CONTINUATION/HOLD-
LENGTH not price; tracking overlay = risk-control (net-of-fee EV delta vs fixed-hold) not new alpha; FILE
DISCIPLINE (edit live files first, only new-file if none exists -- fold don't duplicate); leakage gate
before any backtest; exclude the settle window; net-of-fee maker AND taker; zero synthetic; provisional-
until-live; NYMEX=canary, fire on Kalshi; Databento = pay-once to the git branch; weather forecaster =
Greg's spec HANDS OFF; DATABENTO_API_KEY is a secret.
