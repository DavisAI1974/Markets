DROP-IN — DavisAI Markets S71

⛔ READ FIRST: STRATEGY_INVENTORY.md → the **OPERATING CONTRACT + ✅ LIVE section**. It is BINDING and
OVERRIDES any stale drop-in / S-block / handoff. The greedy capital strategy is locked there in plain words
(8 points). Then this drop-in + SESSION_HANDOFF_2026-07-08_S70.md. Do NOT read the whole strategy doc — the
LIVE section is the truth; everything under 🗑️ SCRAP HEAP is history/dead.

BRANCH: reconcile to canonical FIRST (recurring wrong-parent bug). If S70 files are missing (the OPERATING
CONTRACT block in STRATEGY_INVENTORY, SESSION_HANDOFF_2026-07-08_S70.md, scripts/pool_book_kraken.py) the
branch was cut from a stale parent → `git reset --hard origin/<newest davisai-sXX tip>`. "5c5vg9" is a LABEL,
not a SHA.

THE LOCKED RULES (from the contract — do NOT re-derive or re-litigate):
- FIRING is FIXED per coin (LIVE table = `platform.KRAKEN`). Never touch except on Greg's explicit say-so.
- GREEDY is already LIVE (`odcore/allocator.py` + `platform.run_portfolio`). Only ADJUST how it fills the $5k.
- LIVE CODE ONLY for tests. A "does it run" functional check + the live run is fine; **NO canary as a ritual**.
  No tape proxy, no bolt-on, no reimplementation of the executor/fill/fees.
- ALWAYS FRONT-OF-LINE (queue is NOT a mode). BOOK = fills, TAPE = plumbing/structure only (never price on tape).
- THE $5k GREEDY CAPITAL MODEL (LIVE §, 8 pts): ONE bank kept ~100% deployed; best edge-per-$ sets funding
  ORDER only (NEVER a gate — "never fund negative edge" was struck S70); fill each to its COUNTERPARTY
  CAPACITY, cascade the leftover to the next-best; positions are per-coin & mixed-side (short BTC + long SOL
  at once); never DROP a coin. ONE CHANGE AT A TIME, go slow.

WHERE WE ARE (S70 done):
- Docs restructured: OPERATING CONTRACT + ✅ LIVE vs 🗑️ SCRAP HEAP; greedy strategy locked in plain words +
  corrected (negative-edge GATE STRIPPED from the allocator — tested, +14.51 unchanged, safe no-op on the 5).
- Live driver `scripts/pool_book_kraken.py` = book → `run_cell`(front-of-line) → `run_portfolio` greedy $5k;
  honest TIME-weighted deployment metric added to run_portfolio (`time_in_play_frac`; the old mean_util was
  event-sampled/misleading).
- BASELINE (5 majors, 41.9h book, front-of-line, kr_mk0, caps=pool): **POOL +14.51 $/hr, money in play 97%**,
  BUT ONE coin at a time (caps=pool serializes → 550/1996 legs funded, 72% dropped; the $5k wants ~3.5 coins
  concurrently). $608/41.9h = thin per-leg edge (1–6 bp) on a low-edge window, concentrated on best-available.

JOBS (in order):
1. ⭐ THE OTHER GREEDY ADJUSTMENT — wire COUNTERPARTY CAPACITY into greedy so multiple positions run at once
   (short BTC + long SOL together), per the locked strategy. **Capacity = the L2 BOOK DEPTH — resting bid/ask
   SIZES, NOT trade volume (Greg firm: read what's actually there to trade against).** Mechanism: add an
   optional `leg_caps={coin:[$ per leg]}` param to `run_portfolio` (~6 lines; None = unchanged), feed each leg
   its book-depth capacity ($ = size × mid), run `pool_book_kraken.py`, compare POOL $/hr to +14.51 and check
   funded% jumps from 28%. ONE change, live run. (`load_book` keeps only top-of-book size today — extend it to
   the depth you need.)
2. Grade the 16 candidate minors ON BOOK as their books accrue (never tape); seat the ones that grade for
   capacity breadth. Then the ~116 THIN-OK −2bp small-cap sleeve.
3. Longer-window confirm of any config before any live-registry change.

NOTES:
- ⭐ COINBASE LIQUIDITY PROGRAM (Greg evaluated it COLD this session — a Highspot doc, NO rep engaged): it is
  the **Coinbase Exchange (US spot) market-maker program** — **0bp maker at ALL tiers** (this CORRECTS the old
  S49/S57 belief that "Coinbase has no reachable 0bp maker"). BUT qualification is a **%-of-exchange-volume
  gate** (Tier 4 = >0.8% of Coinbase maker volume ≈ $B-scale) that a $5k book cannot hold organically; the only
  on-ramps are the temporary New-Client Intro Rate / Jumpstart. **→ PARKED, NOT a re-pivot.** Kraken stays
  primary (0bp majors at an absolute $10M/30d we self-sustain). Revisit ONLY if a Coinbase rep engages (a
  relationship rate) or our capital scales. Full terms: the June-2024 program PDF.
- BOOKS: 5 majors `data/{btc,eth,sol,xrp,doge}-kraken-book` (BTC ~42h) + 16 minors now collecting via
  `kraken_candidate_book_collectors_durable.yml` (default branch, cron 03/09/15/21 UTC) —
  HYPE/SUI/ADA/ZEC/XMR/AVAX/XLM/AAVE/LTC/NEAR/TAO/LINK/BCH/BNB/TON/XPL. Materialize:
  `git show origin/data/<coin>-kraken-book:<coin>_kraken_book.jsonl.gz | gunzip > /tmp/kbook/<coin>_book.jsonl`.
- TAPE re-pullable via `backfill_kraken_trades.py` — STRUCTURE only, never pricing/capacity (dies on recycle).
