┌─ DROP-IN — DavisAI Markets S68 ─────────────────────────────────────────────────────┐
│ Branch: reconcile to canonical FIRST (designated branch keeps getting cut from a stale │
│   parent — `git reset --hard origin/<canonical S67 tip>` if the S67 files are missing). │
│   S67 tip carries the capital model. "5c5vg9" is a session LABEL, not a git SHA.        │
│ WE ARE ON KRAKEN (Greg). Coinbase parked. Bybit banned, MEXC dead. Fee = $10M tier      │
│   (majors 0bp / rebate cohort −2bp), treated as REAL (carried as per-cell maker_fee).   │
│                                                                                        │
│ ⭐ READ FIRST: STRATEGY_INVENTORY.md (S67 block) → SESSION_HANDOFF_2026-07-07_S67.md.    │
│   Capital-model files: odcore/allocator.py, odcore/platform.py::run_portfolio,          │
│   scripts/portfolio_sim_kraken.py, scripts/grade_coin_kraken.py. Agent: KRAKEN_MAJORS_  │
│   SWEEP_S67.md. RULES: SIM = LIVE CODE (decide through run_kraken_cell/run_portfolio —   │
│   never reimplement odcore/); $5k = ONE shared POOL (never per-coin slice); additive     │
│   only (live path byte-untouched, CANARY must stay bit-for-bit); per-cell law; update    │
│   inventory AS YOU GO.                                                                   │
│                                                                                        │
│ ⭐ THE LANDING (Greg, S67): the $5k is the ENTIRE stack (per-coin $5k was the temp tuning │
│   artifact). Job = BEST DEPLOYMENT of ONE $5k for max profit/hr. FILL THE BEST edge-per- │
│   $ FIRST (greedy). NEVER DROP a coin — idle costs nothing; don't fund negative edge.    │
│  • Capital model BUILT + CANARY PASS (allocator 'off' == today's numbers, |diff|=0).     │
│    greedy allocator = default; run_portfolio reports POOL RETURN (not sum-@-$5k).         │
│  • Majors graded on 14d tape: BTC +5.10 / ETH +5.89 SEAT; SOL/XRP/DOGE MARGINAL. XRP was  │
│    a book-overfit CONFIG (−0.68) → REVERSED REV0.20 = +2.61 (sim GRADED_OVERRIDE; live    │
│    registry untouched). Candidate majors: LTC SEAT, AVAX/ADA MARGINAL, SUI REJECT (all    │
│    seated as backups; deep book ≠ edge). Pool-bound: 5 majors saturate $5k (98% util) —   │
│    backups buy POOL HEADROOM, not per-$ edge.                                            │
│  • ⚠ Tapes are LOCAL + die on container recycle → restore: `python backfill_kraken_       │
│    trades.py --days 14 --pair {SOLUSD,XXRPZUSD,XDGUSD,ADAUSD,SUIUSD,LTCUSD,AVAXUSD}`      │
│    (BTC/ETH `--days 28`). ~10 min, resumable.                                            │
│                                                                                        │
│ ✅ $10M-TIER VOLUME ANSWERED (Greg's Q): based on the 5 majors' tape, our capacity-capped  │
│   $5k pool trades ~$49M notional over 13.8d (12,295 funded legs, open+close) = ~$3.5M/day  │
│   → ~$106M/30d. CROSSES $10M in ~2.8 DAYS. Tier self-sustains once running; only cost = the │
│   pre-$10M RAMP (~first 3d at higher maker fee) = Greg's deferred "pre-10M" adjustment.     │
│                                                                                        │
│ JOBS (S68):                                                                            │
│  1. ⭐ TUNE ALL 4 CANDIDATE MAJORS (Greg): ADA, SUI, AVAX, LTC — don't just accept the base │
│     grade. Per-coin stack tune (grade_coin_kraken + live path): finer REV grid, early-arm  │
│     (eps), deep-bail, side re-confirm → each coin's best config before final seating.       │
│     (S67 base: LTC SEAT, AVAX/ADA MARGINAL, SUI REJECT — tuning may move them.)             │
│  2. ⭐ TUNE THE BEST $5k DEPLOYMENT (the open "best deployment" work): idle-vs-deploy      │
│     threshold (per-batch greedy funds thin coins during BTC/ETH idle windows + some lose  │
│     on the 14d window — don't fund unless edge clears a bar), optional preemption /       │
│     per-coin exposure cap; settle edge-weight source (tape vs book); longer-window        │
│     confirm of LTC + XRP-reversed before any live-registry change. All via portfolio_sim  │
│     _kraken (--mode greedy --seats all). Numbers PROVISIONAL (14d tape-proxy, not sizing).│
│  3. ⭐ SMALL-CAP STRATEGY (Greg's 3rd ask, queued): the ~116 THIN-OK −2bp sleeve — rebate  │
│     economics, queue-honest fill, per-cell grade via grade_coin_kraken; architect S1–S3   │
│     thin-sleeve proof (ARCHITECT_READ_S66.md). Restart eligible-alt collection.           │
│  4. Later: per-COIN cap → per-LEG cap swap (odcore/capacity.py, the right granularity);   │
│     pin capacity on the ~30h Kraken BOOK; wire run_portfolio to the paper cron.           │
└────────────────────────────────────────────────────────────────────────────────────────┘
