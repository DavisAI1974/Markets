# KICKOFF — Session 32 — written 2026-06-21 (end of the S31 DATAFIX sub-session)

**Where to open:** the sibling worktree `E:\Markets\.claude\worktrees\suspicious-payne-518711`
(branch `claude/suspicious-payne-518711`). It already holds the gap-#1 integrity outputs and
can read the corrected data by absolute path — no branch merge needed. Memories auto-load.

## STATE COMING IN — the historic-trade DATA FIX is DONE
Read first: `research\strategy_evolution\live_mock_replay\HINDSIGHT_AUDIT_ORACLE_FIX_2026-06-21.md`
(one-page status), then `SESSION_HANDOFF_2026-06-13_S30.md` + `KICKOFF_2026-06-13_S31.md`.

- **Hindsight audit oracle: FIXED.** Snapshot-clamp bug removed. Every row recomputed per-row
  on its own `ts_utc`, best-favorable exit within the true horizon from the history archive.
  Canonical file (absolute path, any session):
  `E:\Markets\research\strategy_evolution\live_mock_replay\live_hindsight_missed_winner_audit_rows.corrected.csv`
  21,182/21,184 rows corrected (2 in a real ETH/Coinbase data gap, flagged `no_entry_bar`).
  `miss_type` reconciled; aggregate summaries regenerated (`..._audit.corrected.json`/`.corrected.md`,
  validated). Winners 11,521→17,232; oracle ceiling $308k→$178k. Mirrored to all 3 KBs under
  `markets_hindsight_audit_fix_20260621/`. Memory: [[markets-oracle-audit-snapshot-clamp]].
- **Classify winners on `is_oracle_winner_after_fees`** in the corrected CSV — NOT on the original
  blocker/decision provenance.

## TODO THIS SESSION
1. **Propagate THIS worktree's integrity outputs (gap #1).** `realbins_clean\*kraken*` (btc+eth,
   spike-flagged + grid-normalized) → copy to the 3 KBs (`E:\refrag\discoveries`, `E:\refrag\docs`,
   `F:\Factory\knowledge`) and supersede the tainted snapshot (CLEANUP_RUNBOOK 3-copy policy +
   `.SUPERSEDED` rename convention). This is the one data-fix carry-over still owed.
2. **BACK TO THE CANONICAL PLAN** (the real forward work). `QUOTE_SERVICE_PLAN.md`: T3.1
   `mm_passive` maker-rebate spread capture, gated by the regime classifier + OI/CB-premium/basis/liq
   monitors, with OD coupling/lead-lag/decoupling/**dipole as GATES + spread adjusters, NOT entry
   signals**. Identify the plan by Greg's PySR/Julia/5-step test, not by filename.

## OUT OF SCOPE (separate, deeper — do not conflate)
The win/lose **provenance confound** (win pool 05-23/24 missed-winner opportunities vs lose pool
05-04/11 backtest slices — disjoint in time + pipeline). Needs both classes from the same universe.
[[markets-win-lose-pool-provenance-confound]]. Leave it unless explicitly told.

## ENVIRONMENT
`E:\refrag` mounted (code + data; pipeline PySR/Julia-free). Fragile 4-CPU box — any discovery rerun
caps at `--workers 3`, batch-100, resume-safe. Bars: `markets_bar_loader.load_closes(asset, venue, t_min, t_max)`.
