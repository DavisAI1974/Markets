# KICKOFF S91 — Kalshi. Verify the year on S3, rotate keys, finish JOB 2, spin the agents.

Run the **`kalshi-session-start` ritual** (`.claude/skills/kalshi-session-start/SKILL.md`): branch check
(you'll land on the stale S70 tip `3c70ff5` -> rebase onto the trunk `claude/kalshi-s79-kickoff-ij8t9o`),
then read in order. NOTE (S90): the session-start skill now restores the NYMEX + weather tapes from **S3**
(needs the AWS keys) - git holds NO bento data anymore.

READ (in order): `SESSION_HANDOFF_2026-07-13_S90.md` -> `CLAUDE.md` -> `KALSHI_TRADING.md`.

## SECRETS to re-paste (not in git; Greg pastes)
`DATABENTO_API_KEY` (db-...), `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` (IAM user `Claude`, now has
EC2FullAccess), `AWS_DEFAULT_REGION=us-east-2`. **ROTATE these early** (Q5) - they sat in the EC2 box's boot
config this session.

## Where S90 left it (the big one)
S90 found + fixed a CRITICAL flush bug (80% data loss - non-Friday days truncated to 1-row stubs) and moved
EVERYTHING to AWS. A durable **EC2 box `i-0017dc36072eaa6c8`** (us-east-2) is running the recovery+resume of
the full-raw MBP-10 year to `s3://bento-568968024170-us-east-2-an/nymex/nymex_cont/` via
`pull_year --reuse-done-jobs` (rebuild paid months free + submit the rest). ALL bento data + raw hourly
weather are on S3. Full detail in the S90 handoff.

## JOB 1 — verify the year finished + is CLEAN
1. `aws s3 ls .../nymex/nymex_cont/ | ...` — confirm all 12 months CL+NG present, and NO 1-row stubs (the
   S90 bug signature). Spot-check a few days per month are full (tens of MB). Resume any missing/corrupt
   months (`pull_year --reuse-done-jobs` from the box or a container).
2. Confirm the EC2 box's state (it was unconfirmed at S90 close - headless, S3-only visibility). If healthy
   and done, STOP it (cost) or repurpose for the daily lifecycle. If it failed, relaunch (fix the boot).
3. Verify the raw hourly weather ingest completed (`.../weather/nws_hourly/` all station-months); resume if
   partial (`nws_temp_feed --ingest-hourly ... --dest s3://...` is resumable).

## JOB 2 — finish the raw-S3 scoring rework (S90 did the continuous-tape core)
DONE S90: `event_move_baseline.load_cont_day(root, day, source="s3")` + `normalize_mbp10_row` (trade-filter
+ ladder-aggregate at read time); `month_characterize --source s3`. REMAINING: rework the RELEASE-WINDOW
loaders (`bucket_continuation.py` via `event_move_baseline.load_tape`/`load_tape_depth`) to slice release
windows from the raw S3 continuous tape instead of the old local nymex_mbp10 tape. Then re-run scoring on
the full year. Also: refactor `nws_temp_feed.realized_index` to aggregate daily HDD/CDD FROM the raw S3
hourly instead of re-fetching IEM (aggregate on the trade side).

## AGENTS to spin up (Greg S90)
- **NYMEX-products agent:** survey the NYMEX catalog (CL/NG/HO/RB/BZ futures + options + calendar spreads),
  find the best fit for our system (sub-second reversal / dipole-exhaustion + the raw MBP-10 tape), TEST the
  top 5. Small futures edges fit HIGH VOLUME + frequency (deep book, ~0.3-tick fee floor).
- **Kalshi product-ranking agent** ran in S90 - fold its result in (top-5 Kalshi products by per-day fit).

## STANDING research
- **NG exhaustion read** (S90 canary = faint right-signed pulse ~+2-3pp on 1 day): run it properly - per-cell
  (tod x regime x vol), full S3 year, fine-resolution reversal entry, net-of-fee maker AND taker.
- **Weather:** the OD temp forecaster rerun doc (`od_weather_discovery_knowledge.json` part 2) lands on the
  trunk from another session - score per cell vs market via the S90 interface spec
  (`WEATHER_FORECAST_INTERFACE_S90.md`); confirm the exact KXHIGH settlement station per city.
- Daily forecast/trade lifecycle trigger (weather + NYMEX path) - wire once the scorer exists (see the
  FORECAST WORKFLOW block in KALSHI_TRADING.md).

## Rules (unchanged)
Historical data RAW / keep-all-info / aggregate only on the trade side; leakage gate before any scoring;
per-cell never pooled; distributions not means; net-of-fee maker AND taker; exclude settle window; zero
synthetic; provisional-until-live; weather forecaster = Greg's spec HANDS OFF; keys are SECRETS. git = source
of truth for CODE; ALL data (bento + weather) is on S3.
