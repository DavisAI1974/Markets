# S63 KICKOFF — the 1-HOUR TREND flip + spin the trend agent

⛔ BANS unchanged: BYBIT never; MEXC dead. KRAKEN PARKED. Fee frame = COINBASE (Greg handles the
fee tier; tests fee=0, flip = 22bp taker cross — locked S62). Branch: reconcile to canonical
`5c5vg9` FIRST (designated branch keeps getting cut from the wrong parent — reset --hard); sync
canonical after every push; a paper_trade cron pushes to canonical — rebase, don't clobber.

Read first: `SESSION_HANDOFF_2026-07-06_S62.md` + `S62_RESUME.md`.

## STATE (S62 close)
- **3-piece rig rebuilt, reproduces R11 exactly** (`_s62_3piece_harness.py`): SOL baseline +1.77,
  oracle +26.02, 508/508 winners kept. Entry-flip oracle per coin: SOL +30 / ETH +23 / DOGE +22 /
  XRP +22 / BTC +14 $/hr (the prize; winner-preserving).
- **BREAKTHROUGH — 3-piece at E300** (`_s62_e300_3piece.py`): mid-leg depth predicts DEATH at
  AUC 0.69-0.77 all 5 coins; BTC +1.29/hr (crosses zero, 4/5wk), XRP +0.81. Earns via death-vs-
  WINNER (depth) + flatten. Deep-arm variant (`_s62_armed_trend.py`) = ETH/BTC/DOGE +1.1..1.6 at -20/-30.
- **REFRAME:** big losers = shorts into uptrends (wrong DIRECTION, not win/lose). Win-long/win-short
  coeffs = perfect mirror. But direction is ~chance at entry from coeff/momentum/dipole at the 600s
  scale (market mean-reverts there = the machine's edge). Death-vs-RECOVERY = ~chance everywhere.

## ⭐ FIRST THING (Greg, S62 close): the 1-HOUR TREND flip
The big losers FADE a strong 1-HOUR trend. Among big losers the 1h trend (mom 3600s) predicts the
forward direction at 0.58-0.67 (btc 0.672, eth 0.633, xrp 0.655) while overall momentum is BELOW 0.5
(mean-reverting). "Know the trend — short or long" at the RIGHT horizon.
- BUILD: at entry, signal = -side*mom1h (machine fading a strong 1h trend). Flip the strongest 1h-
  trend-fades. GRADE: per-week OOS $/hr @ $5k vs baseline, shuffle floor, big10-flip (do the 10
  biggest losers flip?), fee-accounted (flip = 22bp taker). Sweep the trend window (1h/2h/4h) + cutoff.
- Connect the pieces: R13e flip pre600<-80 (+0.61, the 600s freight-train) is the shallow-horizon
  version; the 1h horizon is the refinement. S57/S56 worst-10 = fade-the-freight-train.
- WIN CONDITION: the 1h-trend-fade flip clears its shuffle floor AND per-week robust (not the wk4-
  fragile pattern) -> a candidate robust edge on the ENTRY flip (better than the E300 mid-leg stop
  because it catches the whole move, not just past E300).

## JOBS
1. **1h-TREND-FADE FLIP** (above) — the live lead.
2. **SPIN THE DIPOLE/TREND AGENT BACK UP** — build the ACCURATE 1h trend-following read (dipole at
   the 1h scale + momentum + the S36 divergence). Can a trend read at 1h flag + flip the big losers
   at entry, better than raw -side*mom1h? (The S62 agent tested death-vs-recovery = chance; it did
   NOT test the 1h-trend direction — that is the new question.)
3. **E300 mid-leg management** — the working earner (BTC +1.29). 2nd-window confirm; wire the SOL
   trend-efficiency action tuning (its deep dip-recoveries are the collateral).
4. **FLIP-THEN-MANAGE (R13f)** — still queued (flip pre600<-80, then price-manage the reversed leg).

## Rules (standing): crypto only; zero synthetic; per-cell deploy; never tune off one window;
graded beats gated; PROFIT-PER-HOUR is the scoreboard (per-leg bp = diagnostics); sandbox-first +
baseline canary bit-identical; git = source of truth; MPLBACKEND=Agg (pip install matplotlib);
token 403 on GHA dispatch (Greg clicks Run workflow).
