# KICKOFF — S48: confirm the two-factor SIZING edge on a 2nd window, cut the taker rate, then wire it

Branch: dev/push on `claude/crypto-liquidity-signals-s47-3l25k5` (S47 tip). Keep canonical
`claude/crypto-liquidity-signals-5c5vg9` synced. If the harness lands stale, `git merge --ff-only
origin/claude/crypto-liquidity-signals-s47-3l25k5` first.

READ FIRST: `SESSION_HANDOFF_2026-06-30_S47.md` → `S47_SIZING_FINDINGS.md` → S47 delta atop `CLAUDE.md`.

RULES: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git=source of truth (commit+push
often); **never tune off one window** (all S47 numbers are ONE 11.7h window == S46's); falsification-first
(every new signal: `assert_no_leakage` + a random/shuffle control before you trust it). OD-BOOK
`.ttest_committed.json` sentinel FROZEN.

## Where S47 landed (one paragraph)
Entry timing (job #2) and wrong-tail GATING (job #4) are **dead** — entries are already near-optimal and the
losing ~26% are not separable at entry (every gate loses total net; the wrong-tail is irreducible). The real
edge is **two-factor conviction SIZING** (keep all trades, size up the good ones): **SIZE axis** (vol_60,
volat_120, runup, **dive_depth** = Greg's S40 depth→size, climax) predicts |move| consistently on all 5 cells;
**QUALITY axis** (climax-volume only) predicts win/lose. Size by `rank(climax) × rank(size-axis)`
(E[net]=P(reversal)·E[|move|]) → matched-capital total-net lift vs random control **SOL +3.6σ, DOGE +3.2σ,
BTC +2.2σ** (XRP/ETH null). Net-of-cost: **razor-thin, requires maker fee ≤ 0**; survives on **SOL/BTC** at
realistic mk0/tk5 (sizing improves it); DOGE dies on a 47% taker rate (execution, not signal); a rebate
extends to XRP/ETH. Everything PROVISIONAL on one window.

## ⚠ DO NOT REINVENT — use the EXISTING files (Greg, hard)
- `odcore/swing_maker.py` — the executor (`simulate_swing_maker`; supports `maker_fee_bps`/`taker_fee_bps`/
  `entry_gate`). Wire conviction→SIZE here (per-leg size multiplier), don't fork.
- `odcore/flip_detector.py` — `detect_flips` (filter), `retime_flips` (timing; leakage-clean, OFF — timing is
  dead, leave off), `lean_series`.
- `_render_trades.py` — `swing`/`swingwalk` drivers. `_liquidity_dive.build_channels` / `median_spread_bps`,
  `_birth_probe.load_book`/`to_grid` (book→grid; GZIPPED). `odcore/leakage.assert_no_leakage` (mandatory gate).
- The S47 sizing/fee logic lives in the scratchpad prototypes (proto_twoaxis.py, proto_fees.py, proto_size_
  screen.py) — PORT the validated logic into odcore, don't rebuild from scratch.

## THE S48 JOB (in order)
0. **PAPER TRADING is live — run it FIRST each session + read the accruing ledger.** `python scripts/paper_trade.py`
   (re-fetch books first, gzipped). It dedups by (cell, entry_ts) and appends to `paper_ledger.jsonl`, so each
   run over the rolling window accrues a FORWARD record — this IS the multi-window out-of-sample test. Read the
   per-cell shakeout it prints; watch whether the forward (newly-added) trades keep the seed's net-of-fee
   profile (sol/btc/xrp/eth +, doge ~breakeven via sizing). ACTIVATE the cron by placing
   `.github/workflows/paper_trade.yml` on the DEFAULT branch (Greg; token can't dispatch GHA) so it self-accrues.
1. **2nd-WINDOW CONFIRMATION (THE gate).** Re-fetch the books (cron should have advanced the rolling window):
   `for c in sol doge xrp eth btc; do git fetch origin data/$c-book; git show origin/data/$c-book:${c}_coinbase_book.jsonl.gz > /tmp/${c}_coinbase_book.jsonl.gz; done` (NO gunzip — books load gzipped).
   CHECK the window is actually NEW (head/tail ts ≠ S46's 2026-06-29 11:49→23:29). Re-run proto_twoaxis.py +
   proto_fees.py (or just trust the accruing paper ledger). Does the SOL/BTC two-factor lift (+σ vs random) AND
   the net-of-fee survival REPRODUCE on the fresh window? **This gates real sizing — do not size until it passes.**
   If the buffer hasn't advanced, the paper ledger (run #0) is the accrual path; off-git storage is the durable fix.
2. **CUT THE TAKER RATE** (the doge lesson; thins fee drag everywhere). Forced taker flattens are the cost sink
   (doge 47% → dead). Improve maker fill / true front-of-queue / a smarter last-option so high-taker cells stop
   bleeding the spread+taker fee on exits. Re-measure net-of-fee.
3. **WIRE conviction→SIZE into `simulate_swing_maker`** (the job-#3 scale-in, done right): per-leg size =
   clip(1+alpha·z(conviction)); conviction = rank(clmx_60)·rank(size-axis), CAUSAL ROLLING normalization (not
   full-window z); run `assert_no_leakage` on the conviction signal FIRST. Per cell; deploy SOL/BTC (rebate →
   XRP/ETH). Then the per-cell emit path (`odcore/quiet_registry.py`).
4. **Confirm maker fee ≤ 0 / rebate is real** for these cells/venue before sizing for real — the whole edge
   depends on it (mean swing 2-4 bps; a 1 bp maker fee is fatal).

## DEAD — do not re-chase (S47 proved these on this window)
Entry-timing retiming (price-reversal/early-arm — entries already optimal); wrong-tail ENTRY GATES (dipole-
exhaustion, climax-gate, adverse-stop — all lose total net); spread/dive as TIMING triggers (fire early);
stacking net-screened levers onto climax (dilutes). The wrong-tail is irreducible — SIZE around it, don't gate it.

## TOOLS (S47, scratchpad — port the survivors)
proto_twoaxis.py (two-factor sizing + random control), proto_fees.py (net-of-cost scenarios), proto_size_
screen.py (lever screen vs |swing| and net), proto_sizing.py (matched-capital + added-capital), proto_dive.py
(depth lever), proto_losers.py (loser characterization). All under the session scratchpad.

## NOTES
- Books GZIPPED; `pip install matplotlib`, run renders `MPLBACKEND=Agg`.
- The integration token can't dispatch GHA (403) — book runs are cron/button. Alt books near the 100 MiB push
  cap in a few days → off-git storage (blocked on cloud auth) is the durable fix + the only path to a clean 2nd
  window without waiting.
