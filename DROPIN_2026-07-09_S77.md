# DROP-IN — S77 (2026-07-09) — confirm the BOOK ride-to-reversal swing on KRAKEN (multi-window), wire ETH

READ, IN ORDER: `STRATEGY_INVENTORY.md` (OPERATING CONTRACT + ✅ LIVE) → `SESSION_HANDOFF_2026-07-09_S76.md`
(the BTC/ETH book-swing result) → `SESSION_HANDOFF_2026-07-09_S75.md` (SOL = the direction/spread-capture wall,
DONE) → `BTC_SIGNAL_SESSION_2026-07-08.md` (Greg's paper — the strategy).

BRANCH: `claude/curve-shape-gate-s75-06m1vf`. Books: `git show origin/data/<coin>-kraken-book:<coin>_kraken_book.jsonl.gz
| gunzip > /tmp/kbook/<coin>_book.jsonl` (btc/eth). No pip — the scripts shim matplotlib.

## ⭐ WHERE WE LANDED (S76)
- **SOL is DONE** — a small-band, mean-reverting, SPREAD-CAPTURE coin. Win/lose = DIRECTION, and direction is
  un-callable at entry (book flat, dipole ~chance), uncorrectable mid-trade (winners invisible), and its moves are
  too small to flip on distance. Capped ~62% / +11.26 $/hr on the entry axis = its ceiling. Don't re-fight SOL.
- **The BTC/ETH edge = the BOOK ride-to-reversal swing** (Greg's `early_signal.py`), a SEPARATE directional strategy
  from the maker zigzag (the book is a coin-flip on the maker legs' spread-driven win/lose — it predicts the 60s
  mid move, which the zigzag doesn't trade). Enter on a strong book lean, ride ~60s, exit on a wide trailing stop
  (30 bps) / max hold (600s).
- **60/40 WALK-FORWARD (Greg's "tune on 60%") VALIDATED it OOS on Kraken:**
  - **ETH ⭐: OOS +7.23 bps/trade @0% maker, +2.23 @taker (SURVIVES), 59.5% win** (train-picked enter_z 1.5).
  - **BTC: OOS +2.09 bps/trade @0% maker (dead at taker)** — needs the 0% maker floor; moves are small.
- **⚠ tuning trap:** optimizing train **$/hr @0** overfits to CHURN (dead at taker). **Tune by per-trade QUALITY
  (bps/trade), keep the low-frequency ride (trail 30 / hold 600).**

## ⭐ S77 JOB — KRAKEN ONLY (Greg: focus Kraken; no other venues)
1. **MULTI-WINDOW CONFIRM (the decisive test).** S76's OOS is ONE ~35–40h window split 60/40 (ETH OOS n=84). Re-run
   `book_swing_kraken.py` walk-forward as the Kraken books accrue more hours / across regimes. Does ETH's +7.23 (and
   BTC's +2.09) hold on fresh/independent windows? Nothing sizes until this holds.
2. **Keep the ride config; tune by bps/trade quality, not $/hr** (the churn trap). Low-frequency ride (trail 30/hold 600).
3. **If multi-window holds: wire the ETH book-swing as its own SANDBOX directional cell** (separate from the maker
   zigzag); BTC as a 0%-maker-floor cell. Firing/direction stays LOCKED (Greg-only) — this is a NEW cell, not a change.
4. Optional: does BTC improve with a horizon-matched exit (its 71% aggregate hit is at 60s; the ride holds 600s)?
   Test in the walk-forward, tuned by QUALITY — but don't chase churn.

## RULES (standing)
KRAKEN ONLY · 0% maker = the $10M tier · firing LOCKED (Greg-only) · use Greg's `early_signal.py` AS-IS (don't
reimplement) · walk-forward (train 60% / test 40%) before any claim · tune by per-trade QUALITY not $/hr churn ·
the book swing is a SEPARATE directional cell from the maker zigzag · SOL done · doge on its own track.
