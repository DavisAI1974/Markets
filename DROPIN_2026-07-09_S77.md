# DROP-IN — S77 (2026-07-09) — confirm the BOOK ride-to-reversal swing on KRAKEN (multi-window), wire ETH

READ, IN ORDER: `STRATEGY_INVENTORY.md` (OPERATING CONTRACT + ✅ LIVE) → `SESSION_HANDOFF_2026-07-09_S76.md`
(the BTC/ETH book-swing result) → `SESSION_HANDOFF_2026-07-09_S75.md` (SOL = the direction/spread-capture wall,
DONE) → `BTC_SIGNAL_SESSION_2026-07-08.md` (Greg's paper — the strategy).

BRANCH: `claude/curve-shape-gate-s75-06m1vf`. Books: `git show origin/data/<coin>-kraken-book:<coin>_kraken_book.jsonl.gz
| gunzip > /tmp/kbook/<coin>_book.jsonl` (btc/eth). No pip — the scripts shim matplotlib.

## ⭐ WHERE WE LANDED (S76)
- **SOL is DONE** — a small-band, mean-reverting, SPREAD-CAPTURE coin. Win/lose = DIRECTION, and direction is
  un-callable at entry (book flat, dipole ~chance), uncorrectable mid-trade (winners invisible), and its moves are
  too small to flip on distance. Capped ~62% / +11.26 $/hr on the entry axis = its ceiling. Don't re-fight SOL.
- **The BTC/ETH edge = the BOOK ride-to-reversal swing** (Greg's `early_signal.py`), a SEPARATE directional strategy
  from the maker zigzag (the book is a coin-flip on the maker legs' spread-driven win/lose — it predicts the 60s
  mid move, which the zigzag doesn't trade). Enter on a strong book lean, ride ~60s, exit on a wide trailing stop
  (30 bps) / max hold (600s).
- **60/40 WALK-FORWARD (Greg's "tune on 60%") VALIDATED it OOS on Kraken. OBJECTIVE = MAX $/hr @0% maker (Greg —
  trade count / per-trade size irrelevant; more trades = more $/hr is fine):**
  - **ETH ⭐: OOS +21.7 $/hr @0% maker** (z1.5, trail 30, hold 600, 6 trd/hr; also taker-survivable +2.23 bps/trd, 59.5% win).
  - **BTC: OOS +15.9 $/hr @0% maker** (z1.0, trail 15, hold 60, 45 trd/hr; low bps/trade but high $/hr — that's fine).
- **⭐ ALL 5 COINS HAVE THE BOOK EDGE, JUST SMALLER (Greg + paper: 5 wins, per-cell weighting).** BTC/ETH biggest;
  SOL/XRP/DOGE a smaller version, weighted low — NOT zero.
- ⚠ TWO real caveats (NOT quality): (1) assumes **0% maker on BOTH entry AND exit**; the reversal exit may need taker,
  which COLLAPSES the high-freq configs → **maker-exit-at-0% is the deploy gate**; (2) P&L is **mid-price (upper bound)**.

## ⭐ S77 JOB — KRAKEN ONLY (Greg: focus Kraken; no other venues)
1. **RUN THE PER-CELL MAX-$/hr WALK-FORWARD ON ALL 5 COINS** — `book_swing_kraken.py` is READY (train grid now sweeps
   `min_conv` so flat-book coins SOL/XRP/DOGE enter; guards the empty case). Rank by OOS $/hr @0% maker; expect BTC/ETH
   biggest, the others smaller-but-present.
2. **MULTI-WINDOW CONFIRM (the decisive test).** S76's OOS is ONE ~35–40h window. Re-run as the Kraken books accrue /
   across regimes. Nothing sizes until it holds on fresh windows.
3. **Nail the MAKER-EXIT-at-0% question** — can the reversal exit rest as a 0% maker on Kraken? Gates the high-freq $/hr.
4. **If it holds: wire the top-$/hr book-swings as their own SANDBOX directional cells** (separate from the maker
   zigzag). Firing/direction stays LOCKED (Greg-only) — a NEW cell, not a change.

## RULES (standing)
KRAKEN ONLY · 0% maker = the $10M tier · **objective = MAX $/hr @0% maker (count/quality irrelevant)** · firing LOCKED
(Greg-only) · use Greg's `early_signal.py` AS-IS (don't reimplement) · walk-forward (train 60% / test 40%) before any
claim · the book edge is on ALL 5 coins, per-cell weighted · the book swing is a SEPARATE directional cell from the
maker zigzag · maker-exit-at-0% is the deploy gate · SOL zigzag stays (spread capture) · doge on its own track.
