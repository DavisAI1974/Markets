# SESSION HANDOFF - S101 (work dates 2026-07-20/21) - TWO GROUPS WALKED (G12 + G13, blind->refine->merge each), brain s101.2 -> s101.6, day-class doctrine family landed, the REST-OF-YEAR DATA MACHINE launched detached, CL repairs running - G14 opens S102 fresh

Branch: `claude/ng-coach-agent-loop-5ha5bf`. git = CODE, S3 = DATA. The parallel session's
S100.1/S100.2 docs (options coach, dashboard) are SEPARATE PIECES per Greg - referenced, never
folded into this record.

## SESSION TOTAL (chronological)

1. **G12 ONE-SHOT BLIND** (Sun Feb 1 - Fri Feb 13; brain s101.2; fresh-session hygiene honored;
   roll-check subagent protocol first use - 0213 roll 1021->996 spread -0.124 marked): direction
   6/12, close-cum -6600g vs -13320a, drift +5480. THE BLOCK STORY: the walk's largest weekend gap
   in either direction - 0201 Sunday reopened -7080 (anchor 4.416 -> 3.708 open) as the Feb
   dead-sponsor squeeze unwind; the blind's day-book was good from the wrong level (re-anchored
   view: residual drift -1750). BLIND-WALL DISCLOSURE (the agent's own): the state file's in-block
   days carried prior-day actuals in contract_structure/vol_regime/cash_basis - G12 recorded as a
   WEAKENED holdout; the masking fix became a build item and shipped before G13.
2. **G12 REFINE (Greg: "refine until it scores"; two iterations)**: v2 = direction 11/12, final
   drift -220. THE WEEKEND-GAP RESOLUTION (Greg's first-task order): gap OWNERSHIP declared at the
   prior close (anchor_structural > weekend_cycle_weather > chain_drift; new play
   magnitude.block_gap_ownership) + the DEAD-SPONSOR SIZING RULE (premium P from the pre-squeeze
   shelf; echo run E post-expiry; weekend gap = -E band 0.75-1.25x, 0201 = 1.09E; next session
   unwind-to-shelf; shelf HOLDS in the squeeze form vs breaks in the weather-parabola form) - all
   anchor-measurable, 13-gap walk back-check zero out-of-band, 0125 re-classed structural (resolves
   the s100.2 gap-sign conflict).
3. **BRAIN MERGES (all on Greg's approval): s101.3** (the 12-item G12 package incl. flip
   gap-as-counter addendum, storage-Thursday chain-STATE scoping, B2 capitulation scoping, MOS
   forward-test fold, squeeze_watch prompt-month fix as named build, masking protocol);
   **s101.4** (Greg: DAY-CLASS = the OVERARCHING top-level descriptor; classify first; forecast and
   score ONLY within class - "no Mondays scoring Fridays"; cross-class = context only);
   **s101.5** (Greg: holiday-extended 3-day weekends = their own weekend sub-class; same-holiday
   comparability spans the whole closure); **s101.6** (the G13 package below + Greg's SEASONAL
   SALIENCE SLIDER: weather max in deep winter/summer, shoulders shift weight to
   flows/maintenance/storage-trajectory/structure; winter-calibrated-by-circumstance named; G14 =
   first shoulder test). Backups kept at every step. 27 plays at close.
4. **PRE-G13 BUILDS** (orchestrator, serial): squeeze_watch PROMPT-EXPIRY fields + dead-sponsor
   unwind_watch (pinned: 0202 = NGG26/3 sessions/True; arms again 0226); decision-state
   `--mask-after` = the ONE-SHOT MASKING FIX (price-derived blocks frozen at the anchor-CLOSE
   vintage, exogenous feeds live, deterministic clock live in flow_calendar; pinned).
5. **G13 ONE-SHOT BLIND** (Sun Feb 15 - Fri Feb 27, THE SQUEEZE TEST; brain s101.5; FIRST MASKED
   STATE - the agent verified the mask on all 12 days, NO disclosure needed; in-session run,
   holdout caveat recorded in-file per hygiene; roll 0225 996->1008 spread -0.022 marked):
   direction 4/12, +750g vs -2270a, drift +2800. THE ANSWER: NO SQUEEZE FIRED - the delivery
   gauntlet passed in a thin bleed-down that BROKE the 3.159 shelf (close 2.857); leg counts
   collapsed to 2-30/day (vs G12's 53-122). Presidents Day ran the named-gap Sunday-like prior.
6. **G13 REFINE + THE SQUEEZE-FINGERPRINT RETROSPECTIVE (Greg's order, pre-onset primary)**:
   refine v-final = direction 12/12, drift -250 (independently verified). FINGERPRINTS (onset
   located per instance; quiet-day controls; windows never blurred): **F1 failed-counter-flush
   CONJUNCTION** (pre-onset, up class, n=3 G9/G11 - noise standalone, 3/3 with a live structural
   pull state; PLAY tape.squeeze_ignition_flush_conjunction); **F2 thin-regime RATCHET confirm**
   (progress-without-aggression, 38-47t/klot vs controls <=17; PLAY tape.squeeze_ratchet_confirm);
   F3 down-class stalemate + F4/F5 (incl. the 0130 stranded-close inverse: size selling into the
   rising close for two hours before the -7080 gap) = WATCHES; trade-rate ramp + VWAP posture =
   NOISE. Other resolutions: the MODERATE weather gap arm (NEAR-window cycle-delta-sum bands;
   both G13 gaps in-band; 13-gap back-check clean); the PRICING-TIME rule (shock adds price at
   appearance, moderate adds at near-window entry; MOS appearance side-caller 0/3 forward,
   DEMOTED); CHAIN-STATE-IS-TAPE-STATE (blind chain reads demote to basing after 2 consecutive
   misses - the phantom-chain fix); shelf-holds scoped to the unwind's own sessions; 0223
   fade-vs-ratify separator (driver AHEAD = ratify, BEHIND = fade; Monday phase structure 3/3);
   Presidents registry n=1 (+380, catalyst priced on the dead desk).
7. **DAY-CLASS LAYER, full lineage (Greg desk doctrine)**: weekend toolbag (STATE reads survive a
   closure, RHYTHM reads void - per-play validity table), Monday 06-10 ET catch-up window (9 fade /
   3 ratify events; the ratifies are the flip Mondays), Friday bag (flow-calendar first; the Friday
   close IS the weekend anchor; no blanket squaring damper), Tue-Thu interchangeable core,
   holidays same-holiday-only (named gap; Sunday-like default), extended weekends own sub-class.
   Promoted to OVERARCHING in s101.4; falsifiable-hypothesis stance retained; G13 first forward
   scoring recorded (Monday phases 3/3; 0227 Friday miss = existing terminal-squaring note
   unapplied, now n=2).
8. **THE REST-OF-YEAR DATA MACHINE (Greg: "start it all now... not tethered to one session")**:
   `deploy/aws/pull_rest_2026.py` running DETACHED on box i-08cee7171c0a76a04 - NG.n.0/n.1 trades
   per-day Mar 1 -> present; NG.FUT + ON/LNE.OPT statistics+definitions monthly raw
   (nymex/contract_structure/raw_2026ext/, options_ng/raw/ext_2026/); CL.n.0/n.1 trades FULL YEAR
   Jul 2025 -> present (nymex/cl_cont_n0|n1/); CL.FUT + LO.OPT statistics+definitions
   (nymex/contract_structure_cl/raw/, options_cl/raw/). Cost: $1.10 total measured (CL year
   trades; everything else $0.00 in-sub); $25 guard armed; resumable skip-if-on-S3; log
   s3://.../logs/pull_rest_2026.log. **CL JULY RAW TOP-UP** (`pull_july_2026_cl.py`, CL year store
   ended 20260630) launched in-session. **CL 51 STUB-MONDAY REDECODE** launched detached
   (`deploy/aws/cl_redecode_runner.py`): $0 VERIFIED before download (69 done jobs, expirations
   Aug 12-14, 51/51 covered); first repair confirmed (460B -> 60.7MB); ETA ~06:00Z. A box
   check-in trigger fires ~02:12Z into this session (re-arms; stops the box when jobs finish).
9. **FEED EXTENSIONS (session-side, free)**: consensus HOLE CLOSED 18/18 report weeks Mar 12 -
   Jul 9 (TE + investing occurrence-history via Wayback; FF structurally dead named; houses not
   independent - investing == TE-morning on all 9 checkable weeks); STEO apr26-jul26 vintages
   (release dates MEASURED; completion-leads-release trap re-confirmed on all four); flow_calendar
   -> 2026-12-31 (EIA H2 shifts from the published schedule; **the 2026-11-25 pile-up: NGZ26
   expiry + EIA print + bidweek same day**; 16/16 expiry cross-check); solar -> 2026-12-31;
   vol_regime -> Mar 13 (v0 sessions carried verbatim - NAMED TRAP: naive --build without the v0
   tape local re-drops them); ngwu verified covered (Mar 12 issue parse gap named, verbatim text
   carried); March tape days verified pure instrument 1008 and copied into the local n0 store.
   All finished stores pushed to S3 with manifests.
10. **FLAGGED, NOT RESOLVED - THE MARCH BASIS DIVERGENCE**: Databento's literal NG.n.0 maps to the
    LESS-ACTIVE contract in March (0311: n.0 = 23,188 trades vs n.1 = 47,064 = instrument 1008,
    the calendar-front April) - the OI-rank quirk from the G13 window recurring. BOTH series now
    exist on S3 (box = literal n.0; the definitions raw identifies contracts authoritatively).
    **G14 SETUP MUST DECIDE THE BASIS** (roll-check subagent compares both + definitions; the
    PASS2 recommendation - define rolls ourselves - is the eventual fix). Also named: Databento
    flags 2026-03-15 as a degraded-quality day.
11. **VENUE RESEARCH (Greg asked; web-verified 2026-07-21)**: POLYMARKET STRUCK from the roadmap -
    international CLOB US-geoblocked; Polymarket US (QCX, CFTC DCM, live Dec 2025) BANNED IN OHIO
    (Greg's state, one of 8). Kalshi-Ohio fight is SPORTS-ONLY (commodity contracts uncontested;
    carried as a jurisdiction watch). **NEW THIRD-LANE CANDIDATE: CME NG event contracts** - trade
    on Globex (our data sub already covers the venue), US-legal via IBKR; ForecastEx = API-first
    but no NG. Databento hub sweep (measured, $0): 55 MMBtu futures products on GLBX; ~25-30
    non-HH NA basis codes; code->hub-name mapping pass QUEUED; the deep basis complex lives on ICE
    (IFUS.IMPACT, separate licensing).
12. Protocol/ops notes: subagent fetch-waypoint stalls recurred twice (MOS, CL redecode) - the fix
    is a continue-to-completion nudge; the stop-hook fired repeatedly on live agent WIP files
    (correctly left uncommitted until verification); parallel-session pushes rebased cleanly.

## IN FLIGHT AT CLOSE (all survive this session)

- Box: pull_rest_2026 (NG+CL layers) + cl_redecode (51 Mondays, ETA ~06:00Z) + the ~02:12Z
  check-in trigger (verifies DONE, stops the box, re-arms if running).
- CL July top-up (in-session background; verify on next look; resumable re-run if the container
  died first).
- MOS FAMILY EXTENSION: **LANDED AND SHIPPED before session end** - all four stores through
  2026-07-20 (262 days each, zero missing dates; 119 walked days byte-identical; 136k+ wall
  stamps 0 violations; all selftests PASS), committed and S3-pushed with manifests. NAMED for
  future sessions: (a) normals gap 07-22..08-31 (21 far-horizon vs_normal nulls on decision days
  07-15..07-20 only); (b) the raw cache filenames claim ets 2026-07-30 but the archive edge was
  07-20 at fetch - a re-run trusting the cache will not see 07-21..07-30 runs; (c) one -0.0 -> 0.0
  sign-of-zero cell (2026-01-16 MEX h0), numerically equal.

## POST-CLOSE ADDENDUM (container restart + box check-in #1, ~02:12Z)

- CHECK-IN #1 RESULTS: NG layers COMPLETE on S3 (n0/n1 223 files each through 20260720;
  ngfut raw 10/10; options raw 19 files); pull_rest on the CL-year leg (~Aug 2025, $1.10 stable);
  cl_redecode ~30/51 Mondays repaired (80-128MB files), on pace.
- Greg declined further self-scheduled check-ins - the box finishes unattended; S102's opening
  status-check (or Greg) stops it. Idle cost ~$0.17/hr after jobs complete.
- CL JULY TOP-UP: COMPLETE before the container restart - 14 trade days Jul 1-17 on S3 incl. the
  0712 Sunday. NAMED GAP: the 0705 Sunday stub (848KB/17,949 rows) was held by the >=1MB verify
  gate and its local copy died with the restart; NG's July pull dropped BOTH its Sundays
  (0705/0712) at the same gate. NOT walk-blocking (the n0 trades store carries all July Sundays
  from the box job). S102 small: recover CL_20260705 + NG_20260705/0712 raw from the done batch
  jobs with a Sunday-stub gate exception - $0 until the jobs expire ~mid-Aug.
- CL Jul 3: 656k rows on the "full CME holiday" flag day - the Jul-3 holiday-vs-tape discrepancy
  now reproduced on a second root (standing small).

## OPEN / CARRIED

- **pyth_collector SUNSET - DEADLINE JUL 31, TEN DAYS OUT** (trunk-branch workflow; needs Greg's
  explicit go to touch the trunk). NOT DONE. Escalate at S102 open.
- Two-coach spec approval (Tier 3 item 6) still pending Greg's nod.
- G14 basis decision (item 10). Structure/options STORE builds from the new raw (S102, no key
  needed). audit-joins span extension past Feb 27 (S102, with the G14 state gen).
- CME NG event contracts scoping + the hub-mapping doc (queued smalls).
- Repo still PUBLIC (Greg TODO). Jul 3 holiday-vs-tape verify; ISO day-ahead clocks;
  non-Thursday 10:00-10:30 characterization (standing smalls).

## RULES (unchanged; see KICKOFF_2026-07-21_S102.md guards block)
