# S59 KICKOFF — ENTRY ENDGAME + THE KRAKEN GATE

⛔ STANDING BANS unchanged: BYBIT never (US-ineligible, struck S57); MEXC dead. ELIGIBILITY
BEFORE ANY VENUE WORK. Record: `S57_VENUE_FINDINGS.md`.

Read first: `S58_ENTRY_NOTES.md` (THE record — mistakes ledger, 6 rounds, five-verdict board,
fingerprint map, parked items) -> `SESSION_HANDOFF_2026-07-03_S58.md` -> S57 handoff if fee
context needed. Branch: designated claude/*; reconcile to canonical `5c5vg9` tip (fetch +
ff-only else reset --hard; the S58 branch was cut from the WRONG parent — check first);
sync 5c5vg9 every push. Default branch = crons (now incl. repaired BTC book workflow +
rotation guardrails, commit 45c145b).

## THE FRAME (S58 verdicts, carried)
- ENTRY ONLY until Greg calls it done (piece discipline). Exit/hold findings -> PARKED notes.
- PRICE MECHANICS PORT, FLOW READS DON'T: Coinbase deploy shapes = NAIVE k0 (sol th100,
  xrp th80 + death-combo rider, doge th100 w/ clmxexh candidate, btc pending books);
  bins member maps = research-only until per-venue books validation.
- ETH DROPPED (re-entry test in notes). Entry-alone ceiling: -1.4..-4 $/hr cb_real — the
  budget Piece 2 inherits (~123bp/side R8 exit prize still untouched, PARKED).
- **kr_mk0 IS REAL**: Kraken Pro US 0bp maker at $10M/30d (verified; July-9 schedule change
  — re-confirm post-facto). Fine band = research-reopened, KRAKEN-GATED (4 conditions in
  notes). Kraken vol = 35-42% of Coinbase (live snapshot).

## JOBS (one round = one defined test; renders/notes every round; update S58_ENTRY_NOTES
or successor EVERY round — chat is not a record)
1. **kr_mk0 re-price** of the round-6 machine tables (add the 0/10 column; 5 minutes).
2. **KRAKEN BOOK COLLECTOR**: new durable workflow (S37 `kraken_collector.py` bins pattern +
   the S58 rotation guardrail; data/<coin>-kraken-book branches; SOL first). Gates the
   fine-band reopen AND all Kraken cells. Greg clicks Run workflow (token 403 on dispatch).
3. **Fingerprint wire-in prep** (micros tier): per-cell mid-band winner/loser onset buckets
   from the 30d legs; dual-print scorer (match-winner MINUS match-loser); leakage-gate;
   preconditions in notes (S35b onset canary before any wiring).
4. **Entry DoD when Greg calls it**: promote armed machine (mode-0 fix + baseline fallback +
   c-scaled confirm + per-cell registry) into odcore; sandbox-first; baseline canary
   bit-identical; then Piece 2 opens.
5. Background: fold the CFM perp fee agent report into S57_VENUE_FINDINGS (was running at
   S58 close — re-run if lost); BTC books accrual check; DOGE clmxexh books validation as
   windows accrue; July-9 Kraken schedule confirm.

## DATA (/tmp dies): Coinbase books x5 per S58 pattern (data/<coin>-book branches);
Binance 30d bins: python backfill_binance_spot.py per coin (~40min x5 parallel, S54 cmds).

## Rules (standing): crypto only; zero synthetic; per-cell deploy; never pool sign-blind;
NEVER tune off one window; no-gates law; graded beats gated; scale-locality (every (band,
descriptor) pair revalidates); venue law (nothing flow-based ports without per-venue pass);
eligibility before venue work; git = source of truth; falsification-first; worst-10/
smallest-10 renders read with Greg; MPLBACKEND=Agg.
