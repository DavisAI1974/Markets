# KICKOFF — Session 29 — written 2026-06-09 (end of S28)

Read order: `CLAUDE_session_note_2026-06-09_S28.md` → `SESSION_HANDOFF_2026-06-09_S28.md` → `CLEANUP_RUNBOOK.md` →
this file. NOT the 207 KB `CLAUDE (5).md` (archive). Branch: `claude/beautiful-shaw-040328` (canonical OD line).
KEEP PAIRS SEPARATE; zero synthetic data; Result Discipline.

## STATE COMING IN (S28 close)
- **Binning corruption fixed forward** (Kraken snapshot dedup + anti-clobber push guardrail), live on default
  `new-session-o3vnm` + code ref `continue-phase-2-pipeline-UFiGY` + canonical `beautiful-shaw`. First clean
  scheduled run pending.
- **Tools in repo:** `scripts/bins_integrity.py` (`--report`/`--normalize`), `odcore/io.load_bins(mask_spikes=)`.
- **Win-bucket `entry_ts` collapse CONFIRMED** (Greg's screenshots): all 12 `*_win.json` = `1 unique_ts`;
  `*_win.fixed_ts.json` patched versions exist; loses fine. Root cause is refrag-side (not in this repo).

## TODO — in priority order
1. **Resolve the win-coeff question (the only open re-run decision).** On the LOCAL box:
   a. Find which win-bucket file the coeff runs consumed (`.json` collapsed vs `.fixed_ts.json`).
   b. Open 2–3 per-trade discovery JSONs from one win bucket — does each coeff's window key match the trade's real
      time (→ keep wins) or the collapsed `entry_ts` (→ re-run wins from `.fixed_ts.json`)?
   → If paste-able, hand the agent those JSONs + the coeff-run command; it will make the call.
2. **Fix `entry_ts` at source (refrag trade-gen).** Stop overwriting win `entry_ts` with a constant so freshly built
   win buckets aren't 1-unique_ts; supersede the `_patch_win_buckets_entry_ts.py` band-aid. Rebuild win buckets.
3. **Kraken bins repair** (cheap, optional): `bins_integrity.py --normalize` the kraken sources or load with
   `mask_spikes=True`; drop the ≤4% spike-window trades OR re-run the 8 kraken buckets. coinbase/bybit untouched.
4. **Re-validate:** `od_larger_set_val.py` once win `entry_ts` is real → honest temporal split (replaces the
   sorted-filename floor), expect z near +9.6 on the clean set.
5. **Then resume the S27 chunked coeff-gen line** (parallel driver in `_run_top20_bottom20_pairs.py`, 3-KB archive
   policy) on the clean data; doge/link/xrp upstream; 44 h full-set in chunks.

## ENVIRONMENT
- Heavy coeff-gen + pool building are refrag-bound (`E:\refrag`, `F:\Factory`, `live_data_history`) → LOCAL box only,
  not this cloud container. From cloud you have the repo code + the `data/*` bins branches (materialized to `realbins/`).
- PySR/Julia fixed + hooked on the OD branch (`scripts/session_start.sh`); the quote/phase-2 line needs neither.
