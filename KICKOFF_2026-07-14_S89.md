# KICKOFF S89 — Kalshi. ONE JOB THIS SESSION: build the RAW-DATA INGESTION WORKFLOW.

Run the **`kalshi-session-start` skill FIRST** (branch check, data restore, read order). You'll likely land
on the stale S70 tip `3c70ff5` -- `git log --oneline -1` to confirm, then rebase onto the trunk:
  git fetch origin claude/kalshi-s79-kickoff-ij8t9o
  git checkout -B <your-branch> origin/claude/kalshi-s79-kickoff-ij8t9o
Push all work to the trunk `claude/kalshi-s79-kickoff-ij8t9o` (= repo default; pull/rebase before push).

FIRST: re-export **DATABENTO_API_KEY** (SECRET, `db-` prefix, not persisted -- Greg pastes it).

READ (in order): `SESSION_HANDOFF_2026-07-13_S88.md` -> `CLAUDE.md` -> `KALSHI_TRADING.md`.

## The principle (load-bearing, Greg S88)
- **Historical data is RAW. We keep ALL of it** -- every message, every column the dataset carries. We
  paid for the full dataset; we store the full dataset. The agent sifts the raw data for correlations
  (how events / weather / storage / curve move price). We SCORE the raw data; we do not reduce it.
- **Gates live ONLY on our side, for TRADE SIGNALS** (the execution layer). Never on the historical data.
- The MBP-10 writer already keeps everything raw (S88 `487e633`, `databento_backfill._write_mbp10_df`):
  every message + all 10 book levels + all fields, zero reduction.

## THE JOB: a durable RAW-INGESTION WORKFLOW (Greg S88)
Reuse one of the **coin durable-collector workflows** (the S20-S37 crypto collectors that ran as long
GitHub Actions crons) as the template, point it at this data, and make it **longer duration**:
- Pull the continuous MBP-10 year (CL+NG, 2025-07..2026-07) as RAW (the writer already keeps all info).
- **gzip per month** into the `data/nymex-ticks:nymex_cont/` branch, delete the raw local after each
  chunk (the size plan: raw is huge but gzips hard; local only ever holds one chunk). If a month's local
  decode is too big for the container disk, **gzip per DAY** so local never holds more than a day.
- Long/durable so it survives a full year pull without a live session babysitting it.
- Resumable (skip months/days already on the branch).
- **Re-pull May-2026 in full-raw** -- the only month currently on the branch is the OLD reduced format;
  redo it so the corpus is uniformly raw.

The manual `pull_year_mbp10.py` proved the pull mechanics but needs a live session and kept dying on
restarts; the durable-workflow approach is the fix.

## State at S88 close
- Data feeds built + validated: `nws_temp_feed.py` (gas-weighted HDD/CDD+precip), `forward_curve.py`
  (backwardation/contango). Raw MBP-10 writer fixed to keep all info.
- The forecaster/scoring pieces built earlier in S88 (`month_characterize.py`, `bucket_continuation.py`,
  `forecaster_month_pass.workflow.js`) are SCAFFOLDING that must be reworked to ingest the RAW tape (they
  currently pre-process the data on the ingest side -- that belongs on the trade-signal side only). Do the
  ingestion workflow FIRST; rework the scoring to read raw AFTER the corpus is landing.
- Corpus on branch: `nymex_cont/` = 2026-05 only (reduced, re-pull). `nymex_mbp10/` = 24 Apr-Jul release
  windows (older reduced tapes). Temp cache committed (`data/nws_temp/gw_degree_days.json`).

## Rules
git is source of truth; commit+push regularly. No emojis. Branch discipline (don't strand on the stale
tip). DATABENTO_API_KEY is a secret. Weather forecaster = Greg's spec, HANDS OFF.
