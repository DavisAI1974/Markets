# DECISIONS.md — the binding-decision ledger (append-only)

Born S110 (turnaround memo 2.5). One line per binding decision. **Status vocabulary:**
DECIDED (stated, nothing enforces it yet) -> BUILT (mechanism exists) -> WIRED (mechanism is in the
live path) -> VERIFIED (negative-tested / proven executing) | RETIRED (no longer binding, reason).
**"Enforced by: doc-only" is the dangerous class** — the conformance sweep flags any DECIDED/doc-only
line older than two sessions, because decided-then-dropped-then-relitigated is the disease this file
kills. Never delete a line; supersede it with a new one.

**INSTANCE-INLINE RULE (Greg, S110):** every claim in this file - and in every ledger and handoff - carries its supporting INSTANCE in the same sentence or the one beside it, never in a separate section and never by reference. If a line says a thing happens, the case it happened in is right there. Two reasons: a reader must never have to go hunting to find out whether evidence exists, and a claim whose instance cannot be written next to it is a claim we do not actually have yet. Where the instance is absent, say so in place ("corpus searched, none found" / "not yet searched") per D24.

| # | session | decision (verbatim where possible) | status | enforced by |
|---|---|---|---|---|
| D1 | S107 (Greg) | Keys do NOT rotate during the walk; rotation happens after, as its own item | DECIDED | doc (CLAUDE.md AWS section + every drop-in) — expires at walk end, then becomes paper-trading item G5 |
| D2 | S102/S105 (Greg) | The blind's ONLY deliberate mask is the PRICE CURVE; anything else missing is a DEFECT | VERIFIED | state_health hard gates + the data doctrine in agents/README.md |
| D3 | S109 | Causality is physics, not a mask: specialists run on per-day causal slices; a second job with an earlier decision point gets its own earlier slice | VERIFIED | build_causal_slices.py self-audit + forward_stamps + SOP STEP 3 |
| D4 | S108 (Greg) | NEVER average above and below; report per-day, sum-abs-err, drift AND survival together | WIRED | blind_score_nonpooled.py + SOP STEP 3.5 |
| D5 | S105 (Greg) | One group at a time; stage all, run one; windows Sunday -> second Friday | WIRED | SOP + group_config |
| D6 | S97 (Greg) | Refined rules NEVER override blind-applicable ones; the blind-vs-refined gap IS the measurement | WIRED | brain play schema (requires/scope/forward_evidence) + merge adjudication |
| D7 | S105 (Greg) | Blind and refine are ONE engine; no blind-specific rule file may exist | VERIFIED | verify_gold runtime-drift wall + the deleted blind stack |
| D8 | S104/S105 | Brain merges are PROPOSAL FILES + adjudication, incumbents byte-identical; never a direct edit | WIRED | merge protocol + backups (ng_brain_*_backup.json) |
| D9 | S108 | Before any refine, the blind archives by MOVE; a refine posterior byte-identical to its blind archive is rejected | VERIFIED | archive_blind.py + assert_not_the_blind (negative-tested on the G21 near-miss) |
| D10 | S110 (Greg) | NOTHING runs off-SOP: gap -> STOP -> propose diff -> Greg go -> version log -> execute; deviations are recorded nonconformances; gates cannot be skipped or softened | WIRED | agents/RUN_SOP.md v1.1 CHANGE CONTROL + this ledger |
| D11 | S110 | No fix is DONE until a test proves the fixed path EXECUTES and the guard fires on the original defect (the f2 lesson: S108's fix was verified on one reader, dead on the other, gate watching the wrong reader for two groups) | DECIDED | SOP v1.2 STEP 2 (this session) — becomes VERIFIED when the first fix ships under it |
| D12 | S110 | A feed enters the decision state only with a NAMED CONSUMER (play, specialist directive, or another feed that reads it) or an explicit PARK note | DECIDED | SOP v1.2 (this session); enforcement mechanism = the consumer map sweep in the QC checklist |
| D13 | S110 (Greg) | Gas only — the platform scopes to NG/KXNATGASD/Henry Hub until further notice | DECIDED | doc-only (memo + this ledger) — acceptable: it is a scope statement, checked by the QC sweep against new non-gas builds |
| D14 | S110 (memo, Greg go) | Pyth collectors (WTI/XAU/XAG) RETIRED — free era ended 2026-07-31, not gas | DECIDED | pending: workflow retirement on the trunk branch (needs a trunk touch; queued) |
| D15 | S110 (memo, Greg go) | The live-orchestrator open item is CLOSED — the sequenced spawn has run five groups without an ordering failure; the S105 escalation clause self-reactivates if waves misbehave | RETIRED | this ledger line supersedes the S105/S108 open-list entries |
| D16 | S110 (memo, Greg go) | G21 round 2: CLOSED as not-run; the r1 result stands on its own record | RETIRED | this ledger line supersedes the S109 open-list entry |
| D17 | S109 (Greg) | Audit and forecast are SEPARATE roles; the auditor reads the whole block, emits no numbers, and runs BEFORE every blind | WIRED | agents/state_auditor.md + SOP STEP 1 |
| D18 | S110 (Greg) | Paper trading go-plan approved; Greg provisions the Kalshi demo account/keys (G0); dock builds run in parallel while the walk finishes G22/G23 | DECIDED | memo Part 3 + task list — G0 in flight with Greg |
| D19 | S106/S103 (Greg) | Scoreboard = forward-curve error per day, never daily hit-rate alone; P&L framing per-event only | WIRED | blind_score_nonpooled + render rules |
| D20 | S98 (Greg) | The futures->Kalshi LAG is ESTABLISHED (S80/S81/S91); never retest it — live telemetry only | VERIFIED | gate doc section 0c + kalshi_fill_model/lag_execution_map headers refuse relitigation |
| D21 | S110 (Greg) | Reasoning must be TIED to the decision it produced - "I don't want a context file out there that isn't tied to the decision it was used to make." Answer + explanation co-located; ledgers carry a machine-checked DECISION CLAIMS table | BUILT | decision_trace.py (decision_id = sha256(group\|date\|owner\|number)[:12]; `verify` fails any unresolved id and names UNDECLARED unbound ledgers); SOP v1.4 close-out |
| D22 | S110 | The LEDGER IS NOT A SECOND BRAIN. Lessons reach the BLIND only through the adjudicated brain (proposal -> adjudication -> merge). Ledgers are for humans, refines and post-mortems. A same-group ledger is a look-ahead leak; a prior-group ledger is un-adjudicated narrative that would anchor independent derivation | DECIDED | SOP v1.4 (spawn templates cite the brain, never ledgers); enforced by the blind's read-list being explicit in BLD-1/BLD-2 |
| D23 | S110 (Greg) | **OPEN — DESIGN DECISION PENDING, DO NOT IMPLEMENT.** "We can only pay attention to SHAPE for forecasting purposes. We can be hyper-focused on the value in order to match it to real-world data, but any time you use it after that, the value has to be replaced." Flagged on seeing decision_id bind to an exact number: value-keyed knowledge is good for one instance and discarded in all others — "we're basically starting from scratch every time." MEASURED EVIDENCE (S110 audit): 24 of 76 brain plays carry an absolute bar in trigger/read/call; the plays that FAILED this session are value-keyed ones (divergence_resolution HDD>=16.4 unreachable in summer; shoulder_weather_band_void <=13.5 trivially satisfied; the 0.547 and 0.55 big-print bars). Greg: think it through BEFORE replacing the value mechanism | OPEN | nothing implemented; Claude STOPPED on instruction. Next step is Greg's design call, not a build |
| D24 | S110 (Greg) | **RETRO-INSTANCE PROGRAM — queued, high priority.** "When we find stuff like this, it needs to be included with the context too. It needs to have PAST INSTANCES to fall back on as evidence... we are going to have to do a lot of investigating of past runs to get the context to update that file. This might be what gets some serious improvement for blind." A newly-discovered mechanism (e.g. the k3 impact-coefficient collapse, C-0714) is n=1 until the historical corpus is searched for its instances; the ledger must carry those instances WITH their context, not just the discovering case. Corpus is committed and offline-searchable (G3-G23 posteriors, evidence files, states, actuals). C-0714 did this BY HAND for k3 (n=6 spanning g20+g23, 5/6 at <=$200 vs a 30% base rate) - the program is to systematize that. **Why it may matter most: k3_prev is PRE-CUTOFF information, so anything validated this way is blind-legal** - the improvement lands where the walk is weakest **QUALIFICATION (Greg, same session, load-bearing): past evidence is used WHEN AVAILABLE - a finding is NEVER disregarded for lacking it.** The scan ENRICHES, it does not gate. A new mechanism stands on its own at n=1, honestly labelled; the corpus is searched to add instances where they exist. Three states are distinct and each must be recorded as itself: (a) instances FOUND (n rises, context attached), (b) corpus SEARCHED and none found (a real result, and evidence the mechanism is regime-specific or new), (c) NOT YET SEARCHED (an open task, not a verdict). Without this, the program would systematically discard exactly the novel behaviour it exists to catch - anything genuinely new has no past instances by definition (the tropical/hurricane channel has none at all). | OPEN (queued) | nothing built; scoped tomorrow with D23 (shape), which it feeds: k3 is a RATIO against the tape's own recent behavior, i.e. already shape-form, and the retro-scan is the natural test bed for shape-keyed knowledge |
| D25 | S110 (Greg) | **OPEN — Greg fleshes out tomorrow. NOTE ONLY, DO NOT DESIGN YET.** "It matters the ORDER of reasoning the agents use to make their decisions. There has to be a certain order of steps." Today the shared directive and the lens files specify WHAT a specialist must evaluate and largely leave the SEQUENCE to the agent. SUPPORTING INSTANCE FROM TONIGHT (observation, not a proposed design): E-0710 ranked its own three failure causes and the first was an ordering fault - "the burn gate run as a suppressor, which set -150 BEFORE magnitude was derived," i.e. a gate applied ahead of the derivation it was meant to condition, capping a correct read before it existed. E-0717 is the mirror: its counter-fade STAND-DOWN was evaluated before the sign was committed, and that ordering saved the call. Same engine, opposite outcomes, sequence the difference **GREG'S CORRECTION (same session, and it reframes the stage): the specialists DO have options - the option set is DISPOSITION TOWARD THEIR OWN READ: {COMMIT, STAND DOWN / NO CALL, OVERRIDE}.** Claude had argued they only produce an estimate; the instances run Greg's way. OVERRIDE produced the disasters: B-0713 had its own instrument pointing down (D-1 aggregate -1,225 SELL) and went against it on a handed-down verdict table ('not by my override'), the block's worst wrong-direction day; E-0710 let the burn gate cap a correct down-read to -150 against a -660 actual. STAND DOWN produced the saves: E-0717's counter-fade stand-down (firing it would have inverted a correct sign), C-0701's accumulation arm on three measured legs, D-0625's 'already priced' limb. COMMIT produced the wins where the number came from an arithmetic chain (C-0714 k3, C-0707 leave-one-out). CONSEQUENCE FOR THE ORDER SPEC: **the OVERRIDE option must not be reachable before an independent read EXISTS** - B-0713 overrode a read it had never generated, skipping information straight to disposition with nothing to weigh the table against. **AND A STRUCTURAL FINDING: one of the three options is UNAVAILABLE IN OUR MACHINERY.** C-0715 wrote that the correct handling of 0714 was 'NO CALL' and could not emit it - the output contract requires a numeric expected_magnitude_usd and the coordinator hard-fails on missing/non-numeric, so the system FORCES a call on every day, and a forced number is indistinguishable downstream from a confident one. Adding explicit ABSTENTION (scored as abstained, not wrong) may be worth more than any new play. **GREG'S SECOND CONTRIBUTION: DUAL-PROCESS AS THE OVERRIDE GUARD.** Require the FAST read (System 1) to be emitted and written BEFORE the slow work, so an override must state what beat it. B-0713 is the case: it had an instrument pointing down (D-1 aggregate -1,225 SELL) and there is NO independent number anywhere in its blind - the override was invisible because nothing existed to override against. CONSTRAINT THAT MAKES THIS PART OF D25 RATHER THAN SEPARATE: for an LLM the two systems are not cleanly separable (same forward pass), so a fast read taken AFTER the chain inputs are opened is already contaminated - it must be emitted before, which makes ORDER load-bearing in a way it is not for humans; a first impression cannot be retrofitted. EXISTENCE PROOF, SELF-GENERATED: E-0717 did a version of this unasked on the block's only exact call, publishing derived_centre_standalone_usd 170 beside the confirmed +180 so the confirm could be AUDITED rather than taken on faith - cost, one field - and that is what let it separate right-for-the-right-reasons from right-by-cancellation. BONUS: the delta between the pre-chain and post-chain read IS the chain's contribution, which we have only ever inferred (from one G17 incident, ~79% of that miss); a two-stage emission measures it every seam, and a SIGN difference between the two stages is a mechanical cascade detector. | OPEN | nothing implemented; Claude noted only, on instruction. Sequence spec is Greg's design call, then SOP change control (the order would live in mbo_refine_shared.md, which is gold-vaulted - a change there is a versioned vault event, not an edit) |
| D26 | S110 (found by C-0715) | **HARNESS SCORING CONVENTION — OPEN, affects every scored day of the walk.** The render-day is scored **20:00-to-20:00** while the state's own `_information_clock` declares the session closing at **17:00**, so every non-Friday scored day carries the **first two hours of the next exchange session**, and the specialists' stated convention ("17:00 close to 17:00 close") describes a different object than the one scored. MEASURED g20-g23: exactly **0 on 8/8 Fridays** (which identifies the mechanism), and on the 32 non-Friday sessions **21 negative / 9 positive / 2 zero, median -30, sum -1,420**. Normally noise; on 0715 it was **-230, the largest in 40 sessions and the only SIGN FLIP** (in-session +170 at 17:00, scored -60 = 230 of that day's 360 error). C-0715 explicitly refused it as an excuse - the tail was its own session's absorption turn and was its to catch - but it must be **reconciled ONCE at the harness level instead of re-derived by each specialist** | OPEN | nothing changed; scoring convention is a Greg call (it re-defines every historical score) |
| NC-1 | S110 (nonconformance, mine) | **A RUN DIRECTIVE CARRIED A FALSE PREMISE.** The G23 refine spawn for 0715 asserted "first post-roll session"; C-0715 checked `flow_calendar` and found **`in_bcom_roll` TRUE, `bcom_roll_day_n` 5** - the legs are offset (GSCI 0708-0714, BCOM 0709-0715), so 0715 is BCOM roll day 5 of 5 and the first clean session is 0716. The same false premise sat in the day's BLIND posterior, which read only the GSCI leg. Deterministic, never-masked, blind-available field: **operator error, not a data gap** - and I propagated it into the directive. C caught it and corrected the record rather than reasoning on it. Recorded per SOP CHANGE CONTROL item 3 (deviations are nonconformances, never silently absorbed) | RECORDED | fix: SOP slot-fill rule already says CAL_FACTS are mechanical lookups only - this one was asserted from prose instead of looked up. Tighten at close-out: calendar premises in a directive must be quoted from `flow_calendar`, never paraphrased |
| D27 | S110 (Greg spotted, Claude measured) | **WHY THE RENDERED LINES DID NOT CONNECT — root-caused.** Greg: "is that why the lines weren't connecting?" Measured: the coordinator advances the running level by NET (day-move minus gap) but DRAWS each day to its last path point, so a path emitted as cum-from-PRIOR-CLOSE ends one whole gap above where the next day begins. **g22 0/10 days mismatched; g23 8/10, and both Mondays off by exactly their +400 gap** (0706 path-end +500 vs net +100; 0707 -350 vs -310 = its -40 gap). Cause is a SPEC AMBIGUITY, not agent error - the contract did not say whether cum runs from the open or the prior close, and the two groups read it differently. **NO SCORE IS AFFECTED**: scoring reads guess_day_move_usd and never touches the path; G23's 5/10 and sum-abs 5,920 stand as reported - the picture was wrong, not the scoreboard | BUILT (announce) | SOP v1.6 pins cum-from-OPEN with last point == day-move minus gap; coordinator prints a per-day continuity warning. **Left as ANNOUNCE not HARD-FAIL deliberately: hard-failing would invalidate committed artifacts - promoting it to a gate is Greg's call** |
