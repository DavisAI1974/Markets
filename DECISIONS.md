# DECISIONS.md — the binding-decision ledger (append-only)

Born S110 (turnaround memo 2.5). One line per binding decision. **Status vocabulary:**
DECIDED (stated, nothing enforces it yet) -> BUILT (mechanism exists) -> WIRED (mechanism is in the
live path) -> VERIFIED (negative-tested / proven executing) | RETIRED (no longer binding, reason).
**"Enforced by: doc-only" is the dangerous class** — the conformance sweep flags any DECIDED/doc-only
line older than two sessions, because decided-then-dropped-then-relitigated is the disease this file
kills. Never delete a line; supersede it with a new one.

**INSTANCE-INLINE RULE (Greg, S110):** every claim in this file - and in every ledger and handoff - carries its supporting INSTANCE in the same sentence or the one beside it, never in a separate section and never by reference. If a line says a thing happens, the case it happened in is right there. Two reasons: a reader must never have to go hunting to find out whether evidence exists, and a claim whose instance cannot be written next to it is a claim we do not actually have yet. Where the instance is absent, say so in place ("corpus searched, none found" / "not yet searched") per D24.

| # | session | decision (verbatim where possible) | status | enforced by |
|---|---|---|---|---|
| D39 | S114 (Greg, correcting me mid-rehearsal) | **WE TEST ON THE RUNS, NOT AGAINST THEM - SO CONTAMINATION IS NOT A DEFECT, WRONGNESS IS.** Greg, S114, verbatim: *"I don't care if it's look ahead as long as it's right. We're not testing against the runs We're testing on the runs to get better."* The correction landed on me flagging `storage_consensus.next_print.estimates[*].snapshot_utc` forward-stamps on the g22 rehearsal as a 'look-ahead surface' - applying blind-purity logic to a DEVELOPMENT run, where it does not belong. This restates S107's framing (*the walk is a development loop, not a controlled experiment - improve group over group; never hold a group back on a degraded input set to preserve a comparison*) and makes it binding on how a run is JUDGED. **THE OPERATIVE QUESTION FOR A DEVELOPMENT RUN IS 'IS THE VALUE RIGHT', NOT 'WAS IT KNOWABLE AT THE DECISION POINT'.** A value captured after the decision day is fine if it is correct; a value that is WRONG is a defect whether or not it was knowable. **WHAT THIS DOES NOT TOUCH, stated so the scope is not over-read:** (1) a SCORED BLIND measuring skill still needs its decision point respected, because the number it produces is a measurement rather than a development artifact - D2's one deliberate mask and D3's causal slices stand; (2) at the LIVE and PAPER stages the constraint returns in a different form and for a different reason - a play that can only fire on a value which does not exist at decision time cannot trade, which is an operational fact, not a purity rule. **CONSEQUENCE, and it raises the rehearsal's value rather than lowering it:** the g22 rehearsal outputs are worth reading for what the specialists actually concluded, not merely for whether the plumbing turned over. | SCOPED (Greg). Applies to development runs; scored blinds and the live/paper lanes keep their decision points | doc + judgment for now. The mechanical consequence: `build_causal_slices`' forward_stamps notes stay REPORTED-not-fatal (SOP STEP 3.1 already says so) and a development run is not to be held back over them. What DOES warrant a gate is the untested half - whether a forward-stamped value CHANGED between the decision day and its capture, which is a correctness question, not a purity one, and nobody has measured it. Registered as the next thing to check rather than asserted. |
| NC-4 | S114 (nonconformance, mine; caught by git diff one command later) | **I RAN THE BLIND COORDINATOR ON AN ALREADY-REFINED GROUP AND IT OVERWROTE THE IMMUTABLE BLIND RECORD.** To D11-verify a newly wired due-gate I ran `group_coordinate_blind.py g22` - intending only to prove an import executed. After a group's refine, `archive_blind.py` has MOVED the blind posteriors away and the canonical names `grp<N>_mbo_specialist_<X>.json` hold the REFINE posteriors, so the blind coordinator assembled REFINE numbers and rewrote `forecasts/grp22.json` from the true blind (**4/10 dir, sum|err| 5,965**) to the refine's (**10/10, 500**) - a 12x improvement in the permanent record, from a command not meant to write anything. Caught by `git diff`, restored from git; in a dirty tree it would have been invisible. **THIS IS THE S108 FILENAME-COLLISION FAMILY, FOURTH OCCURRENCE**, and it is the exact asymmetry D9 left open: `assert_not_the_blind()` guards a refine posterior identical to the blind archive, and NOTHING guarded the blind coordinator being fed refine posteriors. **AND THE FIRST FIX WAS ALSO WRONG, WHICH IS THE SAME LESSON TWICE:** I guarded on the ARCHIVE'S EXISTENCE, but only g19-g22 have archives - g15, g17, g18 and g23 are refined with none - so the presence check passed on four of eight exposed groups and its own negative test walked straight into overwriting `grp18.json` (also restored). The working guard is a RECONCILIATION against an independent source (S108 hole #8's lesson): assemble first, compare against the committed blind record, refuse to write if any day disagrees. **VINDICATES GREG'S CALL** to rehearse on an accessible block before spending a fresh one - the kink surfaced within minutes, on committed data, at zero cost. | CLOSED (both artifacts restored from git; guard built and negative-tested) | `group_coordinate_blind.assert_not_the_refine(gid, block)` - runs AFTER assembly, compares every day's assembled `guess_day_move_usd` against the committed `grp<N>.json` phase-blind record, HARD FAILS naming the disagreeing days, and says whether the original blind is recoverable from an archive or whether the committed record is the only copy. Negative-tested: fires on g17/g18/g22/g23 (10 days disagree each), silent where the canonical files ARE the blind. **Known scope limit, stated not hidden:** pre-S105 groups (g15 and earlier) keep blind and refine in ONE combined file with no `phase: blind`, so the guard returns early - they have no separate blind record to destroy and no `*_actual.json` on disk, so the coordinator cannot run on them today. **The transferable rule: a read-only-LOOKING verification command can be a write. Before running any pipeline stage to 'just check it imports', ask what it writes.** |
| D38 | S113 (Greg scoped, Claude measured) | **BURN FOLLOWS GENERATION, NOT LOAD - SO NO BA's TURBINES ARE THE SUBJECT.** Greg, verbatim: *"not to get too focused on this systems gas turbines or that ones because even if I don't have gas gen, my neighbor probably has enough for both of us so we need to look at gas demand on the whole for storage purposes and gas gen in hh territory while keeping in mind the neighbors and how much hh territory can import in."* This is the mechanism under D35's stopping rule, and it changes what the per-BA arithmetic is FOR. **A BA with no gas generation still creates gas demand** - it imports the power and somebody else burns the fuel. So a BA's load is NOT a claim on its own gas, and gas burn attributed by demand is attributed to the wrong books. **MEASURED on our own EIA-930 store, 2,689-2,727 days per BA:** net import share (-TI/demand) runs **CISO p50 20.6%, p90 30.7%, MAX 41.7%** (2021-01-02) - between a fifth and nearly half of California's load is served outside CISO, much of it Desert Southwest gas for which we carry no BA at all. MISO p50 5.6% / MAX 15.5%. PJM and SOCO straddle zero and run BOTH WAYS (Greg: *"Power flows both ways in pjm just like every other system. No one is always a net importer"* - PJM exports on 2,691 days and imports on 25). ERCOT is the near-island the DC ties imply (p50 0.1%). **US48 is the ONLY level where the term collapses: p50 0.5%, MAX 2.8%**, and what remains is Canada and Mexico. That is the arithmetic justification for D35's closure rule - US48 is not merely a convenient total, it is **the only boundary at which burn attribution is complete**, because it is the only one whose interchange goes to ~zero. **THE TWO ROLL-UPS ARE DIFFERENT OBJECTS AND MUST NOT SHARE A NUMBER.** (1) STORAGE takes the national total - every molecule counts wherever it burned, so interchange nets out and the per-BA split is pure arithmetic on the way to a sum. (2) HENRY HUB takes gas gen INSIDE HH territory, **bounded by how much power that territory can import** - import capability is the escape valve that lets HH-territory load rise without HH-territory gas burn rising, and it is therefore a CAP on the HH response, not a detail. **CONSEQUENCE, and it is a correction to work already standing:** the per-BA stack subtraction (D35 reason 1) is only a gas-burn statement when the interchange term is carried - load-minus-renewables inside a BA that imports a fifth of its load overstates that BA's gas by roughly the import. `interchange_mwh` is now served for this reason (sign VERIFIED from the accounting identity TI = sum(gen) - demand, NOT from a directional stereotype: positive = net EXPORT; residual/demand p50 0.00% on ERCO/SOCO/SWPP). **AND THE TERM IS A BOUNDARY TERM, SO IT ONLY MATTERS WHERE THE BOUNDARY IS REAL** (Greg: *"So the import/export numbers only really matter in the hh territory"*). Interchange between two BAs inside an aggregate cancels pairwise, so it vanishes from any total that contains both ends - **measured: at US48 net interchange is p50 0.5% of demand, MAX 2.8%**, i.e. already gone. The STORAGE lane therefore needs NO interchange correction: every molecule counts wherever it burned and the national sum of gas generation is the whole answer. The **HH lane is where the term does work**, because HH territory is a SUBSET and its fence carries real flow - imported power is HH-territory load served WITHOUT HH-territory gas, which is exactly the escape valve that decouples HH burn from HH weather. So: national = sum the gas, no interchange; HH = gas gen inside the fence PLUS the fence's own flow. | SCOPED (Greg, sharpened twice in-session). Import shares MEASURED and the US48 cancellation MEASURED. HH territory not yet DELIMITED - A-37 is the blocker on the whole HH lane | A-18 (BA list closes by reconciliation to US48). A-19 (national roll-up - **no interchange term; it nets to zero at US48 and adding one would be double-counting**). **NEW A-36: `interchange_mwh` is carried for the HH-territory boundary, not as a universal per-BA correction** - any aggregate SHORT of US48 that reports gas burn must carry its own fence flow or it is attributing somebody else's generation. **NEW A-37: delimit HH territory and measure its import ceiling** - the HH lane needs a boundary before it can have a number. No per-BA figure is a conclusion (D35) |
| D37 | S113 (Greg, for the tenth time) | **NO AVERAGE MAY BE A VERDICT, AND A REFUTED STORY DOES NOT DEMOTE THE OBSERVATION IT FAILED TO EXPLAIN.** Greg, S113: *"These are all things that we have written over and over in the docs: no averages and because the data is telling us something different doesn't mean it is bad. It means our story was bad and we need to listen to the one it's telling us. We had made a rule that our thoughts are just guesses and that's it."* **TWO RULES, BOTH ALREADY WRITTEN, BOTH RE-DERIVED BY HAND ANYWAY - which is why this line exists as a decision with a mechanism rather than another sentence.** (1) THE AVERAGE RULE has been in CLAUDE.md's trading rules since S80 (*"EACH TRADE INDIVIDUALLY, never average... every aggregate blurs away the per-trade fingerprint that IS the predictive content"*) and was restated as D4 at S108. **MEASURED, all three inside ONE S113 analysis:** a POOLED correlation of storage change against per-BA burn came back **+0.178 for US48 while every constituent seasonal cell was NEGATIVE (-0.685 summer, -0.256 winter)** - pooled, we would have concluded that burning gas FILLS storage; then an **R2 of 0.554 vs a 0.469 benchmark** was reported as promising when the per-week record was **92 improved / 72 worsened, 56%, with the single worst week getting WORSE (55.5 -> 60.0)**; then an **OLS slope - itself an averaged coefficient - was quoted as evidence**. Greg caught all three. (2) THE GUESS RULE: an OBSERVATION and the STORY that explains it are INDEPENDENT objects. **INSTANCE, same session:** the renewables-vs-storage regularity survived every confound check (not the burn level - median burn flat across all seven breadth levels; held inside burn quartiles; **7/7 in the top burn quartile of BOTH seasons**), then TWO proposed mechanisms were refuted by their own tests - gas-backfill (d(wind+solar) failed to reproduce the sort) and weather-extremity (**winter 9 of 21, worse than chance; 2026-01-29 was the coldest week of the set with renewables FALLING** - Arctic highs are calm) - and the observation was then written up as DEMOTED. **That was the error: the evidence was graded by the quality of my explanation of it.** The data did not get worse; the guess did. A refutation is scoped per D31 and the observation stands on its own evidence until its own evidence fails. | WIRED | `research/kalshi/per_event.py` - the rule as a FUNCTION instead of a sentence, because ten restatements did not hold. `report(title, keys, actual, baseline, candidate, explanation=...)` prints the D4 set (sum\|err\|, drift, survival, p50/p90 **and MAX**), an improved/worsened COUNT labelled as a count rather than a rate, **the N largest ACTUAL moves named individually** with each one's error, and the worst remaining events - then the standing line *"A CANDIDATE THAT ORDERS THE QUIET MIDDLE AND MISSES THE LARGEST MOVES HAS NOT HELPED."* **It deliberately returns no scalar**, so there is nothing to quote out of context. `explanation` is printed in its own block flagged as a GUESS whose refutation does not touch the evidence above it. Selftested on a candidate that improves sum\|err\| 93 -> 58 while worsening drift -9 -> -40 and losing 3 of 5 events - the exact shape the rule exists to expose. Also carried in CLAUDE.md's trading rules and `agents/QC_CHECKLIST.md` |
| D1 | S107 (Greg) | Keys do NOT rotate during the walk; rotation happens after, as its own item | DECIDED | doc (CLAUDE.md AWS section + every drop-in) — expires at walk end, then becomes paper-trading item G5 |
| D2 | S102/S105 (Greg) | The blind's ONLY deliberate mask is the PRICE CURVE; anything else missing is a DEFECT | VERIFIED | state_health hard gates + the data doctrine in agents/README.md |
| D3 | S109 | Causality is physics, not a mask: specialists run on per-day causal slices; a second job with an earlier decision point gets its own earlier slice | VERIFIED | build_causal_slices.py self-audit + forward_stamps + SOP STEP 3 |
| D4 | S108 (Greg) | NEVER average above and below; report per-day, sum-abs-err, drift AND survival together | WIRED | blind_score_nonpooled.py + SOP STEP 3.5 |
| D5 | S105 (Greg) | One group at a time; stage all, run one; windows Sunday -> second Friday | WIRED | SOP + group_config |
| D6 | S97 (Greg) | Refined rules NEVER override blind-applicable ones; the blind-vs-refined gap IS the measurement | WIRED | brain play schema (requires/scope/forward_evidence) + merge adjudication |
| D7 | S105 (Greg) | Blind and refine are ONE engine; no blind-specific rule file may exist | VERIFIED | verify_gold runtime-drift wall + the deleted blind stack |
| D8 | S104/S105 | Brain merges are PROPOSAL FILES + adjudication, incumbents byte-identical; never a direct edit | WIRED | merge protocol + backups (ng_brain_*_backup.json) |
| D9 | S108 | Before any refine, the blind archives by MOVE; a refine posterior byte-identical to its blind archive is rejected | VERIFIED | archive_blind.py + assert_not_the_blind (negative-tested on the G21 near-miss) |
| D10 | S110 (Greg) | NOTHING runs off-SOP: gap -> STOP -> propose diff -> Greg go -> version log -> execute; deviations are recorded nonconformances; gates cannot be skipped or softened | WIRED | agents/RUN_SOP.md v1.1 CHANGE CONTROL + this ledger |
| D11 | S110 | No fix is DONE until a test proves the fixed path EXECUTES and the guard fires on the original defect (the f2 lesson: S108's fix was verified on one reader, dead on the other, gate watching the wrong reader for two groups) | DECIDED | SOP v1.2 STEP 2 (this session) — becomes VERIFIED when the first fix ships under it |
| D12 | S110 | A feed enters the decision state only with a NAMED CONSUMER (play, specialist directive, or another feed that reads it) or an explicit PARK note | DECIDED | SOP v1.2 (this session); enforcement mechanism = the consumer map sweep in the QC checklist |
| D13 | S110 (Greg) | Gas only — the platform scopes to NG/KXNATGASD/Henry Hub until further notice | DECIDED | doc-only (memo + this ledger) — acceptable: it is a scope statement, checked by the QC sweep against new non-gas builds |
| D14 | S110 (memo, Greg go) | Pyth collectors (WTI/XAU/XAG) RETIRED — free era ended 2026-07-31, not gas | DECIDED | pending: workflow retirement on the trunk branch (needs a trunk touch; queued) |
| D15 | S110 (memo, Greg go) | The live-orchestrator open item is CLOSED — the sequenced spawn has run five groups without an ordering failure; the S105 escalation clause self-reactivates if waves misbehave | RETIRED | this ledger line supersedes the S105/S108 open-list entries |
| D16 | S110 (memo, Greg go) | G21 round 2: CLOSED as not-run; the r1 result stands on its own record | RETIRED | this ledger line supersedes the S109 open-list entry |
| D17 | S109 (Greg) | Audit and forecast are SEPARATE roles; the auditor reads the whole block, emits no numbers, and runs BEFORE every blind | WIRED | agents/state_auditor.md + SOP STEP 1 |
| D18 | S110 (Greg) | Paper trading go-plan approved; Greg provisions the Kalshi demo account/keys (G0); dock builds run in parallel while the walk finishes G22/G23 | DECIDED | memo Part 3 + task list — G0 in flight with Greg |
| D19 | S106/S103 (Greg) | Scoreboard = forward-curve error per day, never daily hit-rate alone; P&L framing per-event only | WIRED | blind_score_nonpooled + render rules |
| D20 | S98 (Greg) | The futures->Kalshi LAG is ESTABLISHED (S80/S81/S91); never retest it — live telemetry only | VERIFIED | gate doc section 0c + kalshi_fill_model/lag_execution_map headers refuse relitigation |
| D21 | S110 (Greg) | Reasoning must be TIED to the decision it produced - "I don't want a context file out there that isn't tied to the decision it was used to make." Answer + explanation co-located; ledgers carry a machine-checked DECISION CLAIMS table | BUILT | decision_trace.py (decision_id = sha256(group\|date\|owner\|number)[:12]; `verify` fails any unresolved id and names UNDECLARED unbound ledgers); SOP v1.4 close-out |
| D22 | S110 | The LEDGER IS NOT A SECOND BRAIN. Lessons reach the BLIND only through the adjudicated brain (proposal -> adjudication -> merge). Ledgers are for humans, refines and post-mortems. A same-group ledger is a look-ahead leak; a prior-group ledger is un-adjudicated narrative that would anchor independent derivation | DECIDED | SOP v1.4 (spawn templates cite the brain, never ledgers); enforced by the blind's read-list being explicit in BLD-1/BLD-2 |
| D23 | S110 (Greg) | **OPEN — DESIGN DECISION PENDING, DO NOT IMPLEMENT.** "We can only pay attention to SHAPE for forecasting purposes. We can be hyper-focused on the value in order to match it to real-world data, but any time you use it after that, the value has to be replaced." Flagged on seeing decision_id bind to an exact number: value-keyed knowledge is good for one instance and discarded in all others — "we're basically starting from scratch every time." MEASURED EVIDENCE (S110 audit): 24 of 76 brain plays carry an absolute bar in trigger/read/call; the plays that FAILED this session are value-keyed ones (divergence_resolution HDD>=16.4 unreachable in summer; shoulder_weather_band_void <=13.5 trivially satisfied; the 0.547 and 0.55 big-print bars). Greg: think it through BEFORE replacing the value mechanism | OPEN | nothing implemented; Claude STOPPED on instruction. Next step is Greg's design call, not a build |
| D24 | S110 (Greg) | **RETRO-INSTANCE PROGRAM — queued, high priority.** "When we find stuff like this, it needs to be included with the context too. It needs to have PAST INSTANCES to fall back on as evidence... we are going to have to do a lot of investigating of past runs to get the context to update that file. This might be what gets some serious improvement for blind." A newly-discovered mechanism (e.g. the k3 impact-coefficient collapse, C-0714) is n=1 until the historical corpus is searched for its instances; the ledger must carry those instances WITH their context, not just the discovering case. Corpus is committed and offline-searchable (G3-G23 posteriors, evidence files, states, actuals). C-0714 did this BY HAND for k3 (n=6 spanning g20+g23, 5/6 at <=$200 vs a 30% base rate) - the program is to systematize that. **Why it may matter most: k3_prev is PRE-CUTOFF information, so anything validated this way is blind-legal** - the improvement lands where the walk is weakest **QUALIFICATION (Greg, same session, load-bearing): past evidence is used WHEN AVAILABLE - a finding is NEVER disregarded for lacking it.** The scan ENRICHES, it does not gate. A new mechanism stands on its own at n=1, honestly labelled; the corpus is searched to add instances where they exist. Three states are distinct and each must be recorded as itself: (a) instances FOUND (n rises, context attached), (b) corpus SEARCHED and none found (a real result, and evidence the mechanism is regime-specific or new), (c) NOT YET SEARCHED (an open task, not a verdict). Without this, the program would systematically discard exactly the novel behaviour it exists to catch - anything genuinely new has no past instances by definition (the tropical/hurricane channel has none at all). | OPEN (queued) | nothing built; scoped tomorrow with D23 (shape), which it feeds: k3 is a RATIO against the tape's own recent behavior, i.e. already shape-form, and the retro-scan is the natural test bed for shape-keyed knowledge |
| D25 | S110 (Greg) | **OPEN — Greg fleshes out tomorrow. NOTE ONLY, DO NOT DESIGN YET.** "It matters the ORDER of reasoning the agents use to make their decisions. There has to be a certain order of steps." Today the shared directive and the lens files specify WHAT a specialist must evaluate and largely leave the SEQUENCE to the agent. SUPPORTING INSTANCE FROM TONIGHT (observation, not a proposed design): E-0710 ranked its own three failure causes and the first was an ordering fault - "the burn gate run as a suppressor, which set -150 BEFORE magnitude was derived," i.e. a gate applied ahead of the derivation it was meant to condition, capping a correct read before it existed. E-0717 is the mirror: its counter-fade STAND-DOWN was evaluated before the sign was committed, and that ordering saved the call. Same engine, opposite outcomes, sequence the difference **GREG'S CORRECTION (same session, and it reframes the stage): the specialists DO have options - the option set is DISPOSITION TOWARD THEIR OWN READ: {COMMIT, STAND DOWN / NO CALL, OVERRIDE}.** Claude had argued they only produce an estimate; the instances run Greg's way. OVERRIDE produced the disasters: B-0713 had its own instrument pointing down (D-1 aggregate -1,225 SELL) and went against it on a handed-down verdict table ('not by my override'), the block's worst wrong-direction day; E-0710 let the burn gate cap a correct down-read to -150 against a -660 actual. STAND DOWN produced the saves: E-0717's counter-fade stand-down (firing it would have inverted a correct sign), C-0701's accumulation arm on three measured legs, D-0625's 'already priced' limb. COMMIT produced the wins where the number came from an arithmetic chain (C-0714 k3, C-0707 leave-one-out). CONSEQUENCE FOR THE ORDER SPEC: **the OVERRIDE option must not be reachable before an independent read EXISTS** - B-0713 overrode a read it had never generated, skipping information straight to disposition with nothing to weigh the table against. **AND A STRUCTURAL FINDING: one of the three options is UNAVAILABLE IN OUR MACHINERY.** C-0715 wrote that the correct handling of 0714 was 'NO CALL' and could not emit it - the output contract requires a numeric expected_magnitude_usd and the coordinator hard-fails on missing/non-numeric, so the system FORCES a call on every day, and a forced number is indistinguishable downstream from a confident one. Adding explicit ABSTENTION (scored as abstained, not wrong) may be worth more than any new play. **GREG'S SECOND CONTRIBUTION: DUAL-PROCESS AS THE OVERRIDE GUARD.** Require the FAST read (System 1) to be emitted and written BEFORE the slow work, so an override must state what beat it. B-0713 is the case: it had an instrument pointing down (D-1 aggregate -1,225 SELL) and there is NO independent number anywhere in its blind - the override was invisible because nothing existed to override against. CONSTRAINT THAT MAKES THIS PART OF D25 RATHER THAN SEPARATE: for an LLM the two systems are not cleanly separable (same forward pass), so a fast read taken AFTER the chain inputs are opened is already contaminated - it must be emitted before, which makes ORDER load-bearing in a way it is not for humans; a first impression cannot be retrofitted. EXISTENCE PROOF, SELF-GENERATED: E-0717 did a version of this unasked on the block's only exact call, publishing derived_centre_standalone_usd 170 beside the confirmed +180 so the confirm could be AUDITED rather than taken on faith - cost, one field - and that is what let it separate right-for-the-right-reasons from right-by-cancellation. BONUS: the delta between the pre-chain and post-chain read IS the chain's contribution, which we have only ever inferred (from one G17 incident, ~79% of that miss); a two-stage emission measures it every seam, and a SIGN difference between the two stages is a mechanical cascade detector. | OPEN | nothing implemented; Claude noted only, on instruction. Sequence spec is Greg's design call, then SOP change control (the order would live in mbo_refine_shared.md, which is gold-vaulted - a change there is a versioned vault event, not an edit) |
| D26 | S110 (found by C-0715) | **HARNESS SCORING CONVENTION — OPEN, affects every scored day of the walk.** The render-day is scored **20:00-to-20:00** while the state's own `_information_clock` declares the session closing at **17:00**, so every non-Friday scored day carries the **first two hours of the next exchange session**, and the specialists' stated convention ("17:00 close to 17:00 close") describes a different object than the one scored. MEASURED g20-g23: exactly **0 on 8/8 Fridays** (which identifies the mechanism), and on the 32 non-Friday sessions **21 negative / 9 positive / 2 zero, median -30, sum -1,420**. Normally noise; on 0715 it was **-230, the largest in 40 sessions and the only SIGN FLIP** (in-session +170 at 17:00, scored -60 = 230 of that day's 360 error). C-0715 explicitly refused it as an excuse - the tail was its own session's absorption turn and was its to catch - but it must be **reconciled ONCE at the harness level instead of re-derived by each specialist** | OPEN | nothing changed; scoring convention is a Greg call (it re-defines every historical score) |
| NC-3 | S113 (nonconformance, mine; found by the SessionStart hook one turn later) | **I REPORTED A GUARD 'NEGATIVE-TESTED BOTH DIRECTIONS, PASS/PASS' WHEN ITS FIRING BRANCH HAD NEVER EXECUTED.** The guard is the S113 do-not-destroy check in `restore_substrate.py`, written because a rebuilt grid_stack store had been silently reverted to an older S3 copy (Greg: *"Fix that problem asap"*). I said it passed both directions. On the very next run the SessionStart hook raised **`NameError: name 'ROOT' is not defined`** from inside the KEEP branch's log line - the branch I had just claimed to have tested. `ROOT` does not exist in that module; the constant is `REPO`. So the test I ran checked that the code DECIDED correctly (compare timestamps, choose keep-or-replace) and never executed the statement that reports the decision. **THIS IS D11 VERBATIM** - *a fix is not done until the fixed path EXECUTES and the guard FIRES* - and it is the THIRD recorded instance of the same shape on this desk (S110 recorded two: verifying code PARSED rather than RAN, twice). It is also the exact defect class the guard itself exists to catch: something present, well-formed and plausible that has never actually run. **AND IT WAS COSTLY, not cosmetic** - the NameError aborted the whole restore partway, so `storage`, `stor_surprise`, `weather` and `vol_regime` all came back EMPTY on 10/10 days and `state_health` reported five HARD failures that looked like missing stores. A guard written to prevent silent data loss took the data plane down instead. **CLOSED PROPERLY:** `ROOT` -> `REPO`, then the branch was made to EXECUTE against a real newer-than-S3 local file and print - *KEPT LOCAL (newer than S3): data/grid_stack/grid_stack.json.gz - local 2026-08-05 18:03Z vs S3 2026-07-20 14:24Z. NOT overwritten.* Then the rebuilt store was PUSHED to S3, because the guard only stops the store being destroyed; it does not stop it dying with the container. | CLOSED (fixed, executed, and the branch's output witnessed) | D11. **The transferable rule, which the QC checklist should carry: a test that never produced the guard's OUTPUT did not test the guard.** Asserting the return value or the chosen branch is not sufficient - the reporting statement is part of the path and is where this failed |
| NC-1 | S110 (nonconformance, mine) | **A RUN DIRECTIVE CARRIED A FALSE PREMISE.** The G23 refine spawn for 0715 asserted "first post-roll session"; C-0715 checked `flow_calendar` and found **`in_bcom_roll` TRUE, `bcom_roll_day_n` 5** - the legs are offset (GSCI 0708-0714, BCOM 0709-0715), so 0715 is BCOM roll day 5 of 5 and the first clean session is 0716. The same false premise sat in the day's BLIND posterior, which read only the GSCI leg. Deterministic, never-masked, blind-available field: **operator error, not a data gap** - and I propagated it into the directive. C caught it and corrected the record rather than reasoning on it. Recorded per SOP CHANGE CONTROL item 3 (deviations are nonconformances, never silently absorbed) | RECORDED | fix: SOP slot-fill rule already says CAL_FACTS are mechanical lookups only - this one was asserted from prose instead of looked up. Tighten at close-out: calendar premises in a directive must be quoted from `flow_calendar`, never paraphrased |
| NC-2 | S111 (nonconformance, mine; found by S112) | **I POINTED THE NEXT SESSION AT A FILE THAT DIES WITH THE SESSION.** The S111 82-play audit ran as a workflow whose script lived under `~/.claude/projects/.../workflows/scripts/`, i.e. session scratchpad, and `DROP_IN_S112` item 5a pointed S112 at that path. S112 checked - `git ls-files | grep brain-play-audit` returns **ZERO** - and correctly identified it as another **A-7 instance: the machine that did the work living outside the store**, written in the very session that diagnosed A-7. It also found two committed drop-in items already done (`sections --write`, the `condition_audit` fix) and the andon's `data-plane` row PASSing on a non-empty `data/` while the SessionStart hook printed NG DATA PLANE NOT RESTORED - both true, because the hook had materialized crypto realbins: present, non-empty, right owner, wrong content, the exact family this desk hunts. **All four fixed in S111 after the report**: `research/kalshi/brain_audit.py` committed (prompts GENERATED BY LOOKUP from the brain, validator negative-tested to reject unknown play ids, bad enums, found-with-no-instances and incomplete batches); the drop-in destaled and repointed; the andon row now checks for NG stores BY NAME rather than for any bytes; BOX 1 now tests that the harness and registry exist. Recorded per SOP change control item 3 - deviations are nonconformances, never silently absorbed | RECORDED | the transferable rule, and it is A-7 restated: **anything the next session is told to run must be in git.** A path under `~/.claude` is scratchpad and does not exist on a fresh container |
| D36 | S112 (Greg asked the question that found it) | **A RESEARCH BRIEFING'S NUMBERED RECOMMENDATIONS BECOME REGISTRY LINES IN THE SESSION THE BRIEFING LANDS.** Greg asked what looked like a bookkeeping question - *"your 13 build suggestions from last session are still listed right?"* - and the audit came back **12 of 13 NOT TRACKED**. The S111 synthesis's section 3 ranked thirteen under-used data sources; the G-series items captured a few HEADLINES (G-10 the tape, G-1 the NGWU risk, G-3 COT level-to-change) and none of the specific sub-recommendations, which are the actionable content. **Untracked included a live WRONG-SIGN risk** (the tropical feed's sign inverted post-shale, and we built and smoke-tested that feed at S110), **a leakage question** (vol_regime's smoothed state probability is computed with hindsight; the filtered state is what a live system had), and **a one-directional growing bias** (EIA-930 excludes behind-the-meter solar entirely, in exactly the daylight hours that matter for cooling). **THE MECHANISM, and it is A-17 one level up.** A research briefing is a RECORD - dated, frozen, correctly never edited. Its recommendations therefore live in prose that nothing counts. `plant_status` could say '48 open items'; it could not say 'and twelve recommendations belonging to no item'. The registry catches items that were ENTERED, and cannot catch a finding that was never entered - exactly the failure A-17 recorded for a commitment agreed twice in conversation. **THE RULE:** when a briefing, memo or external hand-off lands, every numbered recommendation in it gets a registry id **before the session ends**, even if the verdict is 'do not do this' - a rejected recommendation with a recorded reason is tracked; an unrecorded one comes back as a surprise. Registered this session as G-16 through G-28. | RULE (applied retroactively to the S111 synthesis) | OPEN_ITEMS.json; and the same discipline already carried by A-9 (drop-in work list generated from the registry) and `chatgpt_handoff.delegated_prior` (a brief cannot re-ask a delivered question). **All three are the same defect: a document that states work, sitting apart from the list that counts it** |
| D35 | S112 (Greg scoped, Claude measured the qualifier) | **THE TARGET IS TOTAL GAS DEMAND, NOT ATTRIBUTION.** Greg, verbatim: *"Because we are trying to find overall gas demand. Don't care who is burning it or where."* So NO per-BA number is ever a deliverable - no per-BA scoreboard, no play that concludes "SOCO drove it". Per-BA is ARITHMETIC, not a goal. **AND IT IS ARITHMETIC WE CANNOT SKIP, for two reasons.** (1) The stack is a per-BA subtraction: gas is load minus wind minus solar minus hydro minus nuclear minus coal, and that only means anything inside the unit that actually balances - ERCOT's wind does not serve PJM's load, so a national "load minus wind" is not a quantity. (2) The response is CONVEX, so the total is not recoverable from the average: a national CDD of 10 is uniform mildness (low burn) or Texas at 20 against the Northwest at 0 (high burn). Greg's own cancellation rule proves it - Charlotte hot and Raleigh rainy cancel *to some degree*, and it is the incompleteness that forces you to compute the pieces and sum them. **THE STOPPING RULE THIS GIVES US, which we did not have:** add BAs until the served set reconciles to US48 within tolerance, then STOP. US48 is carried as its own EIA-930 respondent, so the ground truth is already in hand and the gap is computable today. Not "cover the map" - "close the sum". **THE QUALIFIER, from Greg's own next question ("what does HH really aggregate - is it a generic national price?") - MEASURED, and the answer is NO.** Henry Hub is ONE PHYSICAL POINT: piping owned/leased by Sabine Pipe Line LLC near **Erath, Louisiana**, about nine interstate plus three-to-four intrastate interconnects, with NYMEX NG physically deliverable FOB at the buyer's interconnect. It is not an index of many points. Every other region prices at a BASIS DIFFERENTIAL reflecting local conditions, transport cost and available transmission capacity. **So the target is not "national demand" - it is DEMAND HENRY HUB CAN SEE.** New England load can spike Algonquin with little HH response because the binding constraint is pipe into the region, not gas at Henry; Gulf-coast LNG feedgas is the most HH-coupled demand there is. **Banked, not assumed: the Southeast BAs this session has been arguing for sit on the Gulf corridor and are among the better-transmitting ones, so that work is well-aimed at the target rather than incidental to it.** **CONSEQUENCE FOR THE ROLL-UP (A-19):** weight the national number by gas burn **x HH transmission**, not by burn alone - and MEASURE the transmission by regressing HH day-moves on per-BA burn changes rather than asserting it from a pipeline map. | SCOPED (Greg). HH qualifier MEASURED. Transmission weights NOT yet measured | A-19 (per-BA degree days, national roll-up weighted by measured burn x HH transmission), A-18 (BA list closed by reconciliation to US48, not by map coverage), A-20 (the hydro carry reports a Southeast gas-fill total, never a per-BA attribution). **No per-BA figure may be reported as a conclusion** |
| D34 | S112 (Greg) | **THERE IS NOTHING LOCAL. EVERYTHING LIVES IN GIT OR S3, AND NO ARTIFACT MAY CARRY A PATH FROM ANYONE'S DESKTOP.** Greg, S112, in three statements that tighten one rule: *"all of this stuff is going to live in aws or git so there should be no paths from my desktop"*, *"there should be nothing local"*, and *"right now everything gets pushed to git. zero to e drive."* **THIS GENERALISES D33 FROM EXECUTABLES TO EVIDENCE.** D33 said the machine that does the work must live in the store; D34 says the same of every path any artifact NAMES. The split is unchanged and is the whole rule: **git = code and records, S3 = data, `data/` is disposable and rebuilt by `restore_substrate.py`** - and a path that is neither is a path only one machine can open. **MEASURED INSTANCE, and it is why this is a decision line rather than a preference:** `BRAIN_AUDIT_PARTIAL_S111.json` carries 140 instances, and **51 of them - across 11 distinct plays - cite `E:/Markets/research/kalshi/...`**, an absolute path on Greg's box. The files they name are real and committed; the CITATIONS are unopenable by anyone else, including by the next session on a fresh container. That partial is the declared input to S111-3, the backfill, which writes instances **into the brain** - so without this rule 51 desktop paths become permanent evidence in the one artifact every specialist loads. **THE FAILURE MODE IS THE DESK'S SIGNATURE, TWELFTH INSTANCE:** present, well-formed, plausibly-shaped, right owner, and unresolvable - the same shape as holes #7, #8 and #9, and it passed the committed validator with **zero errors** until S112 measured it. **The tempting repair was the wrong one and was rejected on Greg's call:** the first version of the guard silently re-rooted `E:/Markets/...` to the repo, on the reasoning that the evidence exists and only the prefix is non-portable. That hides the defect instead of fixing it, exactly as a prose rule hid NC-2. A desktop path now FAILS with a message naming the fix | WIRED | `brain_audit.py` `_is_machine_path()` - a drive letter, a leading `/` or a leading `~` in any instance `source_file` is a hard validation error reading "DESKTOP PATH - cite repo-relative, everything lives in git or S3". **D11-verified in both directions, 14/14 negative tests via `brain_audit.py selftest`:** the guard fires on `E:/Markets/<a real committed file>` (proving it catches the path form and not merely a missing file), and does NOT fire on the repo-relative citation of the same file. **AND IT WAS CALIBRATED AGAINST REAL CITATIONS BEFORE IT SHIPPED, WHICH CHANGED IT TWICE:** the first draft required one literal path and would have failed **93 of the 140** real instances, because auditors legitimately cite `fileA + fileB`, `g{18,20,22}_actual.json`, `g17..g23_actual.json`, a bare directory, and `grp22_C_20260623.json, _20260630.json`. A guard that always shows red is one people learn to ignore (D33's own lesson), so each of those forms is now parsed and each has its own negative test. Final state on the S111 partial: **89 clean, 51 desktop-path, 0 unresolved.** Open consequence, carried as registry item S111-3's precondition: **those 51 citations must be rewritten repo-relative before the backfill consumes them** |
| D33 | S111 (Greg) | **NOTHING THE NEXT SESSION MUST RUN MAY LIVE OUTSIDE GIT.** Greg, after it happened twice: *"fix in whatever doc you need to that things don't go on scratchpads anymore. this was in the audit file!"* A path under `~/.claude/projects/`, `workflows/scripts/`, `AppData/Local/Temp` or `/tmp` is session scratchpad - it does not exist on a fresh container and dies with the session. **INSTANCE (NC-2):** the S111 drop-in pointed S112 at an audit harness under `~/.claude/projects/.../workflows/scripts/`; S112 ran `git ls-files`, got ZERO, and had to re-author it - and this was written in the very session that diagnosed the disease as A-7, which is why a sentence was never going to be enough. **This is the generalisation of A-7 from spawn templates to everything: the machine that does the work must live in the store.** | WIRED | `plant_status.py` scratchpad gate: scans the NEWEST drop-in, NEWEST handoff and `RUN_SOP.md` for scratchpad markers and for referenced repo paths not tracked by `git ls-files`, and **FAILS** on either, which exits non-zero and stops the line. **D11-verified BOTH ways:** PASS with exit 0 on the clean tree; FAIL with exit 1 naming all three bad references when NC-2 is reintroduced verbatim. Deliberately scoped to the LIVE instructions only - historical drop-ins are records, not instructions, and a gate that always shows red is one people learn to ignore (DROP_IN_S104 legitimately references `agents/blind_shared.md`, deleted by design in S105). Also `RUN_SOP.md` v1.7 change-control item 5. **AND THE GUARD ITSELF FAILED TWICE BEFORE IT HELD, which is the D11 lesson in miniature and is recorded rather than tidied away.** (a) The first marker test was a regex character class containing a backslash; shell escaping mangled it, it compiled WITHOUT ERROR and silently matched nothing - it "passed" its negative test by failing to fire at all. Rewritten as plain substring matching on a slash-normalized lowercased line, which cannot break that way. (b) The rewritten guard then FAILED ON THE SOP ITSELF the moment the rule was written, because the rule text NAMES the forbidden markers as examples and a substring check cannot tell "do X" from "never do X". `RUN_SOP.md` is now exempt from the MARKER scan only - it is the file that defines the prohibition, so it must be able to name what it prohibits - and remains subject to the untracked-path scan, which is verified separately by planting an untracked reference in it. Three negative tests now pass: scratchpad marker in the drop-in, untracked path in the drop-in, untracked path in the SOP |
| D32 | S111 (Greg, across the whole session) | **THE FORECAST ARCHITECTURE, stated so it is not relitigated.** The product is a **PRICE CURVE** (trader's noun - the pricing curve we have been trying to match; do not read it as a claim about smoothness). The machine is **ANALOG RETRIEVAL**: project market conditions forward, reason to how the session should BEHAVE, then find a past trading day whose SHAPE matches the behaviour already concluded, and re-anchor its level. **The analog does not do the forecasting - it renders it**, supplying real traded intraday texture instead of a smooth guess; Greg: *"we're always going to have to be reconstructing no matter what."* **Library key = a two-week calendar window** (day length and weather forecasts are the two biggest drivers; a ±2wk window holds day length near-constant by construction, and `solar.gw_day_length_h` is already served). **The only free parameter in the transplant is LEVEL.** Amplitude is a **REJECTION TEST, not a knob** - Greg: *"if you are also adjusting amplitude more than just a little then you should be using a different forecast... a little knob turning is fine but if you're doing it too much then you're no longer forecasting"* - which is the refine's own "magnitudes DERIVED, never fitted" rule applied one step later, and it makes the retrieve-vs-construct boundary EMERGENT rather than legislated (retrieve when a candidate passes; when nothing passes, that failure IS the signal to construct). **Live = the refine running forward**: refine-grade information up to now, the analog supplying the rest, with the tape confirming continuously - so intraday is NOT a blind problem, though the pre-open entry and any multi-day horizon still are. **The scrap signal is SLOPE, not level** - Greg: *"level movement keeps the same linear equation"*; differencing removes the level automatically, and the principle underneath is that **level errors are correctable and slope errors are not**. Adjust once, adjust again and you are on notice, keep adjusting and the analog is wrong; direction disagreement short-circuits to immediate scrap. **Checks are triggered by the TAPE, not the clock** (our `path_p50_curve` 2-hourly grid is a human summary, not what the monitor consumes - the retrieved analog's own tick tape is the reference). **THE ADJUSTMENT LOOP IS THE PRODUCT, not a safety net** - Greg: *"no trader has this information, you just have to adjust faster and better than everyone else."* **One object, three lanes, diverging as LATE as possible** (shared: data plane, condition projection, behaviour reasoning, library, retrieved analog; divergent: how each product reads it): Kalshi wants a probability at a point, futures a trajectory, options a distribution over trajectories - and per S98 the ledgers are never pooled, so a good number in one lane is not evidence in another. **Dispersion comes free and resolves the pick-one-vs-distribution tension**: one analog for the central path, the cohort that PASSED the match test but was not chosen for the band - nothing averaged. **Accuracy bar (Greg): direction of each leg must be right, turn timing close, peaks and valleys a little wrong is tolerable, level free.** That is a scoring function, and it is the one we do not compute | DECIDED | nothing built. Written here because it is the session's main output and existed only in conversation. Next: a design document, then the SOP/contract changes it implies (curve scored instead of the scalar; monitor spec; NO CALL) |
| D31 | S111 (Greg) | **NOTHING WE DECLARED DEAD IS ACTUALLY DEAD - a refutation is SCOPED to the cell and the instrument it was measured on, never converted into "dead".** Greg: *"we should bring it back and not consider anything that we said was dead as actually dead."* This is his own per-cell doctrine applied to refutations: *a signal that survives on a SUBSET of cells is kept and used on those cells; partial coverage is not failure; report "works on X, not Y", never "X failed"* - and by that rule `weather.burn_conversion_gate` should have been SCOPED to summer injection-season days, not retired. **A specific reason it deserves reinstatement arrived the same session:** the load-bearing kill argument was D-0709's constant limb (`d_gw_cdd` positive 20/20), but gas-weighted degree-day weights are **heating-derived and the vendor methodology says plainly they are wrong in summer** - we measured a summer CDD limb on a winter-weighted index, so a limb that never changed sign may be a property of a mis-specified instrument rather than of the market. **The decisive test is cheap and already on the build list:** rebuild the weights with a seasonal switch to power-weighted CDD from the EIA-930 BA gas generation we already ingest, then re-measure whether the limb still runs 20/20. **THE GUARD AGAINST THE OPPOSITE DITCH:** "nothing is dead" must not mean "everything is alive", which makes the brain unfalsifiable - the flaw A found in S106 when covering-self-limiting pointed DOWN whether it fired or not. A refutation stays in the brain **with its refuting evidence AND its scope**, and is re-tested when either the cell or the instrument changes. **Others needing the same re-reading, each killed on one corpus in one regime:** the S98 C2 ratio reformulation; `l1_book.quote_bid_share` divergence (C-0715, "null" at 9 fires 4 hits - and the diagnosed cause was **aggregation destroying it**, an instrument fault not a signal fault); the forward-CDD-slope rescue; `flow.price_free_absorption_proxy` centroids; the Monday 06:00-10:00 ratification arm (0-for-2); the S107 pre-print timing limb retracted in s103.6; and the dipole directional map at `DEPLOY_VALIDATED=False`, where divergence and exhaustion were never the thing that failed | DECIDED | nothing implemented. The retirement class itself needs redefining before the brain is touched, and that is a versioned change. **Related: `NOVEL_N1` added to the support enum in `brain_schema.py` on Greg's "some things won't have past instances because it was the first time we saw them but that doesn't make them bad"** - an argued mechanism seen once is not the same object as a claim with no argument, per D24's qualification |
| D30 | S111 (mine, from a measured drop) | **A FINDING WITH NO HOME IN THIS FILE DOES NOT EXIST. Any memo or audit finding marked FIX becomes a DECISIONS line with a status, or it is not a finding.** INSTANCE, and it is the memo that created this file: `TURNAROUND_MEMO_S110.md` section 1.3 carried four items. The two marked **CLOSE** became D15 and D16 and are properly retired. The two marked **FIX** never became decision lines - *"Brain status taxonomy drift (FIX, S): 68 plays carry ten different free-text statuses including one 40-word sentence"* and *"Two schema dialects (FIX, S): the blind coordinator reads `guessed_net_usd`, the refine coordinator reads `expected_magnitude_usd`"*. **Neither was done.** By S111 the brain carried **twelve** statuses across 82 plays including that exact 40-word sentence, and the two magnitude dialects are still live - the S111 shrinkage query needed a fallback across both names to read our own forecasts. Nothing enforced them, nothing flagged them overdue, and the conformance sweep does not look at the brain. **So the disease this file was built to kill happened to the memo that created it, inside the same session** | WIRED | **ENFORCED S111**: `research/kalshi/OPEN_ITEMS.json` is the tracked work registry and `plant_status.py` prints it at EVERY bring-up - open count, items carried two or more sessions, and the quickest-first list - so an open item cannot quietly stop existing. It immediately surfaced **A-6 (test the dipole exhaustion arm on gas) open since S90, twenty-one sessions**, and the seven S110 memo items that were never done. The andon board also names `FORECAST_ARCHITECTURE_S111.md` on its own row, because Greg asked how we stop the architecture doc being overlooked and the honest answer is that hoping someone reads it is the failure this board exists to catch. Still open: a brain-schema item in `QC_CHECKLIST.md` (registry item M-7) |
| D29 | S111 (Greg: "there should be some schema that everything in the brain is organized into and is searchable") | **THE BRAIN HAS A SCHEMA.** Measured before: 82 plays carrying **97 distinct field names**, most appearing exactly once (`s101_6_0223_fade_vs_ratify`, `s102_1_unowned_premium_full_fade`, `mis_scaled_not_refuted_tails_only_s110`), with even the core field fragmented - `forward_evidence` 77 times plus `forward_evidence_g20`, `forward_evidence_g21_leg_vs_day`, `g15_forward_evidence`, `g16_forward_evidence` as four separate keys holding the same kind of thing. **Consequence: nothing could be asked of the brain as a whole, and every audit in S111 had to be a bespoke regex.** After: **24 field names, 7 statuses.** **THE GOVERNING RULE: every field must be queryable across ALL plays; a session recording something new puts it in a TYPED SLOT with a session tag inside it, never in a new field name.** Slots: `scope`, `conditions[]` (quantity, state_path, reference, comparator, threshold, units), `support`, `mechanism`, `falsifier`, `instances[]` (each traced to a posterior or an actual - date, group, source_file), `corpus` (d24_state + **searched_on**, because searched-none decays as the corpus grows), `forward[]`, `provenance`, `health`. **`conditions[].state_path` is the slot doing the most work**: it makes the D28 degeneracy check run automatically at every merge instead of by hand-curated registry, which would have caught the burn gate's constant limb before the merge rather than on a forward test a session later. **LOSSLESS BY CONSTRUCTION** - nothing deleted, 367 ad-hoc keys preserved verbatim under `legacy_notes`, and `--write` refuses unless a round-trip check proves every leaf value still reachable (1,652 distinct leaves before, **0 lost**). **`conditions[]` is populated ONLY from a hand-curated map, 8 of 82** - regex-parsing prose triggers is the fuzzy-matching error that produced holes #8 and #9, so the other 74 are marked `unparsed`, an open task and never a guess. `DESCRIPTOR` added to the status enum on Greg's call: it is a real category, not sloppiness - `structure.squeeze_unwind`'s own `call` field reads *"DESCRIPTOR grade - regime context, not a scored play"* | BUILT | `brain_schema.py` (validate / migrate / report; dry-run default, backup `ng_brain_s105.0_preschema_backup.json` written before any write). **The dry run stopped itself twice, which is what it is for**: once on a real losslessness bug I introduced by paraphrasing status strings instead of preserving them, once on the unmapped `DESCRIPTOR` per the SOP rule that a gap stops the line. **The work list is now visible instead of invisible: 82 of 82 unaudited for support, 82 of 82 corpus-unsearched, 74 conditions unparsed, 65 with no falsifier** |
| D28.1 | S111 (correction to D28, same session) | **CORRECTION TO D28's WORDING, stated not absorbed.** D28 and its commit message carried the sentence that `gw_hdd >= 16.4` "never discriminates INSIDE a block". **That is FALSE and `condition_audit.py`'s own output contradicts it**: the bar is degenerate in 7 of 14 blocks and DOES split in the other seven (g7 2/10, g8 4/10, g9 19/20, g10 9/11, g13 10/12, g14 4/12, g15 3/12). The true shape is worse than the false claim and more useful: **degenerate at BOTH extremes** - always-on in deep winter, always-off in summer - **discriminating only in the SHOULDER**, i.e. exactly the season the S102 salience slider says weather matters least. The D28 conclusion is unaffected; only its supporting sentence was wrong. Recorded as its own line rather than edited into D28, because this file is append-only and a silently corrected claim is indistinguishable from a claim that was always right | RECORDED | corrected at source in `condition_audit.py` with the correction stated inline |
| D28 | S111 (measured against D23, on Greg's "do your fixes") | **THE D23 DISEASE IS NOT VALUE-VS-SHAPE. IT IS CENTRAL-VS-EXTREME - and there are TWO diseases, which we had been conflating.** Built `condition_audit.py` (report-only, exits 0 always, writes nothing) to ask of every brain condition the question we had never asked: **can it change state inside the block a specialist is actually standing in?** The measure is the **DEGENERATE-BLOCK SHARE** - of the blocks where a condition was evaluable, in how many did it return the same answer on every single day - **not the pooled fire rate, which is a cancellation artifact in exactly the shape D4 forbids, applied to a trigger instead of to an error.** INSTANCE: `gw_hdd >= 16.4` fires **46.6% pooled over 161 sessions across 14 groups** and looks healthy, while per block it is **12/12 g11, 12/12 g12, 19/20 g9 and 0/10 on all four summer blocks g20-g23** - degenerate in 7 of 14, discriminating only in the shoulder blocks (g7 2/10, g8 4/10, g14 4/12, g15 3/12), i.e. **it carries information only in the season the S102 salience slider says weather matters least.** **THE RESULT THAT INVERTED MY OWN RECOMMENDATION, and it is the reason the instrument was built before the rewrite:** the two SHAPE-form percentile conditions I had recommended as the cure are **the worst performers in the whole set - `<= 5th percentile` is degenerate in 10 of 11 blocks (91%)**, because a 5%-rate condition on a ~10-day block (D5) expects 0.5 fires; while **the healthiest condition measured, degenerate in 0 of 3 blocks, is `session_b_share_two_sided < 0.50` - an ABSOLUTE bar, sited at the CENTRE of its distribution (the 50/50 physics line) on a correctly-normalized series.** So: **(1) TRANSFER failure** - the bar means something else in a new regime; cured by shape form; real, and `gw_hdd >= 16.4` is the instance. **(2) DISCRIMINATION failure** - the condition returns the same answer on every day of the block in front of the specialist; cured by siting the bar near the CENTRE of its own distribution; **a RATE property, not a FORM property.** (2) is the one that costs us, because **D3 (per-day causal slices) means a specialist owns ONE day inside ONE block and never sees the pooled corpus** - a condition that is 0-of-10 in its block told that specialist nothing, however cleanly it transfers. A healthy condition needs BOTH, and shape form buys only the first - **which is why S98's ratio reformulation of C2 was REFUTED on comparable data (0.714 on a true instance vs 0.718 on a false one, identical on every arm): converting an absolute bar to a ratio destroyed the discrimination.** Also measured: the same written bar under two encodings, `session_b_share < 0.50` raw vs two-sided, fires **96.6% vs 51.7% and disagrees on 13 of 29 sessions** - an absolute bar silently absorbs a defect in its own input (the S108 denominator hole) and has no way to declare it. Inventory: **48 threshold literals across 29 of 82 plays**, in four populations - COUNT (regime-proof, leave alone), SHAPE, ABSOLUTE, and SHAPE_QTY_VALUE_BAR (shape quantity carrying a value bar: `k3 <= 0.05`, `\|dip_imb_level\| >= 0.15`). 8 conditions are **UNCHECKABLE from the committed state and recorded as open tasks, never as passes** (D24 state c) - 5 of them because the quantity is price content masked by design (D2), which is not a defect | BUILT (report-only) | `condition_audit.py`, D11-verified: executes on the default corpus, on a single group, and degrades to a clean message with exit 0 on an unknown group. **It is a measuring instrument, not a gate - promoting any verdict to a hard gate is Greg's call, same posture as D27.** **THIS DOES NOT CLOSE D23:** the design call - what reference window a relative condition measures against, and what target fire rate makes it discriminate - is still Greg's, and the measurement above is what it should be decided on |
| D27 | S110 (Greg spotted, Claude measured) | **WHY THE RENDERED LINES DID NOT CONNECT — root-caused.** Greg: "is that why the lines weren't connecting?" Measured: the coordinator advances the running level by NET (day-move minus gap) but DRAWS each day to its last path point, so a path emitted as cum-from-PRIOR-CLOSE ends one whole gap above where the next day begins. **g22 0/10 days mismatched; g23 8/10, and both Mondays off by exactly their +400 gap** (0706 path-end +500 vs net +100; 0707 -350 vs -310 = its -40 gap). Cause is a SPEC AMBIGUITY, not agent error - the contract did not say whether cum runs from the open or the prior close, and the two groups read it differently. **NO SCORE IS AFFECTED**: scoring reads guess_day_move_usd and never touches the path; G23's 5/10 and sum-abs 5,920 stand as reported - the picture was wrong, not the scoreboard | BUILT (announce) | SOP v1.6 pins cum-from-OPEN with last point == day-move minus gap; coordinator prints a per-day continuity warning. **Left as ANNOUNCE not HARD-FAIL deliberately: hard-failing would invalidate committed artifacts - promoting it to a gate is Greg's call** |
| D45 | S114 (Greg) | **THE REFINE DOES NOT NEED THE BLIND WALL.** Greg, verbatim: *"And we don't need blind wall for refine"*. The refine sees the price curve by design, so blind-wall considerations must never gate, delay or constrain a refine round. INSTANCE: the S114 forward-forcing feed was blind-wall audited (12Z cycle of D-1, 10 of 10 days, zero violations) - that audit is for the BLIND's benefit and is explicitly not a refine gate. Consequence: when staging for a refine, stage UNMASKED and do not withhold anything on blind-wall grounds. | STANDING | stated in the S114 handoff's refine runbook; the refine stages UNMASKED and blind_legality/window redaction are not applied to it |
| D46 | S114 (Greg, said three times) | **THE RENEWABLES FORCING IS A PREREQUISITE, NOT A BACKLOG ITEM.** Greg: *"I keep saying that we have to build the renewables part. That has to be done before refine runs."* Built and wired at S114 as `weather_forcing_forecast` (G-5/A-51): forward wind and solar as a 31-member GEFS ensemble density, served per day, wind and solar NEVER summed (they are seasonally anti-correlated - D37). THE MEASURED JUSTIFICATION: four g24 specialists independently reported that no forward wind expectation existed anywhere, while `grid_stack.wind_mwh` is D+2 stale BY CONSTRUCTION - so on the exact channel the 0629 lesson is about (gw_cdd rose as forecast and burn FELL 4.2 Bcf/d on a 62% wind rise), wind could only ever corroborate, never forecast. | BUILT | BUILT - forecast_harness._weather_forcing_block + state_health.REQUIRED_EVERY_DAY (so its absence hard-fails a stage) + restore_substrate prefix + store on S3 |
| D47 | S114 (measured, not decided) | **A STORE REBUILT IN A SESSION IS NOT A FIX UNTIL IT IS ON S3.** `data/` is disposable and `restore_substrate` pulls from S3, so a fresh session silently restores whatever S3 holds. MEASURED at S114 close, BEFORE pushing: S3 still held the PRE-FIX copies of `model_disagreement`, `storage_regional`, `vol_regime` and all five COT books, and `eia_surprise.json` was ABSENT FROM S3 ENTIRELY. Starting the next session at that moment would have undone nearly every data fix of the session, silently, with no error. RULE: a session that rebuilds a served store pushes it to S3 in the same session, and verifies by reading it BACK from S3 - not by trusting the upload. | STANDING | verified at S114 close by reading each store BACK from S3; the handoff's step 2 makes restore_substrate the first action of the next session |
| D48 | S114 (Greg) | **ALL FOUR KEYS LIVE IN ONE ENV FILE, AND IT IS NOTED.** Greg: *"make sure that the keys get put into an env file and it's noted somewhere"*. `~/.config/markets/env`, chmod 600, outside the repo, holding AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY + DATABENTO_API_KEY + EIA_API_KEY as NAME=VALUE lines. Documented in KEYS.md with a per-key verification table (names and holders only, never a value). The container's `proxy-injected` placeholder still shadows the real AWS pair in the process env, which is why `creds.aws_client()` remains the ONLY sanctioned way to build a boto3 client. | STANDING | creds.py resolves env -> ~/.config/markets/env -> legacy, and ignores the container placeholder; KEYS.md carries the per-key verification table |
| D49 | S115 (numbering hygiene, found by the PRR audit) | **D40-D44 WERE NEVER ASSIGNED, AND THIS LINE RECORDS THAT AS A FACT RATHER THAN LEAVING A HOLE.** The S115 platform audit found five phantom decision ids: D39 exists, D45-D48 exist, and D40 through D44 appear nowhere in the repo. CAUSE, and it is benign: S114 numbered its four new decisions off the ledger's ROW COUNT (44 rows at the time, a count that includes NC-1..NC-4 and D28.1 alongside the D-numbers), so it continued at D45 instead of D40. Nothing was lost and no decision is missing - the numbering simply skipped. **WHY IT NEEDED A LINE:** this file is append-only and its ids are cited from the brain, the SOP, CLAUDE.md and every handoff, so an unexplained gap is an invitation for a future session to either 'fill' it - minting a D41 that collides with nothing but reads like a decision older than D45 - or to cite D42 ambiguously. A recorded gap cannot be reused by accident. **THE RULE GOING FORWARD: the next id is one past the highest EXISTING D-number, never the row count.** Never backfill D40-D44. | RECORDED | this line. The gap is now a documented fact rather than an absence, which is the same discipline D24 applies to evidence (searched-and-none-found is a result; unrecorded is not). Next decision id is D50. |
| D50 | S115 (correcting a dead-guard claim in this ledger, PRR audit D9-08) | **D13's ENFORCED-BY CLAIMED A SWEEP THAT DOES NOT EXIST - CORRECTED HERE RATHER THAN QUIETLY.** D13 (gas-only scope: NG/KXNATGASD/Henry Hub until further notice) records its status as DECIDED with 'enforced by: doc-only (memo + this ledger) - acceptable: it is a scope statement, checked by the QC sweep against new non-gas builds'. **MEASURED S115: `agents/QC_CHECKLIST.md` contains no gas-only item and no non-gas check of any kind.** So the ledger asserted an enforcement mechanism that was never built. That is worse than an honest doc-only line, because a reader auditing the ledger sees a guard where there is none - the same species as the S114 'live status on dead evidence' correction, applied to a decision instead of to a play. **THE SCOPE DECISION ITSELF IS UNCHANGED AND STILL BINDING** (Greg, S110): gas only. What changes is the honesty of its enforcement claim - D13 is doc-only, full stop, and this line supersedes its enforced-by text. Whether to build the one-line QC check (flag any new build, feed or collector that is not NG/KXNATGASD/Henry Hub, report-only) is a cheap call worth making, but until it exists the ledger must not say it does. | RECORDED (supersedes D13's enforced-by claim; D13's scope decision stands unchanged) | doc-only, stated honestly. Registered as a registry line so the option to build the QC item is tracked rather than re-argued. **The transferable rule: an 'enforced by' cell is a claim like any other and must be true when written - a ledger that describes guards it does not have is the failure this file exists to prevent.** |
| D51 | S115 (Greg, said twice) | **THE EXCHANGE DEFINES MARKET STRUCTURE, NOT OUR TAPE.** Greg, verbatim: *"we should follow what the market does and not what our data says. we might be looking at it wrong"*, and then *"what does cme consider a 'trading day' ... that should be our guide"*. A boundary read off our own data is a hypothesis about the venue, and our tape will agree with a wrong rule as readily as a right one - it cannot falsify what it was used to derive. INSTANCE, and it is mine: the Frankie A-arm session boundary (D6) was undefined, so `continuity_segment` was whatever a caller passed. I first proposed the 21:00 UTC anchor our tape exhibits, then wrote it as 16:00 America/Chicago, and both times cited our own mass-withdrawal groups as CONFIRMATION. Going to the source instead gave three things the tape could not: the CME halt is **Monday-Friday only** (which is WHY Sunday differs, not merely that it does); **Friday's close is a ~49h weekly gap, not a 1h daily one** (a different censoring event, and 20211001 is a Friday); and the CME **trade date begins 17:00 CT on the previous calendar day**, which made the S104 Sunday fold fall out of the definition instead of being hand-coded as an exception. Phase came from the same authority - the NYMEX Energy Futures Daily Settlement Procedure fixes NG settlement at 14:28:00-14:30:00 **Eastern**, giving PRE_OPEN / PRE_SETTLEMENT / SETTLEMENT / POST_SETTLEMENT / POST_CLOSE with every boundary cited. Settlement resolves in ET and the session in CT, each in the zone the exchange states, never converted. COROLLARY: write a boundary in EXCHANGE LOCAL TIME. A literal 21:00 UTC constant reproduces the roster and is then silently one hour wrong from 2021-11-07, inside the source window's own neighbourhood. Our tape is now a CHECK on the rule (3 of 4 roster days show the close; the one that does not is the Sunday) rather than its basis | DECIDED | `research/kalshi/frankie_raw_mbo_benchmark/native_session.py` + 41 tests; the exchange citations are in the module docstring so the authority travels with the code. Frankie D5 also closed here: no clustering in this run |
| D52 | S115 (Greg) | **THE CME TRADING-DAY SCHEDULE IS THE HOLIDAY AUTHORITY.** Verbatim: *"we follow cms [CME] trading day schedule."* D51 applied to holidays: the calendar comes from the exchange, never from our tape. Consequence built into `native_session`: a `full_closure` is not a trade date and is skipped by the same loop that skips a Saturday; a `partial_session` / `early_close` IS a trade date because the book opens. **`flow_calendar` calling a partial session "not a business day" is the SETTLEMENT-counting sense** - conflating it with the trading-day sense would move every expiry-adjacent segment by a day. Classes come from `plant_calendar`'s RULES, not a date table: the roster year is 2021 and `CME_HOLIDAYS` starts 2025-09-01, so a table lookup would read "not a holiday" for every date in the source window (the S112 expiring-table finding). **Shortened close TIME is recorded nowhere, and a partial session runs no settlement cycle, so `phase_within` REFUSES rather than answering from ordinary hours** | CLOSED, BUILT | `native_session.is_trading_day` / `holiday_class` / `phase_within`; 7 tests; roster verified unchanged by execution |
| D53 | S115 (Greg) | **THE A-ARM INPUT UNIT IS THE F_LAST GROUP, ONE UNIT ACROSS ALL SIXTEEN SECTIONS.** Sections 4.6-4.16 were closed at boundaries but never fed, and the contract specifies the CALCULATION not the input event ("construct a complete causal runway for each candidate" never defines a candidate). Decided: a candidate IS one F_LAST group - the same unit `describe_structure` already hashes into `candidate_family_id`, and the unit the member-first run cut 4,758 families from over 4.26M groups. **Chosen so reconciliation against the existing roster holds by construction** rather than introducing a second traversal vocabulary (the `_family_id` defect of 2026-08-29 was exactly that shape). Known cost, recorded not hidden: 4.6 order survival and 4.9 ladder topology are not naturally group-shaped and may be distorted by the choice | DECIDED | to be enforced by the 4.6-4.16 adapters; reconciled by `AssignmentLedger` + the denominators gate |
| D54 | S115 (Greg) | **NO HELPER LANES AND NO SPECIALISTS ON THE A ARMS.** Verbatim: *"we aren't doing the helpers or the specialists anymore. Rt and forecaster can call one as part of their tools and there's options as to the role."* Two roles only - `REAL_TIME_FRANKIE` and `FORECASTER_FRANKIE`; a helper is a TOOL INVOCATION inside a role, never a parallel lane with its own knowledge profile or output, and the role a helper is called under is selectable. Confirms the section-1 correction and answers Question 3 in the ingestion paper. Consequence: `extra_agent_four_helper_architecture_roles` describes a SUPERSEDED architecture and must NOT be made model-visible - showing Frankie a dead architecture is worse than showing nothing. **Also kills the section 10a conjecture** that four helper lanes were a 4-vCPU parallelism artifact: the box has 8 | CLOSED | registry keeps the four entries as `STATIC_REQUIRED_INPUT` on a `DIRECT` route (configuration, not a lane); layer stays `PROVISIONAL_SHADOW` / `model_visible: False` |
| D55 | S115 (Greg) | **THE RECORDED BOX IS STALE - IT WAS UPGRADED, AND IT MAY STILL BE TOO SMALL.** Verbatim: *"We switched to the larger box with 64g ram and 8 agents because of the big computes and this might not be enough for this. We had 6 cpus running and 2 for the planes. We also had a ram swap which was needed too."* So the machine is **64 GB / 8**, split **6 for work and 2 for the planes**, **with swap that was required, not optional**. `LIVE_TELEMETRY_S100.md` records a t3.xlarge (4 vCPU / 16 GB) and that record is WRONG - which is precisely the failure section 10a predicted in writing ("if the instance was resized, the record was not updated, and the record is what the next session will believe"). **Not verified live** (no resolvable AWS credentials this session), so Greg's statement is the authority, not a probe. **Sizing is NOT settled**: Greg flags 64 GB may not be enough for sixteen sections over 4.26M groups, against a member-first run that took 2,985s and wrote 1.5 GB covering 5 of 16 **VERIFIED LIVE 2026-08-29** (Greg: *"proceed"*). The probe ran and **Greg was right on every count while the record was wrong on every count**: instance **`r6i.2xlarge`** (not t3.xlarge), running in us-east-2b, **8 cores**, **61.8 GiB** total memory (64 GB nominal), **32.0 GiB of swap PRESENT**, 128.2 GiB free disk. The credentials are GitHub-secret scoped, so an interactive session resolves none and only a workflow can settle it - `.github/workflows/frankie_box_sizing_probe_20260829.yml`, run 33242769879. **SIZING REMAINS OPEN:** knowing the box is 8/62+32 does not establish that sixteen sections over 4.26M groups fit in it, and Greg's *"this might not be enough"* stands until a slice is measured | RECORD CORRECTED AT SOURCE; SIZING STILL OPEN | `LIVE_TELEMETRY_S100.md`, `PLANT_MAP.md`, the October sharded handoff and `DROP_IN_FRANKIE_A_ARM_NEXT.md` all carry a dated correction; historical S91-S93 handoffs left untouched, since a t3.xlarge was true when they were written |
| D56 | S115 (Greg) | **SIZE THE BOX UP BEFORE THE RUN, THEN WATCH IT GROW.** Two statements, in order. First: *"we can put a monitor probe on the box and watch it grow through the run. if it looks like that's still too small we can stop the run at a checkpoint and up it."* Then, decisively: *"start with a bigger box than the 64g's. the less rich data run almost used the 64 g up so logic says that it def won't be enough for this."* So sizing is NOT a debate to be settled by argument - the member-first run covered **5 of 16 sections** over 4.26M groups and nearly exhausted **61.8 GiB**, which is the measurement that decides it. 16 sections is 3.2x that count and the dominant term (per-group state held across sections) scales with it, so ~200 GiB: **r6i.4xlarge at 128 GiB is only 2x and still short; the target is r6i.8xlarge, 32 vCPU / 256 GiB**, same family so no architecture or AMI surprise. **Correction to a thing said in passing:** an EC2 instance type CANNOT be changed on the fly - there is no live resize, the instance must stop. So the monitor is the WATCH during the run, not the sizing decision, and "stop at a checkpoint and up it" is the fallback the `periodic_checkpointer` already exists to make cheap | RESIZE BUILT AND ARMED, NOT FIRED (size is a ~4x hourly cost step and needs Greg's word); MONITOR LIVE | `.github/workflows/frankie_box_resize_20260829.yml` + `frankie_box_monitor_20260829.yml` |
| D57 | S115 (Claude, reported to Greg) | **THE TWO ALWAYS-FAILING WORKFLOWS NEVER TOUCHED STEP1 DATA, AND THE CAUSE IS A REUSABLE LESSON.** Greg: *"we def don't want step1 data exposed now."* Measured before acting: `ng_exhaustion_step1_receipt_count_20260823` and `ng_exhaustion_step1_oom_continue_v2_20260827` produced a run on EVERY push to ANY branch and failed every time (runs 100-107), despite branch filters excluding those branches. **Both files have INVALID YAML** - an embedded heredoc dedents to column 0 and breaks its enclosing block scalar - and **GitHub creates a jobless startup-failure run to REPORT a broken workflow, ignoring branch and paths filters.** Those runs had **ZERO jobs**: nothing started, nothing authenticated, no step1 data was fetched, printed or written. Removed from this branch (both duplicated on their home branch). **The other 47 step1 workflows were deliberately KEPT**: valid YAML, branch-filtered, and they produced zero runs across eight pushes - deleting them would be scope-widening with test risk and no exposure benefit. **The lesson generalises and I then made the same mistake myself** an hour later in the resize workflow, caught by `bash -n`: a heredoc inside a YAML `run:` block must never dedent below the block's indentation | CLOSED | 0 of 186 workflows on this branch now fail to parse; every new workflow is verified by parsing the YAML and running `bash -n` on each step, and by rendering the remote script rather than reading it |
| D58 | S115 (Greg) | **START THE A-ARM BOX AT 128 GiB, NOT 256.** Greg: *"for the box size, start with 128 and see if that looks like it will work. if not we can upsize without stopping frankies"* - so the armed resize is retargeted from `r6i.8xlarge` (32 vCPU / 256 GiB) to `r6i.4xlarge` (16 vCPU / 128 GiB). The 200 GiB figure it previously targeted was an EXTRAPOLATION from a 5-section run (which nearly exhausted 61.8 GiB), scaled linearly by section count to 16 - not a measurement of a 16-section run. 128 GiB is the cheapest way to find out whether that scaling holds. **THE CONDITION, and it corrects the premise of the instruction: there is no live resize.** An EC2 instance type cannot change on a running instance - the workflow stops, modifies and starts, and it REFUSES outright if the box looks busy, precisely so a resize cannot kill a run in flight. So "upsize without stopping Frankie" is not available: at 128 GiB an out-of-memory death currently restarts the run from zero. Upsizing mid-run only becomes affordable once the periodic checkpointer is on the LAUNCH path, at which point a stop costs one save point instead of the whole run. That makes the checkpointer wiring a precondition of this sizing choice, not an independent queue item. | OPEN (armed, not fired) | `frankie_box_resize_20260829.yml` `TARGET_TYPE: r6i.4xlarge` / `TARGET_VCPU: 16`, RUN MARKER deliberately NOT bumped; the busy-box guard and the full rollback path are in the same workflow; the reasoning is written into its header comment so it is checked rather than inherited |
| D59 | S115 (Claude, needs Greg) | **D54 IS CONTRADICTED BY THE ENFORCEMENT LAYER AND HAS NOT LANDED - AND THE `PROVISIONAL_SHADOW` QUESTION AS ASKED DOES NOT EXIST.** The prelaunch state doc recorded that `extra_agent_corrected_information_and_gap_diagnoses` and `extra_agent_four_helper_architecture_roles` were both `PROVISIONAL_SHADOW` with `model_visible: False`, one ruled on (D54, stays shadow) and one open. Read from the registry JSON and the gate: **neither is shadow.** The two `PROVISIONAL_SHADOW` layers are `s137_cognitive_shadow_runtime` and `hipporag_associative_retrieval` (`principal_route: SHADOW_ONLY`); computed policy counts match `EXPECTED_POLICY_COUNTS` exactly, so those two are the whole set. Both `extra_agent_*` layers are `STATIC_REQUIRED_INPUT` in `corrected_extra_agent_carryforward`, `principal_route: DIRECT`, `activation_stage: PRE_CALL`, on both arms - and `native_ingestion_layer_registry.py:348` enforces `("AVAILABLE", model_visible=True, "SHA")` for that policy. **So Frankie already SEES both, and a pre-call receipt reporting either invisible FAILS the gate.** D54 ruled the helper-roles layer stays shadow and invisible because it documents a superseded architecture; the registry requires the opposite and no registry change was made. This is the S115 lesson in its worse form - not a decision nothing enforces, but a decision something enforces the REVERSE of. It has not bitten only because no run has reached the pre-call gate. **THE OPEN RULING is one question asked twice, once per layer: does it stay a required, model-visible input, or move to shadow?** Both are `v3_derived: true` and are the only members of `ALLOWED_V3_LAYER_IDS`; their sources include `NG_EXHAUSTION_V3_NONAUTHORITATIVE_RESULTS_EXTRA_AGENT_V4_CARRYFORWARD_20260820.md`, so "required and visible" means Frankie reads V3 carryforward marked NONAUTHORITATIVE as a binding input on every run. Moving either changes `EXPECTED_POLICY_COUNTS`, `EXPECTED_ARM_LAYER_COUNTS`, `EXPECTED_LAYER_ID_SET_SHA256`, the surface inventory hash and the manifest - which is why it is a ruling and not an edit. | OPEN (needs Greg) | corrected in `FRANKIE_A_ARM_PRELAUNCH_STATE_20260829.md` section 3; the contradiction itself is enforced by `native_ingestion_layer_registry.py:348`, which is what makes D54 detectable as unlanded rather than merely unimplemented |
| D60 | S115 (Greg, STANDING RULE) | **NO DATA MAY BE DROPPED WITHOUT DISCUSSING IT FIRST.** Greg, verbatim: *"we have all this data for a reason. do not drop any of it without discussing with me first. make that a rule"*, and the cost that motivates it: *"this is the problem that i have been fighting the whole time that things are dropped for whatever reason and not discussed and then we find out we have to rerun because it was important."* Also *"do not leave any of the book data out. it may not seem relevant to you but it may to frankie."* **THE RULE:** a row, field, message or observable that reaches our code is either USED, or RETAINED and counted, or REFUSED loudly. It is never silently ignored, and the judgement "this looks like nonsense" is not a licence to discard - relevance is Frankie's to decide, not the ingest layer's. Dropping anything is a DECISION that goes to Greg first. **WHY IT BINDS HARDER HERE THAN IT LOOKS:** a dropped input does not fail, it produces a number that is present, typed, in range and wrong, so it is discovered only after a full run - and a rerun is 4.26M groups on a paid box. The drop is cheap; the rerun is not. **TWO INSTANCES, both found the day the rule was written.** (1) The first `native_rt_book.ReplayBook` silently ignored four row classes `InstrumentBook` ACTS on - a sentinel or absent price on an A/M, the `F_TOB` sentinel side-wipe, `order_id == 0`, and a modify on side `N` - and kept no anomaly counters at all, so an unsided add, a cancel for an order that never rested, the quantity swallowed by an over-cancel and every `F`/`T`/`N` row left no trace. A differential harness against `InstrumentBook` found each one silently moving a `view` number. Corrected the same session: the book now MIRRORS even where mirroring looks wrong, and counts what it cannot act on. (2) **`native_replay_driver._on_group` line 218 discards the adapter's legacy rows** - `frame, _legacy = self.adapter.apply(...)` - and those rows carry the projected 10-level MBP-10 depth (bid/ask price, size and order count at each of ten levels) at every trade and at group end. The registry group `legacy_observable_crosswalk` is **CAUSAL_STREAM_REQUIRED** and holds `legacy_price`, `legacy_native_signed_flow`, `legacy_per_second_roll20`, `legacy_book_imbalance` and `legacy_structure_observables` - `legacy_book_imbalance` cannot be computed from anything else the traversal keeps. This one is PRE-EXISTING and unfixed. **THE ONE EXCEPTION (Greg, same session):** *"if a row is truly blank and is measuring nothing then you can leave it off."* TRULY BLANK - carrying no measurement at all. Not "looks irrelevant", not "we do not use it", and never "it would cost memory" - Greg ruled memory out explicitly: *"i don't care about memory. restore every piece... let him figure out what he uses but he has to see everything."* The burden of proof is on the drop, and it must be shown from the row itself. | OPEN (rule standing; instance 1 fixed, instance 2 fixed) | Instance 1 is enforced by `test_native_rt_book.py::RetentionTests` and by the differential suite, which asserts an anomalous row is refused loudly or mirrored exactly and never accepted-then-ignored. Instance 2 has NO enforcement yet and is the next thing to fix. The rule itself is now in `CLAUDE.md` under Operating discipline, so it is loaded every session rather than remembered |
| D61 | S115 (Claude, under D60) | **RESTORE WHAT THE MBO ADAPTER DROPS BY WRAPPING IT, NEVER BY EDITING IT.** `research/ng_exhaustion_mbo_v4_state_adapter_20260820.py` is **HASH-LOCKED**: `test_checked_in_finalizer_lock_pins_every_executable_byte` and three October full-stack supply-chain tests pin its bytes, and other pipelines' provenance rests on that hash. Restoring the D60 drops by editing it in place was tried and **broke six of those locks in one commit** (wider suite 7 pre-existing failures -> 13). Reverted, and rebuilt as `native_full_capture_adapter.FullCaptureAdapter`, a subclass that keeps everything the base discards while the locked file stays byte-identical - verified by `git diff --quiet`. **This is not a compromise on D60; nothing is left out.** It is the only form of the fix that does not invalidate a prior run's provenance in order to improve this one. **WHAT IT RESTORES, all of it computed on every record and then discarded:** the per-record `ApplyEffect` (bound to `_` in `V4MboAdapter.apply`, so NO per-record book effect reached the traversal at all - and `top_before_price_raw`, the prevailing touch immediately BEFORE the record, and `removed`, whether the record killed a resting order outright, had **zero readers anywhere in the repo**); the reconstructed FIFO queue (every frame asserted `fifo_priority_reconstructed: True` while asking for `include_order_ids=False`, so it carried none of the reconstruction that is surface #1 in the adapter's own docstring); everything below the top ten levels; per-side event COUNTS (`action_side_count`, maintained in five windows with full add/remove bookkeeping and published by none - quantity is not count, a 500-lot `C_B` is one pull or five hundred, which are opposite microstructure); top-of-book touch quantity for `T`, `F` and `M`; the out-of-order `ordered` flag; **anomaly MAGNITUDES the book knows only at the instant** - the quantity an over-cancel swallows past the clamp, the depth and order count destroyed by an `R` clear or an `F_TOB` side wipe, and forward SEQUENCE GAPS, where only backwards regressions were counted while a forward jump is the standard signature of dropped packets on a CME channel; and the per-group `integrity_delta`, because a cumulative counter says "this has happened 47 times" and never says where. **The resume path is closed too** (`from_restored`): `restore_adapter_state` returns a plain `V4MboAdapter`, so a resumed run would have fallen back to the dropping version for its whole remainder - a drop that appears only on resume, which is exactly where nobody would look. | CLOSED | `tests/test_native_full_capture_adapter.py`, 12 tests, every one a DIFFERENTIAL against the locked adapter - asserting only that the wrapper HAS a field would still pass if the base had it all along, and the claim is that this data was being LOST. Two of them pin substitutability: identical `record_count`, `completed_event_group_count` and book set, and no key the base frame carried is missing from the wrapped one |
| D62 | S116 (Greg, and it changes the build plan) | **THE FOUR ESTIMAND PROPOSALS ARE WITHDRAWN: THE PRIOR EXHAUSTION PROGRAM ALREADY BUILT AND FROZE THAT VOCABULARY.** Greg: *"this should have already been built in some capacity for the exhaustion prediction part. is this related?"* and then *"please search to make sure that the other 3 haven't already been built."* They had. Roughly 200 files, 2026-08-16 to 2026-08-25, in places frozen and hash-bound. **t0 is a DIPOLE FLOW SPIKE** - trailing-20s signed aggressor imbalance, 85th-percentile adaptive bar, +/-5s local max, prominence rank, 45s refractory - and *"price is never used to find or orient the dipole event"*; my proposal made it a price-level event. **PRIOR/T0/H+N exists by name as `TIMING_LADDER`**, settled 2026-08-19/20, classes strictly ORDERED with `primary_success_definition: PRIOR_BIRTH_PREDICTION_ONLY`. **A call is a persistence run on signed flow** with `structural_onset_idx` and `causal_confirmation_idx` kept apart; my depth-falls-by-cancel predicate has no counterpart anywhere. **SAME/FLIP means polarity versus the LATEST PREDECESSOR**, frozen with committed counts 1,546 FLIP / 1,883 SAME of 3,429, gated in four files - the words are taken and the existing meaning has published results. **The mirror is the SIDE-SWAPPED SIDE STRING** (`mirror_identity()`, already computed, 966 mirror-ready keys), not a price reflection. **`t` IS A PRICE TICK**: `3t/5t/8t/13t` are ZigZag reversal thresholds and 358/993/1802/4386 are the median leg durations IN SECONDS they produce; Fibonacci is coincidental, since 2t was dropped after a five-point sweep. **Greg's duration ruling restates a correction frozen 2026-08-18** - *"exhaustion is a duration / remaining-runway signal, not a direction predictor"* - and the frozen program solves the clock problem better than my revision did, keeping absolute-second horizons anchored on the **dynamic episode endpoint, "never t0+60 by fiat"**, which preserves comparability across events. **THE BUILD PLAN CHANGES: the missing layer is the per-second roll20/dipole SUBSTRATE, not six F_LAST-group adapters.** The built candidate is a 1-second flow event and the A-arm unit is the nanosecond F_LAST group - 3,429 frozen events against 4.26M groups - the registry requires the bridge as `CAUSAL_STREAM_REQUIRED`, and nothing in the benchmark computes roll20 or any dipole from the MBO stream. That is why 4.10/4.11/4.12 have nothing to feed them. **TWO CORRECTIONS TO MY OWN CLAIMS:** `ng_exhaustion_v4_exact_candidate_freeze.py` is a CI release freeze carrying git SHAs, NOT a market candidate definition (the real one is `ng_exhaustion_chain_canonical_table_20260817.py:220-267`); and **there is NO event clock in the built work** - the only `event_clock` is a boolean availability flag, built durations are seconds and the new modules' are nanoseconds, so on Greg's rule both are guilty. **`PHASE_INDEX` scores 0 of 11** against the built corpus with four names reused for a different referent, which is worse than absence; its provenance is one prose line in the 2026-08-28 mission doc. **`SEED_STATES` (P/O/S/X) and `RecognitionLabel` (PRIOR/T0/H+N) ARE correct and must stay** - they are the model for how the rest should reuse the frozen vocabulary. | OPEN (needs Greg: is an F_LAST-group-local candidate admissible at all?) | `research/kalshi/FRANKIE_A_ARM_PRIOR_WORK_RECOVERY_20260829.md` carries every citation; the proposals file is banner-marked WITHDRAWN rather than deleted, per the rule that a superseded value is a deliberate record; `DROP_IN_FRANKIE_A_ARM_NEXT.md` leads with "read the recovery before any build" |
| D63 | S117 (Greg, reaffirming D54 against the canonical procedure) | **NO HELPERS OR SPECIALISTS - AND THIS IS THE APPROVED EXCEPTION TO THE CANONICAL MONTHLY PROCEDURE.** The handoff raised a live conflict rather than picking a side: `research/kalshi/NG_EXHAUSTION_FRANKIE_MONTHLY_RUN_PROCEDURE.md` (adopted 2026-08-24) mandates the four-helper paired-lane architecture in step 11 - *"within one lane run recurrence on CPU 0, extension on CPU 1, timing on CPU 2, and context on CPU 3 concurrently on the identical immutable prefix. Frankie is call five and starts only after all four helpers finish"* - and repeats the role-to-CPU mapping in its permanent execution invariants. D54 (S115) had already retired that architecture for the A arms, and the binding mission (2026-08-28) postdates the procedure. **The procedure's own header requires exactly this: *"If it conflicts with the canonical procedure, stop and resolve the conflict explicitly rather than silently choosing one."*** Raised, and Greg ruled: **"No helpers or specialists."** **APPLIED:** two roles only, `REAL_TIME_FRANKIE` and `FORECASTER_FRANKIE`; a helper is a TOOL INVOCATION inside a role, never a parallel lane with its own knowledge profile or output; the procedure's step-11 concurrency and role-to-CPU mapping do not bind the A arms; the five walk specialists are not used here. **WHAT THIS DOES NOT BY ITSELF DO:** D59 remains half open. Both `extra_agent_*` layers are `STATIC_REQUIRED_INPUT` and model-visible, so Frankie would still READ the dead four-helper architecture as a required input every run, and *"showing Frankie a dead architecture is worse than showing nothing."* Moving either changes `EXPECTED_POLICY_COUNTS`, `EXPECTED_ARM_LAYER_COUNTS`, `EXPECTED_LAYER_ID_SET_SHA256`, the surface inventory hash and the manifest - which is why it stays a ruling and not an edit. The architecture question is settled; spending those hash changes is not. | DECIDED (architecture); D59 remainder OPEN | `DROP_IN_FRANKIE_A_ARM_NEXT.md` carries the ruling in its second section, before the read order, so no session reaches the build plan without it; the superseded box is kept at `DROP_IN_FRANKIE_A_ARM_20260829_SUPERSEDED.md` |
| D64 | S117 (Greg, ruling the open half of D59) | **GET ANY MENTION OF THE FOUR HELPERS OUT - A HELPER IS A TOOL INVOCATION WITH A SELECTABLE PERSONA.** Greg, verbatim: *"For d59, get any mention of the 4 helpers out. He can call with different persona options as part of his tools. We've covered this more than once."* D54 retired the four-helper architecture for the A arms and D63 reaffirmed it against the canonical procedure, but **the enforcement layer went on REQUIRING it**: `native_ingestion_layer_registry.py:348` enforces `("AVAILABLE", model_visible=True, "SHA")` for `STATIC_REQUIRED_INPUT`, and `extra_agent_four_helper_architecture_roles` sat in that policy on both arms - so Frankie would have read a superseded architecture as a binding input on every run, and a pre-call receipt reporting it invisible would have FAILED the gate. That is the S115 lesson in its worse form (a decision something enforces the reverse of), and it had not bitten only because no run reached the pre-call gate. **SIX layer identities removed:** `extra_agent_four_helper_architecture_roles`, the entire `helper_role_configuration` group (`helper_pair_triplet_recurrence_scout`, `helper_extension_propensity_scout`, `helper_timing_lifespan_family_scout`, `helper_true_false_context_investigator`), and `output_helper_evidence_movie` - an output surface for a lane that does not run can only ever be reported absent. Union **105 -> 99**, arms **102/104 -> 96/98**, `STATIC_REQUIRED_INPUT` **24 -> 19**, `APPEND_ONLY_OUTPUT` **11 -> 10**, new `EXPECTED_LAYER_ID_SET_SHA256` `fbb79cde...`, new `registry_sha256` `6e67ffc5...`, six surfaces out of the execution gate, `ALLOWED_V3_LAYER_IDS` down to one member. **NOTHING SOURCE-LEVEL WAS DROPPED, and this is what makes it D60-clean:** the removed carryforward layer named exactly the same three V3 files as `extra_agent_corrected_information_and_gap_diagnoses`, which remains required and model-visible, and **those files contain no helper text at all** - measured, 0 matches. The architecture lived in the layer DESCRIPTION and in feed-inventory section 10, never in the evidence, so what was removed is the INSTRUCTION to read those files as a helper architecture rather than any evidence. Feed inventory section 10 is retired in place, keeping its number so every cross-reference still resolves; the canonical monthly procedure goes to **version 2** (step 11's helper concurrency, the role-to-CPU invariant, the four-thread/helper-overlap limb of the step-12 gate, and the Nucleus-replacement paragraph all retired). **91 concrete layers still clears the hard floor of 90, with a margin of nine** - worth knowing before anything else is removed. **THE SECOND HALF OF D59 IS UNCHANGED AND STILL OPEN:** whether `extra_agent_corrected_information_and_gap_diagnoses` stays a required, model-visible input is a different question and was not asked again. | CLOSED (helper half); D59's corrected-information half still OPEN | `test_no_layer_carries_the_retired_four_helper_architecture` asserts over layer ids AND DESCRIPTIONS and over group ids, because an id-only check would have passed on a layer still telling Frankie to read those files as a helper architecture - the description was the whole of it |
| D65 | S117 (Greg chose B; Claude measured) | **STREAM THE EXACT LEDGERS TO DISK - AND THE ARMED 128 GiB RESIZE WOULD ALSO HAVE FAILED.** Peak RSS (`VmHWM`, isolated processes, so a true peak and not a delta against a contaminated baseline): in-RAM **33.95 MiB per 1k groups at 20k, 33.00 at 60k -> 137-141 GiB** over the roster's 4,256,603 groups; streamed **2.49 then 1.59 MiB per 1k -> 6.6-10.3 GiB**, and the curve IMPROVES with scale as fixed overhead amortises. Throughput is unchanged or better (1,772 rec/s streamed at 60k against 1,486 inline, where memory pressure had begun to bite). **TWO CONSEQUENCES.** (1) **D56/D58's sizing is superseded: `r6i.4xlarge` at 128 GiB was UNDERSIZED, not merely unfired** - the in-RAM path needs ~137 GiB, so the armed resize would have died the same death one step later. (2) **No resize is needed at all:** 7-10 GiB against the current `r6i.2xlarge`'s 61.8 GiB is roughly six times the headroom. **THIS IS NOT A D60 DROP AND THE DISTINCTION IS THE POINT:** every row is still retained, counted, complete and in emission order; it lives in JSONL on disk instead of a Python list. Greg's rule is USED, or RETAINED and counted, or REFUSED loudly, and that memory is never a reason - disk honours that as fully as RAM. **IT ALSO FIXES SOMETHING ON THE WAY PAST:** nothing verified the in-RAM lists either - the gate for exact members beneath every summary reads `member_rows_written`, a COUNTER, and would have passed with an empty list. Each sink now reconciles its own FILE against that counter, rows read back off disk rather than an internal tally agreeing with itself, and a mismatch REJECTS. **THE TRAP IT NEARLY WALKED INTO:** `roll20` was fed by SLICING the fully retained legacy list from a cursor, so streaming would have handed it an empty tail - and `legacy_per_second_roll20` is `CAUSAL_STREAM_REQUIRED`, so it would have gone quietly wrong with nothing failing. Now fed from a pending buffer that says what it is. **AND GREG'S OWN CHECK FOUND THE REAL HOLE:** *"Frankie is still ingesting the full raw mbo data to check against?"* He was not - the spawn request named every identity hash and NOT ONE PATH, so everything was retained, reconciled, hashed and unreadable. **Retained is not the same as reachable, and only one of them is worth anything.** | DECIDED, BUILT, PROVED | `native_row_sink.py` + `test_native_row_sink_differential.py`: one slice run BOTH ways, the whole result compared field for field with only the declared retention keys removed. It caught `result_hash` on its first run (correctly - the artifact's retention representation is what changed, so it is now asserted to DIFFER rather than excluded) and then caught the evidence hash folding in ABSOLUTE PATHS, which made the same evidence hash differently in two directories. A single run agrees with itself; only a two-directory comparison finds that. The comparator is mutation-tested |
| D66 | S117 (Greg, ruling D62) | **BOTH UNIT LEVELS, AND FRANKIE SEES IT AS WE WOULD IN RT.** Greg: *"I don't want to limit that just on our wrong coding or assumptions. It should see that as we would in rt."* D62 asked whether an F_LAST-group-local candidate is admissible at all, given the frozen program's candidate is a 1-second dipole flow event (3,429 over 54/55 weeks) against 4,256,603 groups over four days. **RULED: both.** Groups are the MEMBER unit - 4.1-4.9, 4.13-4.15, all naturally group-shaped, and the four fed in T1 (4.8, 4.9, 4.13, 4.14) are exactly the group-shaped ones. Dipole flow events are the CANDIDATE unit - 4.10, 4.11, 4.12, 4.16, which are exactly the four recorded as blocked. **That split was never designed; it fell out of which sections need an event with a before and an after, which is the evidence for the ruling.** D53's group unit stays correct for what it was chosen for and nothing built is discarded. **THE RT CONSTRAINT HAS TEETH AND DISQUALIFIES PART OF THE FROZEN DETECTOR, MEASURED:** `ng_exhaustion_chain_canonical_table_20260817.py:194-198` sets `day_thresholds[d] = quantile(|roll20| over the WHOLE DAY, PEAK_Q)` - **the bar for 09:00 is set using 15:00's data, which is hindsight and not RT-legal**; the A-arm detector needs a trailing causal threshold. `LOCAL_RADIUS = 5` means a spike at `t` is only KNOWABLE at `t+5`, which is a lawful availability delay rather than a violation but must be stamped at `t+5` or 4.11's PRIOR/T0/H+N arithmetic is wrong at the root. `endpoint()` walks FORWARD and is an outcome, never available at the decision point. **DECLARED NOW RATHER THAN DISCOVERED LATER: the A-arm event set will NOT equal the frozen 3,429** - a causal bar finds a different set than a hindsight bar, and that is correct behaviour to declare, not to reconcile away. **THE SUBSTRATE ALREADY EXISTS:** `native_roll20.SecondBinner.series()` yields the per-second buy/sell arrays and `roll20()` builds the rolling series, which is precisely `detect_week_events`' input - so this is porting a frozen detector onto a substrate we have, not a rewrite. | DECIDED (supersedes D62's open question) | six adapters remain and four of them are unblocked by this: the causal detector first, then 4.10/4.11/4.12/4.16 on it, then 4.6 (no invention needed, `ReplayBook` supplies `book_view`) and 4.7's observation half (one declared choice, the tick neighbourhood). 4.15 stays out by D5 |
| D67 | S118 (Greg flagged it; Claude measured, and the measurement moved it) | **THE NOVA REDUCER IS TWO DIFFERENT THINGS AND ONLY ONE OF THEM SAVES US ANYTHING.** Greg: *"something that we have completely overlooked is the nova token reducer for the outputs. we won't mess with it for the Sunday but something to keep in mind"* and *"look in the git repo also"* - which is how `research/kalshi/frankie_nova_optimizer.py` was found, after a first search that stopped at ten results and treated the visible list as complete. **MEASURED ON AN A-ARM SHAPED ROW: `FrankieNovaOptimizer.compact_payload` returns a string BYTE-IDENTICAL to what `native_row_sink` already writes, so lossless minification saves EXACTLY ZERO on our ledgers.** The optimizer reports a 13.99% reduction, but that is against its own `indent=2` baseline, which we never write - a number present, typed, plausible and measuring something other than what its name implies, the same shape as the 24 KB per record that cost a run. **WHERE THE REDUCTION ACTUALLY IS: key names are 57.3% of that 492-byte row.** The ledgers are sort-keyed JSONL, so every row repeats every key name in full, and A-arm names are long. So the applicable mechanism is the KEY ALIASING in `research/nova_ng_exhaustion_packet.py` (`KEY_TO_ALIAS` with a reverse map, values verbatim), which needs an A-arm alias table because the existing one is the exhaustion schema. Lossless, so it is a re-encoding and not a D60 drop. **AND THE LARGER THING NOVA BRINGS IS NOT COMPRESSION AT ALL:** `plan_retrieval` returns `refuse_full_read` on a 12 GB day against its 262,144-byte cap AND DECLARES THE WITHHELD CONTENT, and the module's own rule is that silent truncation is forbidden and withheld content must be explicit. That is D60 arriving from the other direction, and it is how Frankie reads a day without either blowing context or quietly seeing less than he was told he saw. **NOT APPLIED TO THE SUNDAY RUN**, on Greg's instruction, so that run measures the unreduced size honestly. | MEASURED, NOT APPLIED | Sunday's per-field table sizes the aliasing win exactly: `bytes_by_field` counts `len(json({key: value}))`, which INCLUDES the key name, so key bytes split from value bytes on real rows rather than on the one row measured here. `frankie_nova_optimizer` is imported by nothing in the A-arm benchmark; `nova_ng_exhaustion_packet` only by `ng_exhaustion_nearlive_overlay.py`. |
| D68 | S118 (Greg) | **AFTER SUNDAY, A FULL IN-DEPTH REPORT FROM FRANKIE, THEN STOP FOR THE REVEAL.** Greg: *"when this Sunday is done we're going to need a full indepth report from Frankie. on the calcs, on the full raw mbo, all of it. once that is done we'll stop so we can do the reveal and then go from there"*. The report is not the size table - that is ours and it is only what things COST. Frankie's report is on the calculations themselves, on the raw MBO, and on what carries value, which is the half no measurement can supply. **THE STOP IS PART OF THE INSTRUCTION**: nothing further is run until the reveal has happened, so day two does not start on assumptions the reveal would have corrected. **This needs the second half of the spawn contract, which no workflow performs.** The launcher produces EVIDENCE and never calls a model; the traversal stages a spawn request at each lawful cutoff and moves on, and `attach_principal_findings` is the only route into the findings layer. A traversal is therefore ACCEPTED as evidence ONLY, and gate 8 says so in words. So the report comes from an agent session reading the staged request plus the evidence and emitting a committed artifact, exactly as the blind and refine specialists ran. | ORDERED, NOT YET RUN | Sunday's run stages roughly 20 spawn requests under `spawn_requests/` at a cadence that scales with the slice - a defect fixed in the same session, because the box path had been taking the 250,000-group default and would have staged NONE. `native_staging.load_principal_artifact` hard-fails on a missing artifact, on findings citing a different evidence hash, and on an empty findings list, so an unrun spawn cannot be recorded as zero findings. |
| D69 | S119 (Greg's standing doubt, closed by measurement) | **THE 246,030 BYTES PER RECORD FIGURE IS CONFIRMED BY A WITNESS THAT HAS NO STAKE IN IT - AND THE FIRST RUN OF THE WITNESS CONFIRMED THE WRONG RUN.** Greg, at S118 close: *"i still feel the numbers need to be rechecked but we'll do that in the next session."* Held open because four size figures that session were present, typed, plausible and measuring something other than what their name implied, and the replacement was still SELF-REPORTED - `ledger_retention[*].bytes` is the sink counting its own writes. **MEASURED against S3's own `ContentLength`, recorded at PUT time by a party with no stake in the answer:** every ledger object's size equals the sink's count exactly, all three record counts agree (`layers.identity_receipt.total_mbo_records`, `traversal.records_seen`, `layers.identity_receipt.coverage.records_seen` = 50,001), and the smallest ledger was DOWNLOADED and its sha256 matched the receipt - so length AND content are witnessed, not just length. Run 33596898227, verdict CONFIRMED, **246,030 bytes per record (240.3 KiB)**, unchanged from S118. **THE DEFECT THIS THREW UP IS THE MORE VALUABLE HALF:** the witness's first live run reported CONFIRMED against run 33599514613, a push-CI canary, because the default lookup took the NEWEST `calculation_result.json` under the tree. Every step passed, the verdict was green, and nothing on the page named the run - so the tool built to catch numbers that measure the wrong thing produced one. Fixed three ways: the report now NAMES the run and its record count, derived from the S3 keys so a caller cannot mislabel it; the default is a PINNED run id and `newest` must be asked for by name; and the markdown is echoed to the log, because a step summary cannot be read back through the API. **THE STANDING RULE, from the drop-in and now enforced rather than asserted: no per-record figure without naming the two independent quantities it divides.** `verify_ledger_size_witness` REFUSES to emit one when either half is missing, and a test drove out the first violation - with one of the three record counts absent and the other two agreeing, it still emitted a figure. | VERIFIED | `research/kalshi/frankie_raw_mbo_benchmark/verify_ledger_size_witness.py` + 16 tests, wired into `frankie_run_size_report_20260902.yml`. Three outcomes, three exit codes: CONFIRMED 0, CONTRADICTED 1, WITNESS_UNAVAILABLE 2 - and 2 is RED on purpose, because a packet that never landed or landed gzipped produces a legitimate mismatch that must never read as the sink being wrong, and a green run would read as item zero settled. Live evidence: run 33602694575. |
| D70 | S119 (Greg) | **THE PRINCIPAL SEAT IS A DECISION, NOT AN ARTIFACT FIELD - AND FRANKIE HAS NOW ACTUALLY RUN.** D68 stood at ORDERED, NOT YET RUN since it was written, and the mission's own section 10 says a replay completion is not proof Frankie ran. Greg, this session, on who sits in the seat: *"i'm completely fine with you running it and we don't need bedrock. that's not a critical piece for this. this is basically a test piece to run our native boss against so i truly don't care who runs it."* **So the requested principal for run 33605852433 is `claude-opus-5`, not `gpt-5.6-sol`, and that is recorded here rather than left implicit in an artifact field** - section 10 binds to THE REQUESTED principal, so a seat change that lives only in the output is a gate passing on a premise nobody wrote down. **THE REASON THE SEAT COULD MOVE AT ALL:** C2C-014 switched Frankie to Sol to *"stop treating Bedrock Opus 5 as a blocker"* - the blocker was BEDROCK, the box's model path, which failed its preflight. It has no bearing on a session-driven principal, and the mechanism Greg specified is a session over committed files with no API call either way. **WHAT ACTUALLY RAN:** REAL_TIME_FRANKIE over the complete Sunday session, 57,027 records, 43,569 F_LAST-closed groups, 19 cutoffs, all 16,293 averaged rows read section by section; 44 findings; artifact sha256 147c6485...; accepted by `load_principal_artifact` against `evidence_result_hash` cb685e0e..., so the findings are provably derived from that run and not another. All sixteen contract sections are accounted for - twelve carry findings, 4.3 and 4.4 appear in joint findings, and 4.2 and 4.15 are reported ABSENT with cause, which is the mission's absence-is-a-result rule rather than a gap. | VERIFIED | `research/kalshi/frankie_raw_mbo_benchmark/principal_runs/33605852433/` holds the artifact and the emitted prompt, which is the receipt for what he was run against. The emitter (`emit_frankie_spawn.py`) is the piece whose absence was the real reason this had never happened: staging existed, validation existed, nothing rendered the prompt between them. |
| D71 | S119 (Greg asked for the token estimate; the measurement scoped D67) | **KEY NAMES ARE 49.5% OF WHAT THE PRINCIPAL READS, AND ALIASING SAVES A THIRD OF IT - SO D67 IS SCOPED, NOT WRONG.** Greg: *"we need to factor the token reducers into the other days and estimate how much that will save us."* Measured on run 33605852433's real averaged companions, counting every key at every depth: **16,293 rows, 20,023,101 compact bytes, 56 distinct key names repeated 788,868 times, 9,914,257 bytes of key names = 49.5%**; 1-2 character aliasing saves **6,758,785 bytes, 33.8% of the section, about 1.69M tokens on Sunday alone**. Top-level keys are only 7.0% - the other 42.5% is NESTED, inside `stratum`, `declaration`, `value` and `excluded_missing_members`, so a measurement stopping at the outer level misses six sevenths of the cost. The largest single item is the Kaplan-Meier survival object: `at_risk` / `censored` / `events` / `survival` / `time`, each appearing 41,996 times. **D67 SAID ALIASING SAVES ESSENTIALLY NOTHING AND THAT IS TRUE OF WHAT IT MEASURED** - exact member rows, where `book_full` is one enormous nested value that swamps every name around it. The averaged companions are the opposite shape and the opposite answer. **The two statements stand side by side, each scoped: STORAGE is 10.8 GB of ledgers dominated by `book_full`, where aliasing saves nothing and the disk conversation is unchanged; TOKENS are the averaged companions, the surface a principal is actually handed, where aliasing removes a third.** Answering one question with the other's number is how the general claim got made. **THE EXTRAPOLATION IS DELIBERATELY LEFT SOFT.** At Sunday's 0.2857 rows per record the other three days project to ~1.60M rows, ~1.97 GB, ~492M tokens, saving ~0.67 GB and ~166M tokens - but averaged rows scale with STRATA and strata SATURATE, and both measured points (0.2857 and the canary's 0.2597) sit well inside the un-saturated regime, so linear is an upper bound rather than an estimate. **The RATE transfers because it is a property of the row shape; the ABSOLUTE does not, and one weekday traversal is the only thing that settles it.** Naming a confident per-day byte figure now would be the 215-KB-per-record error in a new costume. | MEASURED, NOT APPLIED | `research/kalshi/FRANKIE_MEASURED_TOKEN_REDUCTION_20260902.md`. Not applied to any run: Sunday measured the unreduced size honestly, per Greg's standing instruction on D67. `plan_retrieval` is untouched and is still where D67's real value sits - the skeleton is 137 KB and Frankie read the rows section by section, so the practical cost was never 5M tokens. Aliasing lowers the ceiling; declared refusable retrieval decides how much of it anyone reaches. |
| D72 | S119 (Frankie's own top priority, then the review that found it evadable) | **A NINTH SECTION-6 GATE, AND THE ONLY HORIZONTAL ONE: TWO SECTIONS COMPUTING ONE ESTIMAND MUST AGREE.** Frankie, unprompted: *"If only one of those three ships, ship the gate. The binding fixes one defect; the gate is what would have caught it."* The eight existing gates each check a section against ITSELF - declared denominators, reconciled populations, exact members beneath every summary - and a one-sided book satisfies every one of them perfectly. On run 33605852433, 4.9 returned exactly +/-1.0 on **152 of 154** readings of `(bid-ask)/(bid+ask)` while 4.12 computed the identical formula on the same instrument and day and returned **[0.0116, 0.1109] on 3,454**, never once reaching a bound. **All eight gates passed.** The only thing separating a correct book from a one-sided one was a second computation of the same quantity sitting in the same artifact, and nothing compared them. **The test is DISTRIBUTIONAL, not a range check** - 4.9's [-1, 1] CONTAINS 4.12's range, so only shape separates them. **A code review then found the first version evadable by the exact defect class it was built for:** mean and extreme share BOTH cancel under sign symmetry, so a 100%-degenerate section passes simply by being stratified differently, and the real run only fired because 4.9's strata happened to be sign-pure. Closed by adding the population-weighted **SECOND MOMENT**, `sum(sum_of_squares)/sum(n)` - already emitted on every distribution row and going unread. It does not cancel and does not move under re-stratification: on the real artifact it separates **0.9871 from 0.0031** against a 0.10 tolerance, against the mean's 0.1291 against 0.05. | BUILT AND ARMED | `native_cross_section_agreement.py` + `native_calculation_runner.GATE_CROSS_SECTION`. Five further evasions closed, each reproduced before and after: a populated row with a missing `sum` read as a mean of exactly 0.0; `value["n"]` alone reads zero for RATIO_PAIR and SURVIVAL rows, so a future register entry naming one would be permanently blind; reversed bounds silently disabled the extreme-share test while reporting a clean pass; values outside their own declared bounds passed; and four legitimate one-sided readings could reject a whole run. Register entries are added when a shared estimand is IDENTIFIED, never when a disagreement is found - a register that grows only in response to known defects tests only the defects already known. |
| D73 | S119 (the shape that recurs across the sixteen-defect register) | **SEVEN OF SIXTEEN DEFECTS WERE A CORRECT CALCULATOR THAT NOTHING EVER CALLED, AND EACH REPORTED A ZERO.** The decision clock (`f_last_to_decision_delay_ns`, n=0 with all 43,569 members excluded - no caller ever passed a decision time); `phase_depletion` and `phase_refill` (0.0 at every quantile in all 109 strata, zero exclusions - dataclass fields nothing wrote); the SEARCHED phase duration (min = max = 0.0 for all 91 candidates - entered and left at the same nanosecond); `recurrence_count` (min = max = 0.0 in all 28 strata - `note_recurrence` existed and nothing called it); the 4.13 censored age (n=0 with 21,651 excluded - derived from an exit stamped only by a successor a censored stage by definition never gets); and `ExhaustionCalculator.complete` (never called, so all 91 runways ended censored and the reversal duration everyone read was a censoring artifact). **A measure that ran perfectly and was handed nothing is indistinguishable, in every gate we had, from a measure that ran and found nothing.** Two more were sections BUILT AND DARK: 4.2 did not exist as a module at all, and 4.4's matcher was correct but absent from the runner's section map, the numbering jumping 4.2 straight to 4.5. **A passing verdict is not evidence that a section ran.** | ALL CLOSED, AND REGRESSED AGAINST THE SHAPE | `tests/test_native_replay_driver.DarkSectionRegressionTest` asserts the book reaches 4.2, the mirror matcher is actually called, zero pairs still produce a diagnosis whose reasons sum to the population, and every registered section contributes either a summary or an averaged row. Aimed at the failure mode, not at the two instances. `tests/test_native_candidate_adapter.py` is new - the adapter driving 4.10/4.11/4.12 had NO test file, which is why three of these survived a full run. |
| D74 | S119 (D-8, and the two causes that were both wrong) | **A MEASURE CAN BE CORRECTLY WIRED, CORRECTLY ATTRIBUTED, AND STRUCTURALLY UNABLE TO FIRE.** `same_side_replacement_ratio` had `numerator_total` 0.0 in all 205 strata against a denominator of 44,710 over 18,952 member observations. Frankie offered two causes - *"either same-side replacement is not being attributed to the runway, or the numerator is not wired"* - and **it was neither.** It was scoped to a single F_LAST group, and on this tape depletion and same-side addition are mutually exclusive inside one: **24,617 of 43,569 runways are INDETERMINATE (zero depletion - the pure-add groups) and the 18,952 carrying depletion contain no adds at all.** A maker replacing size it just lost does so in a LATER group. **No amount of tape would have made that numerator nonzero.** The scope was the defect. Fixed with a declared 60-second horizon collected AFTER the runway closes, matching 4.7's window because it is the same physical question of the same tape and two windows in one artifact would make the sections incomparable for no statable reason. Resolves only once stream time passes maturity; a runway whose horizon had not elapsed at stream end is CENSORED and counted, never resolved at whatever numerator it happened to reach - which would measure the end of the stream rather than the market. | BUILT | `native_absorption.AbsorptionCalculator.note_same_side_add` / `advance` / `finalize` / `close_continuity_segment`, plus `ReplacementHorizonTest`. Attribution multiplicity is published at the phase, the section and the traversal, because F-29 caught 4.7 attributing every refill to every pending episode 18.18 times over with the multiplicity invisible in the ratio's name. |
| D75 | S119 (D-12, and the prerequisite that would otherwise have fabricated data) | **FIXING A DEFECT CAN ARM A SECOND ONE THAT WAS INERT UNDER IT - CHECK BEFORE SHIPPING THE FIX.** 4.12 recorded 3,454 stages across 90 paths and **not one was REVERSAL**, while 4.10 recorded 90 of 91 of the same runways as reversed. The cause: the stage for second S was appended and observed BEFORE the phase transition for that second was evaluated, so every stage carried the PREVIOUS second's phase (also why BIRTH held 220 stages against 90 paths), and the terminal phase was entered inside `_close`, which observes nothing and then drops the episode. No stage could ever be filed under a phase that only exists after the last observation. **But `close_segment` ALSO closed via REVERSAL, for episodes merely cut off at a boundary.** That stamp was inert while stages carried the previous phase; the moment stages are filed under the phase they actually sat in, a boundary-censored episode emits REVERSAL stages for a reversal that never happened - **manufacturing exactly the evidence the original defect made unreachable.** Censored episodes now end in a registered DISCOVERED phase, `CENSORED_AT_OBSERVATION_END`, deliberately absent from `TERMINAL_PHASES` so `complete` refuses it. | BUILT | `native_candidate_adapter.advance` / `_close`, `PhaseAttributionTest`. Deciding the phase before observing the stage is not lookahead: the third consecutive opposite reading IS the evidence and it arrives at that second. |
| D76 | S119 (Greg, on the D68 question) | **FRANKIE DOES NOT HAVE TO FIND SOMETHING TO DROP. "KEEP EVERYTHING" IS AN ANSWER, NOT A FAILURE.** Greg, verbatim: *"and frankie doesn't have to find something that we should drop. if he says to keep everything then we keep it."* The measurement already points there: **all sixteen calculations combined are ~1.5% of the bytes and the most expensive, `replenishment`, is 0.6%**, so dropping a calculation saves essentially nothing. The only thing with mass is the per-group `book_full` snapshot at **93.47%**, which Greg has already ruled stays. Pairs with D60 - a question framed as "what can we drop" pressures the answer toward a casualty, and this programme has already paid for exactly that. | STANDING | The D68 report is to be framed as an open question with keep-everything as a first-class outcome, never as a hunt. Greg's plan: rerun the canary AND Sunday on the corrected code, so there are **two independent slices** to judge against before the weekday run. |
| D77 | S119 (measured, after a subagent swept the shared tree) | **PARALLEL AGENTS MAY NEVER TOUCH GIT STATE IN A SHARED WORKTREE.** A subagent ran `git stash` to measure a baseline and reverted every session's uncommitted work - my 4.2 wiring plus four agents' output - in one command. Recovered in full from the stash and nothing was lost, and the agent reported it against itself. A second agent's in-progress file was then swept into an unrelated session's commit. **The parallel fan-out was still worth it** - six defects in disjoint sections closed concurrently - but the isolation is the file boundary only, and the tree is shared. Agents are given an explicit file allowlist, told to report wiring rather than perform it in shared files, and must not run any git command that mutates state. | STANDING | Prompt discipline, plus scratchpad recovery copies. The alternative - `isolation: worktree` per agent - was not used and should be next time. |
| D78 | S120 (measured, applying the S119 token measurement) | **A REDUCER THAT CHANGES THE ARTIFACT'S SHAPE IS OFF BY DEFAULT, AND THE ARTIFACT DECLARES WHICH FORM IT IS IN EITHER WAY.** Key-name aliasing removes about a third of the averaged companions - the read surface the principal is handed - and nothing of the ledgers, where `book_full` swamps every name. It is applied as a SERIALIZATION transform: the gates run on the unaliased rows `_averaged_companions` returns, so no gate, reconciliation or verdict can move under it. `key_alias_form` is stamped as positively when PLAIN as when ALIASED, because a field present in only one form makes its absence ambiguous - an old artifact and a plain one would read the same. The legend travels IN the same layer as the rows it decodes, which is the structural answer to a mismatch that cannot be detected from the rows alone: a foreign alias code is indistinguishable from a name that was never aliased, and a guard claiming otherwise would be a guard that can never fire. | STANDING | `native_key_alias.read_averaged_rows` is the only supported way to read the rows; a direct lookup on an aliased layer succeeds, returns the right count, and reports every row under `None`. Pinned by tests that both forms give the same verdict, failed gates and reconciliation, and that the emitter's per-section table is identical in both. |
| D79 | S120 (Greg, on the reruns) | **SUNDAY RUNS IN THE CONFIGURATION THE REMAINING DAYS WILL RUN IN, NOT IN THE ONE THAT IS EASIEST TO COMPARE.** Greg: *"Run Sunday with it on so we can measure it how it's going to run for the remaining days."* The instinct to hold the rerun byte-comparable to run 33605852433 was overruled, and correctly: a comparison against a configuration nobody will run again measures a configuration nobody will run again. The two changes do NOT confound, which is what makes this affordable - aliasing changes `layers.averaged_companions` in the result JSON and the event-driven change points change the response section's rows on disk, so they are separately attributable and there is no net to disentangle. The canary keeps the plain baseline. | STANDING | `report_ledger_size` reports the read surface separately from the disk tables and measures the aliasing saving by decoding the run's own rows rather than extrapolating. |
| D80 | S120 (measured, wiring 4.16) | **AN EVENT-DRIVEN EMISSION IS TRIGGERED BY AN EVENT, NEVER BY THE CLOCK.** 4.16's `observe_change_point` was correct and had NO CALLER ANYWHERE - the eighth instance of the shape S119 closed seven of, and it was found by wiring rather than by any gate. The contract requires emission "at every available event-driven change point AND at versioned fixed H+N horizons", so firing once per second would collapse change points into a fourth fixed cadence and retain (open tracks x seconds) under D60 to record readings the horizons already carry. The trigger is the observable state's fingerprint moving - price, both depths, depth scope, signed flow - which are exactly the quantities that build a channel reading. **AND A FIRING TEST IS NOT OPTIONAL:** the small driver fixtures open ZERO 4.16 tracks, so every assertion about change points in them is vacuously true; the test asserts tracks_opened > 0 FIRST, then holds the tape constant and flips only the flag. | STANDING | `ChangePointsActuallyFireTest` in test_native_replay_driver, on the candidate-detecting fixture. Unfed, the section declares NOT_FED_BY_THE_TRAVERSAL rather than reporting a zero. |
| D81 | S121 (Greg, 2026-09-02) | **THE PRINCIPAL RECEIVES THE COMPLETE RAW MBO FOR THE DAY BEING RUN, DELIVERED AS IT ARRIVES IN REAL TIME, AND COMPUTES THE SIXTEEN SECTIONS HIMSELF.** Greg, verbatim: *"he gets every record of every field for Sunday, the date and time we are running. and Monday will get the same thing for Monday and so on."* and *"this has to exactly mimic how it's going to come in rt."* **THE RULE:** F_LAST-closed native event groups in `ts_recv_ns` causal order, each visible only once its F_LAST record has been received, never ahead; every field of every row passes through untouched (D60); the principal computes the sixteen calculation-contract sections from that stream; the runner captures, retains and proves nothing was dropped, and its calculations are NOT his evidence - `calculation_result.json` is retained for the section 6 gates and may be compared AFTER he files. **WHAT WAS WRONG, MEASURED:** mission section 5 said *"The runner calculates; you interpret"* while section 3 said *"At every F_LAST-closed group, receive and study the complete lawful envelope"* and 55 of the registry's 99 layers carried `principal_route: CAUSAL_GROUP_STREAM` / `activation_stage: EACH_F_LAST_CUTOFF` - and every session built to section 5. `validate_causal_group_delivery_receipt` (`GROUP_DELIVERY_SCHEMA`) had NO caller outside its own module and its tests - the same shape S119 closed seven instances of. Run 33605852433's principal read 16,293 averaged rows and zero exact ones because the 10.6 GB member ledger stayed on the box while he received the ~34 MB result. **THE ROUTE:** a session holds no AWS credential, so a workflow presigns the pinned run's objects for seven days and publishes a manifest artifact; `fetch_frankie_ledgers` downloads, verifies the gzip length against S3's ContentLength and the plain bytes against the box's PLAIN_SIZES / PLAIN_SHA256SUMS, and writes a receipt in which any mismatch is a refusal; `CausalGroupStream` delivers the ledger forward-only and byte-identical with peek / seek / rewind refused, attaches lifecycle and legacy rows only by their OWN clocks and withholds-and-counts the rest (the lifecycle ledger carries no uniform availability stamp and the mirror rows carry none - F-20), and receipts every delivery through the registry's validator; the emitter refuses to spawn without a receipt in which every exact ledger is VERIFIED and bound to the run's sink sha256; the staging gate refuses NOT_READ on any artifact citing a delivery receipt. | BUILT AND WIRED; NOT YET RUN AGAINST THE SUNDAY LEDGERS | `native_causal_stream.CausalGroupStream` (the only caller of `validate_causal_group_delivery_receipt`); `emit_frankie_spawn._load_delivery_receipt` (`--delivery-receipt` required, every ledger VERIFIED, delivered sha256 equal to the run's sink receipt); `native_staging.load_principal_artifact` (NOT_READ refused when `delivery_receipt_sha256` is cited); `fetch_frankie_ledgers` (LENGTH_MISMATCH / SHA_MISMATCH / MISSING are refusals); `.github/workflows/frankie_ledger_delivery_20260902.yml`; mission sections 2 and 5 rewritten and re-pinned in KNOWLEDGE_MANIFEST_20260828.json. Tests: test_native_causal_stream (27, including the traversal's real ledgers), test_fetch_frankie_ledgers (14), test_frankie_ledger_delivery_workflow (12), DeliveryReceiptGateTest, DeliveredLedgersMustBeReadTest. |
| D82 | S121 (measured S120, rule stated 2026-09-02) | **A LAYER'S EVIDENCE BINDS TO THE FILE THAT CARRIES IT, NEVER TO A DOCUMENT THAT DESCRIBES IT.** Measured at the S120 close: **91 of the 99 registry layers bound their evidence hash to one markdown document**, `research/kalshi/NG_EXHAUSTION_FRANKIE_DATA_FEED_INVENTORY_20260824.md`, so the pre-call gate proved that the document was unchanged and nothing about ingestion or delivery - and **2 of the 75 A-clean-applicable inputs reached the principal** (mission, contract). A gate that reads status off a policy is not a gate. **THE RULE:** every static knowledge layer binds to its real KEEP file(s) from the source-file inventory (`NG_EXHAUSTION_FRANKIE_SOURCE_FILE_INVENTORY_20260824.md` sections C, D, E, F and the brain), every causal-stream layer to the row field or ledger that carries it, and a layer whose only source path is a descriptive document computes as `BOUND_TO_INVENTORY_DOCUMENT` - never AVAILABLE, never DELIVERED. The canonical input list is the feed inventory PLUS the source-file inventory (149 paths); the registry had never bound to the second. "Done" for a delivery is a row from a real run naming the layer, the carrier and the receipt hash. | STANDING | S121: the 14 knowledge layers rebound to their KEEP files by script (merged 8b47df6); the crosswalk names BOUND_TO_INVENTORY_DOCUMENT and computes status from receipts (merged 9f38038); the 63 KEEP files are artifacts of the knowledge manifest (merged 5b7b3db). Still to land: the knowledge receipt and the gate call in emit() (F-22 slices 3 and 5, F-24), and F-27 (the crosswalk's static bound flag). |
| D83 | S121 (Greg, 2026-09-02, twice) | **ZERO HARDCODED TIME INTERVALS FOR ANYTHING. EVERY TIME IS DERIVED FROM THE ACTUAL EVENTS ON THE CLOCKS BUILT FOR IT, AND THE CLOCKS ARE WIRED INTO FRANKIE.** Greg, verbatim: *"hardcoded windows we do not want these! Again i say it! All times are derived by their actual events, prebirth findings, h times if we don't find them in prebirth, etc all by using the clocks we made just for this reason. There should be zero hard coded time intervals for anything."* and *"The clocks need to be wired into Frankie."* Measured before the ruling: `ACTIVITY_WINDOWS_S = (1, 5, 20, 60, 300)` at `research/ng_exhaustion_mbo_v4_state_adapter_20260820.py:51`, feeding `activity` and `activity_full` on every member row through `native_full_capture_adapter.py` and rebuilt on resume by `mbo_resume_state.py`; 4.16's versioned `HORIZON_SETS` in `native_response.py`; 4.11's H ladder; `WARMUP_SECONDS = max(ACTIVITY_WINDOWS_S)` in the Step-1 shards. The registry's seven causal clocks (event, receive, event_known_by, feature availability, prospective discovery/confirmation, model evaluation, lock) had no producer mapping and the row carried five clock fields. **THE RULE:** no fixed-seconds window, ladder or horizon anywhere in the principal's path. Activity is measured from event anchors on named clocks (since the last trade, the last touch change, the last reset or snapshot, session open, the last same-side F_LAST, and over the last N F_LAST-closed groups where N is a count of events); prebirth lead is measured from the prebirth finding to birth; H+N is the observed elapsed time from birth to first recognition when no prebirth finding existed; 4.16 horizons mature on realized responses (D80), never on a ladder. **Each of the seven clocks gets a producer and reaches him by its registry layer id**: five in the stream (event, receive, event_known_by, feature availability, prospective discovery/confirmation), model evaluation stamped by the spawn request at the cutoff he is invoked, lock time by his own FIRST_LOCK entries. **The removal of the fixed-interval DERIVED blocks is Greg's own instruction, which is the discussion D60 requires**; raw fields are untouched, and D61 holds - the hash-locked adapter is wrapped, never edited. | DECIDED (build in progress S121) | S121: the seven clocks by registry id on every member row and GroupDelivery with the availability chain checked (merged a540b2d, F-26 slice a); the windows-and-clocks packet and build spec (merged b3f31eb); no fixed interval removed yet - F-26 slices b-d and F-20 continue on persona/s121-wire-clocks-windows. |
| D84 | S121 (Greg, 2026-09-02, after four personas were killed mid-slice) | **A PERSONA ON A LONG BUILD PUSHES ITS OWN BRANCH AFTER EVERY COMMIT. A SAVE POINT THAT EXISTS ONLY IN A CONTAINER IS NOT A SAVE POINT.** Greg, verbatim: *"we need to salvage what we can from the agents before they end"* and *"we need to have push spots when agents are doing long builds."* **THE INSTANCE:** four test-engineer personas running in isolated worktrees (D77) were terminated together by the session rate limit. Everything they had COMMITTED survived in their worktrees and was recovered - knowledge 2 commits, crosswalk 1, outputs 7, windows 2 - and their uncommitted slices (a 19-line and a 449-line tests-first file) were salvaged by the coordinator as labelled RED commits; but until those branches were pushed, a lost container would have taken all of it, and S120 had already lost three personas' worth of uncommitted work the same way. **THE RULE:** every persona commits at every slice AND pushes its own branch (`git push -u origin persona/<name>`) immediately after each commit; it never pushes any other branch; the coordinator merges green commits into the development branch as they land, not when the persona finishes, and pushes the development branch after each merge. A persona's final report names every sha AND confirms each was pushed. Pairs with D77 (isolation is the file boundary) and D34 (nothing lives only in a container). | STANDING | S121: applied end to end - four first-round branches and three wiring branches pushed after every commit; the development branch pushed after every merge; two rate-limit kills and one interrupt lost nothing that was committed. |
| D85 | S121 (Greg, 2026-09-02) | **THE A-MEMORY ARM IS RETIRED. THERE IS ONE ARM, A-CLEAN, AND NO MEMORY PACKAGE.** Greg, verbatim: *"We aren't going to worry about a memory anymore. This has been too much of a struggle and we aren't going to worry about it."* Context: the A-memory arm's prior lessons package (`a_memory_prepare_20260828.MEMBER_MANIFEST_SHA256` b487acfb...) and its two positive-knowledge capsules derive from the PRIOR REDUCED run 32851909748-1, the run on the wrong data whose RT output was passed one-way to the Forecaster (F-21); re-deriving them would have cost another correct run before the arm could even start. **THE RULE:** every spawn, gate, receipt, crosswalk and render targets `A_CLEAN`; nothing is built, wired or run for `A_MEMORY`; the registry's `a_memory_overlay` layers, the knowledge manifest's A-memory profiles and capsules, and the A-memory launch workflow remain as inert records until their removal is discussed separately (D60 applies to the removal, not to the retirement). The RT-to-Forecaster one-way handoff, first lock and context manifest (the V2 workmode schemas) are still the machinery Frankie's outputs flow through; they are re-fed from A-clean. | SUPERSEDED by D86 the same hour (the retired arm is A-clean, not A-memory) | F-21 marked SUPERSEDED with this decision; the S121 wiring briefs name A_CLEAN as the only arm; a later registry/manifest cleanup is its own decision. |
| D86 | S121 (Greg, 2026-09-02, correcting D85 within the hour) | **ONE ARM, AND IT IS THE MEMORY ARM: A_MEMORY RUNS, A_CLEAN IS RETIRED.** Greg, verbatim: *"Wait. We are only going to do memory and not clean but still just one arm."* D85 had read his earlier *"We aren't going to worry about a memory anymore"* as retiring the memory arm; the correction is that the memory arm is the one that runs, and the old memory is what we stop worrying about. **THE COORDINATOR'S READING OF 'MEMORY', STATED SO IT CAN BE CORRECTED IN ONE LINE:** memory is Frankie's OWN lawful carry from day to day - his frozen prior-day outputs (the append-only ledgers, the knowledge-verification verdicts, the first locks) are the next day's memory, exactly as real time works and as the brain layer `lawful_prior_session_carry` already names. The prior reduced run's lessons package (32851909748-1, the wrong data; F-21) is NOT his memory and does not go in; on the first day the memory is SEEDED, never faked (see the day-one clause below). **THE RULE:** every spawn, gate, receipt, crosswalk and render targets `A_MEMORY`; the three `a_memory_overlay` layers are rebound from the wrong-data package to the prior day's output receipt (absent on day one, declared); A-clean's overlay, profiles and workflow remain inert records until their removal is discussed (D60 applies to the removal). The Sunday re-run dispatches the A-memory launch workflow. The RT-to-Forecaster one-way handoff, first lock and context manifest (the V2 workmode schemas) are the machinery his outputs flow through, re-fed from this arm. **GREG'S RATIONALE, verbatim, minutes later:** *"In rt, Frankie isn't going to be flying blind so I'm retiring clean because of time."* That settles the reading of 'memory': in real time he carries what he learned the day before, so the arm that runs is the one with memory, and the memory is his own. **DAY ONE IS SEEDED, NOT EMPTY (Greg, minutes later, verbatim):** *"Just give him the canary stuff from the past runs to start it and it will build from there"* and *"And the other stuff too. I'm not picky about this."* So his starting memory is every committed output of the past runs - the principal run 33605852433 (findings, report, assessment, prompt), the prior workmode run 32851909748-1 (all its receipts, handoff, lock and outputs, LABELLED as the reduced wrong-data run), the A-clean and A-memory positive-knowledge capsules and their source reports, the S119 measured knowledge - each file listed with sha256, bytes and a provenance label (run id, data surface, pre- or post-correction), every lesson UNVERIFIED until he verifies it against the stream. Keep-everything applies (D76); nothing is filtered for him. From day two the memory is his own prior-day outputs plus the seed. | STANDING | S121: the knowledge manifest routes the 63 KEEP files to the A_MEMORY profiles and retires the wrong-data external binding from them (merged 5b7b3db); the seed memory A_MEMORY_SEED_20260902.json and the a_memory_overlay rebind are F-22 slice 2, not started. |
| D87 | S121 (Greg, 2026-09-02, the third time) | **NOTHING IS WRITTEN TO THE SESSION SCRATCHPAD OR /tmp. NOT A FETCH, NOT A LOG, NOT A TRANSIENT MEASUREMENT FILE.** Greg, verbatim, S113: *"no more scratchpad. It's in the sop"*; S121: *"we don't use scratchpad anymore. that's a rule in your md file"* and, hours later, *"No scratchpad!! I keep saying it!!"* The rule had been read as D34's narrower form - no committed artifact may NAME a scratchpad path - while the harness's own instructions still steer every session and every persona to the scratchpad for temporary files, and the first-round personas dumped measurement files and a pytest log there. **THE RULE:** the scratchpad and /tmp are not used at all. A transient file, if one is needed, lives under the repo's gitignored `data/` (D34: the only disposable local place) and is deleted when done; a test uses `tempfile` inside the test only. The coordinator checks the scratchpad directory is EMPTY at every close and deletes what it finds after confirming no running persona owns it. Every persona brief carries the rule; every persona report confirms it wrote nothing there. | STANDING | S121 close: scratchpad checked empty (0 entries); every persona report confirmed nothing written there; the rule is in the CLAUDE.md D34 bullet and every persona brief. |
| D88 | S122 (Greg, 2026-09-03) | **THE SEED IS THE LAST RUN, THE CLOCKS AND WINDOWS COME FROM THE STEP-1 FILES, ONLY MEMORY RUNS, AND ONE CANONICAL LAUNCHER CARRIES EVERYTHING.** Four rulings in one message, verbatim: *"if you can't find the canary just use something from the last run. like i said I'm not picky about that and it's wasting time."*; *"look in step1 files and outputs for all of the stuff in clocks, winds."*; *"we aren't running clean anymore only memory."*; *"there is supposed to be a canonical file or a runner, launcher file with everything."* **WHAT EACH CHANGES.** (1) The day-one seed (D86) is built from the LAST RUN's committed outputs - principal run 33605852433 (findings, report, assessment, prompt) - plus every other committed past-run output (the workmode run 32851909748-1 labelled as the wrong-data run, the capsules, the source reports, the S119 measured knowledge); no canary output is committed and none is hunted for. Every lesson UNVERIFIED, provenance-labelled, keep-everything (D76). (2) The clock and window designs on Frankie's path are taken FIRST from the Step-1 files (`research/ng_exhaustion_mbo_2day_full_mbo_step1_20260825.py`: per-event causal_clocks, event_known_by as the next confirming record's receive time, max-of-contributing availability, the post-lock outcome_availability clock) and the step1 modules and outputs, reused by import or faithful port with the source cited; Step-1 RESULT values and answer-key surfaces stay SEALED to him (the design is reusable, the outputs are not deliverable). (3) D86 restated as a standing fact: the A_MEMORY arm is the only arm that runs; nothing is built, defaulted, exemplified or dispatched for A_CLEAN; its overlay, profiles and workflow remain inert records until F-28 is discussed. (4) There is ONE canonical launcher, `native_a_arm_launch.py`, and it carries everything - the seed memory, the seven clocks, change points on, aliasing on, event-anchored activity, the spawn staging - as its DEFAULTS, so the A_MEMORY launch workflow calls it with nothing to remember; a flag exists only to turn a default OFF for a declared comparison. **MEASURED THE SAME HOUR:** the A_MEMORY launch workflow still hash-binds the wrong-data package as memory (`EXPECTED_PRIOR_MEMBER_MANIFEST_SHA256`, the `external:` identities in the launcher, mission lines 46-51) and its last three push canaries failed on that path (runs 33236353037, 33300298751, 33304621728, Aug 29-30); it carries neither the aliasing nor the change-point flags the retired A-clean workflow has. Under this ruling the seed replaces the package and the flags become defaults. | STANDING | S122: the three wiring personas' briefs carry all four rulings; the seed is built by script from the last run (F-22 slice 2); the clocks persona starts from the Step-1 files (F-26); the canonical launcher and the A_MEMORY workflow are the coordinator's Item Three. |
| D89 | S123 (Greg, 2026-09-03) | **THE RUN IS LAST, NOT FIRST - NOTHING IS DISPATCHED UNTIL THE OUTSTANDING WORK IS DONE.** Greg, verbatim: *"we are not doing canary until all of this other stuff is done first."* This SUPERSEDES the ordering `DROP_IN_S123.md` opened with, which said run first and *"do not open another build item before a run exists"*. The reasoning it replaces is still sound in one respect and wrong in the other: a run is indeed the only thing that turns S121's and S122's wiring from claimed into measured (F-31), but a run dispatched over UNFINISHED wiring produces one more set of before-numbers, and the whole cost of run 33630348943 was exactly that - S121 code against pre-census ledgers, every number in the feed record a before-number. **A run is worth dispatching once, on finished code.** So the gate is: the spawn gate called from `emit()` (F-24), the knowledge read gate pointed at `validate_knowledge_use`, an honest fixture, and the three no-rot items (F-34, F-35, F-36) land FIRST; the canary is dispatched after, and the full roster on the box remains a separate spend that is Greg's alone. **No workflow dispatch of any mode is a session's opening move while F-24 is open.** | STANDING | S123: `DROP_IN_S123.md` item zero and item one rewritten to carry the order (the drop-in is a LIVE expectation, so it is corrected rather than stamped - F-35's taxonomy); `CODEX_TASK_S123.md` section 0 rule 10 forbids dispatching any workflow; the canary was NOT dispatched this session. Recorded honestly, because the first draft of this line claimed more than was checked: the seal step was verified passing by execution at the S122 close, but the workflow's `REQUIRED_STARTING_TIP` ancestry gate is NOT verifiable from a container clone - it is shallow, grafted at 140 commits, so `merge-base --is-ancestor` returns a FALSE NEGATIVE that reads exactly like a blocked run. It resolves only in CI, which checks out at `fetch-depth: 0`. The check was run, came back inconclusive, and is reported as inconclusive rather than as a defect - the S113 nonconformance shape is reporting a verification that did not happen. |
| D90 | S123 (Greg, 2026-09-03) | **BUILD THE DAY-OVER-DAY LOOP, PROMOTE AUTOMATICALLY, KEEP A VETO - AND FRANKIE ARGUES FOR HIS OWN CARRY.** Greg, verbatim: *"I'm going to have him build the loop and close this. we'll do an automatic promote but we can veto it. it's only going to be these 4 days. if it's problematic we can change it. we'll have Frankie print this out also and make a convincing argument for us to allow it."* Closes the gap measured this session: **no workflow ingests run outputs into A_MEMORY and none ever did.** `frankie_native_knowledge_refresh_20260828.yml` is a determinism gate whose `git diff --exit-code` step makes ingestion impossible BY DESIGN; `build_a_memory_seed.py` is the only path from a past run into the seed and NO workflow calls it; no workflow references `principal_runs/`, which holds exactly ONE run; and the launch workflow commits nothing, it PUTs to S3 and returns. `8039f39` removed the wrong-data PACKAGE step, not an accumulation loop - there was never a loop to break, so this is a build item and not a regression. **Three things the ruling fixes in shape, not just in scope.** (1) **The promotion is AUTOMATIC**, which is a deliberate rejection of the reviewed-PR pattern the refresh workflow already carries: a carry that needs a human to merge it is not a day-over-day carry. (2) **The VETO IS A LABEL, NEVER A DELETION** - D60 and D76 govern, and the precedent is already in the seed, where the wrong-data run 32851909748-1 sits AS the wrong-data run, labelled, never filtered; a vetoed carry stays with its status and stops being SERVED, so the veto is auditable and the record survives it. (3) **THE CARRY ARGUES FOR ITSELF**: Frankie emits his own case for promoting each lesson, so the veto decision has evidence in front of it rather than a diff. **THE RISK IN (3), NAMED SO IT IS DESIGNED AGAINST: if the gate is "convincing", the thing being optimised is RHETORIC.** So the argument is STRUCTURED EVIDENCE - per lesson, what it claims, the stream evidence that verified it (D86: he verifies every lesson against the stream, and this is where UNVERIFIED becomes VERIFIED), what would falsify it, and what changed since the prior day. **An unpersuasive argument is not grounds for veto; an UNFALSIFIABLE one is.** **Scope: the roster, which `raw_mbo_source_manifest` pins at exactly four objects - so the bound is DERIVED from the manifest and the number four is never typed** (D60). Greg: *"if it's problematic we can change it."* | STANDING | S123: registered F-37 and specified as Task I of `CODEX_TASK_S123.md`. The loop's parts already exist and are unwired - `build_a_memory_seed.py` (membership derived by rule, so a newly committed output lands on the next --write), `register_a_memory_knowledge.py`, `rebind_registry_knowledge_layers.py`, `refresh_native_frankie_knowledge.py` - documented in the seed builder's docstring as a manual four-command sequence that nothing calls. |
