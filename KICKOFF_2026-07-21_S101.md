# KICKOFF - S101 (G12 one-shot blind FIRST - the gate is CLOSED; then G13 prep, then smalls)

**FIRST ACTION, BEFORE ANYTHING ELSE: SWITCH TO THE BRANCH IN THIS BOX.** The harness assigns
you a different auto-named branch - it is NEVER the work. Run:
`git fetch origin claude/ng-coach-agent-loop-5ha5bf && git checkout -B claude/ng-coach-agent-loop-5ha5bf origin/claude/ng-coach-agent-loop-5ha5bf`
The tip message must begin "S100 close-out".

**THE AWS KEY: USE THE PAIR WHOSE SECRET BEGINS `tx`** (ID `AKIAYI6JDCBVLKYQGLMH`, account
...4170). It is GOOD - never re-diagnose it. If STS says InvalidClientTokenId, that is the cloud
container's placeholder env vars overriding `~/.aws/credentials` - run AWS commands via
`bash -lc` or pass creds explicitly. See CLAUDE.md "AWS KEY" section.

Read FIRST, in order: `SESSION_HANDOFF_2026-07-20_S100.md` -> this kickoff ->
`research/kalshi/DATA_GATE_S98.md` (closure status) -> `research/kalshi/knowledge/ng_brain.json`
(**s101.2** - Tier 3 doctrine merged: supersession, one-voice-per-target, scoring split
EFFECTIVE THIS RUN, squeeze doctrine, blind-run hygiene).

Branch: `claude/ng-coach-agent-loop-5ha5bf` (already switched per the top of this box).

## WHY THIS SESSION IS FRESH (doctrine_tier3.blind_run_hygiene)

S100 built feeds over the walk window - it is CONTAMINATED as a referee. THIS session carries
only this box + the brain + decision_state into the blind run. Do NOT read S100's tapes,
renders or measured values for the G12 window before the blind completes.

## ENV ON-RAMP (before anything)

`pip install boto3 databento matplotlib pandas openpyxl requests` ->
`platform_sync.py pull` the store prefixes (the harness needs local caches; see S100 handoff
item: eia_surprise.json + storage_consensus path conventions) ->
`python research/kalshi/forecast_harness.py --selftest` PASS (23 blocks) ->
`audit-joins` 0 violations / 101 days / 14 classes.

## JOB 1 - G12 (Sun Feb 1 - Fri Feb 13 2026), the one-shot blind

1. ROLL CHECK by a SUBAGENT returning ONLY the roll date + spread (the S97 protocol fix).
   Basis: NG.n.0 (the G11 basis; v0 whipsaws through expiry weeks).
2. Spawn the blind agent: brain s101.2 + decision_state ONLY (Feb 1-13). Scoring split is LIVE:
   the day-book is the primary product; block lean = regime-state descriptor.
3. Score per-event; render PRINTED to Greg; refine ONLY after his review; lessons merge before
   G13. Blind wall discipline unchanged.

## JOB 2 - G13 (Sun Feb 15 - Fri Feb 27, the SQUEEZE TEST) after G12's refine merges

Feed I wired; feed M delivered; squeeze doctrine ACTIVE inside squeeze_watch (bands void; never
short the squeeze leg on band logic). The gauntlet: GSCI roll Feb 6-12, BCOM 9-13, bidweek
23-27, opex Feb 24, expiry Feb 25.

## JOB 3 - smalls (when adjacent)

- pyth_collector SUNSET (deadline Jul 31; the workflow lives on the TRUNK branch
  `claude/kalshi-s79-kickoff-ij8t9o` - get Greg's explicit go before touching the trunk).
- CL Mondays FREE REDECODE on the box (window closes ~Aug 12-14 - do not miss it).
- Jul 3 holiday-vs-tape verify; ISO day-ahead clock (verify each ISO's published deadline);
  non-Thursday 10:00-10:30 ET characterization; apr26+ STEO vintages (measure release dates).
- Feed I phase ii when reached: the vendor Black-76 tutorial pattern
  (`vendor/databento_options_iv_black76_example.py`) on SETTLE prices, ON/LNE roots.

## ROADMAP (Greg, 2026-07-20 - looking ahead, NOT in the current build)

Kalshi first; THE MOMENT it works, POLYMARKET wires in as the third lane (free public CLOB API
- fits free-first; needs its own feed-L/M analog: market discovery, microstructure, fee/fill
reality; context-only until then per doctrine). OPTIONS TRADING follows fast (feed I surface +
the vendor Black-76 pattern exist; the gap is execution mechanics - margin, greeks-at-entry,
options-book spread reality). Neither blocks G12/G13.

## THE PLAYBOOK IS NOT FINAL (Greg, 2026-07-20, standing)

There is NO "final" playbook. What closed in S100 is the DATA gate - the inputs G12/G13 needed.
The brain grows with every group: blind -> refine -> lessons merged BEFORE the next group, all
the way through the remaining ~12 months of the walk (G12, G13, G14+ into the KXNATGASD-era
tape), then live-forward. s101.2 is a version, not a destination; open_frontier carries the
problems it knows it has not solved. Any language anywhere implying the playbook is finished is
wrong by doctrine.

## PENDING GREG

TWO_COACH_SPEC_S100.md approval (Tier 3 item 6 - printed S100, not yet nodded); repo-private
TODO; dashboard session coordination (`DASHBOARD_HANDOFF_S100.md` is its landing pad).

## OPS NOTES

Greg runs AUTO-APPROVE: hard pause after questions; short turn-breaks between groups. Background
tasks ride through interrupts (TaskStop only). Live-gateway work = AWS boxes via SSM (container
cannot reach raw TCP). Docs sites: crawler-UA fetch. Selftest pins = measured-then-pinned, keep
pinning. Keys are SECRETS; the repo is PUBLIC.

## GUARDS (unchanged, non-negotiable)

PER-EVENT, never pool/average as a conclusion; drift is a DESCRIPTOR; general rules only, no
point-fitting; blind wall decision-time only (storage strictly-prior, MOS as-of + CYCLE
availability wall, COT publication-keyed, cash knowable_from, STEO release-date, 930 period+2,
nuclear period+1, options session-prior, freeze cycle-wall); one-shot canonical FROM A FRESH
SESSION; refine per group, renders PRINTED first, lessons merged BEFORE the next group;
Sunday-start/Friday-end; refined NEVER overrides blind until forward evidence (supersession =
single ownership, clean handoff, retired rules = caution/watch only); rolls marked never traded;
flips: C1+C3+C4 confirm (C2 scoped), NEVER front-run; thin AMPLIFIES delivery, DAMPS holds;
net-of-fee maker AND taker; git = CODE, S3 = DATA; NG != WTI; weather forecaster HANDS OFF;
provisional-until-live; no emojis.
