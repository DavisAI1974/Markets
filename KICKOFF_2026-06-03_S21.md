# KICKOFF — Session 21 (Markets) — 2026-06-03

Start here for the next Markets session. Branch: `claude/crypto-trading-platform-plan-MpqwG`
(DavisAI1974/Markets). Read `SESSION_HANDOFF_2026-06-03_S20.md` first, then this.

## 0. Session bootstrap (automatic)
The SessionStart hook (`.claude/settings.json` -> `scripts/session_start.sh`) now:
- installs numpy/scipy/scikit-learn + PySR + Julia,
- materializes the real collector bins from the `data/*` branches into gitignored `realbins/`.
The `.claude/settings.json` Bash allowlist should stop the constant permission prompts.
(If prompts persist, the session permission mode may need to be relaxed by Greg.)

## 1. FIRST PRIORITY — unblock the chem dipole
Add `DavisAI1974/Basic_equations` to this session's allowed repositories (also consider
`od-engine`, `evolution`). Then:
- Port `_markets_algebraic_dipole.py`, `s13_chemistry_residual.py`,
  `s12_coupling_decomposition.py` VERBATIM into `odcore/` (mark which reconstruction each
  replaces) and DIFF against the reconstructions already in `odcore/`.
- CRITICAL: capture the EXACT channel construction — what are H_a and H_b (entropy of WHAT
  series), over what window, with what normalization. S20 found that real buy/sell entropies
  are near-symmetric (-> trivial identity, c~=0), so the original recipe almost certainly uses
  a different channel/preprocessing. This is the key to reproducing the chem QUADRATIC dipole.
- Re-run the dipole + the H_a>H_b directional predictor on the real bins; validate with
  walk-forward + the tautology null (already built in `odcore/validation.py`).

## 2. Then continue the build (plan: /root/.claude/plans, mirrored here)
- Backend: add `/api/coupling_matrix`, `/api/strength/{asset}/{venue}`, `/api/leadlag/{asset}`,
  `/api/dipole_signals`, `/api/decoupling` to `backend/api_server.py`; flow OD fields through
  the existing `/api/signals` + `/api/stream` (a `CouplingStore` polled in `_polling_loop`).
- Frontend: `CouplingMatrix.jsx`, `StrengthOverTime.jsx`, `LeadLag.jsx`, `DecouplingFeed.jsx`.
- Executor: wire `odcore.generators.make_od_generators()` into `adaptive_backtester`; add
  `odcore.sizing` into `executor/risk.py`/`executor.py` (replace fixed size); add circuit-
  breaker gates; env-gated ccxt adapter (PaperExchange stays default).
- Housekeeping (move-don't-delete, grep-prove zero callers): consolidate the 5 duplicate
  load_bars into `odcore/io.py`; default DEMO_MODE off; keep the PELT chunker but unwire it
  from the signal path.

## 3. Honest status to carry forward (do NOT relitigate without new data)
- The OD coupling structure on real crypto is REAL (equal-entropy attractor cos 0.9996;
  cross-venue z up to 580; cross-venue reversion tautology z=3).
- But NO net-of-cost edge yet from the unblocked pieces (signals < 3bps cost). The chem
  dipole (Piece 1) and/or cheaper execution are the paths to real alpha. Don't curve-fit a
  positive backtest; promote only what beats baselines net of cost under walk-forward.

## 4. Greg's open questions (BUILD_QUESTIONS.md)
- Maker-fee/rebate venue or tick data? (would change the cost math).
- Want one labeled known-law fixture to prove PySR rediscovery? (only synthetic; currently omitted).
