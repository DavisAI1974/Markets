# S51 KICKOFF — the rebate-venue quantification + SOL-first deploy path

Read first: `S50_CAPACITY_FINDINGS.md` → `SESSION_HANDOFF_2026-07-01_S50.md` → the S50 delta atop CLAUDE.md.
Branch = designated `claude/crypto-liquidity-signals-*` (ff up to canonical `5c5vg9` if the harness lands
stale; keep 5c5vg9 synced every push). Books load GZIPPED from `/tmp/<coin>_coinbase_book.jsonl.gz`
(materialize from `data/<coin>-book` branches after a restart). `pip install matplotlib`, `MPLBACKEND=Agg`.

## Where S50 landed (the state you inherit)
- **S49 2nd-window gate PASSES** (all 5 cells OOS; conviction sizing wired leakage-clean into
  `odcore.swing_maker.size_legs`) — re-verified after a restart. That gate is DONE.
- **Dollar-capacity model built** (`scripts/_capacity_model.py`, results `_capacity_model_results.json`).
  Corrected numbers (mk0, flat, conservative 1s entry window): SOL +$19/hr, DOGE +$9, XRP +$6, ETH ~$0, BTC −$5.
- **The edge is THIN** (SOL 1.75 bps). Two measured truths: conviction sizing is only ~+25% (winners not
  separable at entry, S47), and $/hr = edge × passively-fillable notional, so a thin edge caps the dollars.
- **THE LEVER = a maker REBATE (adds bps directly): a −1 bp rebate ~doubles net/leg → ~doubles $/hr.**
- **Decision (Greg S50): pivot to a REBATE VENUE (market-maker-agreement path); focus SOL initially.**

## S51 jobs (in order)
1. **Quantify the rebate scenario (do this FIRST — it sizes the whole venue decision).** Re-run the capacity
   model at maker −1 and −2 bps (and a wider-spread assumption) on SOL. Show exactly how much $/hr a rebate +
   a wider book buys. This is the number that tells Greg whether a rebate venue clears the "worth it" bar.
2. **Pick 1–2 candidate rebate venues.** VERIFY their maker-rebate terms and market-maker-program reachability
   from primary sources — do NOT cite fee numbers from memory. Candidates to check: derivatives venues with
   MM-rebate programs. Report the reachable rebate + the qualification bar per venue.
3. **Collect that venue's SOL book** (the collectors are symbol/venue-parameterized — extend, don't reinvent)
   and **re-measure spread + fill there** (per-cell rule: a rebate on a tight/deep book won't print — the S43
   btc lesson). This is the decisive test.
4. **v2 fill model** (`odcore/maker_book.py` already has the honest queue): walk-the-book / mark-to-fill
   markdown so $/hr at larger size is honest — resolves both the conservative 1s window AND the optimistic
   per-fill price in one model.
5. Wire a per-leg **size cap** + `size_legs` into deploy sizing (modest lift; do after the venue is chosen).

## DEAD — do not re-chase
Timing retiming, wrong-tail ENTRY gates (exhaustion/climax/stop all lose total net), spread/dive as TIMING,
stacking net-screened levers, and **expecting conviction sizing to be more than a ~+25% garnish** (winners are
NOT separable from losers at entry — S47, verified again in S50 via corr(size,net)=+0.03). Wrong-tail is
IRREDUCIBLE — size around it, don't gate it. OD-BOOK `.ttest_committed.json` sentinel FROZEN.

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy (partial coverage is not failure); never pool sign-blind; NEVER
tune off one window; git = source of truth (commit + push often); falsification-first (`assert_no_leakage` +
random/shuffle control on every new signal). Change EXISTING files in place — do not reinvent
`paper_trade.py` / `swing_maker.py` / `flip_detector.py` / `maker_book.py` / `_capacity_model.py`.
