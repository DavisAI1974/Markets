# KICKOFF — S96 (next session priorities for the NG forecaster loop)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-15_S95.md` -> `research/kalshi/AGENT_RUNBOOK_S95.md` ->
`research/kalshi/REFINE_DIRECTIVE_S94.md` -> `research/kalshi/knowledge/ng_brain.json` (the ONE-FILE brain).
Branch `claude/ng-coach-agent-loop-5ha5bf`. The loop runs IN the Claude env; sub-steps spawned via the Agent
tool (prompts in the runbook). All S95 machinery + renders committed — nothing to rebuild.

## Env on-ramp
- Re-paste (ROTATE) AWS + Databento keys into gitignored `scratchpad/aws.env` (S3 read on
  `bento-568968024170-us-east-2-an`, us-east-2). `pip install boto3 databento matplotlib pandas`.
- Regenerate `data/eia_surprise.json`: `EIA_API_KEY=DEMO_KEY python research/kalshi/eia_surprise.py --out data/eia_surprise.json`.
- The tape/npz cache and eia json are gitignored/regenerable; the brain, forecasts, renders, and scripts are in git.

## JOB 1 — merge the s95.2 refinement proposal into the brain
Review `research/kalshi/knowledge/ng_brain_s95.2_proposal.json` vs `ng_brain.json`, confirm the roll
corrections (void 0925 overnight-gap swing-exhaustion; G3->G4 magnitude was roll-inflated) + the G6 holdout
fold-in, then merge to `ng_brain.json` (MERGE, keep all plays, no averages). Back up first.

## JOB 2 — Group-7, the winter regime test (the decisive one)
Next consecutive block from **Nov 5** (Nov 5 -> ~Nov 18; into the Nov fill-peak -> Dec drawdown, surplus
collapse). BLIND forecaster on the merged brain (runbook Step A): build decision-state, anchor = Nov 4 hr24,
forecast day-into-day -> `forecasts/grp7.json` -> render with `continuous_rt.py --guess` -> score with
`continuous_score.py`. Watch for the next contract roll (~late Nov, Dec->Jan) — `roll_adjust` handles it.

## JOB 3 — attack the LOCATED open problems (the edge still isn't here)
1. The intra-block TURN — catching the V recovery (G5 and G6 were both V's; the forecaster caught the first
   leg, missed the turn back). This is where the next real edge is. Use the fingerprints (continuation-
   collapse + turn_exhaustion at the extreme) to build a live turn-track, not far_thinning (demoted noise).
2. MAGNITUDE undersize on catalyst days (weekend-gap Monday, storage Thursday, swing-reversal) is STRUCTURAL —
   size those up, per event.
3. Open-time from-flat side stays unsolved (dip_imb_level is a nowcast/execution-layer, not open-time).

## GUARDS (unchanged, non-negotiable)
PER-EVENT, NEVER pool/average · drift is a DESCRIPTOR not error · NOT point-fitting (general rules only) ·
brain blinded-merged per group, refine only on past consecutive tapes (never scattered Group-2) · true holdout
+ provisional-until-live · net-of-fee maker AND taker · git=CODE/renders, S3=DATA · flag weekday holidays ·
NG != WTI · weather forecaster HANDS OFF · keys are SECRETS.
