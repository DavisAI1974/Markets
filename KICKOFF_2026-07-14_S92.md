# KICKOFF S92 — Kalshi. Verify the year box landed, rotate keys, migrate data git→S3, validate the lag net-of-fee.

Run the **`kalshi-session-start` ritual** (branch check: you'll land on a stale tip -> rebase onto trunk
`claude/kalshi-s79-kickoff-ij8t9o`; push there). READ (in order): `SESSION_HANDOFF_2026-07-14_S91.md` ->
`CLAUDE.md` -> `KALSHI_TRADING.md` -> `research/kalshi/GOLD_SILVER_LAG_FINDINGS_S91.md`.

## SECRETS to re-paste (never in git; Greg pastes): DATABENTO_API_KEY (db-...), AWS_ACCESS_KEY_ID,
AWS_SECRET_ACCESS_KEY (IAM user `Claude`, EC2FullAccess; NO iam/ssm), AWS_DEFAULT_REGION=us-east-2.
**ROTATE these early** — they sat in the box boot config. Store scratchpad-only, boto3 needs the region set.

## JOB 1 — VERIFY the year box finished + is CLEAN (do FIRST)
Durable box **`i-08cee7171c0a76a04`** (t3.xlarge, 200GB) was running the full-year `--weekly` pull, streaming
its log to S3. STEPS:
1. `aws s3 cp s3://bento-568968024170-us-east-2-an/deploy/box-logs/pull.log -` (+ `heartbeat`, + `DONE` if present).
   Box auto-STOPS when the pull completes. If DONE exists -> pull finished.
2. Confirm coverage: list `nymex/nymex_cont/_done/` (want ~53 weeks x {CL,NG} `.done` markers) AND spot-check
   `nymex/nymex_cont/` daily gz are tens-of-MB (NO sub-5KB stubs) across all 12 months (2025-07..2026-06).
3. If incomplete/stopped early: relaunch (scratchpad `box_userdata2.sh`, boto3-only boot) OR run in-container:
   `python3 research/kalshi/pull_year_mbp10.py --start 2025-07 --end 2026-07 --weekly --dest s3://.../nymex
   --scratch /tmp/x` (marker-resume skips done weeks). Kill the DEAD v1 box `i-0e56896a51243edb2` (stopped).

## PUNCH-OUT LIST (updated from S91) — HH+WTI live + gold/silver depth
### A. Get HH+WTI (and gold/silver) live on Kalshi
- [~] A1 clean year tape on S3 — box grinding; VERIFY (JOB 1).
- [ ] A2 re-validate the lag on the REAL Kalshi book (not trade-reconstructed) — point scans at `data/kalshi-bins`.
- [~] A3 lock the deploy cell-map — NG cross-strike map drafted (dn ITM/ATM pays); lag maps for NG/WTI/gold/silver.
- [ ] A4 Kalshi execution module — DEFERRED TO LAST (Greg). Signed-HTTPS trading client, bounded risk caps.
- [ ] A5 wire live loop; [ ] A6 **paper-trade on Kalshi DEMO** (demo.kalshi.co, play money) a few days first.
  NEED FROM GREG: funded Kalshi account + trading API key + RSA private key + risk limits ($/trade, daily loss).
### B. Gold + silver depth — DONE this session
- [x] B7 KXGOLDD/KXSILVERD in book collector; [x] B8 Pyth XAU/XAG feeds; [x] LAG validated (free).
- [ ] B9 net-of-fee/size at sub-minute (free 1-sec Pyth first; paid COMEX GC/SI tape ~$200, built, ~days, DEFERRED).
### C/D infra
- [ ] C10 fix the FAILING NYMEX-forward workflow (needs DATABENTO GH secret or a Routine).
- [ ] D11 wire durable NWS-hourly FORWARD collector (approved).

## JOB 2 — MIGRATE live data git→S3 (Greg S91, load-bearing: "no more data on git")
(a) Copy existing `data/kalshi-bins` + `data/pyth-ticks` (+ crypto history branches) to S3. (b) Add `--dest s3://`
to `kalshi_collector.py` + `pyth_collector.py` (same pattern as pull_year); reroute the durable workflows — needs
AWS creds where they run (AWS secret in GH Actions, OR move collectors onto the durable box/Routine). (c) Stop
pushing to git data branches. End-state: a durable daily box (SSM role, no on-disk keys) runs ALL collectors +
the daily lifecycle, S3-only.

## JOB 3 — the decisive research: does the lag CLEAR NET-OF-FEE AT SIZE (sub-minute)?
**NG/WTI lag is ALREADY TESTED — do NOT re-test it.** Lag existence (S81 `futures_kalshi_lag.py`) + the
provisional net-of-toll backtest (S81 `lag_exploit_backtest`, S87 `lag_join.py`, CL/NG both positive gated)
are DONE. What's actually open:
- **Gold/silver:** the net-of-fee-at-SIZE test (the lag existence is done S91; the cost/size read is not).
- **All markets — SUB-MINUTE deepening (sharpen, not re-test):** the size-vs-fee margin at 1-sec/tick on the
  clean year tape (JOB 1) + free 1-sec Pyth. This tightens the provisional NG/WTI result and gets gold/silver
  their first cost read. Per-cell, leakage-gated, net-of-fee maker AND taker. Turns "lag exists" -> "deploy cell."

## STANDING / notes
- **Databento native optimization:** we use `split_duration="day"` already; S92 try storing native `.dbn.zst`
  + decode at READ time (reader already decodes on read) -> deletes the decode/flush/re-bin step.
- **SSM:** role added to the box, but MY IAM user lacks ssm:* -> can't drive it. Ask Greg for ssm perms on the
  Claude user IF we want SSM shell; else S3-log-streaming stays the observability path.
- NG exhaustion tick test (S90 standing): at NATIVE TICK once the year tape is clean.
- Weather OD forecaster rerun doc + KXHIGH settlement-station confirm (standing).

## Rules: data RAW / aggregate on trade side; leakage gate; per-cell never pooled; net-of-fee maker AND taker;
exclude settle window; zero synthetic; provisional-until-live; weather forecaster HANDS OFF; keys are SECRETS;
git = CODE, S3 = ALL DATA.
