# KICKOFF S87 — Kalshi, continue. S86 produced the EVENT-STATE MODEL + the pre-release volume detector.

Run the **`kalshi-session-start` skill FIRST** (branch check, data restore, read order). You'll land on a
fresh harness branch cut from the STALE S70 tip `3c70ff5` -- `git log --oneline -1` to confirm, then rebase
onto the trunk:
  git fetch origin claude/kalshi-s79-kickoff-ij8t9o
  git checkout -B <your-branch> origin/claude/kalshi-s79-kickoff-ij8t9o
Push all work to the trunk `claude/kalshi-s79-kickoff-ij8t9o` (= repo default; collectors auto-push there;
pull/rebase before push). `git ls-remote --heads origin` is the authoritative branch view.

session-start restores `data/nymex-ticks` -> `data/pyth_ticks/` (S85 trades tape) + `data/nymex_mbp10/`
(S86 MBP-10 depth tape, 24 windows) + the depth/surprise/volume baselines. Verify it landed. No re-pull.

READ (in order): `SESSION_HANDOFF_2026-07-13_S86.md` -> `research/kalshi/EVENT_STATE_DESIGN_S86.md` (the
MODEL -- read it) -> the S86 findings (`DEPTH_RUNLENGTH_FINDINGS_S86.md`, `EVENT_SURPRISE_FINDINGS_S86.md`,
`PREVOL_FINDINGS_S86.md`) -> `CLAUDE.md` (lean) -> `KALSHI_TRADING.md`. Run the `kalshi-backtest` skill
before building/judging any result.

FIRST: re-export **DATABENTO_API_KEY** in this session (SECRET, not persisted; needs the `db-` prefix; pass
inline as env var, never commit). And for EIA actuals, `DEMO_KEY` works (rate-limited).

WHERE WE ARE (end S86):
- S86's big output is a **MODEL** (`EVENT_STATE_DESIGN_S86.md`): read each energy release CONDITIONED on
  prior + anticipated state. Events STACK; three pillars (news / storage / market-capacity); storage = the
  physical confirmation node; shared drivers with per-market/per-period weights ("same scaffold, different
  values"); weather two-directional (NG temps-demand / CL adverse-supply); news in three tenses + the
  persistent Ukraine/Iran geopolitical regime, weighted by time-to-impact vs the PROMPT tenor we trade; the
  human/emotion factor = the HERD RUN = order-flow footprint. Eyeball-validated (06-17 CL = the 2026 Hormuz
  crisis).
- Three leakage-gated builds on the 24 MBP-10 windows (~$0.42 of the $125 credit), all provisional n=12,
  Apr-Jul only, logged WITHOUT mechanism: (1) depth run-length (push-book one-sidedness vs run length NG
  -0.17 / CL +0.52); (2) EIA seasonal-proxy surprise split (`eia_surprise.py`, 12/12); (3) **pre-release
  VOLUME primed/coiled detector** (NG: quieter pre-release -> bigger move, consistent across cells; the
  first build off the model, NO external feeds). `event_move_baseline.py --selftest` PASS.

S87 PRIORITIES:
1. **P3 -- the lag join** (decision-critical; gates the $130 full-year pull). We have NO Kalshi tape
   overlapping the Apr-Jul futures windows (bins are 1 day; `kalshi_hist_trades/` absent). But
   `kalshi_history.py` pulls it from Kalshi's PUBLIC API (`/markets?status=settled` + `fetch_trades`;
   ~46 settled WTI events, e.g. `KXWTI-26MAY06`, `KXNATGASD-26JUN2517`). So: pull the 24 release events ->
   join the futures move (from `event_move_baseline`) vs the Kalshi echo move NET-OF-FEE
   (`round_up(0.07*C*P*(1-P))` per leg, maker AND taker), per hold-time (NG ~60s fast; CL longer), per cell,
   conditioned on the surprise cell + the coiled/primed gate = realized-EV. Add the daily 5PM-settle tape.
2. **Backwardation pull** (greenlit S86, not done) -- Databento deferred contracts -> the tightness/curve
   axis + the where-on-the-horizon map. Small cost, cost-gate it.
3. **Wire the event-state axes we HAVE** (storage level+surprise, season, the coiled detector) into the
   conditioning so P3 runs stacking-aware; layer NHC archive / OVX / forward degree-days as feeds land.
4. **Full-year MBP-10 pull** ($130, all seasons) -- only AFTER P3 shows the echo pays; widen the pre-window
   (currently 120s) for a better coiled read.

RULES (run `kalshi-backtest`): each trade individually / per-cell / distributions not means; log
correlations WITHOUT mechanism (Greg S86); prior-conditions + seasonality confound everything (Apr-Jul only,
no generalization); NEVER say "X failed" (report "works on {X}, not {Y}"); exclude the settle window;
leakage gate before any backtest; net-of-fee at maker AND taker; zero synthetic; provisional-until-live;
NYMEX=canary, fire on Kalshi; weather forecaster = Greg's spec HANDS OFF; DATABENTO_API_KEY is a secret
(db- prefix); keep CLAUDE.md + KALSHI_TRADING.md lean.
