# SESSION HANDOFF — S57 (2026-07-03) — the ELIGIBILITY session: Bybit STRUCK (US-ineligible,
# permanent ban), MEXC killed, the venue map rebuilt on US-lawful ground; staged-commit sizing
# (Greg's accumulate design per-leg) VALIDATED on two venues' tape; pivot = the MID BAND on
# US-legal fees with staged sizing as the lag-cheap confirm.

## ⛔ STANDING BAN (Greg, this session): BYBIT — NEVER USE FOR ANYTHING
US-ineligible (Bybit's restricted-jurisdictions policy names the United States; UBO-level for
entities; no US-facing intermediary post-Falcon-Labs). All bybit code/data/cells struck this
session (platform SANDBOX emptied; collectors deleted from the default branch; probes and
ledgers git-rm'd; local tapes deleted). NEVER collect its data, run its tape, cite its cells,
or propose it. Record: `S57_VENUE_FINDINGS.md`. Greg UI-deletes data/{sol,eth}-bybit-book.
Same session, same class: MEXC = DEAD VENUE (API maker +6bp overrides the marketing 0%; ToS
bans HFT/API algo; unlicensed everywhere; freeze-profitable-traders pattern) — do not revisit.

## What SURVIVES the venue reset (all venue-agnostic)
- The model (fine lean-flip zigzag), the gate methodology, the executor mechanics, the fee
  scoring discipline, the queue-honest capacity method, per-cell rule — all intact.
- **STAGED-COMMIT SIZING (Greg's S52 accumulate design per-leg) — the S57 validated result:**
  starter in every leg, full-size MAKER add at the first +2bp favorable print, losers die at
  starter size. Confirmation adds beat random-add by 10-14 sigma on BOTH venues tested;
  loser:winner capital 0.33-0.6; add tranche earns +1.4-2.0bp net AFTER the signal (causally
  clean). On eth_coinbase (thin cushion) staged BEAT flat outright (+$32.89 vs +$28.82/hr
  @ s0=0.6/c=2bp, mk0/tk5). Speed of confirm does not grade quality — THAT it confirms does
  (never-confirmers: 69%->52% win vs 91-95% confirmed). Taker adds are fatal at tight
  confirms — adds must post as makers (cover-grace-style discipline).
- Time-to-confirm: 16% of sol legs confirm <1s, 27% <5s — "immediately" is real.

## The venue map after verification (S57_VENUE_FINDINGS.md = the citable record)
- NO reachable maker rebate exists for us anywhere lawful. Coinbase spot floors at 0.00%
  ($250M/30d tier); the $500k/mo fee-upgrade program is REAL but matches proportionally
  (~3-8bp maker for 60 days at our provable volume, once/year) — cannot deliver mk0.
- CFTC-regulated ONSHORE PERPS exist since Jul 2025 (Coinbase CFM ~2bp/side + $0.15/contract
  floor; Kraken/Bitnomial flat $0.15/contract/side). 10-40x too costly for the 2-4bp fine
  scalp; only 3-10% of a 60-100bp mid-band swing.
- OPEN: Kraken Pro US spot maker tiers (possible mk0 at ~$10M/30d organic — verify);
  eligibility-agent parent syntheses may add detail — fold into S57_VENUE_FINDINGS.

## GREG'S PIVOT (this session, load-bearing): THE MID BAND — "there's no other way"
At US-legal fees the fine band is unharvestable; the 60-100bp mid band pays fees at 3-10% of
swing. S55 priced the band (+$2,900-3,700/day 5-coin projection) but coarse confirms cost
25-40bp of lag toll. THE UNLOCK: the staged trigger measured this session = a +2bp fine-scale
confirm — the lag-cheap commit the mid band was missing. Sketch: fine-flip starter at the
turn -> staged adds escalate into the ride -> fine-flip exit. Two-scale machine with SIZING
instead of gating (no-gates-law compliant). S58 JOB 1 = this machine vs the mid-band oracle
on Coinbase books, fee columns = real US tiers (retail spot / mk0-upgrade / flat-fee perps).

## Also this session (findings that stand)
- S56 fee-group correction: rebate tiering verified before the strike — recorded in
  S57_VENUE_FINDINGS for the record only (venue banned).
- Freight-train veto: anatomy real (signed fade-velocity corr -0.31), gate unprofitable
  (no subgroup sums negative; unsigned-velocity first pass was a tool error — fixed).
  Tradeable content = joins outperform -> already inside the deployed size axis.
- Baseline forward ledger healthy: 26,784 trades, all 5 Coinbase cells positive.
- PROBE-THEN-COMMIT across trades (Greg, session close): pay fees on a small probe trade,
  bail-or-all-in on the NEXT trade — first measurement in S58 (note the twxv4t
  "anti-martingale kill" label attaches to the OLD model class; re-measure honestly in the
  fee-paying regime; NO-GATES scoring — size, don't skip).

## NEXT (S58): read `KICKOFF_2026-07-04_S58.md`.
