# S64 KICKOFF — grade the fill on a real window + evaluate the open-source platform + benchmark our results

⛔ BANS unchanged: BYBIT never; MEXC dead. Fee frame = **KRAKEN kr_mk0 (0bp maker)** (Greg pivoted S63;
Coinbase parked). Branch: reconcile to canonical `5c5vg9` FIRST (designated branch keeps getting cut from
the wrong parent — reset --hard); sync canonical after every push; a paper_trade cron pushes to canonical
— rebase, don't clobber.

Read first: `SESSION_HANDOFF_2026-07-07_S63.md` + `S63_TREND_FLIP_FINDINGS.md` (§1–20) +
`KRAKEN_DEPLOY_MAP_S63.md`.

## STATE (S63 close)
- The Kraken strategy is fully mapped: **ride the per-cell early-arm flow-lean zigzag** (ETH/BTC forward,
  SOL reversed) — the edge is mean-reversion; **deep bail (BTC −80 / ETH −100)** = free tail-cap.
- **Direction is dead** (unpredictable at entry AND mid-leg, 4 signals). No stop/TP/flip overlay helps.
- **The gate is the MAKER FILL:** raw fill ~40% → cover_grace + swing floor lift it; ETH fill-viable
  (~1/hr cost), BTC/SOL lossy. Tools: `scripts/_s63_kraken_*.py` (20 scripts, all named _s63_kraken_).
- **BLOCKER = the book window is LOW-EDGE** (ideal fill already −2/−1) so honest $/hr is negative — NOT
  the +8–9/hr tape regime; can't grade viability on ~1 day.

## JOBS (Greg, S63 close)
1. **RE-GRADE THE FILL on a normal-edge window.** Once multi-day Kraken book (`data/*-kraken-book`, 6h
   crons running) covers a normal-vol window, re-run `_s63_kraken_makerfill.py` + `_covergrace.py` +
   `_swingfloor.py` (cover_grace≈300, REV≈0.2) on ETH first. WIN = ETH honest net crosses clearly
   positive. This is the decisive viability test; everything else is secondary until this clears.
2. **EVALUATE THE OPEN-SOURCE PLATFORM `hftbacktest`** (the Lean Lab README's fill-level 2nd gate,
   py-v2.4.x; also Tardis.dev for deep history). Question (Greg): does it IMPROVE ours (a better queue/
   latency/fill model than our swing_maker) — or is it just the BETTER OPTION to build on? Drop our
   ETH flow-lean legs into `hbt_lean_stub.py` and compare its fill verdict to ours. Report: keep-ours /
   adopt-theirs / hybrid.
3. **BENCHMARK OUR RESULTS vs OTHER PLATFORMS** (Greg: "it feels like we aren't getting great results for
   some reason"). Compare ~+8/hr@$5k (paper) and the fill-realistic numbers to published crypto MM/HFT
   Sharpe/$/hr/bps-per-trade. Diagnose WHY if we're low: fee tier (is kr_mk0 real for us? a maker
   REBATE tier changes everything), venue choice, the mean-reversion edge being genuinely thin, or a
   measurement problem. This is a sanity/orientation check — are we chasing a real edge or a mediocre one?

## Rules (standing): crypto only; per-cell deploy; never tune off one window; graded beats gated;
PROFIT-PER-HOUR is the scoreboard; sandbox-first + baseline canary; git = source of truth; MPLBACKEND=Agg
(pip install matplotlib); Kraken tape re-pull = backfill_kraken_trades.py (rate-limited ~1req/s, hours);
book materialize = git show origin/data/<coin>-kraken-book:<f>.jsonl.gz | gunzip.
