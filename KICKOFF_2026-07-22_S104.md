# KICKOFF - S104 (G17 is the opener; CANONICAL AGENT FILES are live - USE THEM, do not re-author)

**FIRST ACTION, BEFORE ANYTHING ELSE: SWITCH TO THE BRANCH.** Harness assigns a different auto-named
branch - NEVER the work. Run:
`git fetch origin claude/ng-coach-agent-loop-5ha5bf && git checkout -B claude/ng-coach-agent-loop-5ha5bf origin/claude/ng-coach-agent-loop-5ha5bf`
The tip message must begin "S103".

**AWS KEY: the tx-pair** (ID `AKIAYI6JDCBVLKYQGLMH`, account ...4170, secret begins `txRGHd`, 40 chars).
Write `scratchpad/aws.env` (KEY=VALUE, no export) + `~/.aws/credentials`; STS verify (pass/fail + tail
only). **THE FIX for InvalidAccessKeyId (S103 confirmed): the container injects placeholder env vars
that OVERRIDE the file via boto3 setdefault. Run platform_sync / any boto3 tool with the placeholders
stripped: `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY python3 ...`** (or pass creds explicitly).
Greg pastes the pair if it is not on disk (fresh container does not persist it).

## THE CANONICAL AGENT FILES ARE LIVE (Greg-ordered, S103) - USE THEM, DO NOT REBUILD
We have STATIC drop-in files. Spin them up UNCHANGED every group. The ONLY things that change
group-to-group are the BRAIN (`knowledge/ng_brain.json`, updates via refine) and the GROUP DATA
(decision-state + anchor + basis).
- **BLIND (the 3-agent panel):** `agents/blind_shared.md` + `agents/blind_angle_storage.md` +
  `agents/blind_angle_positioning.md` + `agents/blind_angle_weather.md`.
- **REFINE:** `agents/refine.md`.
- **RENDERS:** `continuous_rt.py` (`--anchor --start --end [--seams] --tag --guess`) - the jagged
  actual + guess overlay; change DATES only. `continuous_score.py` for the scorecard.
- **The turnkey per-group loop + why:** `agents/README.md`. NO per-group prompt re-authoring, NO
  per-group selftest, NO re-pull of shared stores. This was the S103 wasted-cycles fix - honor it.

## READ, in order
`SESSION_HANDOFF_2026-07-21_S103.md` -> this kickoff -> `agents/README.md` ->
`research/kalshi/knowledge/ng_brain.json` (**s102.3, 36 plays** - the S103 additions:
selector.divergence_resolution, magnitude.s1void_injection_chain_bleed; + s102.2's
giveback_exhaustion flow-absorption arm, shoulder_counter_print_damping, weekend_crest_friday
expiry-covering split) -> `research/kalshi/GROUP_PRECHECK_S103.md` (shared substrate done once + roll
map + group windows) -> `research/kalshi/NG_FORECASTER_PROBLEM_MEMO_S103.md` (the open PROBLEM - see below).

## ENV ON-RAMP (shared substrate = ONCE; see GROUP_PRECHECK_S103.md)
Creds (env -u) -> the full-history stores are the shared substrate (weather, cot, storage family,
nuclear, grid, ngwu, solar, vol, contract_structure, nymex_curve, eia -> data/eia_surprise.json,
consensus -> data/storage_consensus, flow_calendar). Pull once; they cover every group. Per-group you
pull ONLY that group's tape day-files + build its decision-state. No selftest per group.

## JOB 1 - G17 (Sun 04-12 -> Fri 04-24), TURNKEY via the canonical loop
- Basis: **May/NGK26(996) through 04-20, June/NGM26 from 04-21** (Kalshi underlying = 5bd before the
  04-28 May LTD). TWO-LEG block with a seam ~0421 (like G15) - measure the seam, mark never-traded.
  Roll map verified in GROUP_PRECHECK_S103.md. (June store id: resolve the June leg's file source like
  G15 used nymex_cont_ngj26 for the April leg; check nymex/nymex_cont_n0 vs a june-specific store.)
- Anchor = 04-10 actual close + last-hour dir (decision-time-legit). Sessions = the dates with tape.
- LOOP: pull G17 tape -> `forecast_harness.py decision-state --days <sessions> --mask-after 20260410
  --out grp17_state.json` -> spawn the 3 blind agents (the canonical files; GROUP=17, STATE, ANCHOR,
  BASIS+seam) -> SYNTHESIZE per-event (SEE THE PROBLEM MEMO - do NOT average bimodal splits; select the
  dominant driver; test selector.divergence_resolution forward) -> continuous_rt.py (change dates,
  --seams 20260421) + continuous_score.py -> PRINT render -> HARD PAUSE -> refine (agents/refine.md,
  GROUP=17) -> adjudicate/render/PRINT/merge -> s102.4. Then G18 (clean June).

## THE OPEN PROBLEM (Greg handed the memo to ChatGPT; incorporate any insight)
`NG_FORECASTER_PROBLEM_MEMO_S103.md` documents the RECURRING flaw: the blind scores 30-70% and the
refine 90-100% every block, and the blind's MAGNITUDE error FLIPS SIGN block-to-block (G15 over-sized a
give-back -3490 vs -600; G16 under-sized a bleed -1455 vs -3820) from MIS-SCOPED lessons. Plus the
SELECTOR flaw: on divergence days we AVERAGE competing drivers (04-07: A+250/B+500/C-300 -> flat, actual
-220) instead of SELECTING the dominant one. And we UNDER-WEIGHT the order-flow direction nowcast
(G16 sell-share <0.50 all 11 sessions = 11/11 direction if followed). S103 added
selector.divergence_resolution (default to the tape/flow regime; catalyst overrides only above explicit
thresholds) as a first fix - FORWARD-TEST it on G17. If ChatGPT returns insight, fold it in before G17's
synthesis.

## STATUS AT S103 CLOSE
- Brain **s102.3, 36 plays** (G15 refine merged s102.2; G16 blind panel 8/11 under-sized -> refine
  11/11, drift +40, every day <40 -> merged s102.3). Backups: ng_brain_s102.{1,2}_backup.json.
- BOX L1 pull: NG mbp-1 year, UNTETHERED, ~105/250 days at S103 open (up to ~2025-11-20), $0.00 in-sub,
  alive. Check s3 nymex/ng_l1/_DONE; if done, report total + queue CL L1 the same untethered way; stop
  the box only when NG-L1 + CL-L1 + pull_rest all done (pull_rest already DONE), mid-writing day first.
  ChatGPT may also be collecting live MBO on the box + pulling data to S3 - coexist, do not collide.
- pyth: only needed for a daily settle number - NOT a current concern (Greg S103). Do not escalate.

## GUARDS (unchanged, non-negotiable)
PER-EVENT never pool/average as conclusion; general rules only; blind wall decision-time only + the mask
(tape_conditions live-non-price); one-shot from fresh context; renders PRINTED before merges; REFINE
UNTIL IT SCORES (magnitudes DERIVED never fitted - honest under 100 is the S103 bar); day-class first;
seasonal salience S1/S2/S3 every open; rolls marked never traded; flips C1+C3+C4 never front-run;
net-of-fee maker AND taker; git = CODE, S3 = DATA; NG != WTI; weather forecaster HANDS OFF; keys are
SECRETS; no emojis.
