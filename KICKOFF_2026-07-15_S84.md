# KICKOFF S84 — Kalshi, continue (the S83 research priorities, rolled forward)

Read in order: `SESSION_HANDOFF_2026-07-14_S83.md` → `CLAUDE.md` (now LEAN — read the whole thing, it's
~215 lines) → `KALSHI_TRADING.md` (file index). Or simply run the **`kalshi-session-start` skill** —
it encodes the branch check, read order, and data materialization.

Branch: **`claude/kalshi-s79-kickoff-ij8t9o`** (= repo DEFAULT; collectors auto-push here; pull before
pushing). If the harness assigns a fresh branch, `git log --oneline -1` FIRST — the stale S70 tip
`3c70ff5` is the recurring trap; `git ls-remote --heads origin` is the authoritative branch view
(local `git branch -r` can be an incomplete fetch and mislead).

WHERE WE ARE (end S83 — a META session; no research ran, S83's priorities roll here intact):
- CLAUDE.md split: lean live doc (trading rules + OD toolkit + dipole research kept LIVE) +
  `CLAUDE_ARCHIVE_OD.md` (the full verbatim OD/crypto/physics record). Three ritual skills live:
  `kalshi-session-start`, `kalshi-backtest`, `kalshi-roll`.
- **`data/pyth-ticks` did NOT exist at S83 close** (2026-07-12 ~13:00 UTC) — the manually-dispatched
  Pyth run pushes only at end of its ~5h50m cycle; may have landed since, may be stuck.
  `data/kalshi-bins` healthy (6h cadence).
- S82 recap (the load-bearing research state): level-hits mean-revert at 1¢, NO cell pays at maker →
  the edge is EXTERNAL (the futures→Kalshi lag), not the book's own flow. The level-hit that runs is
  the one trailing a fresh NYMEX move — which requires the Pyth join.

S84 PRIORITIES:
1. **Pyth feed verification FIRST**: does `data/pyth-ticks` exist now? If not → check the Actions run
   state; a `queued`/dead run is account-level (billing/minutes/runner cap) = Greg's click. Then
   **SUB-SECOND LAG on accrued ticks** (the decisive test): per contract, never pooled — align each
   Kalshi ms trade tape vs Pyth ticks at 1–5s; which strikes lag by how many SECONDS; does the whole
   reprice clear the toll (`score_hold` at real resolution). Distributions + winner fingerprints.
2. **JOIN the Pyth futures move onto the level-hit dataset** — add `futures_move_bps` / `lag_seconds`
   context columns in `level_hit_dataset.py`: do big-run continuations concentrate on level-hits
   trailing a fresh futures move? (The external-driver hypothesis the S82 dataset was built to test.)
3. **THURSDAY 7/16 EIA natgas (14:30 UTC) LIVE**: `release_book_signal.py --test --series KXNATGASD`
   on the release-spanning bins; busy-day natgas lag on live NGDQ6 ticks; `consensus_poll.py` before
   (forecast) + after (EIA actual).
4. **WEATHER (scoreboard-side; forecaster is Greg's spec, HANDS OFF)**: when the OD operator emits
   `(value,sigma)`, score through `kalshi_score.py` PER REGIME (Denver/NY/Chicago transition cell
   first) vs `WEATHER_BASELINE_S82.md`. Per-bucket-never-pooled; 2 regimes minimum.
5. Standing: roll re-point Pyth front-months (WTIQ6 7/21, NGDQ6 7/29, BRENTU6 7/31 — the `kalshi-roll`
   skill); paper-loop RSA creds.

RULES: run the **`kalshi-backtest` skill** before building/judging any result — it encodes: EACH TRADE
INDIVIDUALLY, never average; distributions not means; per-cell always (moneyness×side×velocity/lag-class
×release for level-hits; regime×city×season×bucket×swing for weather); exclude the settle window;
catalyst = trigger + coarse size / book+flow imbalance + exhaustion = direction + magnitude / herd
breadth = continuation, whale = scalp-only; leakage gate before any backtest; zero synthetic;
provisional-until-live; weather forecaster = Greg's spec, HANDS OFF; `--events` on
news_coupling_research = BASENAME; keep `KALSHI_TRADING.md` current; keep `CLAUDE.md` lean.
