# KICKOFF S93 — Kalshi. RUN THE FORECASTER LOOP (the coach brain), verify the year, wire the workflows.

Run the **`kalshi-session-start` ritual**: branch check (you'll land on the stale S70 tip `3c70ff5` ->
reset onto the trunk `claude/kalshi-s79-kickoff-ij8t9o`), then read in order.

READ (in order): `SESSION_HANDOFF_2026-07-14_S92.md` -> `CLAUDE.md` -> `KALSHI_TRADING.md` ->
`research/kalshi/knowledge/ng_brain.json` + `knowledge/README.md` (the coach brain + the loop) ->
`research/kalshi/NG_BEHAVIOR_KNOWLEDGE.md` (the living knowledge).

## SECRETS to re-paste (Greg pastes; not in git): `DATABENTO_API_KEY` (db-...), `AWS_ACCESS_KEY_ID`,
`AWS_SECRET_ACCESS_KEY` (IAM user Claude), `AWS_DEFAULT_REGION=us-east-2`. **ROTATE early** (standing item).

## Where S92 left it (the big one)
S92 built the **NG intraday forecaster** and **cracked DIRECTION**: `dip_imb_level` (order-flow imbalance)
calls a leg's side — 7%/93% terciles, OOS-validated 100% on strong flow (34/34, 3 unseen days). A NOWCAST,
ideal for the Kalshi lag. Built the **coach "brain"** (`knowledge/ng_brain.json`) + the self-growing loop.
Also fixed the year-box **every-Monday-corrupt** bug (`_flush` 'wb'->'ab') + re-downloaded NG Mondays clean.
Full detail: the S92 handoff.

## JOB 1 (Greg's #1) — RUN THE LOOP
Turn the self-growing forecaster loop and keep it walking forward:
1. An agent LOADS `research/kalshi/knowledge/ng_brain.json`.
2. Forecasts a NEW group of ~12 days BLIND (decision-time state only; do NOT load the actual curves). The
   group-2 decision state is staged: `scratchpad/ng_group2_decision_state.json` (12 fresh days, none seen).
   Tool: `month_characterize.characterize_day("NG", day, source="s3")` (full toolbox per leg).
3. Score vs actual (render guess-vs-actual overlays, per-day, NO pooling — Greg watches them).
4. The agent DISTILLS what it learned and MERGES new/refined plays back into `ng_brain.json` (version bump).
5. Next group -> the year -> loop back and refine the earliest/worst until near-perfect BLIND. Greg marks a
   day "done" when nailed (skip it).
- **THE GUARD:** general rules + mechanism + n, NEVER memorized days; judge skill on a TRUE HOLDOUT (days never
  touched) + forward-live; provisional-until-live. (A coded threshold is a rigid pooled decision; the coach
  reads each instant's fingerprint.)
- **Endpoint:** the brain drives the COACH that calls plays live (enter/hang-back/ride/exit/buy-sell), with a
  live-adjuster agent. We stop coding entrance/exit/hold gates.

## JOB 2 — the intraday COACH replay (the real edge test)
Apply the playbook LEG-BY-LEG on new days, net-of-fee: `direction.flow_nowcast` (take NG's side) ->
`ride.magnitude_staircase` ($350 crossing = 92% ride) -> `exit.recruitment_reversal` (exit when far-side
liquidity stops recruiting). This tests the tradeable edge (vs the day-open shape forecast). Fire on Kalshi
via the lag. Net-of-fee at SIZE is the gate.

## JOB 3 — data + infra hygiene
- **Verify the box year finished** (watch `deploy/box-logs/` -> DONE; span should reach 2026-07). Then run
  `pull_year_mbp10.py --reconcile-names` (rename date-only -> dow + write week markers). Then a **FINAL Monday
  sweep** (`scratchpad/redownload_mondays.py`) — the box minted NEW corrupt Mondays past Sep as it advanced.
- **Characterize NG Mondays** (now clean) -> the distinct weekend-gap shape -> add to the brain.
- **Greg adds the 3 GH secrets** -> enable the S3 workflows (NYMEX-forward AFTER box+reconcile; NWS-hourly
  anytime). Then git data-branch pushes stop (JOB 2 migration done).
- **AWS/Routine autonomous agent** for the loop so Greg's phone isn't tied up (needs DURABLE creds first).

## STANDING research / frontier (from the brain's open_frontier)
- DIRECTION from-flat FORECAST (vs today's nowcast); LIVE running turn-track (far_thinning neg->pos exit);
  WINTER/backwardation regime (the decisive OOS test — all 12 days were contango); direction data (forward
  news, real consensus, day-ahead/spot pipeline noms, overnight-lean).

## Rules (unchanged): each event individually / NEVER pool-as-final (extreme rate = lead, individuals pinpoint
the when) / per-cell / distributions not means / leakage + blind wall / net-of-fee maker AND taker / exclude
settle window / zero synthetic / provisional-until-live / git = CODE, S3 = ALL DATA / NG and WTI SEPARATE /
weather forecaster = Greg's spec HANDS OFF.
