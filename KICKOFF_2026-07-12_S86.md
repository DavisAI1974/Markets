# KICKOFF S86 — Kalshi, continue. Databento is LIVE; event_move_baseline has its first real result.

Run the **`kalshi-session-start` skill FIRST** (branch check, data restore, read order). You'll land on a
fresh harness branch cut from the STALE S70 tip `3c70ff5` — `git log --oneline -1` to confirm, then rebase
onto the trunk:
  git fetch origin claude/kalshi-s79-kickoff-ij8t9o
  git checkout -B <your-branch> origin/claude/kalshi-s79-kickoff-ij8t9o
Push all work to the trunk `claude/kalshi-s79-kickoff-ij8t9o` (= repo default; collectors auto-push there;
pull/rebase before push). `git ls-remote --heads origin` is the authoritative branch view.

The session-start skill now restores the NEW **`data/nymex-ticks`** branch (S85 Databento tape) into
`data/pyth_ticks/` automatically — verify it landed (24 NG/CL window files + `{NG,CL}_definitions.jsonl` +
`data/event_move_{NG,CL}.json`). No re-pull needed.

READ (in order): `SESSION_HANDOFF_2026-07-12_S85.md` -> `research/kalshi/EVENT_MOVE_FINDINGS_S85.md` ->
`CLAUDE.md` (lean) -> `KALSHI_TRADING.md`. Run the **`kalshi-backtest` skill** before building/judging any
result.

WHERE WE ARE (end S85):
- **Databento is LIVE.** Key set (`DATABENTO_API_KEY`, a SECRET — pass inline, never commit; ask Greg to
  re-export it in the new session). `pip install databento` (0.81.0). Pipeline proven end-to-end:
  cost-gate -> pull -> definition schema -> baseline. ~$0.50 of the $125 credit spent.
- **`event_move_baseline.py` ran on real ticks.** 12 NG + 12 CL EIA-release windows, leakage PASS, ticks
  from the `definition` schema ($10/tick both confirmed). Headline = a HOLD-TIME map: **NG front-loaded
  (60s captures 66% of the move, ~$310/contract), CL slower (60s = 27%, a longer hold gets the rest, e.g.
  06-17 built $2,640 over 17min).** BOTH kept, different hold windows; EV-net-of-fee is the gate, not
  frequency. These are FUTURES (canary) moves = the CEILING, not Kalshi P&L.
- **Schema decision: MBP-10** (Greg: "a few bucks over for a better view") — trades + top-of-book +
  10-level DEPTH, ns-stamped, ~$130/yr both (~$5 over credit). MBO stays OFF (50-100GB). Live sub NOT
  bought (research = pay-as-you-go historical).

S86 PRIORITIES:
1. **Extend the writer + baseline to consume MBP-10 DEPTH** (bid/offer size + 10 levels). Test the run-
   length hypothesis: does a thinning book / imbalance predict a LONGER run (the dipole exhaustion read on
   the canary)? Then **batch-pull the full-year MBP-10 for NG+CL** (~$130). Gate every pull on
   `metadata.get_cost` (`--max-cost`). WATCH CONTAINER DISK (multi-GB -> batch mode, compressed,
   process incrementally; delete raw after deriving). Persist derived + a batch-job manifest; raw tape is
   re-downloadable free within 30d.
2. **Historical surprise join** (EIA actuals + street consensus) -> the surprise-cell split
   (beat/miss x big/small). This is where CL's big-day fires show up and where "surprise=unknown" resolves.
   EIA actuals via API (DEMO_KEY worked in EVENT_WEIGHT_STUDY: natgas 2010-2026, crude 1982-2026);
   consensus forward via ForexFactory (`consensus_poll.py`).
3. **The lag join** — Kalshi echo net-of-fee vs the futures move, per hold-time, per contract: turns the
   ceiling into realized-EV. Join the futures move onto the S82 level-hit dataset (it was built to receive
   `futures_move_bps`/`lag_seconds` context). Also the daily tape (KXNATGASD settles on the NGDQ6 5PM
   close EVERY day, not just Thu).
4. Standing: fix bogus `NGDQ6` id in `pyth_collector.py`; score Greg's weather forecaster per-day
   net-of-fee (WHIFF/interior-warm-spike cluster) when a sample of predicted highs lands; Pyth live
   sub-second lag test on a post-reopen WTI tape.

RULES (run the `kalshi-backtest` skill): each trade individually / per-cell / distributions not means;
NEVER lead with the deflationary median or say "X failed" (report "works on {X}, not {Y}"); exclude the
settle window; catalyst=trigger+size / book+flow+exhaustion=direction+magnitude / herd=continuation,
whale=scalp; leakage gate before any backtest; net-of-fee at maker AND taker; zero synthetic;
provisional-until-live; NYMEX=canary, fire on Kalshi; weather forecaster = Greg's spec HANDS OFF;
`DATABENTO_API_KEY` is a secret (pass inline); keep `KALSHI_TRADING.md` + `CLAUDE.md` lean.

--- DATABENTO QUICK-REF (verified live S85, databento==0.81) ---
- Historical = pay-as-you-go $/GB, NO subscription; $125 credit. Dataset GLBX.MDP3 (CME/NYMEX/COMEX).
- Roots: **CL** = "Crude Oil Futures" (WTI Cushing, $10/tick), **NG** = "Henry Hub Natural Gas Futures"
  ($10/tick). Continuous VOLUME front: `CL.v.0`/`NG.v.0` (`databento_backfill.py --roll v`, the default).
- `databento_backfill.py` modes: cost (gate, free) | window (sync, small) | batch (large/cheap) | defs
  (definition schema -> tick size). `_retry` handles transient proxy resets. Needs the key inline.
- "Included data" on the product page = the MENU of schemas (billed separately by usage), NOT a bundle.
  L0 OHLCV/Definitions/Statistics/Status | L1 MBP-1/TBBO/BBO/Trades | L2 MBP-10 | L3 MBO (avoid).
