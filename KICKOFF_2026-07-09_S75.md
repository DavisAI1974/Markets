# DROP-IN — S75 (2026-07-09)

READ, IN ORDER: `STRATEGY_INVENTORY.md` (OPERATING CONTRACT + ✅ LIVE — the **S74 WHOLE-LEG IMBALANCE** +
**S74 EXIT KNOB** + **S74 ENTRY-ENERGY GATE** blocks near the top of LIVE carry the whole S74 result) →
`SESSION_HANDOFF_2026-07-08_S74.md` → `research/shape_s71/leg_imbalance_findings.md` (the 8 equations/coin +
the trade/book imbalance tables + per-cell PNGs).

BRANCH: this session = `claude/entry-shape-classifier-s74-5lb1p5` (= canonical S73 tip + all S74 work). If S74
files are missing: `git reset --hard origin/claude/entry-shape-classifier-s74-5lb1p5`. Restore books:
`git show origin/data/<coin>-kraken-book:<coin>_kraken_book.jsonl.gz | gunzip > /tmp/kbook/<coin>_book.jsonl`
for sol/btc/eth/xrp (doge separate). `pip install matplotlib scikit-learn` (the shape builder needs them).

## ⭐ WHERE WE LANDED (S74)
- **TRADE imbalance is the DECIDER (Greg's hypothesis — confirmed + more).** `(buy−sell)/(buy+sell)`, rolling
  20s, signed to side. DIRECTION = its polarity (born ~−0.85 against-side at the valley, flips with-side at
  onset; onset sign = buy/sell, gap +0.47..+0.72 all 4 coins). DURATION = the onset PEAK HEIGHT (long ~+0.35 vs
  short ~+0.15). It is REAL-TIME CAUSAL — it's exactly what the live flow-lean detector already computes.
- **Win/lose edge is in the TAIL:** winner tail holds ≥0 to close, loser tail flips through zero to −0.14..−0.42.
  BUT the flow-based EXIT is a **per-coin TUNING KNOB** (`balance_exit`, opt-in, built + wired live, canary
  bit-identical) and **baseline wins on all 4 majors so far** (FLOW LAGS PRICE) → keep baseline, tune per coin,
  adopt only where a coin's tuned config beats its baseline.
- **⭐⭐ BTC BOOK imbalance forecasts win/lose PRE-FIRE** (winners born book-positive, losers book-negative, whole
  leg, K1/5/10) — the ONE pre-fire win/lose tell. COIN-SPECIFIC: flat on SOL, weak ETH, long-win-only XRP.
- **Entry ENERGY gate DOUBLES $/hr on BTC/ETH/XRP** (in-sample: 2.5→6.08 / 5.47→6.40 / 2.47→5.18); SOL-specific
  fail → SOL uses a cumulative deep-hole (`late_integral` lowest ~5%) skip instead.
- **Per-cell pre-fire + tail EQUATIONS (8/coin, split at the fire) in `leg_imbalance_findings.md` §4** — ⭐ Greg
  thinks THESE are the winning part.
- The S73 "linear vs non-linear ascent" discriminator is FALSE (all 4 cells are convex hockey sticks); per-trade
  pre-fire SHAPE separation is a wall — the edges are the energy gate (3 coins) + the BTC book lean.

## ⭐ NEXT (S75)
1. **⭐⭐ THE PER-CELL EQUATIONS (Greg's lead — the likely winning part):** get the pre-fire + tail equations
   (`leg_imbalance_findings.md` §4 + `fit_shapes.py`) into usable form and build on them.
2. **⭐⭐ THE BTC BOOK GATE ("worth its weight in gold"):** build + validate the BTC book pre-fire lean as a BTC
   ENTRY gate (skip/down-weight legs BORN book-negative at the valley); confirm OOS / on fresh book.
3. **Validate the ENERGY gate on BTC/ETH/XRP walk-forward** — deploys if it holds; SOL uses the deep-hole skip.
4. **Tune the `balance_exit` knob per coin on fresh book;** keep baseline where baseline wins. Entry is the bigger lever.

## RULES (standing)
Firing LOCKED (Greg-only) · live-code-only (decision via `run_kraken_cell`) · shape/RATIO only (no volume/price) ·
TRADE imbalance = the decider, BOOK imbalance = the BTC pre-fire tell · per cell × per category · the edge is the
CURVE, never an AUC · EXIT = a per-coin tuning knob, keep baseline where it wins · doge on its OWN track (excluded
from the 4-major rules) · use AGENTS for the heavy runs (each `run_kraken_cell` on a ~700MB book is minutes).
