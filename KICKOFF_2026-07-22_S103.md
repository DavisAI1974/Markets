# KICKOFF - S103 (G15 REFINE is the opener - the shoulder magnitude-oversizing problem; then G16; smalls)

**FIRST ACTION, BEFORE ANYTHING ELSE: SWITCH TO THE BRANCH IN THIS BOX.** The harness assigns a
different auto-named branch - it is NEVER the work. Run:
`git fetch origin claude/ng-coach-agent-loop-5ha5bf && git checkout -B claude/ng-coach-agent-loop-5ha5bf origin/claude/ng-coach-agent-loop-5ha5bf`
The tip message must begin "S102".

**THE AWS KEY: USE THE PAIR WHOSE SECRET BEGINS `tx`** (ID `AKIAYI6JDCBVLKYQGLMH`, account ...4170).
GOOD - never re-diagnose. InvalidClientTokenId = the container's placeholder env vars; run AWS via
`bash -lc` or explicit creds (CLAUDE.md "AWS KEY"). Greg pastes the pair; write `scratchpad/aws.env`
(KEY=VALUE, no `export`), `~/.aws/credentials`; STS verify (pass/fail + account tail only).

Read FIRST, in order: `SESSION_HANDOFF_2026-07-21_S102.md` -> this kickoff ->
`research/kalshi/knowledge/ng_brain.json` (**s102.1, 33 plays** - the six new plays, slider S1/S2/S3,
warm-cut headroom gate, class_curve_profiles, open_conditions_protocol, live_architecture_s102) ->
`research/kalshi/S102_MERGE_PROPOSAL.md` (what was merged and why). S100.1/S100.2 = separate parallel
session, do not fold.

## ENV ON-RAMP

`pip install boto3 databento matplotlib pandas openpyxl requests` -> creds -> `platform_sync.py pull`
the store prefixes (all EXCEPT nymex/nymex_cont/ raw + nymex/ng_l1/ [big] + kalshi_echo/ + deploy/;
pull nymex_cont_n0 + _n1 + nymex_cont_ngj26 + contract_structure + nymex_curve to data/; eia_surprise
copies to data/eia_surprise.json). **NOTE: the consensus store dir is `data/storage_consensus/` (the
S102 path fix).** -> `forecast_harness.py --selftest` PASS -> `audit-joins` 0 violations.

## STATUS CHECKS BEFORE JOB 1

1. **BOX**: read s3 logs/ng_l1_pull.log; if nymex/ng_l1/_DONE present, report the total ($ + day
   count) and QUEUE CL L1 the same way (deploy/aws pattern, mbp-1, CL.n.0, 2025-07..2026-07), then
   STOP the box when NG-L1 + CL-L1 + pull_rest all done. If L1 still running, leave it.
2. **G15 actuals exist**: renders/ng_refine_s95/g15_rt.json (12 days, two-leg Kalshi-underlying
   basis, seam -0.037 at 0320) + g15_score.json (blind 8/12, drift -3260) + g15_blind.png. The
   NGJ26 April-leg files are local (data/nymex_cont_ngj26) and on S3.

## JOB 1 - THE G15 REFINE (the opener; refine until it scores, Greg's "lines darn close to perfect"
standard from G14 - EVERY step <= ~250, drift = derived-not-declared, magnitudes from measurable
state)

G15 blind = 8/12, drift -3260. DIRECTION was right (block gave back, actual -600) but MAGNITUDE was
grossly too deep (guessed -3490). This is the FIRST shoulder magnitude-calibration refine and the
first refine of an OPEN-CONDITIONS walk. The magnitude-oversizing IS the problem to solve. Anchor Fri
0313 close 3.132; actuals in g15_rt.json.

THE MISSES to explain per-event (real tape, incumbent rules, general rules only):
- **0318 Wed +1900** (blind -400): a big UP day mid-give-back the blind missed entirely. What owned
  it? (storage-trajectory turn? a cold add pricing? tape_conditions flow?) - the day the block stopped
  giving back.
- **0326 Thu -60** (blind +900): opex + EIA print day; blind called the 1120-alternation + backdrop-
  inversion UP big; actual flat. Over-sized a print-day counter.
- **0327 Fri +1160** (blind -350): April EXPIRY-day (walk instrument = May, once-removed); blind
  called a crest-trim counter; actual rallied. The expiry-day form (0225 covering-into-settle vs the
  crest-trim) mis-called.
- The SHALLOWNESS overall: guessed cum -3490 vs actual -600. Is the give-back magnitude itself the
  miss (shoulder give-backs are shallower than winter?), or a few specific days? PER-EVENT, do not
  rescale globally.

RECOMMENDED refine shape (Greg's model-tiering: Fable orchestrates, Opus/Sonnet workers): a focused
day-sequencing audit (real tape, incumbent rules - the B pattern) FIRST to see how many misses are
the standard one-verdict-propagation vs genuine gaps; a magnitude/shoulder-band investigation
(does the shoulder compress give-back bands the way it inverted warm-cut authority?); a leg-grain
pass only if a magnitude signature needs it. Then iteration-to-derived, render, PRINT, HARD PAUSE,
merge -> s102.2 (or s103.1) before G16. **The open-conditions protocol means G15+ blinds are
conditions-informed - name it in the scoring.**

## JOB 2 - G16 (Sun Mar 29 - Fri Apr 10) after G15's merge

Carries the **dead-sponsor weekend watch 0327->0329** (the April NGJ26 expired Fri 0327 - the sizing
rule's first TRUE forward test IF a premium stranded; likely NO-fire in shoulder like G13, but the
arm arms). Deeper into the spring shoulder / injection season - the slider S1-void regime continues;
watch the storage phase fully flip to injection. Basis: the Kalshi underlying is May through the May
roll (~Apr 24, 5bd before the ~Apr 28 LTD) - stays on May/996 the whole block (verify via a
roll-check if any OI-rank quirk recurs).

## JOB 3 - smalls (when adjacent)

- **pyth_collector SUNSET - Jul 31, ESCALATE AT OPEN** (trunk branch `claude/kalshi-s79-kickoff-ij8t9o`;
  Greg's explicit go required). ~9 days.
- CL L1 pull queue (Job status-check 1). Voxa wiring (pending Greg). Two-coach nod. Repo-private.
- HANDOFF to ChatGPT/others (Greg routes): CME event-contracts memo, hub-mapping doc, research-delta
  build ranking (NG_DAILY_PREDICTORS_SWEEP_S102.md).

## PENDING GREG

pyth sunset go (DEADLINE); Voxa pointer; two-coach spec nod; repo-private; research-build ranking.

## OPS NOTES

Greg runs AUTO-APPROVE: hard pause after questions; short turn-breaks between groups. Background tasks
ride through interrupts. Subagent fetch-waypoint stalls: nudge continue-to-completion (SendMessage the
agent). Box work via SSM bash -lc + boto3 (base64-encode scripts to avoid sh-quoting breaks - the S102
lesson; container cannot reach raw TCP). Model tiering: Fable orchestrates + adjudicates + merges;
Opus/Sonnet workers run the investigations; ChatGPT/people do off-repo docs - full output depth, just
distributed. Keys are SECRETS.

## GUARDS (unchanged, non-negotiable)

PER-EVENT never pool/average as conclusion; drift = descriptor; general rules only; blind wall
decision-time only (+ the ONE-SHOT MASK on price-derived blocks, --mask-after; tape_conditions is
LIVE-but-non-price per the open-conditions protocol); one-shot canonical FROM A FRESH SESSION; renders
PRINTED before refine merges; REFINE UNTIL IT SCORES (magnitudes DERIVED from measurable state, never
fitted - Greg's lines-darn-close standard); Sunday-start/Friday-end; refined never overrides blind
until forward evidence (supersession = single ownership); DAY-CLASS FIRST, no cross-class scoring,
holidays same-holiday-only, extended weekends own sub-class; SEASONAL SALIENCE read at every block
open (S1/S2/S3); rolls marked never traded; flips C1+C3+C4 (C2 scoped) never front-run; squeeze
doctrine in delivery windows; thin amplifies delivery damps holds; net-of-fee maker AND taker; git =
CODE, S3 = DATA; NG != WTI; weather forecaster HANDS OFF; provisional-until-live; no emojis.
