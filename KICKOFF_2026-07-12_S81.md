# KICKOFF S81 — Kalshi: the per-trade LEVEL-HIT dataset + Thursday 7/16 live test

**Read in order:** `SESSION_HANDOFF_2026-07-12_S80.md` → CLAUDE.md S80 delta (top) → this file.

**Branch:** `claude/kalshi-s79-kickoff-ij8t9o` — the repo DEFAULT; push there directly (Greg, S80).
If the harness assigns a fresh branch, it is almost certainly a STALE S70 tip (S79 and S80 both hit
this trap) — check `git log --oneline -1` FIRST; if stale, work on s79 instead. Do NOT cut from stale.

## Where we are (end S80)
- **Cron LIVE** (s79 = default): 6h durable collector accrues order-book bins + consensus continuously;
  data lands on `data/kalshi-bins` at the END of each ~6h run.
- **Historical Kalshi trades retrievable**: 496k real WTI trades on disk-recipe (46 daily events × top-12
  strikes, signed `taker_side`) via `research/kalshi/kalshi_history.py` — store is LOCAL/gitignored, so a
  fresh container must re-pull: `python research/kalshi/kalshi_history.py --series KXWTI --all --top 12 --skip-candles`
  (~7 min).
- **⭐ Futures→Kalshi LAG CONFIRMED one-directional**: WTI 15/41 contracts significant {0m:6, +1m:8, +5m:1},
  Brent replicates {0,+1,+1,+2}; Kalshi NEVER leads (19/19). Half the liquid strikes re-price a FULL MINUTE
  late. Near-ATM: one futures level ≈ 10–25¢ probability vs ~5¢ spread+fee toll → reprice pays the toll,
  1–2 more levels = profit. Two layers: futures ladder = DRIVER, Kalshi book = VEHICLE, lag = bridge.
- **Standing rules added (S80)**: EACH TRADE INDIVIDUALLY, NEVER AVERAGE (platform rule, in CLAUDE.md);
  level-hit reframe (2.00→1.95); HERD(breadth) > WHALE(persistence) for continuation — whale whipsaws/ends,
  herd is steep+sustained, herd SIZE is the magnitude predictor; settlement-window filter (SETTLE_UTC guard);
  news categories beyond releases need per-category historical scores; weather = GREG'S OWN SPEC — leave the
  weather build alone.

## S81 BUILD (priority order — Greg delegated)
1. **THE PER-TRADE LEVEL-HIT DATASET** on the historical WTI tape (the each-trade-individually rebuild).
   One row per level-hit event: context = {strike moneyness (prob band), side, time-of-day, release flag,
   velocity (levels/min into the hit), herd-vs-whale fingerprint (clip-size diversity + arrival acceleration
   vs repeated-similar-clips persistence), exhaustion state (dipole → 0.5), far-side depth n/a historically}
   + outcome = {next level hit before reversal? run length, net of THAT strike's actual spread + fee}.
   Output = distributions + winner fingerprints per cell, NEVER a pooled mean. Leakage-gate the features
   (odcore.leakage). This is the foundation of the continuation predictor.
2. **THURSDAY 7/16 EIA NATGAS (14:30 UTC)** — two tests in one day, data accrues automatically:
   (a) first LIVE release-day book test: `python research/kalshi/release_book_signal.py --test --series
   KXNATGASD --data-dir data/kalshi --consensus data/kalshi/consensus.jsonl` after fetching + gunzipping
   `data/kalshi-bins`; (b) the busy-day NATGAS lag measurement (release day = dense tape):
   `python research/kalshi/futures_kalshi_lag.py --series KXNATGASD --fetch "NG=F"` (pull that day's event
   trades first). Run `consensus_poll.py` BEFORE Thursday (forecast) and after (join actual via EIA —
   `kalshi_weather_forecast.py` already fetches EIA actuals).
3. **Sub-minute lag resolution** on the lag-0 WTI half: Pyth (Kalshi's own settlement source) or another
   fine feed; decides whether the synchronous-looking half is also exploitable.
4. **News-category importance budget, move-first**: scan deep underlying history (CL=F/NG=F daily+intraday)
   for big moves with NO release driver → how much of the move budget is unexplained (≈ unscheduled news:
   war/pipeline/capacity/sanctions/LNG/freeze-offs). News-first RSS category scoring accrues forward.
5. Standing: paper-loop RSA demo creds (Greg); AGA pre-2002 (bonus); OD-weather plugs into
   `kalshi_weather_forecast.py`/`kalshi_score.py` whenever Greg's forecaster is ready.

## RULES (carry forward, load-bearing)
- EACH TRADE INDIVIDUALLY — never average; distributions + per-trade fingerprints, never lead with a mean.
- Per-cell always (moneyness band × side × regime × release-flag); never pool across unlike trades.
- Exclude the daily-settlement convergence window from every historical decision (SETTLE_UTC + guard).
- Merged architecture: catalyst (release/news) = trigger + coarse size; book/flow imbalance + exhaustion =
  direction + magnitude; herd breadth = continuation/size; whale signature = scalp-only, whipsaw risk.
- Hubs: KXNATGASD=Henry Hub, KXWTI=Cushing/ICE-settle, KXBRENTD=Brent; all DAILY price-settled.
- Leakage gate (odcore.leakage) before any signal touches a backtest. Zero synthetic. Provisional-until-live.
- Weather build = Greg's spec, hands off. `--events` on news_coupling_research = BASENAME (joined onto --data-dir).
