┌─ DROP-IN — DavisAI Markets S70 ─────────────────────────────────────────────────────────┐
│ ⭐⭐ STANDING RULE #0 (Greg, S69) — ALWAYS RUN THE LIVE CODE FOR EVERY TEST. NO EXCEPTIONS. │
│  Never estimate fills/capacity/edge with a tape PROXY, a hand-rolled reimplementation, or a │
│  bolt-on (no `capacity.py`-on-tape, no scratch script recomputing eligibility). Every        │
│  fill/capacity/$hr test goes through the actual executor (`run_stream`/`run_kraken_cell` →    │
│  `swing_maker`) on the REAL data, and the honest FILL comes from the L2 BOOK via              │
│  `fill_model="queue"` + real `best_bid_sz`/`best_ask_sz`. `scripts/basket_sim_kraken.py::     │
│  load_book` already does it. If you're reimplementing what the executor does, STOP and call   │
│  the live code. (S69 burned a whole session on tape-proxy capacity — nonsense $25 "caps" —    │
│  when the live queue-fill on the real Kraken book was the answer: BTC ~34% queue / ~90% front │
│  of the $5k.)                                                                                 │
│                                                                                              │
│ BRANCH: reconcile to canonical FIRST. If S68/S69 files (STRATEGY_INVENTORY S67 block,          │
│  SESSION_HANDOFF_2026-07-07_S69.md, the 4 S68 agent docs) are missing, the branch was cut from │
│  a stale parent → `git reset --hard origin/<newest davisai-sXX tip>`. "5c5vg9" is a LABEL,      │
│  not a SHA — never reconcile to it.                                                            │
│                                                                                              │
│ ⭐ READ FIRST: STRATEGY_INVENTORY.md (top standing rule + S67 capital-model block) →           │
│  SESSION_HANDOFF_2026-07-07_S69.md. WE ARE ON KRAKEN. Majors 0bp maker (no rebate); small-cap  │
│  eligible −2bp. Coinbase parked, Bybit banned. $5k = ONE shared pool; per-cell law; never cite │
│  the shuffle FLOOR as edge.                                                                    │
│                                                                                              │
│ WHERE WE ARE (S69 made ZERO code changes — the branch is the clean S68 baseline + a note):     │
│  • Capital model is BUILT + canary-clean (S67): `odcore/allocator.py`, `platform.run_portfolio`,│
│    `scripts/portfolio_sim_kraken.py`, `grade_coin_kraken.py`. The S69 agent VERIFIED the        │
│    allocator is correct (canary bit-for-bit; greedy obeys all rules — shared pool, best-edge-   │
│    first cascade, a single leg may take the full $5k, skip-but-never-drop negative edge).        │
│  • THE ONLY THING WRONG was the capacity INPUT: `portfolio_sim_kraken` builds caps from the     │
│    TAPE (`capacity.py`, whole-hold `window=None` → inflated ~$4k; per-leg FILL_W → nonsense $25).│
│    Capacity/fill is NOT a tape question. The honest fill comes from the L2 BOOK queue model.     │
│  • LIVE book truth (basket_sim_kraken --fill queue, BTC 41.9h book): FRONT 90% fill / +6.47/hr; │
│    QUEUE-honest 34% fill / −2.31/hr (one LOW-EDGE ~40h window). `fill%` = does the $5k fill.     │
│                                                                                              │
│ JOBS (S70 — Greg's directives, in order):                                                      │
│  1. ⭐ ADD THE NEW COINS TO THE STACK + START THEIR BOOKS. Candidates LTC/AVAX/ADA/SUI are in    │
│     `platform.KRAKEN_CANDIDATES[_MARGINAL]`; add the S67-sweep LARGE candidates as ungraded      │
│     seats. ADD them to `.github/workflows/kraken_book_collectors_durable.yml` (today: sol/eth/   │
│     btc/doge/xrp) so their L2 books start accruing NOW (books take days; start the clock).        │
│  2. ⭐ RUN THE HONEST CAPITAL MODEL ON THE REAL BOOK. Materialize `data/<coin>-kraken-book` →     │
│     `/tmp/kbook/<coin>_book.jsonl`; run the shared-$5k pool over the per-cell QUEUE-fill legs     │
│     (`load_book` + `run_cell(..., fill_model="queue")` — LIVE code). Report POOL $/hr + fill%    │
│     per coin. NEVER a tape capacity proxy. Do NOT re-open the tape-capacity thread (S69 dead end).│
│  3. Longer-window confirm of the S68 tuned configs (ADA/LTC + reversed SOL/XRP) before any live  │
│     registry change.                                                                            │
│                                                                                              │
│ DATA: L2 book branches = `data/{btc,eth,sol,xrp,doge}-kraken-book` (BTC ~42h; new coins need     │
│  collectors added). Tape (for STRUCTURE/edge, not fills) re-pullable via `backfill_kraken_        │
│  trades.py --days N --bins-path realbins/<coin>_kraken_bins.json` (⚠ tape dies on recycle).      │
└────────────────────────────────────────────────────────────────────────────────────────────────┘
