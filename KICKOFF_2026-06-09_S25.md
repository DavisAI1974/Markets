# KICKOFF — Session 25 (Markets) — written 2026-06-08 (end of S24)

WORKING BRANCH: `claude/crypto-trading-platform-plan-MpqwG` (DavisAI1974/Markets).
**WRITTEN-IN-STONE GUIDE: `BUILD_PLAN.md`** (commit `da9dc63`, this branch) — the approved OD / PySR-Julia
rebuild plan. Read it FIRST and follow it to a T. ANY deviation must be cleared with Greg before acting
(memory [[follow-canonical-plan-rule]]). Master context: `CLAUDE (5).md`. Zero synthetic data; Result
Discipline in force. Background detail: `SESSION_HANDOFF_2026-06-08_S24.md`.

## 0. FIRST THING S25 DOES — make the PySR/Julia toolchain LOAD (Greg's directive: "start with the pysr/julia part and make sure it all loads")
BUILD_PLAN Phase 0 requires the symbolic-discovery engine online; S24 found it is NOT (PySR 1.5.10 is
installed but `import pysr` hangs on first-run Julia precompile; `julia` not on PATH). Discovery is blocked
until this works. Do, in order:
1. Run/confirm the SessionStart hook ran: `bash scripts/session_start.sh` (pip-installs PySR, pulls the
   Julia backend via juliacall, materializes real bins).
2. Verify it ALL loads: `python -c "import pysr; print(pysr.__version__)"` returns, AND a tiny PySR fit
   completes (Julia precompiles on first fit — let it finish; can take minutes the first time).
3. If Julia is missing / not on PATH: install it (juliaup, or the juliacall-bundled Julia) and re-verify.
   Do NOT proceed to discovery until a real PySR fit returns. Per BUILD_PLAN, the numpy-reconstructed forms
   are a DEGRADED fallback only — flag it if you must use them.

## 1. Fix the dipole the RIGHT way — original 128-dim operator_coefficients, PER PAIR
S24 proved the chem dipole collapses because it was fed a 15-dim hand-built c_i (thin: eff-rank 7.82/15;
non-discriminative: AUC 0.51 OOS), pooled, with a hardcoded quadratic — all three OFF BUILD_PLAN. The fix
is the plan's actual construction:
- The verbatim original is ON DISK: `E:\Markets\_markets_algebraic_dipole.py`. It reads RAW 128-dim
  `operator_coefficients` per pair from `E:\refrag\discoveries\operator_discoveries\<pair>_<win|lose>/*.json`,
  builds win/lose centroids, projects `H_a=<c,c_win>/||c_win||`, `H_b=<c,c_lose>/||c_lose||`, fits
  `H_a^2 = a + b*(H_a*H_b) + c*(H_a*H_b)^2` PER PAIR (12 pairs) and pooled. NO standardization.
- STEP 1 (reproduce "where it worked"): run the original on the real coefficients and confirm c~=1.309,
  R^2~=0.943 comes back. Labeled set on disk: `E:\Markets\_full_pipeline_winners_preentry_cs100_v2\summary.json`
  (1,054 results with `operator_coefficients_head`). If `E:\refrag\discoveries\operator_discoveries\`
  exists, prefer it (it is what the original reads). Confirm the coefficients are full 128-dim, not a head.
- KEEP SOURCES/PAIRS SEPARATE (Greg). Do NOT pool assets/venues.

## 2. Then DISCOVER, don't hardcode (BUILD_PLAN signal D + odcore/symbolic.py)
Once PySR/Julia loads and the original reproduces: run `odcore/symbolic.py` PySR (ops
{+,-,*,/,square,cube,exp,log,sqrt}) to DISCOVER the dipole/coupling/MI form per pair from the raw operator
data. Promote a form ONLY on >=3 seeds with <5% coeff variation AND walk-forward + tautology-null
(`odcore/validation.py`). The reconstructed chem coeffs are a synthetic anchor only (PySR must rediscover
them on a Brusselator) — but Greg's rule is zero synthetic unless unavoidable, so prefer the
real-coefficient reproduction as the anchor.

## 3. Still-missing originals (ask Greg)
Tier 3 not on disk: `s12_coupling_decomposition.py`, `s13_chemistry_residual.py`,
`s12_consolidate_per_domain.py`, `od_per_domain_equations.json`. Tier 1/2 (`_markets_algebraic_dipole.py`,
`_markets_dipole_kfold.py`, `_markets_dipole_separation.py`, `_markets_dipole_chunker_stack.py`) ARE on
`E:\Markets`.

## 4. Validation + wire (deferred until the gate passes)
Unchanged: nothing wires live until a discovered form beats baselines net of fees+slippage under
walk-forward + tautology null. The dipole/leadlag/strength/scanner generators flow through `/api/signals`
+ `/api/stream` (shadow first) only after promotion.

## 5. Housekeeping / environment
- PySR/Julia loading is the gating item (section 0).
- doge/link/xrp bins are local-only (the hook materializes btc/eth only); add `data/doge-bins` etc. + hook
  lines to give web/container sessions the alt-coins.
- `E:\Markets\.claude` is exFAT-hard-locked; work from the worktree + git refs ([[markets-env-hard-lock]],
  [[git-is-source-of-truth]]). `safe.directory '*'` is global.
- Do NOT confuse `MARKETS_OFFENSIVE_LAYER_PLAN.md` (separate older defensive plan) with `BUILD_PLAN.md`.

## Branch
WORKING BRANCH: `claude/crypto-trading-platform-plan-MpqwG` (DavisAI1974/Markets)
