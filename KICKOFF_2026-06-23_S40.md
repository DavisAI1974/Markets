# KICKOFF — S40 (next session)

Branch `claude/crypto-backfill-validation-31tubb` (all S39 work pushed; it's the dev branch — develop +
push here, never another branch without explicit permission). **Read first:** `SESSION_HANDOFF_2026-06-22_S39.md`
then the S39 delta at the top of `CLAUDE.md`. Memories: crypto platform only; zero synthetic; per-cell deploy;
**never flatten/average/smooth — bucket + trade distinctiveness is the goal**; git is source of truth; commit +
push regularly; never tune off one window.

## State you inherit (all committed)
- **Coeff engine works IN-CONTAINER** (the S39 unblock): production 128-dim coeffs are the DETERMINISTIC tier,
  reproducible via `odcore/od_refrag_adapter.py` + the cs100_v2 config in `_run_alt_coeffs.py`. No box/torch/FNO.
- **1-second is the standard.** `realbins/` = 9 cells (local, gitignored; re-pull via the backfill scripts).
- **600 alt coeffs** done (sol/doge/xrp bybit_perp, 100/cell, unique source_ids) →
  `_alt_labels/coeffs/alt_coeff_index.json.gz`.
- Multi-regime gated-swing KILLed the single-window deploy map → no capital on that evidence.

## TASK 1 (primary) — build the AWS package
`aws/` exists but is EMPTY (left for you). Greg wants the heavy jobs on AWS (he can't be blocked on the box; this
container has no AWS creds — verify with `env | grep AWS` + `which aws`; endpoints are reachable, it's a creds gap,
so YOU package and GREG launches with creds). Deliver:
1. `aws/Dockerfile` — slim python + numpy/scipy; copies `odcore/`, `markets_adapter.py`, `_build_alt_winner_labels.py`,
   `_run_alt_coeffs.py`.
2. `aws/run_discovery_s3.py` — env-driven (S3 bins prefix / labels prefix / out prefix / cells / cap): boto3 pulls
   1-sec bins from S3, runs labeling + the in-container coeff discovery, pushes the coeff index back to S3. (This is
   also the **off-git storage** S37 wanted — bins live in S3, not git.)
3. `aws/DEPLOY_AWS.md` — runbook: build/push to ECR, run as AWS Batch (Fargate) OR plain EC2 `docker run`; the exact
   `aws` CLI commands Greg runs with creds; the **Bedrock** note (Claude Code via Bedrock + Knowledge Bases on S3/RDS
   for the agent + data residency).
4. **SageMaker GPU path** (`aws/train_fno/`) — the real production-tier upgrade: train the FNO decoder (manifest:
   4 layers / 16 modes / width 64, loss spectral_coefficient_mse + eigenvalue_preservation, ≥100 training pairs,
   ensemble 5 / mc_dropout 0.1) from the existing deterministic coeffs as training pairs. Skeleton + launch doc;
   Greg runs it on a g5/SageMaker. This is where AWS actually earns its keep (GPU).
Keep it standard + verifiable (you can't test without creds); mark Greg-launches-with-creds clearly.

## TASK 2 — re-discover BTC/ETH coeffs on 1-second (consistency)
Now cheap in-container. Run the same `_run_alt_coeffs`-style pipeline on the BTC/ETH `realbins` cells so all 15
cells share the 1-sec deterministic coeff basis. (Pin the encoder-summary/expansion variant first if you need the
new coeffs to exactly match the committed BTC/ETH index; otherwise per-cell distinctiveness is enough.)

## TASK 3 — wire the per-cell distinctive fingerprint predictor (the S35 goal)
With alts now having coeffs, build/stack the per-bucket distinctive fingerprint (128-dim coeff + the 6 micros +
refrag), predict winners PER CELL, deploy per cell — never pooled, never class-balanced, never separation-graded
(distinctiveness IS the metric). The gated-swing KILL says the flow-only swing edge doesn't clear net-of-cost; the
coeff fingerprint is the complementary lever.

## Carry-overs / cautions
- Default-branch sync NOT done (kept to the assigned dev branch by the hard rule) — only if Greg asks explicitly.
- GHA runs need Greg's click (token lacks actions:write); crons self-fire.
- The XRP-sell=195 in the S38 handoff was a STALE collision artifact (real=2564) — already corrected in S39.
