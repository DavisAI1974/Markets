# S54 KICKOFF — BIG TRENDS (Greg: "we definitely need to start trading the bigger trends") + multi-window confirms

Read first: `SESSION_HANDOFF_2026-07-03_S53.md` → `S52_SIZING_MATRIX.md` (canonical matrix) →
`STRATEGY_accumulate_S52.md`. Branch: designated `claude/crypto-liquidity-signals-*`; reconcile to
canonical `5c5vg9` at start (`git fetch origin && git merge --ff-only origin/claude/crypto-liquidity-
signals-5c5vg9`; reset --hard if the harness cut stale); keep 5c5vg9 synced on every push. Default
branch `claude/new-session-o3vnm` carries the crons — LEAVE IT. Books:
`git show origin/data/<coin>-book:<coin>_coinbase_book.jsonl.gz > /tmp/…` (5 coins, 29–196h) and
`git show origin/data/<coin>-bybit-book:<coin>_bybit_book.jsonl.gz > /tmp/…` (sol, eth).

## ⚠ STANDING (carry forward)
- Corrected price-eligibility fill model canonical; mk0 measured-dead; cite matrix cells, never one $X/hr.
- **NO GATES (Greg, S53):** judge every change on net $/hr AND legs/hr; don't buy win% with trade count.
  D-class (confirm-budget/timeout/stricter confirm) stays excluded ("we'll see about the d's later maybe").
- **Q1 honesty:** the accum edge ≈ $0 under honest queue on EVERY config — front-of-queue tier only.
- Bybit numbers = still the ONE 5.83h window (no new cron window as of S53 close). PROVISIONAL.
- ETH-Coinbase flag: REVERSED control positive on the accum arm this window (S51 disqualifier shape
  on that cell) — do not deploy accum logic on ETH Coinbase without a clean multi-window story.

## JOB 1 (Greg decision, S53 zoom-out): the BIG-TRENDS thread
The fullwindow shapes (docs/renders/s53/fullwindow) show multi-hour 100–300bps waves under our
θ≈23bps ripple scale (SOL window range 630bps, ETH 624, BTC 1094, DOGE 681, XRP 482). At θ~100bps
the fee floor is 2–5% of the swing instead of ~25%.
  a. ORACLE first (S36b discipline): perfect turn-trading at coarse θ (sweep ~60/100/150/250bps) per
     cell on the multi-day Coinbase books — swings/day, med swing, oracle net after fees at the
     matrix tiers. Establishes whether the opportunity is fat enough before ANY strategy code.
  b. Then the causal probe: `_price_zigzag` already parameterizes θ; run one-shot AND accum executors
     on the coarse streams (few trades/day — the 29–196h books are the minimum viable history;
     multi-window/multi-day splits mandatory). assert_no_leakage + shuffle + reversed + Q1 on
     everything. Holding-period reality check: coarse swings = multi-hour holds — report exposure.
  c. Detector question: is the dipole gate informative at THIS scale (DIVW window may need scale-up)?
     Report per cell; no tuning off one window.

## JOB 2: multi-window Bybit confirm (standing gate on ALL venue numbers)
Check the 0 */6 cron actually pushed new windows (S53 found the SAME 5.83h window as S52 — if the
cron is stuck, diagnose from the default branch run logs). Per NEW window: re-run the one-shot rows
+ the S53 SOL candidate; the S52 MM-tier verdicts must reproduce before any sizing.

## JOB 3: the S53 SOL-only candidate (dump_mult 1.5–3 + p2_taker_cap 0)
Re-run `scripts/_s53_accum_sandbox.py --cells` when any cell has a NEW window (Coinbase books roll
forward too). Adopt as default ONLY if the lift reproduces across windows on SOL; the cross-cell
S53 verdict (SOL-only) stands until data says otherwise.

## JOB 4: C1 — dive-timed starter (the only lever aimed at the Q1 problem)
Two candidate triggers, both causal, tested head-to-head inside the accum harness: (1) 1-sec
price-reversal (S36b timing tool), (2) dipole DIVE d(lean)/dt (Greg's S40 overlay chart — pins x=0
on 2000 winners; the FILTER half is wired as the gate, the DIVE half is wired NOWHERE). Better
starter location = earlier queue join = the only idea on the board that attacks Q1≈0.

## JOB 5 (Greg action, standing): Bybit MM application
institutional_services@bybit.com — the measured case: standard mk+2 SOL −$157/hr / ETH −$273/hr
vs MM3 −1.25bp +$115/+$113/hr @$5k. (+ optional vip@backpack.exchange.)

## DEAD — S53 additions at top (full list in S52 kickoff + S51)
**B1 harvest-into-strength rungs** (caps the fat winners that carry the column: −$0.26 vs +$1.60 —
Greg's "eliminating losers eliminates winners" measured on the exit side); confirm-budget blocker
(A1 corr −0.10 — no relation to net); symmetric-band assumption "dumps are mostly real" (61% FALSE,
universal 56–78% across cells). Everything in the S52/S51 DEAD lists stands: mk0 anywhere; the old
no-eligibility fill model; accum on micro streams; TIF-as-rescue; anti-martingale; cap-tightening.

## FILE MAP additions (S53 — change IN PLACE)
- `odcore/swing_accum.py` — opt-in variant params: `dump_mult`, `p2_taker_cap`, `harvest_rungs`,
  `slide_x_mult` (defaults bit-reproduce S52; canary 65 legs/+56.204187 dipole).
- `scripts/_s53_accum_sandbox.py` — diagnostics (default) / `--sweep` / `--cells`; CELLS list has
  all 7 cells' params; `rolling_conf_bps` replicates the executor band math.
- `scripts/_s53_render_fullwindow.py` — zoom-out shape renders (range/drift stamped in titles).
- `scripts/_s52_render_walkthrough.py` — `--worst-accum` / `--small-winners-accum` ranked-tail modes.
- Results: `_s53_accum_sandbox_results.json`, `_s53_accum_crosscell_results.json`.
All S52-and-earlier entries stand (`paper_trade.py` FIRST each session; deployed baseline untouched).

## Rules (standing)
Crypto only; zero synthetic; per-cell deploy; never pool sign-blind; NEVER tune off one window;
git = source of truth (rebase onto 5c5vg9 first — the paper cron moves it); falsification-first
(leakage gate + shuffle/reversed controls on every new signal); verify venue fees from primary
sources; Bybit REST geo-blocked from some DCs (WS-only); `pip install matplotlib`, MPLBACKEND=Agg;
token = 403 on GHA dispatch (crons fire from default branch).
