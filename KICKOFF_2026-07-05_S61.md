# S61 KICKOFF — FINISH THE COINBASE EXIT (fill/fee build order) + UN-PARK KRAKEN

⛔ STANDING BANS unchanged: BYBIT never; MEXC dead. ELIGIBILITY BEFORE ANY VENUE WORK
(`S57_VENUE_FINDINGS.md`).

Read first: `S60_EXIT_NOTES.md` (R1a–R7, the round-by-round) + `SESSION_HANDOFF_2026-07-04_S60.md`
-> `S58_ENTRY_NOTES.md` for the entry record. Branch: designated claude/*; reconcile to
canonical `5c5vg9` FIRST (S58/S59/S60 were ALL cut from the wrong default/crons parent —
fetch + ff-only else reset --hard); sync 5c5vg9 every push. A parallel session may push to
canonical — rebase, don't clobber.

## STATE (S60 close)
- **ENTRY = DONE** (S59 promotion). **KRAKEN TAPE VERDICT = the gross exists on Kraken's own
  prices** (all registry cells kr_mk0-positive, sol +$3.14/hr 5/5wk, eth alive on Kraken).
  Fills = the one open unknown (books accruing).
- **PIECE 2 (EXIT) worked to per-cell verdicts:** zigzag holds; the only exit edge is the
  wrong-side corrector, DIAGONAL per cell (SOL none / BTC plain-stop rider / DOGE cascflip /
  XRP none). Toll law 4/4 both venues; no peak harvester (S35 fingerprint-tier).
- **COINBASE EXIT FIX reframed: FILL/FEE-shaped, not timing.** The sandbox ledger can't see
  the real exit fill cost (128/128 closes booked maker by construction); the honest fill
  machinery exists UNWIRED; grace=0 at mid-band; fee tier > the whole exit prize.
- **DIPOLE PROGRAM: paper printed** (`docs/DIPOLE_PAPER_S60.*` + dive chapter + crypto-uses
  notes). Absorption candidate RESOLVED (decided against; real SOL signal = maker-quoting
  price-discovery lead, filed to fill fingerprint thread).

## ⭐ FIRST TWO THINGS (Greg, explicit at S60 close)
- **A. SPIN UP THE 2 AGENTS THAT DIDN'T FINISH** (S60 interrupt casualties): the SOL
  Coinbase-exit-fix agent and the BTC armed-before recheck. Their charters are in the S60
  record (SOL: protect the one net-positive cell, evidence any exit change at n=56 + power
  bar; BTC: apply SOL's armed-before discriminator to BTC under the full kill battery —
  tail-regime split, blind-timer, winner/loser decomp, top-3-excluded). Conclusion is
  expected (SOL protect / BTC rider) but the explicit numbers were NEVER delivered — get them.
- **B. THIS IS A BUILD SESSION — expect real CODING.** The S60 proposals are code to WRITE and
  wire, not just analyze: the honest fill model (port `maker_book._first_fill_index` into the
  mid-band close), cover-grace at mid-band, the `swing_accum` fee-aware unload rules, and the
  DOGE `_cascflip` sandbox variant (exit_pred generalization of the lean_exit walker + a
  lean_w param on run_stream). Whatever the 2 re-run agents propose adds to the queue.
  Every wire: sandbox-first, baseline canary bit-identical (+0 trades, ledger 26,784 intact),
  assert_no_leakage on any new read, per-cell registry entry with the gate record.

## JOBS (one round = one defined test; update S60_EXIT_NOTES or a S61 successor EVERY round)
1. **COINBASE EXIT FIX — the build order (Greg sign-off first):**
   (a) HONEST FILL MODEL FIRST — port `maker_book._first_fill_index` queue logic into the
       mid-band close leg (opt-in fill arm); re-run `run_midband_cell` on restored Coinbase
       books; report the REAL maker_close% + net/leg vs the saturated-True baseline. This
       makes the fill cost VISIBLE — the prerequisite for everything else.
   (b) COVER-GRACE arm at mid-band (wall-clock-defined per venue; S48 map) — pays once (a) is
       honest; measure taker% + net/leg per cell.
   (c) FEE-AWARE UNLOAD rules from `swing_accum` (slide-cross past X=spread+taker; trailing-
       peg maker) as the toll-attacking execution mechanic.
   (d) DOGE `_cascflip` sandbox variant (needs the exit_pred generalization of the lean_exit
       walker + a lean_w param on run_stream) — sandbox-first, ledger decides (~30-fire bar).
   Sandbox-first + baseline canary bit-identical for every wire.
2. **FEE-TIER VERIFICATION (2 Greg clicks, biggest lever, no tape):** Coinbase fee-upgrade
   program terms (>=$500k/mo proof -> down to 0.0% maker; our cadence ~$25-30M/mo) + the CFM
   perp tier tab (4-7bp RT). Worth 12-16bp RT vs the +3-5 best exit read.
3. **UN-PARK KRAKEN EXITS** once the Coinbase exit lands: the DOGE-flip Kraken shuffle-floor
   question, btc plain40 Kraken premium, eth_kraken exit — all held from S60.
4. **KRAKEN BOOKS accrual -> the fill unknown:** when windows exist, taker-share + fill-depth
   reality at kr_mk0 cadence answers whether the Kraken tape gross survives real fills.
5. **RESEARCH MENU (background, Greg's fascination):** the paper's 29 candidate uses + the
   dive chapter's cross-domain map (seizure-onset flow collapse = the novel standout). Pick
   per Greg's interest; each has a first falsifiable test already specced.

## RE-RUN IF WANTED (S60 casualties of mid-session interrupts): SOL Coinbase-exit-fix agent +
BTC armed-before recheck — strategic conclusion already clear (SOL protect / BTC rider), but
the explicit numbers were never delivered.

## DATA (/tmp dies): Coinbase books x5 from data/<coin>-book; Binance 30d bins via
backfill_binance_spot.py x5 (~40min); Kraken 30d bins via backfill_kraken_trades.py x5
(sequential, hours). Kraken books from data/<coin>-kraken-book (once cron/click fires).

## Rules (standing): crypto only; zero synthetic (tool-validation anchors OK); per-cell
deploy; never tune off one window; graded beats gated; scale-locality; venue law (no flow
read ports without a per-venue pass); office discipline (dipole value = operator x what it's
conditioned against); sandbox-first + baseline canary bit-identical; git = source of truth;
falsification-first; MPLBACKEND=Agg; token 403 on GHA dispatch (Greg clicks Run workflow).
