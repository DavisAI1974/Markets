# KICKOFF — S42 (next session)

Branch `claude/crypto-backfill-validation-31tubb` (S41 pushed; dev here). Read `SESSION_HANDOFF_2026-06-22_S41.md` then the S41 delta atop `CLAUDE.md`. Rules: crypto only; zero synthetic; per-cell deploy; never pool sign-blind; git is source of truth; commit+push regularly; never tune off one window; falsification-first. Method note (Greg, S41): run the agnostic 5-step coupler and read what it couples; do NOT impose a direction/hypothesis. When the 5-step coupler is used, run all 5 steps in order (`odcore.dipole_trade.trade_coupling_vector` / `coupling_scanner.score_pair`).

## State inherited (committed)
- Book file `data/btc-book` (5.83h, 100ms, liquidity+flow+price). realbins 6 cells. Chat run at `research/od_book/chat_runs/OD_book_run_1.docx`.
- Lead/lag (book, S41): LIQ→FLOW r≈0.14 @ +0.7..0.9s; FLOW→PXV coincident; LIQ→PXV @ +0.1s. Full 5-step coupler rolling: depth-imbalance leads price 75.5% / lags 2.8%, mean +0.07s; quiet/loud does not flip it (corr ~0).
- OD coupling on book pairs: structured=False on all 4 tested pairs (buy/sell eq-entropy self-pole; liquidity pairs INFO-024/041 artifacts). PySR: no MI functional family.
- Dipole trend-follow (position=sign of lean, hysteresis): gross negative all thr on btc/eth bybit_perp (lean coincident → enters at tops).
- Agnostic `_coupling_scan.py` over 6 book channels (15 pairs, 5 steps + tautology null): see `_coupling_scan_results.json`.

## Open tasks
1. Agnostic coupling scan: read `_coupling_scan_results.json`; extend to per-cell across coins/venues (realbins flow/price channels; book only on btc_coinbase so far).
2. Dipole trend-following per cell across all coins (per-cell deploy rule) — pooled bybit test was gross-negative; test each cell separately.
3. More book data: `book_collector_btc.yml` GHA accrues `data/btc-book` (needs manual run trigger). Multi-day book required before any book finding is deploy-grade. Collector also exists only for btc_coinbase; add other venues/coins for cross-cell book.
4. OD-BOOK committed T_test (`research/od_book/`) once book is multi-day.
5. Cross-domain falsification: same turn operator on a non-market contested flow.

## Cautions
- realbins + PNGs gitignored. GHA runs need manual trigger. Avoid backticks in `git commit -m`.
