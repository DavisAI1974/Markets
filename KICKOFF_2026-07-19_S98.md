# KICKOFF - S98 (next session priorities for the NG forecaster loop)

Read FIRST, in order: `SESSION_HANDOFF_2026-07-19_S97.md` (the GATE section at the top, then the
session total) -> `research/kalshi/AGENT_RUNBOOK_S95.md` -> `research/kalshi/knowledge/ng_brain.json`
(**s100.2, 23 plays** - the per-instance flip characterization and the requires/scope/forward_evidence
tags are the newest work) -> `research/kalshi/PASS2_CONTINUOUS_SERIES_NOTES.md`.

Branch: S97 pushed to `claude/ng-coach-continuous-curve-7pk2gf` (tip `d7ed89cd`). Reset the new session
branch onto it. Confirm with `git log --oneline -1` - the harness cuts fresh branches from stale tips.

---

## THE GATE (Greg, S97) - NO NEW GROUP RUNS UNTIL THE DATA IS IN

Greg stopped the walk mid-S97 to close the data gaps first. **G12 does not start until every input in
the handoff's "THE DATA GATE" section is built and wired.** That list is the primary deliverable of this
session. Do not skip ahead to a group run.

How to build every one of them (Greg, load-bearing): **we are not here to test theses. We put what we
believe is relevant information in front of the agent and IT decides how to use it.** Never gate an input
on whether it "worked", never score it keep/drop, never remove or quarantine one that looks inert.
Characterize per-instance where conditions discriminate ("works on {X}, not on {Y}") and leave the data
in place.

---

## Env on-ramp

- AWS pair + `DATABENTO_API_KEY` live in gitignored `scratchpad/aws.env` (both verified working in S97;
  ROTATE if they have been exposed since). S3 bucket `bento-568968024170-us-east-2-an`, us-east-2.
- `pip install boto3 databento matplotlib pandas`
- `EIA_API_KEY=DEMO_KEY python research/kalshi/eia_surprise.py --out data/eia_surprise.json`
- `python research/kalshi/forecast_harness.py --selftest` must PASS.
- **GET A REAL EIA KEY.** DEMO_KEY is shared globally and already broke one S97 build mid-run
  (Retry-After ~20h), forcing an unverified xls fallback for the regional storage store.
- **RESTORE DATA FROM S3, DO NOT RE-PULL.** Bucket `bento-568968024170-us-east-2-an`, us-east-2. S97
  pushed 272 objects / 148.4 MB, verified: `nymex/nymex_cont_n0/` + `nymex/nymex_cont_n1/` (the tape,
  Nov 2025 - Feb 2026), `nymex/contract_structure/`, `weather/mos_asof/` (incl. the ~50MB raw archive),
  `cot/`, `storage_regional/`. Pre-existing: `weather/nws_hourly/` (450 objects, current) and
  `nymex/nymex_cont/` (the OLDER `.v.0` series that G3-G10 ran on - do not confuse the two bases).

## JOB 0 - WIRE `decision_state`, SERIALLY, ONE HAND

**All three S97 feeds LANDED, are committed and pushed.** None are wired yet - that is this job, and it
must be ONE serial pass because parallel edits to `forecast_harness.py` collide.

| module | function | data |
|---|---|---|
| `research/kalshi/cot_feed.py` | `cot_asof(date, contract_code="023651", data_dir=None)` | `data/cot/` |
| `research/kalshi/storage_regional.py` | `storage_regional_asof(date)` | `data/storage_regional/` |
| `research/kalshi/contract_structure.py` | `contract_structure_asof(date, root="NG")` - 49 fields | `data/contract_structure/` |

**CRITICAL WHEN WIRING #3 - expose the CALENDAR-FRONT fields, not just the n0/n1 ones.** The
OI-continuous front rolls out of the dying contract early, so on 2026-01-22 `front_next_spread` reads
0.093 while the actual squeeze sat at `calendar_front_next_spread` 1.539. Wiring only the obvious fields
ships a feed that structurally cannot see the event it exists for. Also carry
`days_to_calendar_front_expiry` and respect the `*_pair_changed_*d` flags - cross-roll spread changes are
`None`, not moves.

All new fields ADDITIVE, never replacing existing ones. **MISSING IS EXPLICIT, NEVER ZERO** (a zeroed HDD
in January, a zeroed storage level, or a zeroed front/next spread during a squeeze are each catastrophic
false signals; `None` means unknown, never flat). Re-audit the blind wall across ALL joins afterward -
each feed has its own publication mechanics (COT publishes Friday for Tuesday positions; EIA storage
Thursday 10:30 ET; OI respects CME next-morning publication). Confirm `curve_regime` stops reading
`'unknown'`. `forecast_harness.py --selftest` must PASS.

## JOB 1 - finish the data gate

Items 4-11 in the handoff: MOS cycle timing (a real defect - the Sunday reopen is priced by a later model
cycle than our D-1 evening feed); vol/range regime; **G11 fingerprints on `.n.0` (BLOCKS the C2 ratio
reformulation, which is the flip-rule fix)**; model disagreement as an uncertainty proxy; LNG feedgas
(probably not obtainable - report the gap honestly rather than faking a proxy); options; cross-market.
Plus the **C2 ratio reformulation** itself - derive it properly, the refine's 0.00-0.42 vs 1.23 figure is
a FLAGGED BUILD GAP, not a threshold to adopt.

Without the C2 fix the four-condition flip confirm cannot complete on modern high-activity tapes, and a
G12 blind would inherit G11's exact failure mode.

## JOB 2 - G12 (Sun Feb 1 - Fri Feb 13 2026), one-shot blind, full input set

Clean block, no expiry inside it. Anchor off the last session before Feb 1. **Run the roll check via a
SUBAGENT that returns ONLY the roll date and spread, never prices** - the S97 protocol fix; the
orchestrator cannot run it without seeing the block's price path.

## JOB 3 - G13 (Sun Feb 15 - Fri Feb 27 2026) - THE SQUEEZE TEST

Contains the **Feb 25 2026 front-month expiry**. Because `.n.0` rolls out of the dying contract early,
G13 will NOT contain a price spike even if the expiring contract squeezes; the agent's only view is the
front/next spread blowing out and the expiry clock running down. This is the forward test of whether the
contract-structure inputs let it pick up what it could not see in G11.

## JOB 4 - pass 2 on the series construction

`PASS2_CONTINUOUS_SERIES_NOTES.md`. Only AFTER the first pass over the historical data is complete -
Greg: do not re-base the walk before then.

## GUARDS (unchanged, non-negotiable)

PER-EVENT, never pool/average as a conclusion; drift is a DESCRIPTOR; general rules only, no
point-fitting; blind wall (decision-time only, storage strictly-prior, MOS as-of, COT keyed on
PUBLICATION time not report date); one-shot canonical; refine per group, renders PRINTED first, lessons
merged BEFORE the next group; Sunday-start / Friday-end two-week blocks; refined rules NEVER override
blind-applicable ones, promotion gated on forward evidence; net-of-fee maker AND taker; git = CODE,
S3 = DATA; flag weekday holidays (thin AMPLIFIES delivery, DAMPS holds); rolls marked, never traded;
flips: four-condition confirm, NEVER front-run a flip; NG != WTI; weather forecaster HANDS OFF;
provisional-until-live; keys are SECRETS; no emojis.
